# STAGE-3.5 Extractor Report — STAGE-2 Violations

**Generated:** 2026-03-12-10.21.23
**Agent:** extractor-stage2-violations
**Source:** `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage2/2026-03-11-161143-stage2-final-report.md`

---

## Section 1: Extraction Target

**Files read:**
1. `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage2/2026-03-11-161143-stage2-final-report.md` — STAGE-2 hexagonal quality audit final report (34 files audited, 7 agents, 2 rounds)
2. `/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md` — Section 5 (KNOWN ISSUES, CRITICAL and HIGH lists)
3. `/Users/bruno/.claude/projects/-Users-bruno-sec-llm-workbench/memory/MEMORY.md` — STAGE-2 Results section (violation table + summaries)

**Looking for:** All 7 hexagonal architecture violations with full precision (severity, file path, line reference, description, required fix), plus gaps between the full report and the two reference documents.

---

## Section 2: Extracted Findings

1. **Violation #1**
   - Severity: CRITICAL
   - File: `application/use_cases/ingest_trivy.py`
   - Line: `:17`
   - What is wrong: Directly imports `TrivyParser` from `siopv.adapters`, bypassing the port abstraction entirely.
   - Fix must do: Create `TrivyParserPort` in `application/ports/`, refactor `ingest_trivy.py` to accept it via constructor injection, add DI factory in `infrastructure/di/`.

2. **Violation #2**
   - Severity: CRITICAL
   - File: `application/use_cases/classify_risk.py`
   - Line: `:18`
   - What is wrong: Directly imports `FeatureEngineer` from `siopv.adapters`, bypassing the port abstraction entirely.
   - Fix must do: Create `FeatureEngineerPort` in `application/ports/`, refactor `classify_risk.py` to accept it via constructor injection, add DI factory in `infrastructure/di/`.

3. **Violation #3**
   - Severity: HIGH
   - File: `interfaces/cli/main.py`
   - Line: not specified in report
   - What is wrong: CLI calls `run_pipeline()` without wiring any of the 8 DI adapter ports — all ports default to `None` at runtime, meaning the entire pipeline runs with no real enrichment, authorization, DLP, or classification.
   - Fix must do: Modify `cli/main.py:process_report` to call all `get_*_port()` factories and pass the 8 adapters to `run_pipeline()`.

4. **Violation #4**
   - Severity: MEDIUM
   - File: `adapters/dlp/dual_layer_adapter.py`
   - Line: not specified in report
   - What is wrong: No explicit `DLPPort` inheritance — relies on implicit structural subtyping (PEP 544), weakening contract visibility and mypy enforcement.
   - Fix must do: Add explicit `DLPPort` inheritance declaration to `dual_layer_adapter.py`.

5. **Violation #5**
   - Severity: MEDIUM
   - File: `infrastructure/di/authorization.py`
   - Line: not specified in report
   - What is wrong: Factory `create_authorization_adapter()` is uncached — creates 3 separate `OpenFGAAdapter` instances with separate HTTP pools and circuit breaker state.
   - Fix must do: Add `@lru_cache(maxsize=1)` to `create_authorization_adapter()` to ensure a single shared adapter instance.

6. **Violation #6**
   - Severity: MEDIUM
   - File: `application/orchestration/ingest_node.py`
   - Line: not specified in report
   - What is wrong: `IngestTrivyReportUseCase()` is directly instantiated inside the node without DI, breaking the consistent constructor injection pattern used by the other 4 nodes.
   - Fix must do: Inject `IngestTrivyReportUseCase` (or its port dependencies) via the node closure, matching the pattern of the other 4 nodes.

7. **Violation #7**
   - Severity: LOW
   - File: `application/orchestration/edges.py`
   - Line: not specified in report
   - What is wrong: `calculate_batch_discrepancies()` contains domain-level adaptive threshold policy — domain logic leak into the application/orchestration layer.
   - Fix must do: Extract threshold logic to a domain service (non-blocking; design smell only).

---

## Section 3: Gaps / Deltas

### Gap 1 — Line references missing from MEMORY.md

MEMORY.md omits line numbers for violations #1 and #2. The full report specifies:
- Violation #1: `ingest_trivy.py:17`
- Violation #2: `classify_risk.py:18`

These are precise anchor points for REMEDIATION-HARDENING agents.

### Gap 2 — Imprecise file path for Violation #6 in MEMORY.md

MEMORY.md lists the path as `application/orchestration/nodes/ingest_node.py` (with a `nodes/` subdirectory). The full report lists it as `application/orchestration/ingest_node.py` (no `nodes/` subdirectory). This discrepancy must be verified against the actual codebase before any fix is applied.

### Gap 3 — Violation #7 description imprecise in MEMORY.md

MEMORY.md says "Domain logic in edge routing." The full report is more precise: the specific function is `calculate_batch_discrepancies()` and the issue is an adaptive threshold policy (not generic domain logic). Agents need the function name to locate the fix target.

### Gap 4 — Section 5 of briefing.md does NOT contain the 7 hexagonal violations

Section 5 of briefing.md lists STAGE-1 discovery issues (CLI hollow, SanitizeVulnerabilityUseCase orphaned, LLM confidence heuristic, run_pipeline drops enrichment clients, DLP DI not exported, hardcoded Haiku model IDs, asyncio.run, test coverage, structlog). These are distinct from the STAGE-2 hexagonal violations.

The only partial overlap is between briefing.md CRITICAL #1 ("CLI hollow — 3 TODO stubs, pipeline unreachable") and Violation #3 ("CLI calls run_pipeline() without wiring DI adapters"). These describe the same file but different failure modes: STAGE-1 found the CLI hollow (missing implementation); STAGE-2 found the CLI wires zero DI ports even after `process_report` was implemented. Both must be fixed.

Violations #1, #2, #4, #5, #6, #7 are entirely absent from Section 5 of briefing.md.

### Gap 5 — STAGE-1 Known Issues updated status not in MEMORY.md or briefing.md

The full report contains a "STAGE-1 Known Issues — Updated Status" table that clarifies several items not captured elsewhere:
- STAGE-1 #1 (CLI hollow): Now "PARTIALLY RESOLVED" — `process_report` is implemented but dashboard stub remains for Phase 7
- STAGE-1 #6 (hardcoded Haiku model IDs): Status is "Outside DI scope" — DI reads from `settings.claude_haiku_model` but defaults may still be hardcoded in the `Settings` class; needs Phase 7 review
- STAGE-1 #7 (asyncio.run() in sync nodes): Clarified — no node file uses `asyncio.run()`; the issue resides only in `cli/main.py:87`, which is correct CLI boundary usage

### Gap 6 — Layer Health Assessment table not captured anywhere

The full report contains a 7-row "Layer Health Assessment" table with per-layer status verdicts. None of this is in MEMORY.md or briefing.md. Key verdicts:
- Domain: CLEAN (20/20 files)
- Application/Ports: CLEAN (6/6 ports are pure abstract interfaces)
- Application/Use Cases: ISSUES (violations #1, #2)
- Application/Nodes: ISSUES (violations #6, #7)
- Adapters: MINOR (violation #4)
- Infrastructure/DI: MINOR (violation #5)
- Interfaces/CLI: ISSUES (violation #3)

### Gap 7 — Implementation-level fix guidance in REMEDIATION-HARDENING Priority List

The full report's "REMEDIATION-HARDENING Priority List" contains specific implementation steps not in MEMORY.md. Notably:
- Violations #1 and #2 require creating new port files in `application/ports/` AND new DI factories in `infrastructure/di/` — this is a multi-file change
- Violation #3 is described as "the single highest-impact fix" with rationale: without it, the entire pipeline runs with `None` adapters
- Violation #5 specifies `@lru_cache(maxsize=1)` (not just `@lru_cache`) — exact decorator form matters

---

## Section 4: Notes & Recommendations

### N1 — Executive Summary systemic characterization (not in any reference)

The full report identifies two **systemic** issues underlying all 7 violations:
1. Two application use cases bypass the port pattern (violations #1, #2)
2. The CLI entry point does not wire any DI adapters (violation #3)

These are architectural root causes. Fixing them in isolation (per-violation) is correct, but Stage 4 agents should treat them as a systemic gap, not 3 independent bugs.

### N2 — Violation #3 consequence is severe and runtime-invisible

The report explicitly states: the pipeline "runs structurally but performs no real enrichment, authorization, DLP, or classification." This is a silent failure — no crash, no exception, just `None`-port execution. If any test suite was passing by mocking or skipping the pipeline run, this failure would be invisible. Stage 4 agents writing tests must include an integration test that validates non-None adapters at runtime.

### N3 — Violation #5 has a compounding effect (not just performance)

Three separate `OpenFGAAdapter` instances means three separate HTTP connection pools AND three separate circuit breaker state machines. Under load, this causes inconsistent authorization behavior — one instance may open its circuit breaker while the other two remain closed. This is a correctness issue, not merely a resource waste issue.

### N4 — Strengths inventory for Stage 4 (do not re-audit these)

The full report explicitly certifies the following as working correctly:
- Domain layer purity: 20/20 files clean, zero outward imports
- Port definitions: 6/6 pure abstract interfaces with `TYPE_CHECKING` guards
- Adapter SDK encapsulation: no external library types leak into public method signatures
- `PipelineGraphBuilder` constructor injection: accepts all 8 ports, creates closures correctly
- DI container design: factory functions return port types, use `@lru_cache`, read from `Settings`

Stage 4 code-implementer agents should not touch these areas unless a violation fix requires it.

### N5 — SanitizeVulnerabilityUseCase orphan (STAGE-1 #2) still unexamined

STAGE-2 explicitly states "Not examined — outside STAGE-2 scope." This use case is potentially dead code (`dlp_node` is the real DLP execution path). Stage 4 must assign an agent to confirm and remove it, or re-integrate it if valid.

### N6 — `ingest_node.py` path discrepancy must be resolved before Stage 4

MEMORY.md says `nodes/ingest_node.py`; the full report says `ingest_node.py` (no `nodes/`). Stage 4 agents must glob the actual file path before applying the fix. Do not assume either path without verification.

### N7 — Violation #7 is non-blocking but creates Phase 7 risk

`edges.py` domain logic leak is LOW severity and non-blocking for Phase 7. However, Phase 7 adds a Streamlit human-review layer that will likely interact with escalation logic. If threshold policy stays in `edges.py`, Phase 7 integration tests may need to mock the orchestration layer to test thresholds — a layering violation that compounds over time. Recommend fixing before Phase 7, not after.
