# Stage 4.1 Truth Document — Agent A3: Agent Definitions Batch 3
> Status: COMPLETE
> Files: smoke-test-runner, static-checks-agent, test-generator, best-practices-enforcer, security-auditor

---

## UNCLEAR Items (global)

UNCLEAR-1: Valid values for `memory` field. Round 1 findings list "memory" as one of 13 valid schema fields but do not specify allowed values. Source files use `memory: project` (smoke-test-runner, test-generator) and `memory: true` (best-practices-enforcer, security-auditor). Both forms are present; neither confirmed nor denied by Round 1 findings.

---

### smoke-test-runner.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/smoke-test-runner.md
**Target:** /Users/bruno/siopv/.claude/agents/smoke-test-runner.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode — all in official 13-field schema.
- `memory: project` — value validity UNCLEAR (see UNCLEAR-1 above).
- No color field. No capabilities field. No missing required fields (name, description present).

**Action:**

Change 1 — Project Context section (body lines 11–23): Replace entire meta-project context block.
```
OLD:
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**.
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run python -c "import siopv"
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project).

NEW:
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run python -c "import siopv"
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Step 1 "Resolve Target Project" bash block: Replace `.build/active-project` discovery with fixed path.
```
OLD:
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "FAIL: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"

NEW:
TARGET="/Users/bruno/siopv"
echo "Target project: $TARGET"
```

Change 3 — Step 9 "Save Report" bash block: Remove `.build/current-phase` dependency; use absolute path.
```
OLD:
PHASE=$(cat .build/current-phase 2>/dev/null || echo "0")
TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
REPORT_DIR=".ignorar/production-reports/smoke-test-runner/phase-${PHASE}"
mkdir -p "$REPORT_DIR"
REPORT_PATH="${REPORT_DIR}/${TIMESTAMP}-phase-${PHASE}-smoke-test-runner-smoke-test.md"

NEW:
TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
REPORT_DIR="/Users/bruno/siopv/.ignorar/production-reports/smoke-test-runner"
mkdir -p "$REPORT_DIR"
REPORT_PATH="${REPORT_DIR}/${TIMESTAMP}-smoke-test-runner-smoke-test.md"
```

Change 4 — Report Persistence section directory path: Update relative path to absolute.
```
OLD:
.ignorar/production-reports/smoke-test-runner/phase-{N}/

NEW:
/Users/bruno/siopv/.ignorar/production-reports/smoke-test-runner/
```

**Obsolete content:**
- `.build/active-project` discovery logic (meta-project build artifact).
- `.build/current-phase` reference (meta-project build artifact).
- `sec-llm-workbench/.ignorar/production-reports/` path reference.

**Cross-file dependencies:** None (no `.claude/` file references in body).

---

### static-checks-agent.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/static-checks-agent.md
**Target:** /Users/bruno/siopv/.claude/agents/static-checks-agent.md
**Classification:** COPY AS-IS

**Frontmatter check:**
- Fields present: name, description, tools, model, disallowedTools — all in official 13-field schema.
- No color field. No capabilities field. No missing required fields.

**Action:** Copy source to target unchanged. No color field present to remove.

**Obsolete content:** None. Body uses `{PROJECT_PATH}`, `{REPORT_PATH}`, `{FIXED_TIMESTAMP}` as invocation-time placeholders — not hardcoded meta-project paths. No `sec-llm-workbench` references in body.

**Cross-file dependencies:** None.

---

### test-generator.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/test-generator.md
**Target:** /Users/bruno/siopv/.claude/agents/test-generator.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode — all in official 13-field schema.
- `memory: project` — value validity UNCLEAR (see UNCLEAR-1 above).
- No color field. No capabilities field. No missing required fields.

**Action:**

Change 1 — Project Context section (body lines 11–26): Replace entire meta-project context block.
```
OLD:
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Write, Glob, Grep) must target the **target project**, not the meta-project
- All git operations (`git add`, `git commit`, `git status`, `git diff`) must run from the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run pytest tests/ --cov=src
  ```
- Never commit target project code to `sec-llm-workbench/`
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project) — this is the only thing that stays in the meta-project

NEW:
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Write, Glob, Grep) target the SIOPV project directory.
- All git operations must run from `/Users/bruno/siopv/`.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run pytest tests/ --cov=src
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Report Persistence section directory path:
```
OLD:
.ignorar/production-reports/test-generator/phase-{N}/

NEW:
/Users/bruno/siopv/.ignorar/production-reports/test-generator/
```

**Obsolete content:**
- `.build/active-project` discovery logic.
- `sec-llm-workbench/.ignorar/production-reports/` path reference.
- "Never commit target project code to `sec-llm-workbench/`" line.

**Cross-file dependencies:** None (no `.claude/` file references in body).

---

### best-practices-enforcer.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/best-practices-enforcer.md
**Target:** /Users/bruno/siopv/.claude/agents/best-practices-enforcer.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools, skills — all in official 13-field schema.
- `memory: true` — boolean value.
- `skills: [coding-standards-2026]` — valid schema field. Skill must exist in SIOPV's skills system for this to work; carry forward as-is (skill resolution is runtime concern, not frontmatter schema violation).
- No color field. No capabilities field. No missing required fields.

**Action:**

Change 1 — Project Context section (body lines 12–26): Replace entire meta-project context block.
```
OLD:
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run ruff check src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)

NEW:
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run ruff check src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Tool Invocation section reference to meta-project doc (body line 172):
```
OLD:
**Reference:** For complete tool schemas, parameter definitions, and agent-specific examples, read `.claude/docs/agent-tool-schemas.md` on demand

NEW:
(Remove this line entirely — doc does not exist in SIOPV)
```

Change 3 — Report Persistence section directory path:
```
OLD:
.ignorar/production-reports/best-practices-enforcer/phase-{N}/

NEW:
/Users/bruno/siopv/.ignorar/production-reports/best-practices-enforcer/
```

**Obsolete content:**
- `.build/active-project` discovery logic.
- `sec-llm-workbench/.ignorar/production-reports/` path reference.
- Reference to `.claude/docs/agent-tool-schemas.md` (meta-project-only doc).

**Cross-file dependencies:** `.claude/docs/agent-tool-schemas.md` — referenced in Tool Invocation section. This doc is meta-project-only; line must be removed (see Change 2).

---

### security-auditor.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/security-auditor.md
**Target:** /Users/bruno/siopv/.claude/agents/security-auditor.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all in official 13-field schema.
- `memory: true` — boolean value.
- No color field. No capabilities field. No missing required fields.

**Action:**

Change 1 — Project Context section (body lines 11–25): Replace entire meta-project context block.
```
OLD:
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run bandit -r src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)

NEW:
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run bandit -r src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Self-consistency voting experimental script path (body, sys.path.insert line):
```
OLD:
sys.path.insert(0, '.ignorar/experimental-scripts/self-consistency-voting')

NEW:
sys.path.insert(0, '/Users/bruno/siopv/.ignorar/experimental-scripts/self-consistency-voting')
```
NOTE: This script does not currently exist in SIOPV. The section is already labeled EXPERIMENTAL and non-production. Carry the section forward with updated path; the script must be separately copied from meta-project to SIOPV if used.

Change 3 — Report Persistence section directory path:
```
OLD:
.ignorar/production-reports/security-auditor/phase-{N}/

NEW:
/Users/bruno/siopv/.ignorar/production-reports/security-auditor/
```

**Obsolete content:**
- `.build/active-project` discovery logic.
- `sec-llm-workbench/.ignorar/production-reports/` path reference.

**Cross-file dependencies:** None (no `.claude/` file references in body). Experimental script at `.ignorar/experimental-scripts/self-consistency-voting/self-consistency-vote.py` is outside `.claude/` — not a cross-file dependency per task definition, but requires manual copy to SIOPV if used.

---

## Classification Summary

| Agent | Classification | Reason |
|-------|---------------|--------|
| smoke-test-runner | ADAPT | Meta-project context section; `.build/` paths |
| static-checks-agent | COPY AS-IS | No meta-project paths; placeholders only |
| test-generator | ADAPT | Meta-project context section; `.build/` paths |
| best-practices-enforcer | ADAPT | Meta-project context section; `.claude/docs/` reference |
| security-auditor | ADAPT | Meta-project context section; experimental script path |

**Total: 1 COPY AS-IS, 4 ADAPT**
