# Stage 4.1 Truth Document — Agent A4: Adaptable Agent Definitions
> Status: COMPLETE
> Files: code-reviewer (SIOPV-applicable), xai-explainer, researcher-1, researcher-2, researcher-3 (ADAPTABLE)

---

## Preamble

All findings below are derived exclusively from the 5 source files listed in the task and the Round 1 findings provided inline. No external files were read.

Official 13-field schema (from Round 1): name, description, tools, disallowedTools, model, permissionMode, maxTurns, skills, mcpServers, hooks, memory, background, isolation.
Valid model values: sonnet, opus, haiku, inherit.
Obsolete fields (do NOT carry): color (UI-generated only), capabilities (internal/plugin-dev only).

---

### code-reviewer.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/code-reviewer.md
**Target:** /Users/bruno/siopv/.claude/agents/code-reviewer.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools
- All 7 fields are in the 13-field official schema. No non-schema fields in frontmatter.
- `memory: true` — valid value.
- `permissionMode: plan` — valid.
- Pre-frontmatter line 1: `<!-- version: 2026-02 -->` — HTML comment outside frontmatter block. Not a frontmatter field. Not in official schema. Obsolete — do NOT carry to SIOPV version.
- No `color` field present.
- No `capabilities` field present.

**Reason for ADAPT:** System prompt contains 4 meta-project-specific references that must be replaced for SIOPV use.

**Action — exact diffs:**

Change 1 (Project Context section — lines 12–24):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run radon cc src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are being invoked for the **SIOPV project** (`/Users/bruno/siopv/`).

- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/src/siopv/`
- All `uv run` commands must use `cd /Users/bruno/siopv && uv run radon cc src/`
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

Change 2 (Report Persistence — Directory):

OLD:
```
.ignorar/production-reports/code-reviewer/phase-{N}/
```

NEW:
```
/Users/bruno/siopv/.ignorar/production-reports/code-reviewer/phase-{N}/
```

**Obsolete content:**
- Pre-frontmatter `<!-- version: 2026-02 -->` comment — do not carry.
- `.build/active-project` dynamic path discovery logic — not applicable in SIOPV (project path is fixed).

**Cross-file dependencies:**
- References `uv run radon cc` — assumes uv toolchain present in SIOPV project. Not a file dependency.
- No references to other `.claude/` files.

---

### xai-explainer.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/xai-explainer.md
**Target:** /Users/bruno/siopv/.claude/agents/xai-explainer.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode
- All 6 fields are in the 13-field official schema.
- `memory: project` — UNCLEAR: Round 1 findings documented `memory: true` for code-reviewer. The value "project" is not documented in Round 1 findings. Cannot confirm it is valid per official schema.
- `permissionMode: acceptEdits` — valid.
- `model: sonnet` — valid.
- No `color` field present.
- No `capabilities` field present.

**Action — exact diffs (per adaptation spec):**

Change 1 (frontmatter description):

OLD:
```
description: Invoke to generate ML model explanations using SHAP (global) and LIME (local) for audit and transparency requirements
```

NEW:
```
description: Invoke to generate ML model explanations using SHAP (global) and LIME (local) for SIOPV XGBoost classifier (src/siopv/adapters/ml/) audit and transparency — Phase 3
```

Change 2 (system prompt — model path):

OLD:
```
model_path = Path("models/xgboost_classifier.json")
```

NEW:
```
model_path = Path("src/siopv/adapters/ml/models/xgboost_classifier.json")
```

Change 3 (system prompt — section header after model path check):

OLD:
```
raise ModelNotFoundError("Train model first")
```

NEW:
```
raise ModelNotFoundError("Train SIOPV XGBoost classifier first (Phase 3)")
```

Change 4 (system prompt — report directory):

OLD:
```
.ignorar/production-reports/xai-explainer/phase-{N}/
```

NEW:
```
/Users/bruno/siopv/.ignorar/production-reports/xai-explainer/phase-{N}/
```

Change 5 (system prompt — naming convention):
The xai-explainer uses sequential `{NNN}` numbering, inconsistent with the timestamp-based convention used by code-reviewer (confirmed in Round 1 via code-reviewer source). Per adaptation spec, "replace meta-project-specific paths" — the naming convention is a meta-project pattern. Align to timestamp-based standard.

OLD:
```
{NNN}-phase-{N}-xai-explainer-{descriptive-slug}.md
```

NEW:
```
{TIMESTAMP}-phase-{N}-xai-explainer-{descriptive-slug}.md
```

OLD (How to Determine Next Number section):
```
### How to Determine Next Number
1. List files in `.ignorar/production-reports/xai-explainer/phase-{N}/`
2. Find the highest existing number
3. Increment by 1 (or start at 001 if empty)
```

NEW:
```
### How to Determine Timestamp
Use: `TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)`
```

Change 6 (system prompt — examples under naming convention):

OLD:
```
- `001-phase-3-xai-explainer-shap-global-analysis.md`
- `002-phase-3-xai-explainer-lime-cve-2024-1234.md`
```

NEW:
```
- `2026-03-12-181925-phase-3-xai-explainer-shap-global-analysis.md`
- `2026-03-12-182030-phase-3-xai-explainer-lime-cve-2024-1234.md`
```

**Obsolete content:**
- None in frontmatter.
- Sequential `{NNN}` numbering logic — replaced by timestamp-based (see Change 5).

**Cross-file dependencies:** None — no references to other `.claude/` files.

**UNCLEAR items:**
- `memory: project` value not validated by Round 1 findings — carry as-is pending orchestrator clarification.
- `class_names=["LOW", "MEDIUM", "HIGH", "CRITICAL"]` in LIME explainer — these are SIOPV domain risk levels and appear correct for Phase 3 XGBoost classifier. No contradicting source available to verify against SIOPV feature schema. Carry as-is.
- Actual model artifact path inside `src/siopv/adapters/ml/` — not verifiable without reading SIOPV source files (out of scope for this agent). Path `src/siopv/adapters/ml/models/xgboost_classifier.json` is derived from adaptation spec only.

---

### researcher-1.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/researcher-1.md
**Target:** /Users/bruno/siopv/.claude/agents/researcher-1.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, model, description, tools (YAML list format)
- All fields are in the 13-field official schema.
- No `color` field present.
- No `capabilities` field present.
- `tools` uses YAML list format (multi-line) — valid.
- Missing from official schema (not present, not required): disallowedTools, permissionMode, maxTurns, skills, mcpServers, hooks, memory, background, isolation — all optional.

**Action — exact diffs (per adaptation spec):**

Change 1 (frontmatter description):

OLD:
```
description: "Deep online researcher for testing frameworks, integration testing, and contract testing patterns for async Python projects"
```

NEW:
```
description: "Deep online researcher for integration testing and contract testing patterns for SIOPV async stack (LangGraph, httpx, Presidio, OpenFGA)"
```

Change 2 (system prompt — coordinator reference):

OLD:
```
When done, send a message to "claude-main" confirming completion.
```

NEW:
```
When done, send a message to "orchestrator" confirming completion.
```

**Obsolete content:** None in frontmatter. `"claude-main"` coordinator name is meta-project specific — replaced in Change 2.

**Cross-file dependencies:** None — no references to other `.claude/` files.

---

### researcher-2.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/researcher-2.md
**Target:** /Users/bruno/siopv/.claude/agents/researcher-2.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, model, description, tools (YAML list format)
- All fields are in the 13-field official schema.
- No `color` field present.
- No `capabilities` field present.
- Same optional-field omissions as researcher-1 — all optional, no issue.

**Action — exact diffs (per adaptation spec):**

Change 1 (frontmatter description):

OLD:
```
description: "Deep online researcher for observability, tracing, OpenTelemetry, and runtime monitoring patterns for async Python projects"
```

NEW:
```
description: "Deep online researcher for OTel, LangSmith, and structlog patterns for SIOPV pipeline (FastAPI, httpx, SQLAlchemy)"
```

Change 2 (system prompt — coordinator reference):

OLD:
```
When done, send a message to "claude-main" confirming completion.
```

NEW:
```
When done, send a message to "orchestrator" confirming completion.
```

**Obsolete content:** `"claude-main"` coordinator name — replaced in Change 2.

**Cross-file dependencies:** None.

---

### researcher-3.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/researcher-3.md
**Target:** /Users/bruno/siopv/.claude/agents/researcher-3.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, model, description, tools (YAML list format)
- All fields are in the 13-field official schema.
- No `color` field present.
- No `capabilities` field present.
- Same optional-field omissions as researcher-1/2 — all optional, no issue.

**Action — exact diffs (per adaptation spec):**

Change 1 (frontmatter description):

OLD:
```
description: "Deep online researcher for CI/CD pipelines, smoke testing, chaos engineering, and Docker Compose test environments"
```

NEW:
```
description: "Deep online researcher for CI/CD pipelines and Docker Compose for SIOPV stack (OpenFGA, Keycloak, Postgres, ChromaDB)"
```

Change 2 (system prompt — coordinator reference):

OLD:
```
When done, send a message to "claude-main" confirming completion.
```

NEW:
```
When done, send a message to "orchestrator" confirming completion.
```

**Obsolete content:** `"claude-main"` coordinator name — replaced in Change 2.

**Cross-file dependencies:** None.

---

## Summary

| File | Classification | Changes Required | UNCLEAR Items |
|------|----------------|-----------------|---------------|
| code-reviewer.md | ADAPT | 2 (Project Context section, report path) | None |
| xai-explainer.md | ADAPT | 6 (description, model path, error msg, report dir, naming convention, examples) | 3 (memory value, class_names, model artifact path) |
| researcher-1.md | ADAPT | 2 (description, coordinator name) | None |
| researcher-2.md | ADAPT | 2 (description, coordinator name) | None |
| researcher-3.md | ADAPT | 2 (description, coordinator name) | None |

**Total:** 0 COPY AS-IS, 5 ADAPT

## Global Obsolete Patterns (apply to all 5)

- `<!-- version: 2026-02 -->` pre-frontmatter comment: present only in code-reviewer.md — do not carry.
- `color` field: not present in any of the 5 files — no action needed.
- `capabilities` field: not present in any of the 5 files — no action needed.
- `"claude-main"` coordinator reference: present in researcher-1/2/3 — updated to `"orchestrator"` in all three.

> Status: COMPLETE
