# Stage 4.1 Truth Document — Section A: Agent Definitions + Adaptable Hooks
> Files covered: A1 (5 agents), A2 (5 agents), A3 (5 agents), A4 (5 agents), B1 (4 hooks)
> MERGE decisions applied: MERGE-001-A, MERGE-002-A, MERGE-003-B, MERGE-004-B, MERGE-005-B, MERGE-006-A
> Status: COMPLETE

---

## Domain A1 — Agent Definitions Batch 1
> Source agents: async-safety-auditor, circular-import-detector, code-implementer, config-validator, dependency-scanner

---

### async-safety-auditor.md
**Target path:** `/Users/bruno/siopv/.claude/agents/async-safety-auditor.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/async-safety-auditor.md`

**Action:**
Replace the entire "Project Context (CRITICAL)" section (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "asyncio.run" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands **must use `cd` with the target path**:
  ```bash
  TARGET="/Users/bruno/siopv"
  cd "$TARGET" && grep -rn "asyncio.run" src/
  ```
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Additionally, update all inline bash blocks that reference `$(cat .build/active-project)` or `sec-llm-workbench/.ignorar/`:
- Replace `TARGET=$(cat .build/active-project)` + `TARGET="${TARGET/#\~/$HOME}"` preamble with `TARGET="/Users/bruno/siopv"` in every bash block (lines 42–43, 48–49, 63–69, 86–91, 100–103 in source).
- Replace `sec-llm-workbench/.ignorar/production-reports/` → `.ignorar/production-reports/` (relative to SIOPV root) in the report path line (line 138 in source).

**Dependencies:** None.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No SIOPV-specific files referenced.

---

### circular-import-detector.md
**Target path:** `/Users/bruno/siopv/.claude/agents/circular-import-detector.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/circular-import-detector.md`

**Action:**
Replace the "Project Context (CRITICAL)" section (lines 12–17 in source):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project.
- Target project path is provided in your invocation prompt or read from `.build/active-project`.
- Expand `~` to `$HOME` before use.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project).
```

NEW:
```
## Project Context (CRITICAL)

- You are working on the **SIOPV project** at `/Users/bruno/siopv/`.
- Target project path: `/Users/bruno/siopv/`
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`).
```

Replace Action step 1 (line 28):
- OLD: `1. Read `.build/active-project` to get TARGET. Expand `~` to `$HOME`.`
- NEW: `1. Set TARGET="/Users/bruno/siopv"`

Update inline bash in step 3 (lines 36–37):
- OLD: `cd "$TARGET" && python3 - <<'PYEOF'` (where TARGET was read from `.build/active-project`)
- NEW: `TARGET="/Users/bruno/siopv" && cd "$TARGET" && python3 - <<'PYEOF'`

Report path line is already expressed as relative `.ignorar/...` — no absolute path change needed.

**Dependencies:** None.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No SIOPV-specific files referenced.

---

### code-implementer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/code-implementer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/code-implementer.md`

**Action:**
Replace the "Project Context (CRITICAL)" section (lines 12–29 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Write, Edit, Glob, Grep) must target the **target project**, not the meta-project
- All git operations **must use `git -C`** with the expanded target path — never rely on the current working directory:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  git -C "$TARGET" add <file>
  git -C "$TARGET" commit -m "..."
  git -C "$TARGET" status
  ```
- All `uv run` commands (ruff, mypy, pytest) must run from the **target project directory** using `cd "$TARGET" && uv run ...`
- Never commit target project code to `sec-llm-workbench/`
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project) — this is the only thing that stays in the meta-project
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Write, Edit, Glob, Grep) must target `/Users/bruno/siopv/`
- All git operations **must use `git -C`** with the target path:
  ```bash
  TARGET="/Users/bruno/siopv"
  git -C "$TARGET" add <file>
  git -C "$TARGET" commit -m "..."
  git -C "$TARGET" status
  ```
- All `uv run` commands (ruff, mypy, pytest) must run from the target directory: `cd "$TARGET" && uv run ...`
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Update "Before Writing Code" step 2 (line 49):
- OLD: `2. **Read `.claude/docs/python-standards.md`** → Document standards applied`
- NEW: `2. **Read `.claude/docs/python-standards.md`** (in SIOPV project root) → Document standards applied (create in Stage 4.2 if not present)`

Update "Before Writing Code" step 3 (line 50):
- OLD: `3. **Read `.claude/rules/tech-stack.md`** → Document rules applied`
- NEW: `3. **Read `.claude/rules/tech-stack.md`** (in SIOPV project root) → Document rules applied (create in Stage 4.2 if not present)`

Update Execution Checklist step 10 (line 434):
- OLD: `10. ☐ Save report to `.ignorar/production-reports/code-implementer/phase-{N}/{NNN}-{slug}.md``
- NEW: `10. ☐ Save report to `.ignorar/production-reports/code-implementer/phase-{N}/{TIMESTAMP}-phase-{N}-code-implementer-{slug}.md``

**MERGE-002-A applied:** References to `.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md` kept; note "(create in Stage 4.2 if not present)" added to both.

**Dependencies:**
- `/Users/bruno/siopv/.claude/docs/python-standards.md` — referenced in body; Stage 4.2 must verify or create.
- `/Users/bruno/siopv/.claude/rules/tech-stack.md` — referenced in body; Stage 4.2 must verify or create.

**Deployment notes:** Deploy only after Stage 4.2 confirms or creates the two dependency files. The agent body references them at runtime — missing files cause a soft failure (agent will note the absence) but not a hard crash.

---

### config-validator.md
**Target path:** `/Users/bruno/siopv/.claude/agents/config-validator.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/config-validator.md`

**Action:**
Replace the "Project Context (CRITICAL)" section (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "settings\." src/ --include="*.py"
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All commands **must use `cd` with the target path**:
  ```bash
  TARGET="/Users/bruno/siopv"
  cd "$TARGET" && grep -rn "settings\." src/ --include="*.py"
  ```
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Replace Action step 1 bash block (lines 42–48):

OLD:
```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "ERROR: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
```

NEW:
```bash
TARGET="/Users/bruno/siopv"
if [ ! -d "$TARGET" ]; then
    echo "ERROR: SIOPV directory not found at $TARGET"
    exit 1
fi
```

**Dependencies:** None.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No SIOPV-specific files referenced.

---

### dependency-scanner.md
**Target path:** `/Users/bruno/siopv/.claude/agents/dependency-scanner.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/dependency-scanner.md`

**Action:**
Replace the "Project Context (CRITICAL)" section (lines 11–17 in source):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project itself.
- Target project path is provided in your invocation prompt. If not provided, read `.build/active-project`.
- Expand `~` to `$HOME` in all paths before use.
- All `uv run` and `cd` commands must use the fully expanded target project path.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project directory).
```

NEW:
```
## Project Context (CRITICAL)

- You are working on the **SIOPV project** at `/Users/bruno/siopv/`.
- Target project path: `/Users/bruno/siopv/`
- All `uv run` and `cd` commands must use `/Users/bruno/siopv/` as the working directory.
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`).
```

Replace Action step 1 (line 29):
- OLD: `1. Read `.build/active-project` to get TARGET. Run: `TARGET=$(cat .build/active-project); TARGET="${TARGET/#\~/$HOME}"``
- NEW: `1. Set `TARGET="/Users/bruno/siopv"``

Replace Action step 7 — `.build/current-phase` reference (line 57):
- OLD: `where {N} = content of `.build/current-phase`, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
- NEW: `where {N} = phase number provided in your invocation prompt, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``

**Dependencies:** None. `.build/current-phase` is removed as obsolete.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No SIOPV-specific files referenced.

---

## Domain A2 — Agent Definitions Batch 2
> Source agents: hallucination-detector, import-resolver, integration-tracer, regression-guard, semantic-correctness-auditor

---

### hallucination-detector.md
**Target path:** `/Users/bruno/siopv/.claude/agents/hallucination-detector.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/hallucination-detector.md`

**Action:**
Carry the HTML comment `<!-- version: 2026-02 -->` above the frontmatter opener as-is (it is outside the frontmatter block).

Replace the entire "Project Context (CRITICAL)" block (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run mypy src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && uv run mypy src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

All other body content (Role Definition, Verification Process, Tool Invocation, Role Reinforcement, Actions, Report Persistence, Report Format) is generic — carry unchanged.

**Dependencies:** References `mcp__context7__resolve-library-id` and `mcp__context7__query-docs` (MCP tools, not `.claude/` file references).
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists.

---

### import-resolver.md
**Target path:** `/Users/bruno/siopv/.claude/agents/import-resolver.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/import-resolver.md`

**Action:**
Carry the HTML comment `<!-- version: 2026-03 -->` above the frontmatter opener as-is.

Three changes required:

**Change 1** — Replace "Project Context (CRITICAL)" block (lines 12–19 in source):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project.
- Target project path is provided in your invocation prompt or read from `.build/active-project`.
- Expand `~` to `$HOME` before use.
- All `uv run` and `cd` commands must use the fully expanded target project path.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project directory).
```

NEW:
```
## Project Context (CRITICAL)

- You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).
- Target project path is `/Users/bruno/siopv`. Use `TARGET=/Users/bruno/siopv` in all commands.
- All `uv run` and `cd` commands must use `/Users/bruno/siopv` as the project path.
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

**Change 2** — Replace Step 1 of Actions (lines 35–38 in source):

OLD:
```
1. Read `.build/active-project` to get TARGET. Run:
   ```bash
   TARGET=$(cat .build/active-project); TARGET="${TARGET/#\~/$HOME}"
   ```
```

NEW:
```
1. Set TARGET to the SIOPV project path:
   ```bash
   TARGET=/Users/bruno/siopv
   ```
```

**Change 3** — Replace report path reference in Step 9 (line 158 in source):

OLD: `where {N} = content of `.build/current-phase`, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
NEW: `where {N} = current SIOPV phase number (provide at invocation time), {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``

All other body content (Role Definition, import skipping rules, AST extraction script, resolution logic, PASS/FAIL criteria, Report Format) is generic — carry unchanged.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists.

---

### integration-tracer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/integration-tracer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/integration-tracer.md`

**Action:**
Carry the HTML comment `<!-- version: 2026-03 -->` above the frontmatter opener as-is.

Replace the entire "Project Context (CRITICAL)" block (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "add_node" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && grep -rn "add_node" src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

All other body content (Role Definition, AST-Based Call Graph, Verification Checklist, PASS/FAIL Criteria, Findings Severity, Actions, Report Persistence, Report Format) is generic — carry unchanged.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists.

---

### regression-guard.md
**Target path:** `/Users/bruno/siopv/.claude/agents/regression-guard.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/regression-guard.md`

**Action:**
Carry the HTML comment `<!-- version: 2026-03 -->` above the frontmatter opener as-is.
Note: `permissionMode: default` (differs from most other agents that use `plan`) — carry as-is, do not change.
Note: `disallowedTools` field is absent — valid (optional field). Do NOT add it.

Replace the "Project Context (CRITICAL)" block (lines 12–23 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) and git commands must target the **target project directory**.
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run pytest ...
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project), NOT the target project.
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) and git commands must target `/Users/bruno/siopv/`.
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && uv run pytest ...
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Replace Step 1 of Actions (lines 40–47 in source):

OLD:
```
### 1. Read active project path

```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "ERROR: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```
```

NEW:
```
### 1. Set project path

```bash
TARGET=/Users/bruno/siopv
if [ ! -d "$TARGET" ]; then
    echo "ERROR: SIOPV project directory not found: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```
```

All other body content (Role Definition, Steps 2–7, PASS/FAIL Criteria, Findings Severity, Report Format, Report Persistence) is generic — carry unchanged.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists.

---

### semantic-correctness-auditor.md
**Target path:** `/Users/bruno/siopv/.claude/agents/semantic-correctness-auditor.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/semantic-correctness-auditor.md`

**Action:**
Carry the HTML comment `<!-- version: 2026-02 -->` above the frontmatter opener as-is.

Replace the "Project Context (CRITICAL)" block (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "field_validator" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && grep -rn "field_validator" src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

All other body content (Role Definition, Verification Checklist, PASS/FAIL Criteria, Findings Severity, Actions, Report Persistence, Report Format) is generic — carry unchanged.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists.

---

## Domain A3 — Agent Definitions Batch 3
> Source agents: smoke-test-runner, static-checks-agent, test-generator, best-practices-enforcer, security-auditor

---

### smoke-test-runner.md
**Target path:** `/Users/bruno/siopv/.claude/agents/smoke-test-runner.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/smoke-test-runner.md`

**Action:**
Note: `memory: project` — carry as-is per MERGE-001-A. Do not standardize.

**Change 1** — Replace Project Context section (body lines 11–23):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**.
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run python -c "import siopv"
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project).
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run python -c "import siopv"
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

**Change 2** — Replace Step 1 "Resolve Target Project" bash block:

OLD:
```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "FAIL: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```

NEW:
```bash
TARGET="/Users/bruno/siopv"
echo "Target project: $TARGET"
```

**Change 3** — Replace Step 9 "Save Report" bash block:

OLD:
```bash
PHASE=$(cat .build/current-phase 2>/dev/null || echo "0")
TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
REPORT_DIR=".ignorar/production-reports/smoke-test-runner/phase-${PHASE}"
mkdir -p "$REPORT_DIR"
REPORT_PATH="${REPORT_DIR}/${TIMESTAMP}-phase-${PHASE}-smoke-test-runner-smoke-test.md"
```

NEW:
```bash
TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
REPORT_DIR="/Users/bruno/siopv/.ignorar/production-reports/smoke-test-runner"
mkdir -p "$REPORT_DIR"
REPORT_PATH="${REPORT_DIR}/${TIMESTAMP}-smoke-test-runner-smoke-test.md"
```

**Change 4** — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/smoke-test-runner/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/smoke-test-runner/`

Also remove the meta-project annotation comment from line 5: `# Este hook se ejecuta en el PROYECTO DESTINO, no en el META-PROYECTO`

**Dependencies:** None (no `.claude/` file references in body).
**Deployment notes:** `.build/current-phase` is a meta-project artifact — removed. Phase number no longer appears in the report path for this agent.

---

### static-checks-agent.md
**Target path:** `/Users/bruno/siopv/.claude/agents/static-checks-agent.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/static-checks-agent.md`

**Action:** Copy unchanged. Body uses `{PROJECT_PATH}`, `{REPORT_PATH}`, `{FIXED_TIMESTAMP}` as invocation-time placeholders — not hardcoded meta-project paths. No `sec-llm-workbench` references in body. No `color` field present to remove.

**Dependencies:** None.
**Deployment notes:** No changes required. Placeholders are filled at invocation time by the orchestrator.

---

### test-generator.md
**Target path:** `/Users/bruno/siopv/.claude/agents/test-generator.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/test-generator.md`

**Action:**
Note: `memory: project` — carry as-is per MERGE-001-A. Do not standardize.

**Change 1** — Replace Project Context section (body lines 11–26):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Write, Glob, Grep) must target the **target project**, not the meta-project
- All git operations (`git add`, `git commit`, `git status`, `git diff`) must run from the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run pytest tests/ --cov=src
  ```
- Never commit target project code to `sec-llm-workbench/`
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project) — this is the only thing that stays in the meta-project
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Write, Glob, Grep) target the SIOPV project directory.
- All git operations must run from `/Users/bruno/siopv/`.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run pytest tests/ --cov=src
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

**Change 2** — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/test-generator/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/test-generator/`

**Dependencies:** None (no `.claude/` file references in body).
**Deployment notes:** "Never commit target project code to `sec-llm-workbench/`" line is obsolete — remove it.

---

### best-practices-enforcer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/best-practices-enforcer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/best-practices-enforcer.md`

**Action:**
Note: `memory: true` — carry as-is.
Note: `skills: [coding-standards-2026]` — carry as-is; skill resolution is a runtime concern.

**Change 1** — Replace Project Context section (body lines 12–26):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run ruff check src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run ruff check src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

**Change 2** — Tool Invocation section reference to meta-project doc (body line 172):
- OLD: `**Reference:** For complete tool schemas, parameter definitions, and agent-specific examples, read `.claude/docs/agent-tool-schemas.md` on demand`
- NEW: Remove this line entirely — `.claude/docs/agent-tool-schemas.md` does not exist in SIOPV.

**Change 3** — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/best-practices-enforcer/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/best-practices-enforcer/`

**Dependencies:** `.claude/docs/agent-tool-schemas.md` — present in meta-project only; reference removed (Change 2).
**Deployment notes:** Remove the `agent-tool-schemas.md` reference line before deployment.

---

### security-auditor.md
**Target path:** `/Users/bruno/siopv/.claude/agents/security-auditor.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/security-auditor.md`

**Action:**
Note: `memory: true` — carry as-is.

**Change 1** — Replace Project Context section (body lines 11–25):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run bandit -r src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run bandit -r src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

**Change 2** — Self-consistency voting experimental script path (body, sys.path.insert line):
- OLD: `sys.path.insert(0, '.ignorar/experimental-scripts/self-consistency-voting')`
- NEW: `sys.path.insert(0, '/Users/bruno/siopv/.ignorar/experimental-scripts/self-consistency-voting')`
- NOTE: Script does not currently exist in SIOPV. Section is labeled EXPERIMENTAL and non-production. If used, the script must be separately copied from meta-project to `/Users/bruno/siopv/.ignorar/experimental-scripts/self-consistency-voting/`.

**Change 3** — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/security-auditor/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/security-auditor/`

**Dependencies:** Experimental script at `.ignorar/experimental-scripts/self-consistency-voting/self-consistency-vote.py` — outside `.claude/`; not a hard dependency (section is already labeled optional/experimental).
**Deployment notes:** Deploy in two steps: (1) the agent file itself (Change 1–3) which is safe immediately; (2) optionally copy the experimental script from meta-project to SIOPV if self-consistency voting is needed.

---

## Domain A4 — Adaptable Agent Definitions
> Source agents: code-reviewer (SIOPV-applicable), xai-explainer, researcher-1, researcher-2, researcher-3 (ADAPTABLE)

---

### code-reviewer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/code-reviewer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/code-reviewer.md`

**Action:**
Note: Pre-frontmatter `<!-- version: 2026-02 -->` HTML comment — do NOT carry to SIOPV version (omit entirely).

**Change 1** — Replace Project Context section (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run radon cc src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are being invoked for the **SIOPV project** (`/Users/bruno/siopv/`).

- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/src/siopv/`
- All `uv run` commands must use `cd /Users/bruno/siopv && uv run radon cc src/`
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

**Change 2** — Report Persistence directory path:
- OLD: `.ignorar/production-reports/code-reviewer/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/code-reviewer/phase-{N}/`

**Dependencies:** None to other `.claude/` files. References `uv run radon cc` — assumes uv toolchain present in SIOPV project.
**Deployment notes:** Remove the `<!-- version: 2026-02 -->` pre-frontmatter comment; do not carry it. Deploy after SIOPV `.claude/agents/` directory exists.

---

### xai-explainer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/xai-explainer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/xai-explainer.md`

**Action:**
Note: `memory: project` — carry as-is per MERGE-001-A. Do not standardize.
Note: `permissionMode: acceptEdits` — carry as-is.

**MERGE-005-B applied: BLOCK deployment until Stage 4.2 verifies `class_names` against `src/siopv/adapters/ml/` with a Glob read. Do not deploy this agent until that verification is complete.**

**Change 1** — Replace frontmatter description:
- OLD: `description: Invoke to generate ML model explanations using SHAP (global) and LIME (local) for audit and transparency requirements`
- NEW: `description: Invoke to generate ML model explanations using SHAP (global) and LIME (local) for SIOPV XGBoost classifier (src/siopv/adapters/ml/) audit and transparency — Phase 3`

**Change 2** — Replace model path in system prompt:
- OLD: `model_path = Path("models/xgboost_classifier.json")`
- NEW: `model_path = Path("src/siopv/adapters/ml/models/xgboost_classifier.json")`

**Change 3** — Replace error message in system prompt:
- OLD: `raise ModelNotFoundError("Train model first")`
- NEW: `raise ModelNotFoundError("Train SIOPV XGBoost classifier first (Phase 3)")`

**Change 4** — Replace report directory in system prompt:
- OLD: `.ignorar/production-reports/xai-explainer/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/xai-explainer/phase-{N}/`

**Change 5** — Replace sequential naming convention with timestamp-based:
- OLD: `{NNN}-phase-{N}-xai-explainer-{descriptive-slug}.md`
- NEW: `{TIMESTAMP}-phase-{N}-xai-explainer-{descriptive-slug}.md`

Replace "How to Determine Next Number" section:
OLD:
```
### How to Determine Next Number
1. List files in `.ignorar/production-reports/xai-explainer/phase-{N}/`
2. Find the highest existing number
3. Increment by 1 (or start at 001 if empty)
```
NEW:
```
### How to Determine Timestamp
Use: `TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)`
```

**Change 6** — Replace naming examples:
- OLD: `001-phase-3-xai-explainer-shap-global-analysis.md` / `002-phase-3-xai-explainer-lime-cve-2024-1234.md`
- NEW: `2026-03-12-181925-phase-3-xai-explainer-shap-global-analysis.md` / `2026-03-12-182030-phase-3-xai-explainer-lime-cve-2024-1234.md`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** BLOCKED until Stage 4.2 performs: `Glob("src/siopv/adapters/ml/**")` in SIOPV to verify (a) actual model artifact path and (b) that `class_names=["LOW", "MEDIUM", "HIGH", "CRITICAL"]` matches the XGBoost classifier's output classes. Do not deploy before this check.

---

### researcher-1.md
**Target path:** `/Users/bruno/siopv/.claude/agents/researcher-1.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-1.md`

**Action:**

**Change 1** — Replace frontmatter description:
- OLD: `description: "Deep online researcher for testing frameworks, integration testing, and contract testing patterns for async Python projects"`
- NEW: `description: "Deep online researcher for integration testing and contract testing patterns for SIOPV async stack (LangGraph, httpx, Presidio, OpenFGA)"`

**Change 2** — Replace coordinator reference in system prompt:
- OLD: `When done, send a message to "claude-main" confirming completion.`
- NEW: `When done, send a message to "orchestrator" confirming completion.`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No path changes needed — no hardcoded meta-project paths in body.

---

### researcher-2.md
**Target path:** `/Users/bruno/siopv/.claude/agents/researcher-2.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-2.md`

**Action:**

**Change 1** — Replace frontmatter description:
- OLD: `description: "Deep online researcher for observability, tracing, OpenTelemetry, and runtime monitoring patterns for async Python projects"`
- NEW: `description: "Deep online researcher for OTel, LangSmith, and structlog patterns for SIOPV pipeline (FastAPI, httpx, SQLAlchemy)"`

**Change 2** — Replace coordinator reference in system prompt:
- OLD: `When done, send a message to "claude-main" confirming completion.`
- NEW: `When done, send a message to "orchestrator" confirming completion.`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No path changes needed.

---

### researcher-3.md
**Target path:** `/Users/bruno/siopv/.claude/agents/researcher-3.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-3.md`

**Action:**

**Change 1** — Replace frontmatter description:
- OLD: `description: "Deep online researcher for CI/CD pipelines, smoke testing, chaos engineering, and Docker Compose test environments"`
- NEW: `description: "Deep online researcher for CI/CD pipelines and Docker Compose for SIOPV stack (OpenFGA, Keycloak, Postgres, ChromaDB)"`

**Change 2** — Replace coordinator reference in system prompt:
- OLD: `When done, send a message to "claude-main" confirming completion.`
- NEW: `When done, send a message to "orchestrator" confirming completion.`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** Deploy after SIOPV `.claude/agents/` directory exists. No path changes needed.

---

## Domain B1 — Adaptable Hooks
> Source hooks: pre-commit.sh, verify-best-practices.sh, post-code.sh, pre-git-commit.sh

---

### pre-commit.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-commit.sh`

**Action:**
No hardcoded `/Users/bruno/sec-llm-workbench/` literal strings found. Zero literal path surgery required.
Script uses only `PROJECT_DIR="${1:-.}"` (positional arg or cwd) and generic tool invocations (`uv run ruff`, `uv run mypy`, `uv run pytest`).

Single non-functional change: Remove line 5 annotation comment:
- OLD: `# Este hook se ejecuta en el PROYECTO DESTINO, no en el META-PROYECTO`
- NEW: (remove line — no behavioral impact)

**MERGE-004-B applied:** Register as manual-invocation utility only. Do NOT register in SIOPV `settings.json` as a lifecycle hook event. Document in SIOPV CLAUDE.md under "Manual Utilities" section.

Event type is UNCLEAR: script reads no stdin JSON; uses positional `$1` argument; not structured as a Claude Code lifecycle hook. May be invoked manually or called by another hook passing the project path.
No deprecated `decision` field found. Script produces no JSON output.

**Dependencies:** Requires `uv`, `ruff`, `mypy`, `pytest` installed in SIOPV environment. Self-contained — no other `.claude/` file references.
**Deployment notes:** Place at `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`. Do NOT add to `settings.json`. Add to SIOPV CLAUDE.md as: "Manual utility: run `.claude/hooks/pre-commit.sh <project_dir>` to validate Python code quality before committing."

---

### verify-best-practices.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/verify-best-practices.sh`

**Action:**
No hardcoded `/Users/bruno/sec-llm-workbench/` literal strings found. Zero literal path surgery required.
Script uses only `PROJECT_DIR="${1:-.}"` and `grep` — fully generic.
All checks (type hints, Pydantic v2, requests/httpx, os.path, print statements) are applicable to SIOPV's Python codebase.

No content changes required. Copy unchanged from source.

**MERGE-004-B applied:** Register as manual-invocation utility only. Do NOT register in SIOPV `settings.json` as a lifecycle hook event. Document in SIOPV CLAUDE.md under "Manual Utilities" section.

Event type is UNCLEAR: same pattern as pre-commit.sh (positional arg, no stdin JSON).
No deprecated `decision` field found. Script produces no JSON output.

**Dependencies:** Requires `grep` (standard shell utility). Self-contained — no other `.claude/` file references.
**Deployment notes:** Place at `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`. Do NOT add to `settings.json`. Add to SIOPV CLAUDE.md as: "Manual utility: run `.claude/hooks/verify-best-practices.sh <project_dir>` to check Python best-practices compliance."

---

### post-code.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/post-code.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/post-code.sh`

**Action:**
Event type: PostToolUse — reads stdin JSON, extracts `.tool_input.file_path` and `.tool_name`. Fires after Write or Edit tool calls succeed. Non-blocking (`exit 0` only — no `exit 2`).

No hardcoded `/Users/bruno/sec-llm-workbench/` literal strings found. However, lines 54–58 contain a LOGICAL meta-project-specific block that MUST be removed:

OLD lines 54–58:
```bash
# No procesar archivos del META-PROYECTO
META_PROJECT_DIR="${CLAUDE_PROJECT_DIR:-}"
if [ -n "$META_PROJECT_DIR" ] && [[ "$FILE_PATH" == "$META_PROJECT_DIR"* ]]; then
    exit 0
fi
```

NEW: Delete these 5 lines entirely (no replacement).

**Why:** In SIOPV, `CLAUDE_PROJECT_DIR` will be `/Users/bruno/siopv/`. All SIOPV Python files start with that prefix. Without removal, this block exits 0 early for ALL SIOPV Python files, skipping ruff formatting and verification marker creation entirely — the inverse of the intended behavior.

All other lines use `${CLAUDE_PROJECT_DIR}` or `$PROJECT_DIR` env vars — no further changes needed.
No deprecated `decision` field found. Script outputs to log file only (not stdout).

**Dependencies:**
- `jq` binary (line 38–39, parses stdin JSON)
- `uuidgen` (line 32, with fallback to `date +%s`)
- `uv` binary (lines 77–78, for `ruff format` / `ruff check`)
- Writes to: `${CLAUDE_PROJECT_DIR}/.build/logs/decisions/` (log file)
- Writes to: `$PROJECT_DIR/.build/checkpoints/pending/` (verification markers read by pre-git-commit.sh)
- Reads: `$PROJECT_DIR/pyproject.toml` (detect project root)

**Deployment notes:** Register in SIOPV `settings.json` under `PostToolUse`. Verify `jq` and `uuidgen` are available on the SIOPV host. The `.build/checkpoints/pending/` directory must exist or be auto-created by the script (check mkdir call in script body). Forms chain with pre-git-commit.sh: post-code.sh writes markers → pre-git-commit.sh reads and enforces them.

---

### pre-git-commit.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/pre-git-commit.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-git-commit.sh`

**Action:**
Event type: PreToolUse — reads stdin JSON, extracts `.tool_input.command`; intercepts `git commit` commands. Blocking: outputs `hookSpecificOutput.permissionDecision: "deny"` to block commits with pending verifications.

No hardcoded `/Users/bruno/sec-llm-workbench/` literal strings found. However, lines 51–58 contain a LOGICAL meta-project-specific indirection block that must be collapsed:

OLD lines 51–58:
```bash
# Check the active project's pending dir (post-code.sh writes markers there)
ACTIVE_PROJECT=$(cat "${CLAUDE_PROJECT_DIR:-.}/.build/active-project" 2>/dev/null || echo "")
ACTIVE_PROJECT="${ACTIVE_PROJECT/#\~/$HOME}"  # expand ~ to absolute path
if [ -n "$ACTIVE_PROJECT" ] && [ -d "$ACTIVE_PROJECT" ]; then
    VERIFICATION_DIR="$ACTIVE_PROJECT/.build/checkpoints/pending"
else
    VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
fi
```

NEW (replace entire block with single line):
```bash
VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
```

**Why:** The `.build/active-project` file is a meta-project mechanism for tracking which child project is currently active. In SIOPV, `CLAUDE_PROJECT_DIR` IS the project — there is no indirection. The `else` branch already produces the correct SIOPV path; the entire if/else is collapsed to that line.

**MERGE-003-B applied:** `check-reviewer-score.sh` — do NOT create a stub. The `if [ -f "$SCORE_CHECK_SCRIPT" ]` guard at line 106 already handles the missing file gracefully. No further action needed. Document in SIOPV CLAUDE.md: "Score threshold check is silently skipped if `.claude/scripts/check-reviewer-score.sh` is absent."

Line 104 (`SCORE_CHECK_SCRIPT="${CLAUDE_PROJECT_DIR:-.}/.claude/scripts/check-reviewer-score.sh"`) requires no change — uses `CLAUDE_PROJECT_DIR` env var, not a hardcoded path.

`hookSpecificOutput.permissionDecision` usage is already correct throughout (lines 41–47, 63–71, 91–99, 115–123, 140–147) — no deprecation fixes needed.

**Dependencies:**
- `jq` binary (line 35, parses stdin JSON command)
- `uuidgen` (line 29, with fallback)
- `find` (line 75, counts pending markers)
- Reads: `$VERIFICATION_DIR/` (pending marker files written by post-code.sh)
- Writes to: `${CLAUDE_PROJECT_DIR}/.build/logs/decisions/` (commit decision log)
- Reads (optional, graceful skip): `${CLAUDE_PROJECT_DIR}/.claude/scripts/check-reviewer-score.sh`

**Deployment notes:** Register in SIOPV `settings.json` under `PreToolUse`. This is a BLOCKING hook — test carefully before deployment. Forms chain with post-code.sh. Verify `jq`, `uuidgen`, `find` available on SIOPV host. The `.build/active-project` file reference is completely removed by the block collapse above.

**MERGE-006-A note:** test-framework.sh (a separate utility not covered in domain A1–A4 or B1) stays as manual standalone script. Place in `.claude/hooks/` as a utility — NOT wired to any hook event.

---

## Cross-File System Notes

### post-code.sh → pre-git-commit.sh Chain (SIOPV)
After adaptations applied:
1. `post-code.sh` fires on PostToolUse (Write/Edit), writes markers to `/Users/bruno/siopv/.build/checkpoints/pending/`
2. `pre-git-commit.sh` fires on PreToolUse (Bash `git commit`), reads from `/Users/bruno/siopv/.build/checkpoints/pending/`
3. Chain is self-consistent using only `CLAUDE_PROJECT_DIR` — no meta-project indirection

### settings.json Registration Summary (SIOPV)
| Script | Register in settings.json? | Event |
|--------|---------------------------|-------|
| post-code.sh | YES | PostToolUse |
| pre-git-commit.sh | YES | PreToolUse |
| pre-commit.sh | NO — manual utility only (MERGE-004-B) | — |
| verify-best-practices.sh | NO — manual utility only (MERGE-004-B) | — |

### Deployment Order
1. Create `/Users/bruno/siopv/.claude/agents/` and `/Users/bruno/siopv/.claude/hooks/` directories if not present
2. Deploy all ADAPT/COPY-AS-IS agents (A1–A4 except xai-explainer)
3. Deploy post-code.sh and pre-git-commit.sh hooks; register in settings.json
4. Place pre-commit.sh and verify-best-practices.sh as manual utilities; document in SIOPV CLAUDE.md
5. Stage 4.2: verify `src/siopv/adapters/ml/` structure, then deploy xai-explainer.md
6. Stage 4.2: create `.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md` in SIOPV, then code-implementer.md is fully ready

---

## File Entry Count

| Domain | Files | Count |
|--------|-------|-------|
| A1 | async-safety-auditor, circular-import-detector, code-implementer, config-validator, dependency-scanner | 5 |
| A2 | hallucination-detector, import-resolver, integration-tracer, regression-guard, semantic-correctness-auditor | 5 |
| A3 | smoke-test-runner, static-checks-agent, test-generator, best-practices-enforcer, security-auditor | 5 |
| A4 | code-reviewer, xai-explainer, researcher-1, researcher-2, researcher-3 | 5 |
| B1 | pre-commit.sh, verify-best-practices.sh, post-code.sh, pre-git-commit.sh | 4 |
| **Total** | | **24** |
