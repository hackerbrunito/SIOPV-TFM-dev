# Stage 4.1 Truth Document — Agent D2: Skills Batch 2
> Status: COMPLETE
> Skills: techniques-reference, new-project, scan-vulnerabilities, init-session, run-tests

---

## Skills 2.0 Schema Reference (from Round 1 findings)

11 known fields: `name`, `description`, `argument-hint`, `disable-model-invocation`, `user-invocable`, `allowed-tools`, `model`, `context`, `agent`, `hooks`, `version`

Required fields per Round 1: `name`, `description` (which additional fields are required vs optional: UNCLEAR — Round 1 states "verify which are required vs optional from the 11" but this agent is restricted to 5 source files only; no authoritative schema file was in the file list).

---

## Skills Analysis

---
### techniques-reference/SKILL.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/skills/techniques-reference/SKILL.md
**Target:** /Users/bruno/siopv/.claude/skills/techniques-reference/SKILL.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present (5): `name`, `description`, `user-invocable`, `context`, `disable-model-invocation`
Fields absent from 11-field schema: `argument-hint`, `allowed-tools`, `model`, `agent`, `hooks`, `version`
No non-schema fields found.

**Body content check:**
Line 11: `@../../docs/techniques.md`
- This relative path resolves from `.claude/skills/techniques-reference/` → up two levels → `.claude/` → `docs/techniques.md` = `.claude/docs/techniques.md`
- `.claude/docs/techniques.md` is a meta-project artifact (Vibe Coding 2026 techniques catalog)
- SIOPV does not contain a `.claude/docs/techniques.md`; this is meta-project-only content

**Action:**
ADAPT. Path must be updated to a SIOPV-local equivalent or the @-include replaced.

OLD: `@../../docs/techniques.md`
NEW: `@../../docs/techniques.md` → path requires SIOPV equivalent; if no equivalent exists, remove the @-include and replace with inline content or a note that this file must be created at `/Users/bruno/siopv/.claude/docs/techniques.md`

**Obsolete content:** The `@../../docs/techniques.md` include is meta-project specific. The techniques catalog itself (13 Vibe Coding 2026 techniques) is applicable to SIOPV if the referenced file is created in the SIOPV `.claude/docs/` directory.

---
### new-project/SKILL.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/skills/new-project/SKILL.md
**Target:** /Users/bruno/siopv/.claude/skills/new-project/SKILL.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present (6): `name`, `disable-model-invocation`, `description`, `argument-hint`, `context`, `allowed-tools`
Fields absent from 11-field schema: `user-invocable`, `model`, `agent`, `hooks`, `version`
No non-schema fields found.

**Body content check:**
Line 32: `Usar templates del META-PROYECTO ($CLAUDE_PROJECT_DIR/templates/):`
- Explicit "META-PROYECTO" string reference — meta-project specific
- `$CLAUDE_PROJECT_DIR/templates/` refers to the meta-project's templates directory

Line 51: `Crear \`projects/[nombre].md\` con path, stack, progreso.`
- `projects/[nombre].md` refers to meta-project project registry entries
- SIOPV already exists as a project; this registry creation step is meta-project-specific

**Action:**
ADAPT. Two lines reference meta-project concepts.

Line 32:
OLD: `Usar templates del META-PROYECTO (\`$CLAUDE_PROJECT_DIR/templates/\`):`
NEW: `Usar templates del proyecto (\`$CLAUDE_PROJECT_DIR/templates/\`):`

Line 51:
OLD: `Crear \`projects/[nombre].md\` con path, stack, progreso.`
NEW: `Crear \`projects/[nombre].json\` con path, stack, progreso.`
(Note: SIOPV uses `.json` format per `projects/siopv.json` — `.md` format appears to be a divergence from actual usage)

**Obsolete content:** The term "META-PROYECTO" throughout. The `projects/[nombre].md` format (should be `.json` per SIOPV conventions).

---
### scan-vulnerabilities/SKILL.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/skills/scan-vulnerabilities/SKILL.md
**Target:** /Users/bruno/siopv/.claude/skills/scan-vulnerabilities/SKILL.md
**Classification:** COPY AS-IS

**Frontmatter check:**
Fields present (6): `name`, `disable-model-invocation`, `description`, `context`, `argument-hint`, `allowed-tools`
Fields absent from 11-field schema: `user-invocable`, `model`, `agent`, `hooks`, `version`
No non-schema fields found.

**Body content check:**
No meta-project-specific paths or project names found.
Pipeline reference (`/scan-vulnerabilities → [Ingestion] → [RAG] → [Classify] → [Orchestrate]`) maps directly to SIOPV's pipeline stages (ingest → enrich/RAG → classify → orchestrate).
Workflow steps (read JSON, parse CVE/severity/package/version, deduplicate, validate, summarize) are SIOPV-applicable.

**Action:**
Copy entire skill directory (including SKILL.md) unchanged.

**Obsolete content:** None.

---
### init-session/SKILL.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/skills/init-session/SKILL.md
**Target:** /Users/bruno/siopv/.claude/skills/init-session/SKILL.md
**Classification:** COPY AS-IS

**Frontmatter check:**
Fields present (6): `name`, `disable-model-invocation`, `description`, `argument-hint`, `context`, `allowed-tools`
Fields absent from 11-field schema: `user-invocable`, `model`, `agent`, `hooks`, `version`
No non-schema fields found.

**Body content check:**
Line 27: `.claude/workflow/ (todos los archivos)` — generic path, exists in SIOPV
Line 28: `[especificacion del proyecto segun projects/*.json]` — generic glob, SIOPV uses `projects/siopv.json`
Line 29: `.claude/docs/errors-to-rules.md` — generic path, applicable to SIOPV
Lines 35-38: `$CLAUDE_PROJECT_DIR/.build/checkpoints/daily/` — generic env-var-based path, applicable to SIOPV
No hard-coded project names or meta-project-only paths.

**Action:**
Copy entire skill directory (including SKILL.md) unchanged.

**Obsolete content:** None.

---
### run-tests/SKILL.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/skills/run-tests/SKILL.md
**Target:** /Users/bruno/siopv/.claude/skills/run-tests/SKILL.md
**Classification:** COPY AS-IS

**Frontmatter check:**
Fields present (6): `name`, `disable-model-invocation`, `description`, `argument-hint`, `context`, `allowed-tools`
Fields absent from 11-field schema: `user-invocable`, `model`, `agent`, `hooks`, `version`
No non-schema fields found.

**Body content check:**
All commands use `uv run pytest` with standard paths (`tests/unit`, `tests/integration`, `tests/`) and `--cov=src` — all applicable to SIOPV's test structure.
`uv run bandit -r src/ -f json -o security-report.json` — standard bandit invocation, applicable to SIOPV.
Coverage minimum `80%` — generic threshold. SIOPV's actual minimum per MEMORY.md is 83% overall, but this skill sets a floor, not a ceiling.
No meta-project-specific references.

**Action:**
Copy entire skill directory (including SKILL.md) unchanged.

**Obsolete content:** None.

---

## Summary

| Skill | Classification | Reason |
|-------|---------------|--------|
| techniques-reference | ADAPT | Body references `.claude/docs/techniques.md` (meta-project artifact) |
| new-project | ADAPT | Body references "META-PROYECTO" and `projects/[nombre].md` (.md vs .json divergence) |
| scan-vulnerabilities | COPY AS-IS | No meta-project refs; pipeline maps to SIOPV stages |
| init-session | COPY AS-IS | All paths generic/env-var-based; applicable to SIOPV |
| run-tests | COPY AS-IS | Standard uv/pytest/bandit commands; no project-specific refs |

**Totals:** 2 ADAPT, 3 COPY AS-IS

---

## Old-Format → New-Format Example (required by Stage 4.1 spec)

All 5 skills are already in the **Skills 2.0 directory-based format** (`.claude/skills/<skill-name>/SKILL.md`). No skill was found in the superseded `commands/` flat-file format.

For completeness, the canonical old→new format change would be:

**OLD FORMAT (superseded `commands/` flat-file):**
```
# File: .claude/commands/run-tests.md
---
name: run-tests
description: "Ejecutar suite de tests con cobertura y reportes"
---
[body content]
```

**NEW FORMAT (Skills 2.0 directory-based):**
```
# File: .claude/skills/run-tests/SKILL.md
---
name: run-tests
disable-model-invocation: true
description: "Ejecutar suite de tests con cobertura y reportes"
argument-hint: "[scope]"
context: fork
allowed-tools: ["Read", "Bash", "Glob", "Grep"]
---
[body content]
```

**Changes in new format:**
1. Location: `commands/<skill-name>.md` → `skills/<skill-name>/SKILL.md`
2. Directory-based structure enables asset colocation (e.g., templates alongside SKILL.md)
3. New frontmatter fields: `disable-model-invocation`, `argument-hint`, `context`, `allowed-tools`
4. Old `commands/` format is SUPERSEDED — do not copy skills to `commands/` path

---

## UNCLEAR Items

1. **UNCLEAR:** Which of the 11 schema fields are required vs optional — Round 1 findings state "verify which are required vs optional from the 11" but this agent's file list contains no authoritative schema file. All 5 skills are missing 5–6 fields from the 11-field schema (`user-invocable`, `model`, `agent`, `hooks`, `version`). Whether these absences are violations or acceptable omissions cannot be determined from the source files alone.

2. **UNCLEAR:** For `techniques-reference` ADAPT — the exact NEW content for the `@../../docs/techniques.md` line depends on whether `/Users/bruno/siopv/.claude/docs/techniques.md` will be created as part of Stage 4.1/4.2 deployment. If yes, the old path is valid after copy (resolves correctly from `.claude/skills/techniques-reference/`). If no, the @-include must be replaced or removed. Stage 4.1 spec does not clarify this.

---
> Status: COMPLETE
> Agent: D2
> Files read: 5/5
> Output lines: ~210
