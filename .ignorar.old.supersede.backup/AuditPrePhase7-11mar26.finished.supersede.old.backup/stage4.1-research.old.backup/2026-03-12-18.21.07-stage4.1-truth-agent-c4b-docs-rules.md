# Stage 4.1 Truth Document — Agent C4b: Docs Batch 3 + Rules
> Status: COMPLETE
> Files: verification-thresholds.md, universal-automation-template.md, agent-reports.md, placeholder-conventions.md, tech-stack.md

---

## Round 1 Context (inline — not counted as file reads)

- `.claude/docs/` — NOT auto-loaded; requires CLAUDE.md @import to activate
- `.claude/rules/` — IS auto-loaded; content automatically active without @import
- SIOPV uses `.ignorar/` (not `.ignore/`) — from agent-reports.md source and MEMORY.md references

---

## File Analyses

---

### verification-thresholds.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/docs/verification-thresholds.md`
**Target:** `/Users/bruno/siopv/.claude/docs/verification-thresholds.md`
**Type:** docs
**Auto-loaded:** No (docs/ — requires CLAUDE.md @import)
**Classification:** ADAPT

**Action:**
Adapt the following lines. All other lines: copy unchanged.

1. Line 6 — Last Updated date: informational only, keep as-is.

2. Lines 8–12 — "Referenced by" section: update relative paths to be SIOPV-relative:
   - OLD: `- '.claude/workflow/05-before-commit.md' (before-commit checklist)`
   - NEW: `- '.claude/workflow/05-before-commit.md' (before-commit checklist)` ← no change (relative paths OK for docs)
   - No change needed — relative paths are correct for SIOPV project context.

3. Line 200 (smoke-test-runner — pipeline imports check):
   - OLD: `Pipeline imports cleanly ('uv run python -c "import [project]"')`
   - NEW: `Pipeline imports cleanly ('uv run python -c "import siopv"')`

4. Line 201 (smoke-test-runner — pipeline runs check):
   - OLD: `Pipeline runs start-to-end with CVE-2024-1234 without unhandled exception`
   - NEW: UNCLEAR — `CVE-2024-1234` is a synthetic meta-project test CVE. Replace with SIOPV's actual test CVE ID. Source files do not specify which CVE SIOPV uses for smoke testing. SIOPV team must specify.

5. Line 295 (per-module coverage — script path):
   - OLD: `**Script:** '.claude/scripts/check-module-coverage.py'`
   - NEW: `**Script:** '.claude/scripts/check-module-coverage.py'` ← no change needed (relative path is correct)

6. Lines 346–351 (helper script path):
   - OLD: `- Searches for most recent code-reviewer report in '.ignorar/production-reports/code-reviewer/'`
   - NEW: Same `.ignorar/` convention — SIOPV uses `.ignorar/`, matches. No change.

7. Line 374 (workflow integration — pending dir):
   - OLD: `Cleans markers in '.build/checkpoints/pending/'`
   - NEW: No change if SIOPV also uses `.build/checkpoints/pending/` structure. UNCLEAR — source files do not confirm SIOPV has `.build/` directory. SIOPV team must verify.

**Obsolete content:**
- Line 417: `Last verified: 2026-02-08` — outdated date, update to 2026-03-12 when copying.
- Wave timing references in lines 28–38 ("Phase 1 - Parallel Execution") are in agent-reports.md, not this file — no impact here.

**Cross-file dependencies:**
- `.claude/workflow/05-before-commit.md` (SIOPV must have this)
- `.claude/workflow/04-agents.md` (SIOPV must have this)
- `.claude/hooks/pre-git-commit.sh` (SIOPV must have this)
- `.claude/scripts/check-module-coverage.py` (SIOPV must have this)
- `.claude/scripts/check-reviewer-score.sh` (SIOPV must have this)

---

### 2026-03-12-11.27.33-universal-project-automation-template-teamcreate-orchestrator-agents-rounds-checkpoints-shutdown-file-updates.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/docs/2026-03-12-11.27.33-universal-project-automation-template-teamcreate-orchestrator-agents-rounds-checkpoints-shutdown-file-updates.md`
**Target:** `/Users/bruno/siopv/.claude/docs/universal-project-automation-template.md`
**Type:** docs
**Auto-loaded:** No (docs/ — requires CLAUDE.md @import)
**Classification:** ADAPT

**Action:**
The file header explicitly instructs: "Copy this file into your project at `$HOME/{project-name}/.claude/docs/`" and "Replace all `{project-name}` placeholders with your actual project name."

Required line-by-line replacements:

1. ALL occurrences of `{project-name}` → `siopv`

2. Directory structure section — `.ignore/` convention conflict:
   - OLD (line 43): `└── .ignore/                        ← Local scratch, experiments, backups (excluded from git)`
   - NEW: `└── .ignorar/                       ← Local scratch, experiments, backups (excluded from git)`
   - Reason: SIOPV uses `.ignorar/` (confirmed by agent-reports.md and MEMORY.md references). Template uses `.ignore/`.

3. ALL occurrences of `.ignore/` (as SIOPV local scratch directory) → `.ignorar/`
   - Affects lines: 43, 47, 48 (directory rules), Point 19 backup section, Point 26 .gitignore section

4. Point 26 (.gitignore entries) — `.ignore/` → `.ignorar/`:
   - OLD: `# Local scratch and experiments — never commit` / `.ignore/`
   - NEW: `# Local scratch and experiments — never commit` / `.ignorar/`

5. Target filename: drop timestamp prefix in SIOPV target — use fixed name `universal-project-automation-template.md` for predictable reference.

6. Point 10 (shutdown — last resort cleanup command):
   - OLD: `rm -rf $HOME/.claude/teams/{team-name} $HOME/.claude/tasks/{team-name}`
   - NEW: `rm -rf /Users/bruno/siopv/.claude/teams/{team-name} /Users/bruno/siopv/.claude/tasks/{team-name}`
   - Reason: `$HOME` refers to meta-project user. SIOPV is at `/Users/bruno/siopv/`.

7. All other `{stage-name}`, `{username}`, `{TIMESTAMP}` placeholders: keep as-is — these are runtime placeholders, not project-specific literals.

**Obsolete content:**
- "HOW TO USE THIS TEMPLATE" section (lines 13–18): meta-project onboarding instructions. Can be removed or kept as historical context — not harmful to keep.
- Version header `Version: 1.0 / Created: 2026-03-12-11.27.33` — update version to `1.0-siopv` when copying.

**Cross-file dependencies:**
- References `briefing.md`, `spec-findings.md`, `orchestrator-briefing.md`, `compaction-log.md` in `.claude/workflow/`
- References `errors-to-rules.md` in `.claude/rules/`
- References `MEMORY.md` in `.claude/memory/`
- All these exist in SIOPV based on MEMORY.md and git status entries

---

### agent-reports.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/rules/agent-reports.md`
**Target:** `/Users/bruno/siopv/.claude/rules/agent-reports.md`
**Type:** rules
**Auto-loaded:** Yes (rules/ — automatically active in Claude Code context)

**Classification:** ADAPT

**Action:**
Required line-by-line adaptations:

1. Lines 4–5 — report path template uses `.ignorar/` which matches SIOPV. No change needed on directory name.

2. Line 13 — TIMESTAMP FORMAT CONFLICT:
   - OLD: `**TIMESTAMP format:** 'YYYY-MM-DD-HHmmss' (24-hour format)`
   - NEW: `**TIMESTAMP format:** 'YYYY-MM-DD-HH.mm.ss' (24-hour format, dots between time components)`
   - Reason: universal-automation-template.md Point 21 mandates `YYYY-MM-DD-HH.mm.ss` with dots between time components. This file uses `HHmmss` (no dots). These are conflicting formats in the same meta-project. For SIOPV, adopt the universal template format (dots) as it is the more recently codified standard (2026-03-12 vs 2026-02).

3. Line 23 — bash timestamp command:
   - OLD: `TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)`
   - NEW: `TIMESTAMP=$(date +%Y-%m-%d-%H.%M.%S)`
   - Reason: align with universal template Point 21 format.

4. Lines 27–41 — "Agent Wave Timing (Phase 1 - Parallel Execution)":
   - OLD: `## Agent Wave Timing (Phase 1 - Parallel Execution)` / `Wave 1... Wave 2... Total time: ~12 minutes (vs. ~87 minutes sequential, 86% improvement)`
   - NEW: Replace section header with `## Agent Wave Timing (Stage-based Parallel Execution)` and remove "(Phase 1)" reference.
   - Reason: SIOPV is now stage/round-based (not phase/N-based). The wave agents listed (best-practices-enforcer, security-auditor, etc.) are still valid. Only the header framing needs update.

5. Line 17 — directory pattern `{agent}/phase-{N}/`:
   - OLD: `Directory: '.ignorar/production-reports/{agent}/phase-{N}/'`
   - NEW: `Directory: '.ignorar/production-reports/{agent}/{stage-name}/'`
   - Reason: SIOPV now organizes by stage (stage3, stage4, etc.) not by phase number.

6. Line 5 — report path prompt template:
   - OLD: `"Save your report to '.ignorar/production-reports/{agent-name}/phase-{N}/{TIMESTAMP}-phase-{N}-{agent-name}-{slug}.md'"`
   - NEW: `"Save your report to '.ignorar/production-reports/{agent-name}/{stage-name}/{TIMESTAMP}-{stage-name}-{agent-name}-{slug}.md'"`
   - Reason: align with stage-based convention.

**Obsolete content:**
- "Phase 1 - Parallel Execution" framing throughout. The actual wave agents and timing (~12 min) remain valid.
- "via Task tool" in line 4 — SIOPV uses both Task and Agent tools. Minor wording, not harmful.

**Cross-file dependencies:**
- `.claude/workflow/` files referenced in line 48–49 — exist in SIOPV

---

### placeholder-conventions.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/rules/placeholder-conventions.md`
**Target:** `/Users/bruno/siopv/.claude/rules/placeholder-conventions.md`
**Type:** rules
**Auto-loaded:** Yes (rules/ — automatically active in Claude Code context)
**Classification:** COPY AS-IS

**Action:** Copy unchanged.

Content is fully generic. No project-specific paths, no meta-project references. Conventions (`{{ }}`, `< >`, `[ ]`, `${ }`) apply universally to SIOPV as-is. The "Usage in Workflows" section references `.claude/workflow/` files generically — correct for any project.

**Obsolete content:** None

**Cross-file dependencies:** `.claude/workflow/` files (generic reference, applicable to SIOPV)

---

### tech-stack.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/rules/tech-stack.md`
**Target:** `/Users/bruno/siopv/.claude/rules/tech-stack.md`
**Type:** rules
**Auto-loaded:** Yes (rules/ — automatically active in Claude Code context)
**Classification:** COPY AS-IS

**Action:** Copy unchanged.

Tech stack listed (Python 3.11+, uv, Pydantic v2, httpx async, structlog, pathlib) is SIOPV's actual stack per MEMORY.md architecture notes. Frontmatter `paths: ["**/*.py", "pyproject.toml"]` is correctly scoped to Python files. The `/verify` command reference and Context7 MCP instruction are universally applicable.

**Obsolete content:** None

**Cross-file dependencies:**
- `.claude/docs/mcp-setup.md` — referenced in "Before Write/Edit" section. This file is handled by agent C4a. Must confirm C4a copies mcp-setup.md to SIOPV before activating this rules file, otherwise the cross-reference will be a dead link.

---

## Summary Table

| File | Type | Auto-loaded | Classification | UNCLEAR items |
|------|------|-------------|----------------|---------------|
| verification-thresholds.md | docs | No | ADAPT | smoke-test CVE ID; `.build/` dir existence in SIOPV |
| universal-automation-template.md | docs | No | ADAPT | None |
| agent-reports.md | rules | Yes | ADAPT | None |
| placeholder-conventions.md | rules | Yes | COPY AS-IS | None |
| tech-stack.md | rules | Yes | COPY AS-IS | None |

## Cross-file Conflicts Detected

1. **TIMESTAMP FORMAT CONFLICT**: `agent-reports.md` uses `YYYY-MM-DD-HHmmss` (no dots in time). `universal-automation-template.md` Point 21 mandates `YYYY-MM-DD-HH.mm.ss` (dots). Resolution for SIOPV: adopt universal template format (dots) in SIOPV's `agent-reports.md`.

2. **DIRECTORY NAME CONFLICT**: `universal-automation-template.md` uses `.ignore/`. SIOPV (via `agent-reports.md` and MEMORY.md) uses `.ignorar/`. Resolution: use `.ignorar/` in SIOPV copy of the template.

## UNCLEAR Items

1. **Smoke-test CVE**: `verification-thresholds.md` line 201 uses `CVE-2024-1234` as synthetic test input. SIOPV team must specify the actual CVE ID used for smoke testing.
2. **`.build/` directory**: `verification-thresholds.md` references `.build/checkpoints/pending/`. Source files do not confirm this directory exists in SIOPV. SIOPV team must verify.
