# Stage 4.1 Truth Document — Agent B1: Adaptable Hooks
> Status: COMPLETE
> Files: pre-commit.sh, verify-best-practices.sh, post-code.sh, pre-git-commit.sh (ADAPTABLE)
> Completed: 2026-03-12-18.19.42

---

## Summary

All 4 hooks processed. No deprecated `decision` field found in any script.
No hardcoded `/Users/bruno/sec-llm-workbench/` literal string paths found in any script.
All 4 use `${CLAUDE_PROJECT_DIR}` env var or positional args — no literal path surgery needed.
However, 2 scripts contain LOGICAL meta-project-specific blocks that require removal/simplification for SIOPV.

---

### pre-commit.sh
**Source:** `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-commit.sh`
**Target:** `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`
**Event type:** UNCLEAR — script reads no stdin JSON; takes a positional argument (`$1`); not structured as a Claude Code lifecycle hook command. Comment states "Este hook se ejecuta en el PROYECTO DESTINO, no en el META-PROYECTO". May be invoked by another hook or by a Claude Code `PostToolUse`/`SessionStart` command with the project path as argument.
**Blocking:** No — script exits 0 on success, 1 on error (no JSON output, no `hookSpecificOutput`)
**Classification:** ADAPT

**Action — Path Surgery:**
No hardcoded `/Users/bruno/sec-llm-workbench/` paths found. Zero literal path changes required.

Script uses only:
- `PROJECT_DIR="${1:-.}"` — positional argument or current directory
- `uv run ruff`, `uv run mypy`, `uv run pytest` — tool invocations, no paths

**Deprecation check:**
No deprecated `decision` field found. Script produces no JSON output at all.

**Obsolete content:**
Line 5: comment `# Este hook se ejecuta en el PROYECTO DESTINO, no en el META-PROYECTO` is a meta-project-context annotation. It is non-functional and can be removed or updated in SIOPV. No behavioral impact.

**Cross-file dependencies:**
None. Self-contained script. Requires `uv`, `ruff`, `mypy`, `pytest` to be installed.

---

### verify-best-practices.sh
**Source:** `/Users/bruno/sec-llm-workbench/.claude/hooks/verify-best-practices.sh`
**Target:** `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`
**Event type:** UNCLEAR — script reads no stdin JSON; takes a positional argument (`$1`); not structured as a Claude Code lifecycle hook command. Same pattern as pre-commit.sh.
**Blocking:** No — script exits 1 when errors found (no JSON output, no `hookSpecificOutput`)
**Classification:** ADAPT

**Action — Path Surgery:**
No hardcoded `/Users/bruno/sec-llm-workbench/` paths found. Zero literal path changes required.

Script uses only:
- `PROJECT_DIR="${1:-.}"` — positional argument or current directory
- `grep -rn ... src/` — searches relative `src/` directory after `cd "$PROJECT_DIR"`

**Deprecation check:**
No deprecated `decision` field found. Script produces no JSON output.

**Obsolete content:**
None identified. Script is generic — checks type hints, Pydantic v2, requests/httpx, os.path, print statements. All checks are applicable to SIOPV's Python codebase.

**Cross-file dependencies:**
None. Self-contained. Requires `grep` (standard shell utility).

---

### post-code.sh
**Source:** `/Users/bruno/sec-llm-workbench/.claude/hooks/post-code.sh`
**Target:** `/Users/bruno/siopv/.claude/hooks/post-code.sh`
**Event type:** PostToolUse — script reads stdin JSON and extracts `.tool_input.file_path` and `.tool_name`. Fires after Write or Edit tool calls succeed.
**Blocking:** No — PostToolUse is non-blocking; script uses `exit 0` only; no exit 2.
**Classification:** ADAPT

**Action — Path Surgery:**
No hardcoded `/Users/bruno/sec-llm-workbench/` literal string found. However, lines 54–58 contain a LOGICAL meta-project-specific block that must be removed for SIOPV:

Lines 54–58 (current):
```bash
# No procesar archivos del META-PROYECTO
META_PROJECT_DIR="${CLAUDE_PROJECT_DIR:-}"
if [ -n "$META_PROJECT_DIR" ] && [[ "$FILE_PATH" == "$META_PROJECT_DIR"* ]]; then
    exit 0
fi
```

**Problem:** In SIOPV, `CLAUDE_PROJECT_DIR` will be `/Users/bruno/siopv/`. All SIOPV Python files start with that prefix. This block would exit 0 early for ALL SIOPV Python files, skipping ruff formatting and verification marker creation entirely. This is the inverse of the intended behavior.

OLD lines 54–58:
```
# No procesar archivos del META-PROYECTO
META_PROJECT_DIR="${CLAUDE_PROJECT_DIR:-}"
if [ -n "$META_PROJECT_DIR" ] && [[ "$FILE_PATH" == "$META_PROJECT_DIR"* ]]; then
    exit 0
fi
```

NEW (remove entire block — 5 lines deleted, no replacement):
```
(lines 54–58 deleted entirely)
```

All other lines use `${CLAUDE_PROJECT_DIR}` or `$PROJECT_DIR` env vars — no literal path surgery needed beyond this block removal.

**Deprecation check:**
No deprecated `decision` field found. Script produces no JSON stdout output (writes to log file only, then exits with `exit 0`).

**Obsolete content:**
Lines 54–58 (the META_PROJECT_DIR exclusion block) as documented above. Must not be carried to SIOPV.

**Cross-file dependencies:**
- `jq` binary (used on lines 38–39 to parse stdin JSON)
- `uuidgen` (line 32, with fallback to `date +%s`)
- `uv` binary (lines 77–78 for ruff format/check)
- Writes to: `${CLAUDE_PROJECT_DIR}/.build/logs/decisions/` (log file)
- Writes to: `$PROJECT_DIR/.build/checkpoints/pending/` (verification markers)
- Reads: `$PROJECT_DIR/pyproject.toml` (to detect project root)

---

### pre-git-commit.sh
**Source:** `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-git-commit.sh`
**Target:** `/Users/bruno/siopv/.claude/hooks/pre-git-commit.sh`
**Event type:** PreToolUse — script reads stdin JSON and extracts `.tool_input.command`; fires before Bash tool calls. Intercepts `git commit` commands specifically.
**Blocking:** Yes — outputs `hookSpecificOutput.permissionDecision: "deny"` to block commits with pending verifications; outputs `"allow"` to permit.
**Classification:** ADAPT

**Action — Path Surgery:**
No hardcoded `/Users/bruno/sec-llm-workbench/` literal string found. However, lines 51–58 contain a LOGICAL meta-project-specific indirection block that is not applicable to SIOPV:

Lines 51–58 (current):
```bash
# Check the active project's pending dir (post-code.sh writes markers there)
ACTIVE_PROJECT=$(cat "${CLAUDE_PROJECT_DIR:-.}/.build/active-project" 2>/dev/null || echo "")
ACTIVE_PROJECT="${ACTIVE_PROJECT/#\~/$HOME}"  # expand ~ to absolute path
if [ -n "$ACTIVE_PROJECT" ] && [ -d "$ACTIVE_PROJECT" ]; then
    VERIFICATION_DIR="$ACTIVE_PROJECT/.build/checkpoints/pending"
else
    VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
fi
```

**Problem:** This block reads a `.build/active-project` file that exists in the meta-project to track which child project is currently active (e.g., writing `/Users/bruno/siopv/` into that file). In SIOPV, there is no "active project" indirection — SIOPV is the project itself. `CLAUDE_PROJECT_DIR` will be `/Users/bruno/siopv/` directly.

The `else` branch already produces the correct SIOPV path. The entire indirection can be collapsed.

OLD lines 51–58:
```
# Check the active project's pending dir (post-code.sh writes markers there)
ACTIVE_PROJECT=$(cat "${CLAUDE_PROJECT_DIR:-.}/.build/active-project" 2>/dev/null || echo "")
ACTIVE_PROJECT="${ACTIVE_PROJECT/#\~/$HOME}"  # expand ~ to absolute path
if [ -n "$ACTIVE_PROJECT" ] && [ -d "$ACTIVE_PROJECT" ]; then
    VERIFICATION_DIR="$ACTIVE_PROJECT/.build/checkpoints/pending"
else
    VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
fi
```

NEW (replace entire block with single line):
```
VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
```

Line 104 (current):
```
SCORE_CHECK_SCRIPT="${CLAUDE_PROJECT_DIR:-.}/.claude/scripts/check-reviewer-score.sh"
```
This references `.claude/scripts/check-reviewer-score.sh` via `CLAUDE_PROJECT_DIR`. No hardcoded path — no change needed. SIOPV must provide `/Users/bruno/siopv/.claude/scripts/check-reviewer-score.sh` or the check is gracefully skipped (line 106: `if [ -f "$SCORE_CHECK_SCRIPT" ]`).

All other lines use `${CLAUDE_PROJECT_DIR}` env var — no literal path surgery needed beyond the block simplification above.

**Deprecation check:**
No deprecated `decision` field found. Script uses `hookSpecificOutput.permissionDecision` correctly throughout (lines 41–47, 63–71, 91–99, 115–123, 140–147). Output format already matches the current non-deprecated standard as documented in the Round 1 research report.

**Obsolete content:**
Lines 51–58 (the active-project indirection block) as documented above — meta-project concept, not applicable to SIOPV.

**Cross-file dependencies:**
- `jq` binary (line 35 to parse stdin JSON command)
- `uuidgen` (line 29, with fallback)
- `find` (line 75 to count pending markers)
- Reads: `${CLAUDE_PROJECT_DIR}/.build/active-project` (meta-project concept — remove)
- Reads: `$VERIFICATION_DIR/` (pending marker files written by post-code.sh)
- Reads: `${CLAUDE_PROJECT_DIR}/.build/logs/decisions/` (writes commit decision log)
- Reads: `${CLAUDE_PROJECT_DIR}/.claude/scripts/check-reviewer-score.sh` (optional — graceful skip if missing)

---

## Cross-Hook System Notes

**Verified chain (source-file-based):**
- `post-code.sh` writes verification markers to `$PROJECT_DIR/.build/checkpoints/pending/`
- `pre-git-commit.sh` reads those markers to block commits

**For SIOPV, after both adaptations (post-code.sh block removal + pre-git-commit.sh indirection simplification), the chain becomes:**
- `post-code.sh` fires on PostToolUse (Write/Edit), writes markers to `/Users/bruno/siopv/.build/checkpoints/pending/`
- `pre-git-commit.sh` fires on PreToolUse (Bash), reads from `/Users/bruno/siopv/.build/checkpoints/pending/`
- Chain is correct and self-consistent using only `CLAUDE_PROJECT_DIR`

**pre-commit.sh and verify-best-practices.sh integration:**
- UNCLEAR how these are registered/invoked (no stdin JSON, positional args, not standard lifecycle hooks)
- No path changes required in the scripts themselves

---

## UNCLEAR Items

1. **Event type for pre-commit.sh:** Script does not read stdin JSON and uses `${1:-.}` positional arg. Cannot determine from the script alone what Claude Code event fires it or how it is registered in settings.json. File 5 (research report) does not document this specific registration.

2. **Event type for verify-best-practices.sh:** Same issue as pre-commit.sh. No stdin JSON, positional arg.

3. **check-reviewer-score.sh existence in SIOPV:** pre-git-commit.sh references `${CLAUDE_PROJECT_DIR}/.claude/scripts/check-reviewer-score.sh`. This file does not currently exist in SIOPV (target). The hook handles this gracefully (if-file-exists check), but SIOPV must either provide this script or the score-threshold check will always be silently skipped.
