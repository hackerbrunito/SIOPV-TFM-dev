# Stage 4.1 Truth Document — Agent A2: Agent Definitions Batch 2
> Status: COMPLETE
> Files: hallucination-detector, import-resolver, integration-tracer, regression-guard, semantic-correctness-auditor

---

## Frontmatter Schema Reference (from Round 1)

Official 13-field schema: name [required], description [required], tools, disallowedTools,
model, permissionMode, maxTurns, skills, mcpServers, hooks, memory, background, isolation.
Non-schema fields to strip: color, capabilities.

---

### hallucination-detector.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/hallucination-detector.md
**Target:** /Users/bruno/siopv/.claude/agents/hallucination-detector.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all in official schema.
- Non-schema fields: none.
- Missing required fields: none.
- Note: HTML comment `<!-- version: 2026-02 -->` before the `---` opener is outside frontmatter — carry as-is (it is above the `---` block, not a frontmatter field).

**Action:**
Replace the entire "Project Context (CRITICAL)" block (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run mypy src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && uv run mypy src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

All other body content (Role Definition, Verification Process, Tool Invocation, Role Reinforcement, Actions, Report Persistence, Report Format) is generic and applicable to SIOPV unchanged.

**Obsolete content:** None except the Project Context block replaced above.
**Cross-file dependencies:** References `mcp__context7__resolve-library-id` and `mcp__context7__query-docs` (MCP tools, not file references). None to other `.claude/` files.

---

### import-resolver.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/import-resolver.md
**Target:** /Users/bruno/siopv/.claude/agents/import-resolver.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all in official schema.
- Non-schema fields: none.
- Missing required fields: none.
- HTML comment `<!-- version: 2026-03 -->` is above `---` opener — carry as-is.

**Action:**
Two changes required:

**Change 1** — Replace "Project Context (CRITICAL)" block (lines 12–19 in source):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project.
- Target project path is provided in your invocation prompt or read from `.build/active-project`.
- Expand `~` to `$HOME` before use.
- All `uv run` and `cd` commands must use the fully expanded target project path.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project directory).
```

NEW:
```
## Project Context (CRITICAL)

- You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).
- Target project path is `/Users/bruno/siopv`. Use `TARGET=/Users/bruno/siopv` in all commands.
- All `uv run` and `cd` commands must use `/Users/bruno/siopv` as the project path.
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

**Change 2** — Replace Step 1 of Actions (lines 35–38 in source):

OLD:
```
1. Read `.build/active-project` to get TARGET. Run:
   ```bash
   TARGET=$(cat .build/active-project); TARGET="${TARGET/#\~/$HOME}"
   ```
```

NEW:
```
1. Set TARGET to the SIOPV project path:
   ```bash
   TARGET=/Users/bruno/siopv
   ```
```

**Change 3** — Replace report path reference in Step 9 (line 158 in source):

OLD:
```
   where {N} = content of `.build/current-phase`, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S`
```

NEW:
```
   where {N} = current SIOPV phase number (provide at invocation time), {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S`
```

All other body content (Role Definition, import skipping rules, AST extraction script, resolution logic, PASS/FAIL criteria, Report Format) is generic and applicable to SIOPV unchanged.

**Obsolete content:** `.build/active-project` file read (meta-project routing mechanism). `.build/current-phase` file reference in Step 9.
**Cross-file dependencies:** None to other `.claude/` files.

---

### integration-tracer.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/integration-tracer.md
**Target:** /Users/bruno/siopv/.claude/agents/integration-tracer.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all in official schema.
- Non-schema fields: none.
- Missing required fields: none.
- HTML comment `<!-- version: 2026-03 -->` is above `---` opener — carry as-is.

**Action:**
Replace the entire "Project Context (CRITICAL)" block (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "add_node" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && grep -rn "add_node" src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

All other body content (Role Definition, AST-Based Call Graph, Verification Checklist, PASS/FAIL Criteria, Findings Severity, Actions, Report Persistence, Report Format) is generic and applicable to SIOPV unchanged.

**Obsolete content:** None except Project Context block replaced above.
**Cross-file dependencies:** None to other `.claude/` files.

---

### regression-guard.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/regression-guard.md
**Target:** /Users/bruno/siopv/.claude/agents/regression-guard.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode — all in official schema.
- Non-schema fields: none.
- Missing required fields: none.
- Note: `disallowedTools` field is absent — this is valid (optional field). Do NOT add it.
- Note: `permissionMode: default` (differs from other agents which use `plan`) — carry as-is.
- HTML comment `<!-- version: 2026-03 -->` is above `---` opener — carry as-is.

**Action:**
Replace the "Project Context (CRITICAL)" block (lines 12–23 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) and git commands must target the **target project directory**.
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run pytest ...
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project), NOT the target project.
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) and git commands must target `/Users/bruno/siopv/`.
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && uv run pytest ...
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Additionally, replace Step 1 of Actions (lines 40–47 in source) which reads `.build/active-project`:

OLD:
```
### 1. Read active project path

```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "ERROR: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```
```

NEW:
```
### 1. Set project path

```bash
TARGET=/Users/bruno/siopv
if [ ! -d "$TARGET" ]; then
    echo "ERROR: SIOPV project directory not found: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```
```

All other body content (Role Definition, Steps 2–7, PASS/FAIL Criteria, Findings Severity, Report Format, Report Persistence) is generic and applicable to SIOPV unchanged.

**Obsolete content:** `.build/active-project` file read in both Project Context block and Step 1. Error message referencing `.build/active-project`.
**Cross-file dependencies:** None to other `.claude/` files.

---

### semantic-correctness-auditor.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/semantic-correctness-auditor.md
**Target:** /Users/bruno/siopv/.claude/agents/semantic-correctness-auditor.md
**Classification:** ADAPT

**Frontmatter check:**
- Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all in official schema.
- Non-schema fields: none.
- Missing required fields: none.
- HTML comment `<!-- version: 2026-02 -->` is above `---` opener — carry as-is.

**Action:**
Replace the "Project Context (CRITICAL)" block (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "field_validator" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && grep -rn "field_validator" src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

All other body content (Role Definition, Verification Checklist, PASS/FAIL Criteria, Findings Severity, Actions, Report Persistence, Report Format) is generic and applicable to SIOPV unchanged.

**Obsolete content:** None except Project Context block replaced above.
**Cross-file dependencies:** None to other `.claude/` files.

---

## Summary

| Agent File | Classification | Changes Required |
|------------|---------------|-----------------|
| hallucination-detector.md | ADAPT | Replace Project Context block |
| import-resolver.md | ADAPT | Replace Project Context block + Step 1 + Step 9 `.build/current-phase` ref |
| integration-tracer.md | ADAPT | Replace Project Context block |
| regression-guard.md | ADAPT | Replace Project Context block + Step 1 bash script |
| semantic-correctness-auditor.md | ADAPT | Replace Project Context block |

**Pattern:** All 5 agents share the same meta-project routing mechanism (`.build/active-project`, `sec-llm-workbench/.ignorar/` path). The uniform adaptation is: replace dynamic target resolution with static SIOPV path and update reports root from meta-project to SIOPV project.

**UNCLEAR items:** None.
