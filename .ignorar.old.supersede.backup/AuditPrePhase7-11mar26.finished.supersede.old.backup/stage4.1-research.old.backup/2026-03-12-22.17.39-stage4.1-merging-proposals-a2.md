# Stage 4.1 — Merging Proposals A (Agents A1–A4 + B1)
> Total MERGE proposals: 6
> Files covered: A1, A2, A3, A4, B1
> Agent: merging-proposal-a2 (SECOND RUN)
> Timestamp: 2026-03-12-22.17.39

---

### MERGE-001: `memory` field value — `"project"` vs `true`

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/smoke-test-runner.md`
- `/Users/bruno/siopv/.claude/agents/test-generator.md`
- `/Users/bruno/siopv/.claude/agents/xai-explainer.md`

**Domain reports referencing this:** A3 (UNCLEAR-1), A4 (xai-explainer UNCLEAR item 1)

**Context:** The official 13-field agent schema includes `memory` as a valid field but does not document its allowed values in the Round 1 research findings. Source files use two distinct forms:
- `memory: project` — used in: smoke-test-runner, test-generator, xai-explainer (ADAPTABLE)
- `memory: true` — used in: best-practices-enforcer, security-auditor, code-reviewer

A3 explicitly calls this UNCLEAR-1 and flags it as unresolvable without schema documentation. A4 says "carry as-is pending orchestrator clarification."

**Option A:** Carry all `memory` values as-is — keep `memory: project` for the 3 agents that currently have it, keep `memory: true` for agents that have it.
**Rationale A:** Least-change principle. Both forms came from working meta-project agents, so both presumably work. No confirmed evidence of breakage. A4 recommends "carry as-is."

**Option B:** Standardize all agents to `memory: true` — change `memory: project` → `memory: true` in smoke-test-runner, test-generator, and xai-explainer to match the majority.
**Rationale B:** Consistency across the SIOPV agent suite. `memory: true` is confirmed valid per A4 (code-reviewer). `"project"` is unconfirmed. Reduces risk of a silent schema rejection.

**Recommendation:** A — carry as-is. Both values appear to be in active use in the meta-project without issues. Standardizing prematurely introduces unnecessary churn without confirmed need.
**Human judgment required:** YES — because `memory: project` has not been confirmed valid by official schema documentation. If the orchestrator has access to official Claude Code docs beyond the Round 1 findings, this should be verified before deploying.

---

### MERGE-002: `code-implementer` references to potentially missing SIOPV files

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/code-implementer.md`

**Domain reports referencing this:** A1 (UNCLEAR items 1 and 2)

**Context:** The adapted `code-implementer.md` references two files in the SIOPV project root that may not exist:
- `.claude/docs/python-standards.md` (step 2 of "Before Writing Code" checklist)
- `.claude/rules/tech-stack.md` (step 3 of "Before Writing Code" checklist)

A1 explicitly flags both as UNCLEAR and defers verification to Stage 4.2. The code-implementer body currently instructs agents to `Read` these files before writing any code. If the files do not exist and the agent tries to read them, it will receive a file-not-found error mid-task.

**Option A:** Keep the references as-is with a parenthetical note: `(in SIOPV project root — create if not present)`.
**Rationale A:** The files are worth creating as part of Stage 4.2 setup — they encode SIOPV Python conventions and tech-stack constraints that the code-implementer should follow. Keeping the reference ensures Stage 4.2 knows to create them.

**Option B:** Remove the two `Read` steps from the "Before Writing Code" checklist and replace with inline notes referencing the SIOPV pyproject.toml and existing CLAUDE.md instead.
**Rationale B:** Avoids deploying an agent with broken file reads. SIOPV already has conventions in CLAUDE.md and pyproject.toml; the code-implementer can be pointed to those instead of non-existent docs.

**Recommendation:** A — keep references with a note. The code-implementer is a critical agent and these standards docs represent real SIOPV-specific knowledge that should be created. Stage 4.2 must create both files before deploying code-implementer.
**Human judgment required:** YES — Stage 4.2 scope decision: are `.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md` in scope to create for SIOPV? If the orchestrator defers them, Option B must be chosen instead.

---

### MERGE-003: `check-reviewer-score.sh` — missing in SIOPV

**Files affected:**
- `/Users/bruno/siopv/.claude/hooks/pre-git-commit.sh`
- (optionally) `/Users/bruno/siopv/.claude/scripts/check-reviewer-score.sh`

**Domain reports referencing this:** B1 (UNCLEAR item 3)

**Context:** `pre-git-commit.sh` at line 104 references `${CLAUDE_PROJECT_DIR}/.claude/scripts/check-reviewer-score.sh`. This script checks that the code-reviewer reported a passing score before allowing a commit. The hook handles absence gracefully (guarded by `if [ -f "$SCORE_CHECK_SCRIPT" ]`), so the check silently skips if the file is missing. SIOPV does not currently have this script. The meta-project has it in `.claude/scripts/check-reviewer-score.sh`.

**Option A:** Create a minimal stub at `/Users/bruno/siopv/.claude/scripts/check-reviewer-score.sh` that always exits 0 (pass), with a comment explaining it needs to be implemented.
**Rationale A:** The stub makes the hook aware that the script exists conceptually, prevents confusion about why the score check never fires, and provides a placeholder for Stage 4.2 to complete. No behavioral change vs. missing file (both pass), but explicit intent.

**Option B:** Accept the graceful-skip behavior — do not create the stub, do not change pre-git-commit.sh. The score threshold check will silently not run in SIOPV.
**Rationale B:** Minimal-scope approach. The if-exists guard was designed for exactly this situation. The other pre-commit checks (pending verification markers) still function correctly. Stage 4.2 can add the script later if desired.

**Option C:** Remove the `check-reviewer-score.sh` block from `pre-git-commit.sh` entirely (lines 104–115 approximately) since it will never fire and carrying dead conditional logic into SIOPV adds noise.
**Rationale C:** Clean adaptation — if a feature is known to not function in the target project, removing it is cleaner than leaving silently inert code.

**Recommendation:** B — accept graceful-skip. The guard already handles this cleanly. Options A/C both require additional scope decisions (creating a new file or modifying the hook beyond what B1 specified). Stage 4.2 can revisit.
**Human judgment required:** YES — the choice between A/B/C affects SIOPV security posture (reviewer score enforcement). This is a behavioral decision, not a purely technical one.

---

### MERGE-004: pre-commit.sh and verify-best-practices.sh — registration event type

**Files affected:**
- `/Users/bruno/siopv/.claude/settings.json` (hooks registration)
- `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`
- `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`

**Domain reports referencing this:** B1 (UNCLEAR items 1 and 2, also Summary section)

**Context:** Both scripts use `${1:-.}` positional arguments and produce no stdin JSON parsing, no `hookSpecificOutput`. B1 confirms they are NOT standard Claude Code lifecycle hooks as currently written. The meta-project invokes them via some mechanism not visible in the script files themselves. Round 1 findings document 18 camelCase hook event types — none of which accept positional arguments.

**Option A:** Wrap both scripts as `UserPromptSubmit` hooks in `settings.json`, passing the project path as the first argument via a small wrapper that writes to stdin-compatible format, OR rewrite both scripts to accept stdin JSON and parse the project path from `$CLAUDE_PROJECT_DIR` env var instead of `$1`.
**Rationale A:** Makes both scripts proper first-class Claude Code lifecycle hooks that auto-fire at appropriate times (e.g., before each Claude response). Consistent with the hook system design.

**Option B:** Register both scripts as manual-invocation utilities only — document them in SIOPV CLAUDE.md or a README as scripts to run manually (`bash .claude/hooks/pre-commit.sh /Users/bruno/siopv`) rather than auto-hooks. Do not register in settings.json.
**Rationale B:** Matches actual current behavior in the meta-project (where they appear to be called by other hooks or manually). No script modification needed. Lower risk of unintended auto-fires.

**Recommendation:** B — register as manual utilities. The scripts work correctly as standalone shell tools. Rewriting them to parse stdin JSON changes their behavior and scope beyond the adaptation task. The post-code.sh (PostToolUse) + pre-git-commit.sh (PreToolUse) chain already covers automatic enforcement; pre-commit.sh and verify-best-practices.sh are supplementary.
**Human judgment required:** YES — this determines whether SIOPV gets automatic pre-response linting checks or manual-only checks. Security/workflow posture decision.

---

### MERGE-005: `xai-explainer` class_names — deploy as-is vs. verify first

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/xai-explainer.md`

**Domain reports referencing this:** A4 (xai-explainer UNCLEAR item 2), KNOWN UNCLEAR ITEMS item 3

**Context:** The xai-explainer system prompt contains `class_names=["LOW", "MEDIUM", "HIGH", "CRITICAL"]` for the LIME explainer. A4 states these "appear correct for Phase 3 XGBoost classifier" but explicitly flags: "No contradicting source available to verify against SIOPV feature schema. Carry as-is." The MEMORY.md confirms SIOPV Phase 3 severity categories but does not enumerate them at the field-schema level. If the XGBoost classifier uses different internal class labels (e.g., integers 0–3, or `"low"` lowercase), the LIME explanations would be incorrect.

**Option A:** Deploy xai-explainer with `class_names=["LOW", "MEDIUM", "HIGH", "CRITICAL"]` as-is, based on A4's assessment that they appear correct.
**Rationale A:** A4 reviewed the adaptation spec and found no contradicting source. SIOPV MEMORY.md aligns with these labels. Fast path to deployment; correctness can be verified at runtime.

**Option B:** Block deployment of xai-explainer until Stage 4.2 reads `src/siopv/adapters/ml/` to confirm the actual class labels used in the XGBoost model training pipeline.
**Rationale B:** Wrong class_names in LIME produce silently incorrect explanations — not an error, just wrong audit output. For a security audit tool, silent incorrectness is worse than a deployment delay.

**Recommendation:** B — verify before deploying. xai-explainer produces audit artifacts (SHAP/LIME explanations) that feed compliance and audit workflows. Silent incorrectness in those artifacts is a higher risk than a short delay for a one-file verification read.
**Human judgment required:** YES — risk tolerance decision. If the orchestrator prefers fast deployment with post-deployment validation, Option A is acceptable.

---

### MERGE-006: `xai-explainer` model artifact path — assumed vs. verified

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/xai-explainer.md`

**Domain reports referencing this:** A4 (xai-explainer UNCLEAR item 3), KNOWN UNCLEAR ITEMS item 4

**Context:** A4 proposes changing the model path from `models/xgboost_classifier.json` to `src/siopv/adapters/ml/models/xgboost_classifier.json`. A4 explicitly states: "Actual model artifact path inside `src/siopv/adapters/ml/` — not verifiable without reading SIOPV source files (out of scope for this agent). Path `src/siopv/adapters/ml/models/xgboost_classifier.json` is derived from adaptation spec only." If the actual model is saved at a different path (e.g., `.ignorar/models/xgboost_classifier.json` or `models/phase3/xgboost.json`), the agent will fail to load it.

**Option A:** Use the Stage 4.1 spec-derived path `src/siopv/adapters/ml/models/xgboost_classifier.json` and deploy, letting Stage 4.2 fix if wrong.
**Rationale A:** Reasonable assumption based on hexagonal architecture (ML models belong in the adapters layer). If wrong, the agent fails loudly at runtime (file not found) rather than silently.

**Option B:** Block xai-explainer deployment until Stage 4.2 reads SIOPV source (`src/siopv/adapters/ml/` and pyproject.toml / training scripts) to confirm the exact model artifact path.
**Rationale B:** xai-explainer has a hard dependency on the model file. A wrong path means the agent cannot run at all. Verifying once before deployment is cheaper than a failed run plus fix cycle.

**Recommendation:** B — verify path before deployment. This is a 1-file glob read in Stage 4.2 (`Glob path="src/siopv/adapters/ml/**"`) and prevents a guaranteed deploy failure if the path assumption is wrong.
**Human judgment required:** NO — this is a purely technical verification task that Stage 4.2 must perform regardless. The recommendation is clear.

---

## NON-CONFLICT SUMMARY

Clear-cut items requiring no decision — informational only:

- **A1–A4 (all 20 SIOPV-applicable agents):** Replace Project Context block — uniform pattern, no conflicts.
- **static-checks-agent (A3):** COPY AS-IS — confirmed no meta-project paths in body.
- **code-reviewer (A4):** Strip `<!-- version: 2026-02 -->` pre-frontmatter comment — carry only frontmatter and body.
- **researcher-1, researcher-2, researcher-3 (A4):** Change `"claude-main"` → `"orchestrator"` in coordinator reference — clear, no alternative.
- **best-practices-enforcer (A3):** Remove reference to `.claude/docs/agent-tool-schemas.md` — meta-project-only doc confirmed absent from SIOPV.
- **security-auditor (A3):** Update experimental script path to absolute SIOPV path — clear mechanical change.
- **code-implementer (A1):** Fix report naming `{NNN}` → `{TIMESTAMP}` in step 10 — aligns with established timestamp-based convention in `agent-reports.md`.
- **xai-explainer (A4):** Fix `{NNN}` → `{TIMESTAMP}` naming convention — same alignment as above.
- **dependency-scanner (A1):** Remove `.build/current-phase` reference — replace with invocation-time phase parameter.
- **import-resolver (A2):** Remove `.build/current-phase` reference in Step 9 — replace with invocation-time phase parameter.
- **smoke-test-runner (A3):** Remove `.build/current-phase` reference in Step 9 — replace with invocation-time parameter and absolute report path.
- **post-code.sh (B1):** Remove META_PROJECT_DIR exclusion block (lines 54–58) — would break all SIOPV Python file tracking if kept.
- **pre-git-commit.sh (B1):** Collapse active-project indirection (lines 51–58) to single line `VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"` — meta-project concept not applicable.
- **pre-commit.sh (B1):** No path surgery needed; optionally remove meta-project comment on line 5.
- **verify-best-practices.sh (B1):** No changes required — all generic checks applicable to SIOPV Python codebase.
