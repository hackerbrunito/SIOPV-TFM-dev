# Handoff Document: Stage 4.1 Pre-Guidelines — Complete Session Context

**Generated:** 2026-03-12 15:16:12
**Author:** Claude Code (session handoff — post-compaction resume point)
**Purpose:** Enable a post-compaction Claude instance to resume this session exactly where it left off, with zero information loss.
**Audience:** Any Claude Code instance reading this cold, with no prior session context.

---

## HOW TO USE THIS DOCUMENT

Read this file first. Then read the two files listed in Section 7. Then proceed to write the Stage 4.1 guidelines file (Section 11 tells you exactly what to do). Do not read any other files before writing the guidelines file — everything you need is in this document and the two resume files.

---

## Section 1: What Was Accomplished in This Session (Complete List)

The session accomplished the following, in this exact order:

### 1. Read and confirmed the pre-planning document from the previous session

File read:
`/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4/2026-03-12-14.04.54-stage4-pre-planning-preparation-notes-scope-decisions-research-first-approach-and-open-questions-before-orchestrator-spawn.md`

This document captured all planning decisions from the session immediately before this one. It described the original "Stage 4" as a single unified stage combining research and implementation. Reading it confirmed the prior state and provided the baseline for the redesign decision below.

### 2. MAJOR REDESIGN DECISION: Split Stage 4 into Stage 4.1 and Stage 4.2

The original single "Stage 4" was redesigned as two sub-stages:

- **Stage 4.1: Research & Truth Document** — research only, no implementation
- **Stage 4.2: Implementation** — takes Stage 4.1 truth document as primary input

**Reason for split:** The original Stage 4 was overengineered as a single stage. It combined online research, truth document synthesis, and full `siopv/.claude/` implementation into one orchestration run. That is too much for a single stage — too many failure modes, too many agent handoffs, and no human review window between research and implementation. The split creates a clean boundary: research first, human reviews research output, implementation second. Each stage is independently reviewable and independently resumable.

### 3. Created two new directories

```
/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/
/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.2-implementation/
```

Both directories now exist. `stage4.1-research/` contains the Stage 4.1 pre-planning notes and this handoff file. `stage4.2-implementation/` is currently empty — Stage 4.2 has not started.

### 4. Corrected "REVERSE MOVE" language in three files (via subagent earlier in session)

This correction happened earlier in the session (before the 4.1/4.2 split decision). The word "REVERSE MOVE" was found in documentation describing how files from the meta-project (sec-llm-workbench) go to SIOPV. "REVERSE MOVE" was incorrect and misleading — it implied files would be removed from the source. The correct operation is copy-only.

Corrections made:
- `briefing.md` line 82: "REVERSE MOVE" → "COPY TO SIOPV"
- `orchestrator-briefing.md` line 177: "moves" → "copies" with inline clarification added
- `spec-findings.md`: no correction needed — the problematic language was not present in this file

### 5. Applied the Stage 4.1/4.2 split to all reference documents (via two parallel subagents)

After the redesign decision, all reference documents were updated to reflect the new two-stage structure. Two subagents ran in parallel to do this.

Files updated:

**`briefing.md`** (4 changes):
- Line 16: stage numbering updated to include 4.1 and 4.2
- Line 50: infrastructure status updated
- STAGE-4 section replaced with separate STAGE-4.1 and STAGE-4.2 sections
- Transition point between research and implementation updated

**`orchestrator-briefing.md`**:
- Title updated to: "Stages 1, 2, 3, 3.5, 4.1, 4.2 + Remediation-Hardening"

**`MEMORY.md`**:
- Stage status table split: STAGE-4 row removed, replaced with:
  - STAGE-4.1 ⏳ NEXT
  - STAGE-4.2 ⏳ PENDING
- Stage 3.5 reference updated

**`setup-checklist.md`**:
- 2 occurrences of "reverse move" corrected to "COPY TO SIOPV" (item 8 and completion log)

**Old Stage 4 pre-planning notes file**:
- Superseded notice added at line 1
- File is now historical context only — do not use it for planning

**New Stage 4.1 pre-planning notes file**:
- Written to `stage4.1-research/` with full Stage 4.1 scope, rules, and decisions

### 6. ALL pre-Stage-4.1 preparation is now 100% complete

Nothing remains before the guidelines file can be written. Every reference document has been updated. Every directory has been created. The next action is to write the Stage 4.1 guidelines file (the orchestrator briefing for Stage 4.1).

---

## Section 2: The Stage 4.1/4.2 Split — Full Design Decisions

### Stage 4.1: Research & Truth Document

**What it is:** A pure research stage. No files are created in `siopv/.claude/`. The only output is a truth document — a comprehensive specification that tells Stage 4.2 exactly what to implement.

**Structure:** Two rounds only.

**Round 1: Online Research**
- Topic: Claude Code best practices as of March 2026
- Required research topics (all mandatory, not optional):
  1. Agent definition file formats — current frontmatter schema, required fields, valid values
  2. Hook script formats — `.claude/hooks/` structure, event types, blocking vs. non-blocking, script requirements
  3. `settings.json` structure — current schema, all valid fields, permissions model
  4. **Skills 2.0 (CRITICAL)** — the skills file format changed recently; training data is likely stale on this topic; Round 1 must verify the current format from online sources before any skill files are written
  5. `CLAUDE.md` patterns — what belongs in global vs. project-level CLAUDE.md
  6. Memory file patterns — `.claude/memory/` conventions, file limits, indexing
  7. Workflow file patterns — `.claude/workflow/` conventions, how they are referenced
- Output: research report saved to `stage4.1-research/`
- Agent split for Round 1: orchestrator decides (likely 2–3 parallel researchers)

**Round 2: Truth Document Synthesis**
- Inputs: Round 1 research report + all existing SIOPV files + all meta-project files
- Process: parallel agents split by domain (orchestrator decides exact split — suggested: agent definitions / hooks / settings+workflow / skills)
- Priority rule: research findings (Round 1) take priority over meta-project patterns where they conflict
- Output: the truth document — a single comprehensive file specifying every file that Stage 4.2 must create in `siopv/.claude/`
- Save location: `stage4.1-research/`

**What Stage 4.1 does NOT do:**
- Create any files in `siopv/.claude/` — that directory does not exist yet and must not be created during Stage 4.1
- Implement anything — it only specifies
- Approve its own output — human reviews the truth document before Stage 4.2 is authorized

### Stage 4.2: Implementation

**What it is:** The implementation stage. Takes the Stage 4.1 truth document as primary input and builds the entire `siopv/.claude/` directory structure.

**Key tasks:**
1. Create `siopv/.claude/` and all subdirectories
2. Copy applicable agent definitions from meta-project (adapted per truth document)
3. Copy and adapt hooks from meta-project (all hooks need path adaptation minimum)
4. Create settings.json, workflow files, CLAUDE.md, memory files per truth document specification
5. Create skill files in the format verified by Stage 4.1 Round 1 (Skills 2.0)
6. Final deliverable: Stage 5 (REMEDIATION-HARDENING) orchestrator briefing file
7. COPY TO SIOPV task at end: copy finalized briefing.md, spec-findings.md, compaction-log.md, orchestrator-briefing.md, and applicable hooks from `sec-llm-workbench/.claude/` to `siopv/.claude/` — originals remain intact in the meta-project

**Human review window:** Between Stage 4.1 output and Stage 4.2 start. Human must explicitly authorize Stage 4.2.

### Copy-Only Rule (CRITICAL — carry forward always)

Files from the meta-project (`sec-llm-workbench/.claude/`) are COPIED to `siopv/.claude/`. Originals remain in `sec-llm-workbench/.claude/`. Nothing is moved. Nothing is deleted from the meta-project. Ever. Any agent that deletes or moves a meta-project file must be considered to have failed and its work must be rolled back.

---

## Section 3: The Truth Document — What It Must Contain

The truth document is the core deliverable of Stage 4.1. It must be complete enough that Stage 4.2 agents can implement everything without any inference, improvisation, or guesswork. For every single file in every directory of `siopv/.claude/`, the truth document must specify:

1. **Exact directory placement** — absolute path, not relative
2. **Complete file skeleton / structure** — all sections listed by name, in order
3. **Contents of each section** — what each section must contain and why
4. **Cross-file dependencies** — connections to other files in other directories (e.g., a hook referenced in settings.json must match the hook filename exactly)
5. **File action classification** — one of:
   - `copy as-is from meta-project` — exact copy, no changes
   - `adapt from meta-project` — copy then modify, with diff specified
   - `create from scratch` — entirely new, no meta-project equivalent
6. **What is obsolete or superseded** — explicit list of anything from the meta-project version that must NOT be carried over, with explanation of what modern best practice replaces it with

### Skills 2.0 Specific Requirement

The skills file format changed between the version in the meta-project and March 2026 best practice. This is the highest-priority research item in Round 1. The truth document must:
- State the current (March 2026) correct format for skill files
- Provide a complete skeleton for each skill file in the correct format
- Mark the old format as obsolete with explicit notation of what changed
- Show the diff between old and new format for at least one example skill

### Directory Structure to Cover

The truth document must cover every file in:
```
siopv/.claude/
├── CLAUDE.md
├── settings.json
├── agents/
│   └── [all applicable agent definitions from meta-project inventory]
├── hooks/
│   └── [all adapted hooks — none carry over as-is]
├── workflow/
│   └── [all workflow files — adapted from meta-project]
├── docs/
│   └── [all doc files — adapted from meta-project]
├── rules/
│   └── [all rules files — adapted from meta-project]
├── memory/
│   └── [MEMORY.md — new, SIOPV-specific]
└── skills/
    └── [all skill files — in Skills 2.0 format]
```

For each file, the truth document must include the full specification as described above.

---

## Section 4: The Guidelines File — What It Is and What It Must Contain

### What the Guidelines File Is

The guidelines file is the Stage 4.1 orchestrator briefing. It is the document passed to the orchestrator when the Stage 4.1 team is spawned. It is NOT the truth document. It is the instructions for HOW to produce the truth document.

The guidelines file is the NEXT ACTION after compaction. Writing it is the immediate task upon resuming.

### What the Guidelines File Is NOT

- It is not a summary — it is an operational briefing
- It is not the truth document — it does not specify what goes in `siopv/.claude/`
- It is not a planning document — it is a complete execution specification

### Requirements for the Guidelines File

The guidelines file must be:
- **Comprehensive** — covers every aspect of Stage 4.1 execution
- **Granular** — point by point, no room for interpretation or improvisation
- **Self-contained** — the orchestrator must be able to plan and execute Stage 4.1 from this file alone, without reading any other planning document
- **Technical** — specifies agent definitions, round structure, file naming, aggregation protocol, human checkpoints

### Content the Guidelines File Must Cover (minimum)

1. **What Stage 4.1 is and is not** — the scope definition (research only, no implementation, no `siopv/.claude/` creation)

2. **Reading list** — the 6 files the orchestrator must read before planning:
   - This handoff file
   - `briefing.md`
   - `stage4.1-research/` pre-planning notes
   - The meta-project agent inventory catalog
   - The Stage 3.5 final brief (input context for the truth document)
   - The Stage 2 final report (hexagonal violations — context for what SIOPV needs)

3. **Round 1 specification:**
   - Mandatory research topics (all 7 listed in Section 2)
   - Emphasis: Skills 2.0 is the highest-priority topic — must be researched first
   - What the research report must contain (structure, headings, content per topic)
   - Where to save it: `stage4.1-research/` with timestamp prefix
   - Agent configuration for Round 1 researchers (model: sonnet, tools: WebSearch + WebFetch + Context7)

4. **Round 2 specification:**
   - Inputs: Round 1 report + all existing SIOPV files + all meta-project files
   - How to split agents by domain (suggested: agent defs / hooks / settings+workflow / skills)
   - What the truth document must contain (full detail per Section 3 of this handoff)
   - Priority rule: research findings override meta-project patterns
   - Where to save: `stage4.1-research/` with timestamp prefix
   - Aggregation: orchestrator assembles agent outputs into single truth document

5. **Truth document structure and required content** — verbatim from Section 3 of this handoff, so the orchestrator knows exactly what to tell Round 2 agents to produce

6. **Human checkpoint protocol:**
   - After Round 1 research report is complete: orchestrator sends summary to team-lead → team-lead relays to human → human approves → Round 2 begins
   - After truth document is complete: orchestrator sends summary to team-lead → team-lead relays to human → human reviews truth document → human explicitly authorizes Stage 4.2 (or requests corrections)
   - Orchestrator's idle/waiting state is NOT approval — human must explicitly type approval

7. **Absolute rules** — all rules from Section 5 of this handoff, listed explicitly in the guidelines file so the orchestrator enforces them on sub-agents

8. **Team-lead role** — what team-lead does and does not do (from Section 6 of this handoff)

9. **Agent failure protocol:**
   - If any Round 1 agent fails to complete its research: orchestrator re-assigns to a new agent, does NOT proceed to Round 2 with incomplete data
   - If any Round 2 agent produces incomplete truth document section: orchestrator re-assigns, does NOT submit incomplete truth document
   - Orchestrator reports all failures to team-lead via SendMessage — team-lead does not approve re-assignment, orchestrator handles autonomously

10. **File naming convention:** `YYYY-MM-DD-HH.mm.ss-stage4.1-{agent-name}-{slug}.md` (dots between time components, not colons)

11. **Save location for all reports:** `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/` (absolute path, hardcoded)

12. **Model specification:** every Agent() call must use `model: sonnet` — Opus is never used

### Where to Save the Guidelines File

```
/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/YYYY-MM-DD-HH.mm.ss-stage4.1-guidelines-orchestrator-briefing.md
```

Replace `YYYY-MM-DD-HH.mm.ss` with the actual timestamp at time of writing.

---

## Section 5: Absolute Rules Agreed in This Session (Carry Forward to All Work)

These rules were explicitly agreed in this session and apply to all Stage 4.1 and Stage 4.2 work:

| Rule | Detail |
|------|--------|
| Model | `model: sonnet` on every Agent() call — Opus is never used |
| Directory creation | No agent creates directories — team-lead creates all directories before spawning agents |
| Paths | All paths hardcoded absolute — no `$HOME`, no `~`, no relative paths |
| Research priority | Online research findings take priority over meta-project patterns where they conflict |
| No improvisation | No agent improvises — if unclear, stop and report to team-lead via SendMessage |
| Human checkpoints | Human approves at every checkpoint — orchestrator's idle state is NOT approval |
| Copy-only | Copy only from meta-project — never move, never delete |
| Stage 4.1 output location | All Stage 4.1 output goes to `stage4.1-research/` only — never to `siopv/.claude/` |
| `siopv/.claude/` timing | `siopv/.claude/` does not exist yet — team-lead creates it only during Stage 4.2, after human authorization |
| Timestamp format | `YYYY-MM-DD-HH.mm.ss` (dots between time components, not colons) |
| Sub-agent returns | Agents return condensed summaries 1,000–2,000 tokens to coordinator — never full file dumps |
| Agent body limit | 200 lines per agent definition maximum — beyond this, quality degrades |
| Blocking hooks | `PreToolUse` is the ONLY blocking hook type — use it for security gates |

---

## Section 6: Team-Lead Role (Final, Agreed)

The team-lead is the Claude Code main instance (this session). The team-lead is NOT the orchestrator. These roles are separate.

### What Team-Lead Does (in order)

1. Write the Stage 4.1 guidelines file (the orchestrator briefing)
2. Create the team with `TeamCreate`
3. Spawn the orchestrator (model: sonnet) with a prompt pointing to the guidelines file
4. Relay the orchestrator's plan to the human — human approves, not team-lead
5. Relay human approval back to orchestrator via `SendMessage`
6. After Round 1 is complete: receive summary from orchestrator, relay to human, wait for human approval before signaling Round 2 start
7. After Round 2 is complete: receive truth document summary from orchestrator, relay to human, wait for human review and explicit Stage 4.2 authorization
8. Shut down agents when 100% certain they have finished all work (no human approval needed for shutdown)
9. Call `TeamDelete` when Stage 4.1 is complete
10. Update `briefing.md` and `MEMORY.md`: mark STAGE-4.1 as ✅ COMPLETE

### What Team-Lead Does NOT Do

- **Does NOT approve** — human approves, always
- **Does NOT plan** — orchestrator plans the agent strategy
- **Does NOT create `siopv/.claude/`** — that happens in Stage 4.2, after human authorization
- **Does NOT improvise** — follows guidelines file exactly
- **Does NOT do research directly** — that is Round 1 agents' job
- **Does NOT write the truth document** — that is Round 2 agents' job

---

## Section 7: Files to Read After Compaction (Resume Checklist)

Read in this exact order to resume. Do not skip any.

**Step 1:** You are reading it — this handoff file. Continue reading it completely before taking any action.

**Step 2:** Read the master briefing (updated with 4.1/4.2 split):
```
/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md
```

**Step 3:** Read the Stage 4.1 pre-planning notes (the active planning document):
```
/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/2026-03-12-stage4.1-research-pre-planning-notes-scope-decisions-and-rules.md
```

**Then proceed immediately to write the Stage 4.1 guidelines file.** No additional research or reading is needed before writing. Everything required is in this handoff document and the two files above.

---

## Section 8: File Inventory — What Exists in stage4/ and stage4.1-research/ Right Now

### `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4/`

| File | Status | Purpose |
|------|--------|---------|
| `2026-03-12-11.56.25-stage4-pre-task-inventory-and-classification-of-meta-project-agents-and-hooks-for-siopv-reuse.md` | ACTIVE — reference | Catalog of all 23 meta-project agents and 9 hooks, classified as SIOPV-APPLICABLE / ADAPTABLE / META-ONLY. Used as input to Stage 4.1 Round 2. |
| `2026-03-12-14.04.54-stage4-pre-planning-preparation-notes-scope-decisions-research-first-approach-and-open-questions-before-orchestrator-spawn.md` | SUPERSEDED — has notice at top | Old pre-planning notes from before the 4.1/4.2 split. Historical context only. Do NOT use for planning. |

### `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/`

| File | Status | Purpose |
|------|--------|---------|
| `2026-03-12-stage4.1-research-pre-planning-notes-scope-decisions-and-rules.md` | ACTIVE | Stage 4.1 pre-planning notes (post-split, current state) |
| `2026-03-12-15.16.12-handoff-stage4.1-pre-guidelines-complete-session-context.md` | This file | Post-compaction resume document |

### `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.2-implementation/`

Currently empty. Stage 4.2 has not started.

### Reference Documents Updated This Session

All the following files were updated with the 4.1/4.2 split:

| File | Location |
|------|----------|
| `briefing.md` | `/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md` |
| `orchestrator-briefing.md` | `/Users/bruno/sec-llm-workbench/.claude/workflow/orchestrator-briefing.md` |
| `MEMORY.md` | `/Users/bruno/sec-llm-workbench/.claude/projects/-Users-bruno-sec-llm-workbench/memory/MEMORY.md` (auto-memory) |
| `setup-checklist.md` | `/Users/bruno/sec-llm-workbench/.claude/workflow/setup-checklist.md` |

---

## Section 9: Key Catalog Findings (from Meta-Project Agent & Hook Inventory)

Full catalog: `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4/2026-03-12-11.56.25-stage4-pre-task-inventory-and-classification-of-meta-project-agents-and-hooks-for-siopv-reuse.md`

### Agent Classification Summary (23 total)

| Classification | Count | Examples |
|----------------|-------|---------|
| SIOPV-APPLICABLE | 16 | code-implementer, async-safety-auditor, hallucination-detector, security-auditor, best-practices-enforcer, code-reviewer, test-generator, integration-tracer, import-resolver, semantic-correctness-auditor, and others |
| ADAPTABLE | 4 | xai-explainer (adapt for SIOPV domain), researcher-1, researcher-2, researcher-3 (adapt research scope) |
| META-ONLY | 3 | final-report-agent, report-summarizer, vulnerability-researcher — discard entirely |

### Hook Classification Summary (9 total)

| Classification | Count | Notes |
|----------------|-------|-------|
| ADAPTABLE | 4 | `pre-commit.sh`, `verify-best-practices.sh`, `post-code.sh`, `pre-git-commit.sh` — all need path adaptation minimum |
| META-ONLY (discard) | 5 | `pre-write.sh`, `test-framework.sh`, `pre-compact.sh`, `session-start.sh`, `session-end.sh` — SIOPV needs new hooks pointing to `siopv/` paths |
| SIOPV-APPLICABLE as-is | 0 | No hooks carry over without modification |

**Key insight:** All 9 hooks reference meta-project absolute paths. Even "ADAPTABLE" hooks need path surgery before use in SIOPV. Stage 4.2 must verify every path reference in every hook before deployment.

### P1 Agents for SIOPV (Deploy First in Stage 4.2)

These four agents are highest priority for Stage 4.2 deployment:

1. **code-implementer** — primary builder for remediation (Stage 5) and Phase 7/8 implementation
2. **async-safety-auditor** — addresses SIOPV Issue #7 (asyncio.run() in sync nodes) and Phase 7 event loop safety
3. **integration-tracer** — addresses Issue #1 (CLI hollow) and Issue #4 (parameter dropping in run_pipeline)
4. **hallucination-detector** — Phase 7/8 library API verification (critical given stale training data risk)

---

## Section 10: SIOPV Known Issues (Context for Stage 5 Remediation-Hardening)

These issues are NOT fixed in Stage 4.1 or Stage 4.2. They are fixed in Stage 5 (REMEDIATION-HARDENING). They are listed here for context — the truth document will reference them when specifying agent definitions, because agent definitions for SIOPV must be scoped to these issues.

### CRITICAL Issues (4)

| # | Issue | Location |
|---|-------|----------|
| 1 | CLI hollow — 3 TODO stubs, pipeline unreachable from CLI | `src/siopv/interfaces/cli/main.py` |
| 2 | `SanitizeVulnerabilityUseCase` orphaned — dead code; `dlp_node` is the real data path | `src/siopv/application/use_cases/` |
| 3 | LLM confidence is a heuristic — `_estimate_llm_confidence()` is pure math, not LLM; `adapters/llm/` is empty | `src/siopv/adapters/llm/` |
| 4 | `run_pipeline()` drops enrichment clients — does not pass nvd/epss/github/osint/vector_store | `src/siopv/application/orchestration/graph.py:439-444` |

### HIGH Issues (5)

| # | Issue | Location |
|---|-------|----------|
| 5 | DLP DI not exported — missing `get_dlp_port`, `get_dual_layer_dlp_port` | `src/siopv/infrastructure/di/__init__.py` |
| 6 | Hardcoded Haiku model IDs — 4 DLP files hardcode `"claude-haiku-4-5-20251001"` | `src/siopv/adapters/dlp/` (4 files) |
| 7 | `asyncio.run()` in sync nodes — breaks inside async runners | `dlp_node`, `enrich_node`, `authorization_node` |
| 8 | No tests for Phase 2 adapters — NVD, EPSS, GitHub, Tavily, ChromaAdapter at 0–20% coverage | `tests/adapters/` |
| 9 | `structlog format_exc_info` deprecation — UserWarning on every test run | `src/siopv/infrastructure/logging/setup.py` |

### Key SIOPV Architecture Facts

- **Graph flow:** START → authorize → ingest → dlp → enrich → classify → [escalate] → END
- **DLP path:** `dlp_node` is the real path (NOT `SanitizeVulnerabilityUseCase` — that is dead code)
- **LangGraph state:** TypedDict (not Pydantic) — by design
- **Checkpointing:** SQLite (`siopv_checkpoints.db`)
- **DI pattern:** `lru_cache` factory functions in `infrastructure/di/`
- **Docker:** `docker-compose.yml` runs OpenFGA + Keycloak + Postgres
- **Interrupt point (Phase 7):** `interrupt_before=["escalate"]` — checkpointer must be present at compile time

---

## Section 11: Next Action — IMMEDIATE After Compaction

**Write the Stage 4.1 guidelines file.**

### Protocol for Writing the Guidelines File

Per global rules, you must announce TeamCreate before any task. However, writing the guidelines file is a pure writing task. The user will likely say "do it yourself" or "use a subagent" — this is fine and expected. Wait for explicit override before proceeding.

If the user says "do it yourself": write the guidelines file directly, in this session, right now.

If the user says "use a subagent": spawn a single subagent to write the guidelines file.

If the user says nothing / types nothing: wait for override before proceeding (the announcement is a gate, not theater — Section 11 of `errors-to-rules.md`).

### What to Write

Write a comprehensive Stage 4.1 orchestrator briefing. The guidelines file must be complete enough that an orchestrator spawned with only this file can plan and execute Stage 4.1 without reading any other planning document.

Use this handoff document's Section 4 as the specification for the guidelines file content. Specifically, ensure the guidelines file includes all 12 content areas listed in Section 4.

### Save Location

```
/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/YYYY-MM-DD-HH.mm.ss-stage4.1-guidelines-orchestrator-briefing.md
```

Replace `YYYY-MM-DD-HH.mm.ss` with the actual current timestamp at time of writing.

### After the Guidelines File Is Written

1. Show the user the guidelines file path and a brief summary of what it covers
2. Ask the user to review it (this is a human checkpoint — human must approve before the team is spawned)
3. After human approves: create the team with `TeamCreate` and spawn the orchestrator
4. Name this instance "claude-main" in the team so the orchestrator can reach you via `SendMessage`

---

## Appendix A: SIOPV Stage Status (as of end of this session)

| Stage | Name | Status |
|-------|------|--------|
| STAGE-1 | Phases 0–6 Discovery | ✅ COMPLETE |
| STAGE-2 | Hexagonal Quality Audit | ✅ COMPLETE |
| STAGE-3 | SOTA Research & Deep Scan | ✅ COMPLETE |
| STAGE-3.5 | Reports Extraction & Aggregation | ✅ COMPLETE |
| STAGE-4.1 | Research & Truth Document | ⏳ NEXT — guidelines file is the first deliverable |
| STAGE-4.2 | Implementation | ⏳ PENDING — after Stage 4.1 human approval |
| STAGE-5 | Remediation-Hardening | ⏳ PENDING — after Stage 4.2 |
| PHASE-7 | Human-in-the-Loop (Streamlit) | ⏳ PENDING |
| PHASE-8 | Output (Jira + PDF) | ⏳ PENDING |

---

## Appendix B: Critical Path to Phase 7

Understanding why Stage 4.1 matters for Phase 7:

1. Stage 4.1 produces the truth document — specifies the complete `siopv/.claude/` setup
2. Stage 4.2 implements `siopv/.claude/` — creates all agents, hooks, settings for SIOPV
3. Stage 5 uses those agents (especially code-implementer and async-safety-auditor) to fix the 9 CRITICAL/HIGH issues
4. The 9 issues fixed in Stage 5 are prerequisites for Phase 7 — particularly Issue #7 (`asyncio.run()` in sync nodes) which would break the Streamlit async bridge needed for Phase 7

If Stage 4.1 produces an incomplete or incorrect truth document, Stage 4.2 will implement the wrong thing, Stage 5 agents will not work correctly, and Phase 7 implementation will fail at the async bridge.

**This is why Skills 2.0 research is the highest-priority item in Round 1** — if the skill file format is wrong, agents cannot be invoked correctly, and the entire Stage 5 implementation chain breaks.

---

## Appendix C: Context7 Coverage Gaps (from Stage 3.5)

Libraries NOT covered by Context7 that Round 1 researchers must use WebSearch/WebFetch for:

| Library | Why It Matters for SIOPV |
|---------|--------------------------|
| LIME | Phase 7 XAI explanations — memory leak risk (`plt.close(fig)` after `st.pyplot()`) |
| Jira v3 | Phase 8 output — description requires ADF format, plain strings silently rejected |
| Redis | Phase 7 state persistence — must use `redis.asyncio`, not `aioredis` |

Libraries where Context7 is useful but must be cross-checked with WebSearch:
- Streamlit (Skills 2.0 format may also be on Streamlit docs if applicable)
- LangGraph (`interrupt()` requires checkpointer at compile time — verify current API)
- fpdf2 (`fname` required in `add_font()` since 2.7 — verify current breaking changes)

---

*End of handoff document. Post-compaction Claude: read Section 7 for the resume checklist, then proceed to Section 11 for the immediate next action.*
