# Stage 4.1 — Unified Merging Proposals (Final)

> Total MERGE proposals: 18
> Proposals requiring human judgment: 12
> Proposals auto-applicable: 6
> Source files: proposals-a2, proposals-b2, proposals-c2

---

## Proposals Requiring Human Judgment

### MERGE-001: `memory` field value — `"project"` vs `true`

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/smoke-test-runner.md`
- `/Users/bruno/siopv/.claude/agents/test-generator.md`
- `/Users/bruno/siopv/.claude/agents/xai-explainer.md`

**Source reports:** proposals-a2 (A-MERGE-001 → domain reports A3, A4)

**Option A:** Carry all `memory` values as-is — keep `memory: project` for the 3 agents that currently have it, keep `memory: true` for agents that have it.
**Rationale A:** Least-change principle. Both forms came from working meta-project agents. No confirmed evidence of breakage. A4 recommends "carry as-is."

**Option B:** Standardize all agents to `memory: true` — change `memory: project` → `memory: true` in smoke-test-runner, test-generator, and xai-explainer to match the majority.
**Rationale B:** Consistency across the SIOPV agent suite. `memory: true` is confirmed valid per A4 (code-reviewer). `"project"` is unconfirmed. Reduces risk of a silent schema rejection.

**Recommendation:** A — carry as-is. Both values appear to be in active use in the meta-project without issues. Standardizing prematurely introduces unnecessary churn without confirmed need.
**Human judgment required:** YES — `memory: project` has not been confirmed valid by official schema documentation. If official Claude Code docs beyond the Round 1 findings are accessible, this should be verified before deploying.

---

### MERGE-002: `code-implementer` references to potentially missing SIOPV files

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/code-implementer.md`

**Source reports:** proposals-a2 (A-MERGE-002 → domain report A1)

**Option A:** Keep the references as-is with a parenthetical note: `(in SIOPV project root — create if not present)`.
**Rationale A:** The files are worth creating as part of Stage 4.2 setup — they encode SIOPV Python conventions and tech-stack constraints that the code-implementer should follow. Keeping the reference ensures Stage 4.2 knows to create them.

**Option B:** Remove the two `Read` steps from the "Before Writing Code" checklist and replace with inline notes referencing the SIOPV pyproject.toml and existing CLAUDE.md instead.
**Rationale B:** Avoids deploying an agent with broken file reads. SIOPV already has conventions in CLAUDE.md and pyproject.toml; the code-implementer can be pointed to those instead of non-existent docs.

**Recommendation:** A — keep references with a note. Stage 4.2 must create both files (`.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md`) before deploying code-implementer.
**Human judgment required:** YES — Stage 4.2 scope decision: are `.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md` in scope to create for SIOPV? If deferred, Option B must be chosen instead.

---

### MERGE-003: `check-reviewer-score.sh` — missing in SIOPV

**Files affected:**
- `/Users/bruno/siopv/.claude/hooks/pre-git-commit.sh`
- (optionally) `/Users/bruno/siopv/.claude/scripts/check-reviewer-score.sh`

**Source reports:** proposals-a2 (A-MERGE-003 → domain report B1)

**Option A:** Create a minimal stub at `/Users/bruno/siopv/.claude/scripts/check-reviewer-score.sh` that always exits 0 (pass), with a comment explaining it needs to be implemented.
**Rationale A:** Makes the hook aware that the script exists conceptually, prevents confusion about why the score check never fires, and provides a placeholder for Stage 4.2. No behavioral change vs. missing file (both pass), but explicit intent.

**Option B:** Accept the graceful-skip behavior — do not create the stub. The score threshold check will silently not run in SIOPV. The if-exists guard in pre-git-commit.sh handles this cleanly.
**Rationale B:** Minimal-scope approach. The guard was designed for exactly this situation. The other pre-commit checks (pending verification markers) still function correctly.

**Option C:** Remove the `check-reviewer-score.sh` block from `pre-git-commit.sh` entirely since it will never fire and carrying dead conditional logic into SIOPV adds noise.
**Rationale C:** Clean adaptation — if a feature is known not to function in the target project, removing it is cleaner than leaving silently inert code.

**Recommendation:** B — accept graceful-skip. The guard already handles this cleanly. Options A/C both require additional scope decisions. Stage 4.2 can revisit.
**Human judgment required:** YES — the choice between A/B/C affects SIOPV security posture (reviewer score enforcement). This is a behavioral decision, not a purely technical one.

---

### MERGE-004: pre-commit.sh and verify-best-practices.sh — registration event type

**Files affected:**
- `/Users/bruno/siopv/.claude/settings.json` (hooks registration)
- `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`
- `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`

**Source reports:** proposals-a2 (A-MERGE-004 → domain report B1)

**Option A:** Wrap both scripts as `UserPromptSubmit` hooks in `settings.json`, OR rewrite both scripts to accept stdin JSON and parse the project path from `$CLAUDE_PROJECT_DIR` env var instead of `$1`.
**Rationale A:** Makes both scripts proper first-class Claude Code lifecycle hooks that auto-fire at appropriate times. Consistent with the hook system design.

**Option B:** Register both scripts as manual-invocation utilities only — document them in SIOPV CLAUDE.md as scripts to run manually rather than auto-hooks. Do not register in settings.json.
**Rationale B:** Matches actual current behavior in the meta-project. No script modification needed. Lower risk of unintended auto-fires. The post-code.sh (PostToolUse) + pre-git-commit.sh (PreToolUse) chain already covers automatic enforcement.

**Recommendation:** B — register as manual utilities. The scripts work correctly as standalone shell tools. Rewriting them to parse stdin JSON changes their behavior and scope beyond the adaptation task.
**Human judgment required:** YES — determines whether SIOPV gets automatic pre-response linting checks or manual-only checks. Security/workflow posture decision.

---

### MERGE-005: `xai-explainer` class_names — deploy as-is vs. verify first

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/xai-explainer.md`

**Source reports:** proposals-a2 (A-MERGE-005 → domain report A4)

**Option A:** Deploy xai-explainer with `class_names=["LOW", "MEDIUM", "HIGH", "CRITICAL"]` as-is, based on A4's assessment that they appear correct for Phase 3 XGBoost classifier.
**Rationale A:** A4 reviewed the adaptation spec and found no contradicting source. SIOPV MEMORY.md aligns with these labels. Fast path to deployment; correctness can be verified at runtime.

**Option B:** Block deployment of xai-explainer until Stage 4.2 reads `src/siopv/adapters/ml/` to confirm the actual class labels used in the XGBoost model training pipeline.
**Rationale B:** Wrong class_names in LIME produce silently incorrect explanations — not an error, just wrong audit output. For a security audit tool, silent incorrectness is worse than a deployment delay.

**Recommendation:** B — verify before deploying. xai-explainer produces audit artifacts (SHAP/LIME explanations) that feed compliance and audit workflows. Silent incorrectness in those artifacts is a higher risk than a short delay for a one-file verification read.
**Human judgment required:** YES — risk tolerance decision. If fast deployment with post-deployment validation is preferred, Option A is acceptable.

---

### MERGE-006: test-framework.sh — hook event binding vs standalone manual script

**Files affected:**
- `/Users/bruno/siopv/.claude/hooks/test-framework.sh`
- `/Users/bruno/siopv/.claude/settings.json`

**Source reports:** proposals-b2 (B-MERGE-001 → domain report B2)

**Option A:** Keep as a manually-invoked standalone validation script only. Place it in `.claude/hooks/` as a utility script (or move to `.claude/scripts/`) with documentation that it is run manually. Do NOT register it in settings.json.
**Rationale A:** The script does not output JSON hook format — wiring it to a hook event would require substantial rewriting. Its plain-text PASS/FAIL output is useful interactively. Keeping it manual avoids unexpected blocking behavior.

**Option B:** Wire it to a hook event (e.g., `TaskCompleted` or `Stop`) as an automated framework integrity check. The script would need to be modified to output JSON hook format.
**Rationale B:** Placing a validation script in `hooks/` without binding it to any event is architecturally inconsistent — hooks should hook into events. Wiring to `Stop` would run it before every session exit.

**Recommendation:** A (keep as manual script). Script does not output JSON format and would need significant rewriting for hook compatibility; manual invocation preserves its diagnostic utility without risk of false-positive blocking.
**Human judgment required:** YES

---

### MERGE-007: UserPromptSubmit hook — keep inert vs remove for SIOPV

**Files affected:**
- `/Users/bruno/siopv/.claude/settings.json`

**Source reports:** proposals-b2 (B-MERGE-002 → domain report C1)

**Context:** The `UserPromptSubmit` hook at settings.json line 240 scans `$CLAUDE_PROJECT_DIR/projects/$name.$ext` to detect project names. SIOPV is a single project and does not use a `projects/` subdirectory under `.claude/`. MEMORY.md records `projects/siopv.json` at the project root (not `.claude/projects/`), so the hook may fire if the user types "siopv" in a prompt.

**Option A:** Keep the hook as-is (harmless, inert). If SIOPV ever gains a second project file, the hook activates automatically.
**Rationale A:** Zero maintenance cost. Follows the COPY/ADAPT approach — do not delete without cause.

**Option B:** Remove the UserPromptSubmit hook entirely from SIOPV's settings.json.
**Rationale B:** The hook was designed for the meta-project's multi-project disambiguation workflow. In SIOPV, it represents unused meta-project logic. Inert hooks that serve no purpose create noise when debugging settings.json.

**Recommendation:** B (remove). The hook was designed for multi-project disambiguation, a meta-project concept; SIOPV is a single-project repo and clarity in settings.json outweighs the marginal value of carrying inert code.
**Human judgment required:** YES

---

### MERGE-008: SIOPV spec path conflict — C2 vs C3

**Files affected:**
- `/Users/bruno/siopv/.claude/workflow/spec-findings.md`
- `/Users/bruno/siopv/.claude/workflow/orchestrator-briefing.md`

**Source reports:** proposals-b2 (B-MERGE-005 → domain reports C2 and C3)

**Conflict:** C2 and C3 give conflicting instructions for the SIOPV spec file path.
- **C2** (spec-findings.md): update path to `/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt` — states file "already exists" there.
- **C3** (orchestrator-briefing.md): keep path as `/Users/bruno/sec-llm-workbench/docs/SIOPV_Propuesta_Tecnica_v2.txt` — "spec lives in meta-project."

**Option A:** Use SIOPV path (`/Users/bruno/siopv/docs/`) in both files.
**Rationale A:** C2 explicitly states the file already exists at the siopv path. A cross-session reference to the meta-project from SIOPV's orchestrator introduces a dependency that may not be accessible when working in an isolated SIOPV session.

**Option B:** Use meta-project path (`/Users/bruno/sec-llm-workbench/docs/`) in both files.
**Rationale B:** The spec file was created in the meta-project as the source of truth. Duplicating it creates two copies that can drift. Both sessions have filesystem access to the meta-project path.

**Recommendation:** A (use SIOPV path in both files). C2 verified the file already exists at the siopv path; a SIOPV session should be self-contained and not depend on meta-project paths for its primary spec document.
**Human judgment required:** YES — depends on whether the spec file has a canonical home Bruno wants to maintain.

---

### MERGE-009: pre-write.sh `/init-session` skill prerequisite — create new skill vs simplify checkpoint mechanism

**Files affected:**
- `/Users/bruno/siopv/.claude/hooks/pre-write.sh`
- `/Users/bruno/siopv/.claude/skills/` (possible new skill)

**Source reports:** proposals-b2 (B-MERGE-006 → domain report B2)

**Context:** `pre-write.sh` depends on three daily checkpoint files under `.build/checkpoints/daily/` (`briefing-read-${TODAY}`, `spec-read-${TODAY}`, `errors-read-${TODAY}`). These are created by an `/init-session` or equivalent skill. No existing `/init-session` skill is in the SIOPV `.claude/` directory — it would need to be created from scratch.

**Option A:** Create a new `/init-session` skill for SIOPV that creates the three checkpoint files. Define a `SKILL.md` at `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md` that instructs Claude to read briefing.md, spec-findings.md, and errors-to-rules.md, then create the corresponding marker files.
**Rationale A:** Without a skill to create checkpoints, the hook will block every Write/Edit with a permission prompt, making the system unusable or forcing the hook to be disabled. A proper skill preserves the safety intent.

**Option B:** Simplify pre-write.sh to remove the checkpoint dependency entirely — only inject the `additionalContext` reminder without checking checkpoint files (always emit the reminder but never block with `permissionDecision: "ask"`).
**Rationale B:** The checkpoint system was designed for the meta-project's complex multi-stage workflow. For SIOPV's Phase 7/8 work, the overhead of running a skill every session start may be unnecessary. A passive reminder achieves the same anti-autopilot effect with zero maintenance overhead.

**Recommendation:** A (create /init-session skill). The blocking enforcement is the valuable part of pre-write.sh — a passive reminder can be ignored, while the checkpoint gate cannot.
**Human judgment required:** YES — depends on whether full checkpoint enforcement or a lighter-weight approach is desired for SIOPV.

---

### MERGE-010: Smoke-test CVE ID in verification-thresholds.md

**Files affected:**
- `/Users/bruno/siopv/.claude/docs/verification-thresholds.md` (line 201)

**Source reports:** proposals-c2 (C-MERGE-001 → domain report C4b)

**Context:** `verification-thresholds.md` line 201 currently uses `CVE-2024-1234` — a synthetic placeholder from the meta-project. The SIOPV team must specify the actual CVE ID used for smoke testing.

**Option A:** Replace with a specific, known SIOPV test CVE ID (e.g., a CVE already present in SIOPV's test fixtures or integration test data).
**Rationale A:** Most accurate — the smoke test verifies real pipeline behavior. A real CVE ID makes the check meaningful and reproducible.

**Option B:** Replace with a parameterized placeholder `{{ smoke_test_cve_id }}` (Cookiecutter convention per `placeholder-conventions.md`), leaving resolution to the implementer.
**Rationale B:** Keeps the file deployable without requiring an upfront decision. The implementer can substitute the value based on available test data. Safer than leaving `CVE-2024-1234` (which could silently pass on wrong data).

**Recommendation:** B — parameterize. The smoke-test CVE ID is an implementation detail, not an architectural decision. Using `{{ smoke_test_cve_id }}` signals clearly that substitution is required.
**Human judgment required:** YES — a human must confirm whether a specific CVE (Option A) or a placeholder (Option B) is preferred. If A, the human must also specify which CVE ID.

---

### MERGE-011: verify/SKILL.md Experimental Batch Mode Section

**Files affected:**
- `/Users/bruno/siopv/.claude/skills/verify/SKILL.md` (lines 31–37 of source)

**Source reports:** proposals-c2 (C-MERGE-003 → domain report D3)

**Context:** `verify/SKILL.md` body contains a batch mode section marked `⚠️ EXPERIMENTAL` referencing `.ignorar/experimental-scripts/batch-api-verification/submit-batch-verification.py`. This script lives in the meta-project's `.ignorar/` directory, which is excluded from git and not part of SIOPV.

**Option A:** Remove the entire experimental batch mode section from the SIOPV copy of verify/SKILL.md.
**Rationale A:** The referenced script does not exist in SIOPV. Keeping a broken `--batch` flag in a production skill is misleading and could cause confusing failures. The standard verification workflow is all SIOPV needs for Phases 7–8.

**Option B:** Keep the experimental batch mode section as-is (copy unchanged alongside the required line 576 path fix).
**Rationale B:** The section is marked EXPERIMENTAL and describes an optional feature. Removing it loses potentially useful documentation. The batch script could be created in SIOPV's `.ignorar/` in the future.

**Recommendation:** A — remove the experimental section. SIOPV is in active development (Phases 7–8 imminent). A non-functional `--batch` flag in a critical `/verify` skill is a maintenance liability.
**Human judgment required:** YES — removing documented functionality, even experimental, warrants explicit human approval.

---

### MERGE-012: `/techniques-reference` Skill Inclusion in SIOPV CLAUDE.md

**Files affected:**
- `/Users/bruno/siopv/.claude/CLAUDE.md` (On-Demand References table)

**Source reports:** proposals-c2 (C-MERGE-005 → domain reports D1 and D2)

**Conflict:** D1 explicitly marks `/techniques-reference` as **OBSOLETE** in SIOPV CLAUDE.md and omits it from the On-Demand References table. D2 classifies techniques-reference/SKILL.md as **ADAPT** (will exist in SIOPV's `.claude/skills/techniques-reference/`). The conflict is about whether the techniques catalog (13 Vibe Coding 2026 techniques) is relevant to SIOPV or meta-project-only.

**Option A:** Exclude `/techniques-reference` from SIOPV CLAUDE.md (follow D1's recommendation). The skill exists and is invocable but isn't highlighted in the reference table.
**Rationale A:** The techniques catalog documents meta-project development patterns, not SIOPV domain logic. Cluttering SIOPV CLAUDE.md with meta-framework references reduces focus on SIOPV-specific context.

**Option B:** Include `/techniques-reference` in SIOPV CLAUDE.md's On-Demand References table, since the skill will physically exist in the project.
**Rationale B:** The 13 Vibe Coding techniques are applicable to any project built with this framework. Having the skill present but undocumented creates discoverability inconsistency.

**Recommendation:** A — follow D1's recommendation. The techniques catalog is meta-framework documentation, not SIOPV domain documentation. The skill can still be invoked by someone who knows it exists.
**Human judgment required:** YES — D1 explicitly marks it obsolete, D2 explicitly copies it. Genuine conflict about what "SIOPV-relevant" means for framework patterns.

---

## Auto-Applicable Proposals

### MERGE-013: `xai-explainer` model artifact path — assumed vs. verified

**Files affected:**
- `/Users/bruno/siopv/.claude/agents/xai-explainer.md`

**Source reports:** proposals-a2 (A-MERGE-006 → domain report A4)

**Context:** A4 proposes changing the model path from `models/xgboost_classifier.json` to `src/siopv/adapters/ml/models/xgboost_classifier.json`. This path is derived from the adaptation spec only; the actual model artifact path has not been verified against SIOPV source files.

**Option A:** Use the Stage 4.1 spec-derived path `src/siopv/adapters/ml/models/xgboost_classifier.json` and deploy, letting Stage 4.2 fix if wrong.
**Rationale A:** Reasonable assumption based on hexagonal architecture (ML models belong in the adapters layer). If wrong, the agent fails loudly at runtime (file not found) rather than silently.

**Option B:** Block xai-explainer deployment until Stage 4.2 reads SIOPV source (`src/siopv/adapters/ml/` and pyproject.toml / training scripts) to confirm the exact model artifact path.
**Rationale B:** xai-explainer has a hard dependency on the model file. A wrong path means the agent cannot run at all. Verifying once before deployment is cheaper than a failed run plus fix cycle.

**Recommendation:** B — verify path before deployment. A 1-file Glob read in Stage 4.2 (`Glob path="src/siopv/adapters/ml/**"`) prevents a guaranteed deploy failure if the path assumption is wrong.
**Human judgment required:** NO — this is a purely technical verification task that Stage 4.2 must perform regardless.

---

### MERGE-014: projects/siopv.json existence — prerequisite for 01-session-start.md

**Files affected:**
- `/Users/bruno/siopv/.claude/workflow/01-session-start.md`
- `/Users/bruno/siopv/projects/siopv.json` (creation decision)

**Source reports:** proposals-b2 (B-MERGE-003 → domain reports C1, C2)

**Context:** `01-session-start.md` is classified COPY AS-IS. The "Continúa con [PROYECTO]" trigger assumes `projects/siopv.json` exists to load project state. MEMORY.md records `State file: ~/siopv/projects/siopv.json` but this was not verified by truth agents.

**Option A:** Adopt 01-session-start.md COPY AS-IS, trusting that `projects/siopv.json` already exists per MEMORY.md.
**Option B:** Verify existence before adopting; if absent, create a minimal `projects/siopv.json` with current state.

**Recommendation:** B — verify then create if absent. A one-time `ls /Users/bruno/siopv/projects/siopv.json` check costs nothing; creating a minimal state file if absent is trivial and prevents silent workflow degradation.
**Human judgment required:** NO — can be resolved by Stage 4.2 implementer with a filesystem check.

---

### MERGE-015: agent-tool-schemas.md line 944 — remove vs replace meta-project reference

**Files affected:**
- `/Users/bruno/siopv/.claude/docs/agent-tool-schemas.md`

**Source reports:** proposals-b2 (B-MERGE-004 → domain report C4a)

**Context:** Line 944 references `.ignorar/production-reports/phase3/2026-02-08-phase3-task34-routing-savings-validation.md` — a meta-project internal report artifact that does not exist in SIOPV.

**Option A:** Remove line 944 entirely. A broken reference to a non-existent file could cause agents to emit incorrect tool calls or report false paths.
**Option B:** Replace with a SIOPV-equivalent placeholder path, e.g., `.ignorar/production-reports/phase-7/EXAMPLE-phase7-agent-report.md`, preserving the illustrative intent.

**Recommendation:** B — replace with SIOPV-equivalent placeholder. The line's purpose is illustrative; a SIOPV-appropriate placeholder teaches agents the correct report path pattern without referencing a non-existent file.
**Human judgment required:** NO — can be resolved deterministically by Stage 4.2 implementer.

---

### MERGE-016: `.build/` Directory Existence in SIOPV

**Files affected:**
- `/Users/bruno/siopv/.claude/docs/verification-thresholds.md` (line 374)
- `/Users/bruno/siopv/.claude/skills/verify/SKILL.md` (body section)

**Source reports:** proposals-c2 (C-MERGE-002 → domain report C4b)

**Context:** Both files reference `.build/checkpoints/pending/`. SIOPV's `.build/` directory existence has not been explicitly confirmed. The pre-commit hook references this path; missing directory = silent hook failures.

**Option A:** Assume `.build/` exists (SIOPV inherits meta-project conventions) and copy references unchanged.
**Option B:** Add an explicit Stage 4.2 pre-flight step: verify `.build/checkpoints/pending/` exists in SIOPV; create it if absent with `mkdir -p`.

**Recommendation:** B — add explicit creation as a Stage 4.2 pre-flight step. Creating an already-existing directory is a no-op (`mkdir -p`); creating a missing one prevents broken pre-commit hook failures.
**Human judgment required:** NO — a human can decide, but Option B is safe regardless of current state.

---

### MERGE-017: techniques-reference `@-include` — cross-agent C4a dependency

**Files affected:**
- `/Users/bruno/siopv/.claude/skills/techniques-reference/SKILL.md`

**Source reports:** proposals-c2 (C-MERGE-004 → domain report D2)

**Context:** `techniques-reference/SKILL.md` body contains `@../../docs/techniques.md`. This resolves correctly to `siopv/.claude/docs/techniques.md` only if that file exists. Agent C4a classifies techniques.md as ADAPT for SIOPV, meaning it WILL be created in Stage 4.2 deployment.

**Option A:** Proceed with COPY AS-IS (the `@../../docs/techniques.md` path resolves correctly after C4a's techniques.md is deployed). Add deployment sequencing note: C4a artifacts must be deployed before D2 artifacts.
**Option B:** Replace with inline content or a note: `<!-- TODO: ensure /Users/bruno/siopv/.claude/docs/techniques.md is created before activating this skill -->`.

**Recommendation:** A — trust C4a's confirmed ADAPT classification. No change to the SKILL.md file itself. Add a Stage 4.2 deployment note in the master checklist that techniques.md must be deployed before techniques-reference skill is used.
**Human judgment required:** NO — C4a's classification resolves this. Deployment sequencing is a Stage 4.2 concern.

---

### MERGE-018: Skills 2.0 Missing Optional Fields — Required vs Optional Designation

**Files affected:** All ADAPT skills with missing schema fields (show-trace, techniques-reference, new-project, verify, generate-report, init-session, run-tests, coding-standards-2026, cve-research, langraph-patterns)

**Source reports:** proposals-c2 (C-MERGE-006 → domain reports D2, D3, D4)

**Context:** Cross-agent disagreement: D2 flagged these as UNCLEAR; D4 definitively resolved — only `name` and `description` are required. D1 already recommends adding `user-invocable: true` to show-trace specifically. The `user-invocable` field has functional significance — it controls whether the `/skill-name` syntax works.

**Option A:** Accept D4's resolution — only `name` and `description` are required. Copy/adapt skills without adding missing optional fields.
**Option B:** Add a minimal recommended set of optional fields to all skills during ADAPT/copy: `user-invocable` (true/false), `context` (fork), and `version` (1.0).

**Recommendation:** Hybrid — add `user-invocable` selectively where functional impact is clear (each domain report already analyzed which skills are user-invocable); leave all other optional fields (model, hooks, agent, version, context) absent. Consistent with D4's conclusion while addressing the one functionally-impactful field.
**Human judgment required:** NO — but the Stage 4.2 implementer should audit each skill's `user-invocable` value before deployment.

---

## Non-Conflict Summary (informational)

Consolidated list of all clear-cut items from all 3 partial files — no human input required. One line each. This gives the Stage 4.2 implementer a complete reference.

**From proposals-a2:**
- A1–A4 (all 20 SIOPV-applicable agents): Replace Project Context block — uniform pattern, no conflicts.
- static-checks-agent (A3): COPY AS-IS — confirmed no meta-project paths in body.
- code-reviewer (A4): Strip `<!-- version: 2026-02 -->` pre-frontmatter comment — carry only frontmatter and body.
- researcher-1, researcher-2, researcher-3 (A4): Change `"claude-main"` → `"orchestrator"` in coordinator reference.
- best-practices-enforcer (A3): Remove reference to `.claude/docs/agent-tool-schemas.md` — meta-project-only doc confirmed absent from SIOPV.
- security-auditor (A3): Update experimental script path to absolute SIOPV path.
- code-implementer (A1): Fix report naming `{NNN}` → `{TIMESTAMP}` in step 10 — aligns with `agent-reports.md` convention.
- xai-explainer (A4): Fix `{NNN}` → `{TIMESTAMP}` naming convention — same alignment as above.
- dependency-scanner (A1): Remove `.build/current-phase` reference — replace with invocation-time phase parameter.
- import-resolver (A2): Remove `.build/current-phase` reference in Step 9 — replace with invocation-time phase parameter.
- smoke-test-runner (A3): Remove `.build/current-phase` reference in Step 9 — replace with invocation-time parameter and absolute report path.
- post-code.sh (B1): Remove META_PROJECT_DIR exclusion block (lines 54–58) — would break all SIOPV Python file tracking if kept.
- pre-git-commit.sh (B1): Collapse active-project indirection (lines 51–58) to single line `VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"`.
- pre-commit.sh (B1): No path surgery needed; optionally remove meta-project comment on line 5.
- verify-best-practices.sh (B1): No changes required — all generic checks applicable to SIOPV Python codebase.

**From proposals-b2:**
- pre-write.sh: CREATE at `/Users/bruno/siopv/.claude/hooks/pre-write.sh` using full script from B2 (absolute paths updated to `/Users/bruno/siopv/`). Requires `briefing.md` to contain `> Last updated:` marker.
- pre-compact.sh: CREATE at `/Users/bruno/siopv/.claude/hooks/pre-compact.sh` using full script from B2 (paths updated).
- session-start.sh: CREATE at `/Users/bruno/siopv/.claude/hooks/session-start.sh` using full script from B2 (paths updated).
- session-end.sh: CREATE at `/Users/bruno/siopv/.claude/hooks/session-end.sh` using full script from B2 (paths updated).
- settings.json (C1 ADAPT): Update 3 absolute hook paths (lines 139, 189, 258) from sec-llm-workbench to siopv; update inline tech stack string (line 130) for SIOPV-specific libraries.
- 01-session-start.md → COPY AS-IS (pending MERGE-014 resolution).
- 02-reflexion-loop.md → COPY AS-IS.
- 03-human-checkpoints.md → COPY AS-IS.
- 05-before-commit.md → COPY AS-IS.
- 04-agents.md (C2 ADAPT): Replace 8× `[TARGET_PROJECT]` with `/Users/bruno/siopv`; keep all other content.
- 06-decisions.md → COPY AS-IS.
- 07-orchestrator-invocation.md → COPY AS-IS.
- briefing.md (C2 ADAPT): 15 path/section changes; remove STAGE-1 through STAGE-4.2 plan; remove CRITICAL TRANSITION POINT block; update Section 7 all hook/log paths; update Section 8 reference table.
- spec-findings.md (C2 ADAPT): 1 path change (pending MERGE-008 resolution for which path).
- orchestrator-briefing.md (C3 ADAPT): ~12 path/section changes; remove meta-project stage subtitle; remove "Scope is always Phases 0–6 only" block; remove REMINDER TO HUMAN; update all canonical paths table entries.
- setup-checklist.md → META-ONLY: do not include.
- compaction-log.md → CREATE FROM SCRATCH: empty log file with 3-line header only (no entries).
- traceability.md → COPY AS-IS.
- errors-to-rules.md (C3 ADAPT): Remove rules 4 and 14; update header project name to "siopv"; renumber remaining rules; keep all 3 "Errores Recientes" entries.

**From proposals-c2:**
- agent-reports.md (C4b ADAPT): Update timestamp format `HHmmss` → `HH.mm.ss`; update `phase-{N}` → `{stage-name}` in directory paths; update wave timing header.
- universal-automation-template.md (C4b ADAPT): Replace `{project-name}` → `siopv`; `.ignore/` → `.ignorar/`; update `$HOME` in shutdown command to `/Users/bruno/siopv/`.
- verification-thresholds.md (C4b ADAPT): Update import check to `import siopv` (line 200); update date to 2026-03-12; resolve MERGE-010 (CVE ID) and MERGE-016 (.build/).
- placeholder-conventions.md (C4b): COPY AS-IS.
- tech-stack.md (C4b): COPY AS-IS — SIOPV stack confirmed matching.
- Global CLAUDE.md: STAYS AT SOURCE — no action.
- SIOPV CLAUDE.md (D1): CREATE FROM SCRATCH at `siopv/.claude/CLAUDE.md` using D1's skeleton (fix duplicate rule #5 numbering — `EXCEPTION: LIME/Jira/Redis` should be renumbered to avoid duplicate); resolve MERGE-012.
- SIOPV MEMORY.md (D1): CREATE FROM SCRATCH at `~/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md` using D1's skeleton (200-line limit respected).
- show-trace/SKILL.md (D1 ADAPT): Add `user-invocable: true` to frontmatter; body unchanged.
- orchestrator-protocol/SKILL.md (D1): COPY AS-IS — relative path resolves correctly; prerequisite: `siopv/.claude/workflow/07-orchestrator-invocation.md` must exist.
- techniques-reference/SKILL.md (D2 ADAPT): Body path `@../../docs/techniques.md` valid after C4a deploys `techniques.md`; no frontmatter changes needed (see MERGE-017).
- new-project/SKILL.md (D2 ADAPT): "META-PROYECTO" → "proyecto" (line 32); `projects/[nombre].md` → `projects/[nombre].json` (line 51).
- scan-vulnerabilities/SKILL.md (D2): COPY AS-IS.
- init-session/SKILL.md (D2): COPY AS-IS.
- run-tests/SKILL.md (D2): COPY AS-IS.
- generate-report/SKILL.md (D3): COPY AS-IS.
- verify/SKILL.md (D3 ADAPT): One path fix `/Users/bruno/sec-llm-workbench/` → `/Users/bruno/siopv/` (line 576); resolve MERGE-011 (batch mode section).
- coding-standards-2026/SKILL.md (D3): COPY AS-IS.
- cve-research/SKILL.md (D3): COPY AS-IS.
- langraph-patterns/SKILL.md (D3): COPY AS-IS.
- openfga-patterns/SKILL.md (D4): COPY AS-IS — tightly coupled to SIOPV domain.
- presidio-dlp/SKILL.md (D4): COPY AS-IS — tightly coupled to SIOPV domain.
- trivy-integration/SKILL.md (D4): COPY AS-IS — tightly coupled to SIOPV domain.
- xai-visualization/SKILL.md (D4): COPY AS-IS — fix typo in deployment step: target path `.clone/skills/` → `.claude/skills/`.
- python-standards.md (C4a): COPY AS-IS.
- techniques.md (C4a ADAPT): Remove Technique 8 "Separación META vs PROYECTO" entirely; update visual summary row to "Hexagonal Architecture".
- mcp-setup.md (C4a): COPY AS-IS.
- model-selection-strategy.md (C4a ADAPT): Remove "Concrete Examples from Master Plan" section (lines 289–384); remove "Validated Savings (from Master Plan)" subsection (lines 443–453); remove Master Remediation Plan reference (line 599).
- agent-tool-schemas.md (C4a ADAPT): Update 13 example paths from sec-llm-workbench to siopv equivalents as specified in C4a lines 117–128; resolve MERGE-015 (line 944).
