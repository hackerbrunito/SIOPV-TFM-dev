# Stage 4.1 Truth Document — Section C: Docs/Rules + CLAUDE.md + Memory + Skills
> Files covered: C4b (docs/rules), D1 (CLAUDE.md + MEMORY.md + 2 skills), D2–D4 (skills)
> MERGE decisions applied: MERGE-009-A, MERGE-010-B, MERGE-011-A, MERGE-012-A, MERGE-013/016/017/018 (auto)
> Status: COMPLETE
> Produced: 2026-03-12-22.39.05
> Total file entries: 22

---

## Part 1: Docs (`docs/`) — from Agent C4b

---

### verification-thresholds.md
**Target path:** `/Users/bruno/siopv/.claude/docs/verification-thresholds.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/verification-thresholds.md`

**Action:**
Apply the following diffs. All other content: copy unchanged.

**Diff 1 — smoke-test import line (SIOPV project name):**
```
OLD: Pipeline imports cleanly ('uv run python -c "import [project]"')
NEW: Pipeline imports cleanly ('uv run python -c "import siopv"')
```

**Diff 2 — smoke-test CVE line (MERGE-010-B):**
```
OLD: Pipeline runs start-to-end with CVE-2024-1234 without unhandled exception
NEW: Pipeline runs start-to-end with {{ smoke_test_cve_id }} without unhandled exception
```
Rationale: `CVE-2024-1234` is a synthetic meta-project test CVE, not a real SIOPV test fixture. Replaced with a Cookiecutter-style placeholder per MERGE-010-B so Stage 4.2 deployer fills in the actual CVE ID used by SIOPV smoke tests.

**Diff 3 — Last verified date:**
```
OLD: Last verified: 2026-02-08
NEW: Last verified: 2026-03-12
```

**Deployment notes (MERGE-016):**
Add the following note at the bottom of the file under a new `## Deployment Notes` section:

```markdown
## Deployment Notes

### Stage 4.2 Pre-Flight
- Run `mkdir -p /Users/bruno/siopv/.build/checkpoints/pending/` before first use.
  The pre-git-commit hook and /verify skill write marker files here; the directory
  must exist or the hook will fail silently.
- Fill in `{{ smoke_test_cve_id }}` with the actual CVE ID used by SIOPV smoke tests
  before running the smoke-test-runner section.
```

**Dependencies:**
- `.claude/workflow/05-before-commit.md` — must exist in SIOPV
- `.claude/workflow/04-agents.md` — must exist in SIOPV
- `.claude/hooks/pre-git-commit.sh` — must exist in SIOPV
- `.claude/scripts/check-module-coverage.py` — must exist in SIOPV
- `.claude/scripts/check-reviewer-score.sh` — must exist in SIOPV

**Deployment notes:** Requires `.build/checkpoints/pending/` directory to exist (MERGE-016 pre-flight). `{{ smoke_test_cve_id }}` must be resolved by Stage 4.2 deployer.

---

### universal-project-automation-template.md
**Target path:** `/Users/bruno/siopv/.claude/docs/universal-project-automation-template.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/2026-03-12-11.27.33-universal-project-automation-template-teamcreate-orchestrator-agents-rounds-checkpoints-shutdown-file-updates.md`

**Action:**
Apply the following diffs. All other content: copy unchanged. Target filename drops the timestamp prefix (use fixed name for predictable reference).

**Diff 1 — ALL occurrences of `{project-name}` placeholder:**
```
OLD: {project-name}  (all occurrences)
NEW: siopv
```

**Diff 2 — Directory convention (`.ignore/` → `.ignorar/`):**
```
OLD: └── .ignore/                        ← Local scratch, experiments, backups (excluded from git)
NEW: └── .ignorar/                       ← Local scratch, experiments, backups (excluded from git)
```
Apply `replace_all` for all occurrences of `.ignore/` (as local scratch directory reference, not `.gitignore`).

**Diff 3 — .gitignore entry:**
```
OLD: # Local scratch and experiments — never commit
     .ignore/
NEW: # Local scratch and experiments — never commit
     .ignorar/
```

**Diff 4 — Shutdown cleanup command:**
```
OLD: rm -rf $HOME/.claude/teams/{team-name} $HOME/.claude/tasks/{team-name}
NEW: rm -rf /Users/bruno/siopv/.claude/teams/{team-name} /Users/bruno/siopv/.claude/tasks/{team-name}
```

**Diff 5 — Version header:**
```
OLD: Version: 1.0 / Created: 2026-03-12-11.27.33
NEW: Version: 1.0-siopv / Created: 2026-03-12-11.27.33 / Adapted: 2026-03-12
```

Note: Runtime placeholders `{stage-name}`, `{username}`, `{TIMESTAMP}` are kept as-is — they are for runtime substitution, not project-specific literals.

**Dependencies:**
- `.claude/workflow/briefing.md`, `.claude/workflow/spec-findings.md`, `.claude/workflow/orchestrator-briefing.md`, `.claude/workflow/compaction-log.md`
- `.claude/rules/errors-to-rules.md`

**Deployment notes:** The "HOW TO USE THIS TEMPLATE" section (meta-project onboarding instructions) may be removed or retained as historical context — it is harmless either way.

---

## Part 2: Rules (`rules/`) — from Agent C4b

---

### agent-reports.md
**Target path:** `/Users/bruno/siopv/.claude/rules/agent-reports.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/rules/agent-reports.md`

**Action:**
Apply the following diffs. All other content: copy unchanged.

**Diff 1 — TIMESTAMP format (align with universal template standard):**
```
OLD: **TIMESTAMP format:** 'YYYY-MM-DD-HHmmss' (24-hour format)
NEW: **TIMESTAMP format:** 'YYYY-MM-DD-HH.mm.ss' (24-hour format, dots between time components)
```

**Diff 2 — bash timestamp command:**
```
OLD: TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
NEW: TIMESTAMP=$(date +%Y-%m-%d-%H.%M.%S)
```

**Diff 3 — Report path prompt template:**
```
OLD: "Save your report to '.ignorar/production-reports/{agent-name}/phase-{N}/{TIMESTAMP}-phase-{N}-{agent-name}-{slug}.md'"
NEW: "Save your report to '.ignorar/production-reports/{agent-name}/{stage-name}/{TIMESTAMP}-{stage-name}-{agent-name}-{slug}.md'"
```

**Diff 4 — Directory pattern:**
```
OLD: Directory: '.ignorar/production-reports/{agent}/phase-{N}/'
NEW: Directory: '.ignorar/production-reports/{agent}/{stage-name}/'
```

**Diff 5 — Wave timing section header:**
```
OLD: ## Agent Wave Timing (Phase 1 - Parallel Execution)
NEW: ## Agent Wave Timing (Stage-based Parallel Execution)
```

Rationale: SIOPV organizes by stage (stage3, stage4, etc.), not by phase/N. The wave agents listed and timing (~12 min total) remain valid. `(Phase 1)` framing was meta-project specific.

**Dependencies:** `.claude/workflow/` files referenced in source (generic, applicable to SIOPV).

**Deployment notes:** Auto-loaded (rules/ directory). Active immediately on copy — no CLAUDE.md @import needed.

---

### placeholder-conventions.md
**Target path:** `/Users/bruno/siopv/.claude/rules/placeholder-conventions.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/rules/placeholder-conventions.md`

**Action:**
Copy unchanged. Content is fully generic — conventions (`{{ }}`, `< >`, `[ ]`, `${ }`) apply universally. No project-specific paths or meta-project references anywhere in the file.

**Dependencies:** `.claude/workflow/` files (generic reference, applicable to SIOPV).

**Deployment notes:** Auto-loaded (rules/ directory). Active immediately on copy.

---

### tech-stack.md
**Target path:** `/Users/bruno/siopv/.claude/rules/tech-stack.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/rules/tech-stack.md`

**Action:**
Copy unchanged. Tech stack (Python 3.11+, uv, Pydantic v2, httpx async, structlog, pathlib) is SIOPV's actual stack per MEMORY.md. Frontmatter `paths: ["**/*.py", "pyproject.toml"]` correctly scoped to Python files.

**Dependencies:** `.claude/docs/mcp-setup.md` — referenced in "Before Write/Edit" section. Confirm mcp-setup.md exists in SIOPV (handled by agent C4a) before activating this rules file, otherwise the cross-reference will be a dead link.

**Deployment notes:** Auto-loaded (rules/ directory). Active immediately on copy. Depends on `mcp-setup.md` being deployed first (C4a artifact).

---

## Part 3: CLAUDE.md + MEMORY.md — from Agent D1

---

### CLAUDE.md (global)
**Target path:** N/A — stays at `/Users/bruno/.claude/CLAUDE.md`
**Classification:** STAYS AT SOURCE
**Source path:** `/Users/bruno/.claude/CLAUDE.md`

**Action:**
No action. Global file applies automatically to all Claude Code sessions including SIOPV. SIOPV project CLAUDE.md must NOT duplicate or override language preferences, TeamCreate default, or error log path that are already established globally.

**Dependencies:** `~/.claude/rules/errors-to-rules.md` (auto-imported via @).

**Deployment notes:** None required.

---

### CLAUDE.md (SIOPV project)
**Target path:** `/Users/bruno/siopv/.claude/CLAUDE.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (sec-llm-workbench CLAUDE.md is entirely meta-project specific and must NOT be copied)

**Action:**
Create with the following complete content. MERGE-012-A applied: `/techniques-reference` is omitted from the On-Demand References table.

```markdown
# SIOPV — Security Intelligence & Orchestration Platform for Vulnerabilities

## Project Identity
- Root: /Users/bruno/siopv/
- Tech stack: Python 3.11+, LangGraph, FastAPI, httpx, Presidio, OpenFGA, ChromaDB, XGBoost
- Architecture: Hexagonal (ports + adapters), async-first, DI via lru_cache factories

## CRITICAL RULES

**YOU MUST** at session start:
1. Read @.claude/workflow/01-session-start.md
2. Read @.claude/briefing.md (SIOPV briefing — primary context source)

**YOU MUST** when writing code:
3. Query Context7 MCP before using external libraries
4. Follow `.claude/workflow/02-reflexion-loop.md` (read on demand)
5. EXCEPTION: LIME, Jira v3, Redis have NO Context7 coverage — use official docs directly

**YOU MUST** before commit:
6. Execute `/verify` (runs 5 verification agents)
7. Follow @.claude/workflow/05-before-commit.md

**IMPORTANT** - Human checkpoints:
8. See @.claude/workflow/03-human-checkpoints.md

## References (read on demand, NOT auto-loaded)

| Topic | File |
|-------|------|
| Reflexion Loop | `.claude/workflow/02-reflexion-loop.md` |
| Agents | `.claude/workflow/04-agents.md` |
| Auto decisions | `.claude/workflow/06-decisions.md` |

## On-Demand References (loaded via skills, NOT at startup)

| Topic | Skill / File |
|-------|-------------|
| Orchestrator invocation | `/orchestrator-protocol` |
| Python standards | `/coding-standards-2026` |
| Agent tool schemas | Read `.claude/docs/agent-tool-schemas.md` |
| Model selection | Read `.claude/docs/model-selection-strategy.md` |
| Verification thresholds | Read `.claude/docs/verification-thresholds.md` |

## Compact Instructions

When compacting context, preserve:
- Current phase number and name
- Active task description and progress
- Pending verification file list
- Error patterns from errors-to-rules.md
- Key architectural decisions made in this session
- STAGE audit status (which STAGEs are complete)
```

Note on MERGE-012-A: `/techniques-reference` is intentionally absent from the On-Demand References table. The skill will be deployed and is invocable via `/techniques-reference`, but is not highlighted in the reference table per MERGE-012-A decision.

Note: `@.claude/briefing.md` replaces `@.claude/workflow/01-session-start.md` as the primary context source for SIOPV — the briefing.md pattern is the authoritative session context entry point for SIOPV.

**Dependencies:**
- `@.claude/briefing.md` — must exist in SIOPV before first use
- `@.claude/workflow/05-before-commit.md` — must exist in SIOPV
- `@.claude/workflow/03-human-checkpoints.md` — must exist in SIOPV
- `@.claude/workflow/01-session-start.md` — must exist in SIOPV

**Deployment notes:** Must be under 200 lines (hard limit for auto-loading). Current skeleton is ~40 lines — well within limit. Workflow files 01–06 must be created separately (separate agent task). The `/techniques-reference` skill is deployable and invocable; omission from this table is intentional (MERGE-012-A).

---

### MEMORY.md (SIOPV project)
**Target path:** `/Users/bruno/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (sec-llm-workbench memory/MEMORY.md lives at a different project path and is not applicable to SIOPV sessions)

**Action:**
Create with the following complete content. Designed to fit within the 200-line hard limit.

```markdown
# SIOPV Project Memory

## Active Project
- **Name:** SIOPV
- **Path:** `/Users/bruno/siopv/`
- **Current Phase:** 7 (Human-in-the-Loop — Streamlit) PENDING
- **Phase 8:** Output (Jira + PDF) PENDING
- **Briefing:** `/Users/bruno/siopv/.claude/briefing.md`

## Phase Status
| Phase | Name | Status |
|-------|------|--------|
| 0 | Setup | ✅ Completed |
| 1 | Ingesta y Preprocesamiento | ✅ Completed |
| 2 | Enriquecimiento (CRAG / RAG) | ✅ Completed |
| 3 | Clasificación ML (XGBoost) | ✅ Completed |
| 4 | Orquestación (LangGraph) | ✅ Completed |
| 5 | Autorización (OpenFGA) | ✅ Completed |
| 6 | Privacidad (DLP / Presidio) | ✅ Completed |
| 7 | Human-in-the-Loop (Streamlit) | ⏳ PENDING |
| 8 | Output (Jira + PDF) | ⏳ PENDING |

## Metrics (as of 2026-03-05)
- Tests: 1,404 passed, 12 skipped
- Coverage: 83% overall
- mypy: 0 errors / ruff: 0 errors

## Key Architecture
- Graph: START → authorize → ingest → dlp → enrich → classify → [escalate] → END
- State: TypedDict (not Pydantic) — by design
- DI: lru_cache factory functions in infrastructure/di/
- Checkpointing: SQLite (siopv_checkpoints.db)

## Key File Paths
| Component | Path |
|-----------|------|
| Graph | src/siopv/application/orchestration/graph.py |
| State | src/siopv/application/orchestration/state.py |
| CLI | src/siopv/interfaces/cli/main.py |
| Settings | src/siopv/infrastructure/config/settings.py |
| DI container | src/siopv/infrastructure/di/__init__.py |
| DLP DI | src/siopv/infrastructure/di/dlp.py |
| Constants | src/siopv/domain/constants.py |

## Pre-Phase-7 Audit Results (2026-03-11 to 2026-03-12)
Reports: /Users/bruno/siopv/AuditPrePhase7-11mar26/
Stage 4.1 input brief: stage3.5/stage3.5-final-reports-summarizer-n-aggregator-for-stage4-input-brief.md

### Open Issues (Priority Order)
| # | Sev | Location | Issue |
|---|-----|----------|-------|
| 1 | CRITICAL | application/use_cases/ingest_trivy.py:17 | Imports TrivyParser from adapters |
| 2 | CRITICAL | application/use_cases/classify_risk.py:18 | Imports FeatureEngineer from adapters |
| 3 | HIGH | interfaces/cli/main.py | All 8 adapter ports = None, DI never wired |
| 4 | MEDIUM | adapters/dlp/dual_layer_adapter.py | No explicit DLPPort inheritance |
| 5 | MEDIUM | infrastructure/di/authorization.py | 3 uncached OpenFGAAdapter instances |
| 6 | MEDIUM | application/orchestration/nodes/ingest_node.py | Directly instantiates use case |
| 7 | LOW | application/orchestration/edges.py:calculate_batch_discrepancies() | Domain logic in edge routing |

## Verified Library Facts (Stage 3)
| Library | Critical Verified Fact |
|---------|----------------------|
| Streamlit | `@st.fragment(run_every="15s")` — not sleep+rerun |
| Jira v3 | Description requires ADF format — plain strings rejected |
| Jira client | Use httpx.AsyncClient — sync libs block event loop |
| fpdf2 | `fname` required in add_font() since 2.7 (breaking change) |
| LangGraph | interrupt() requires checkpointer at compile time |
| Redis | Use redis.asyncio — aioredis merged into redis-py ≥4.2 |
| OTel | HTTPXClientInstrumentor does NOT cover module-level httpx.get() |
| LIME | Always plt.close(fig) after st.pyplot() — memory leak prevention |
| Streamlit | Port via STREAMLIT_SERVER_PORT env var |

## Context7 Coverage Gaps
LIME, Jira v3, Redis — use official docs directly (no Context7 coverage).

## Memory Convention
This file is project-specific. First 200 lines loaded per session (hard limit).
4-type frontmatter (user/feedback/project/reference) is a user-defined convention, not a Claude Code standard.
```

**Dependencies:** `/Users/bruno/siopv/.claude/briefing.md` — must exist when MEMORY.md is initialized.

**Deployment notes:** Directory `/Users/bruno/.claude/projects/-Users-bruno-siopv/memory/` must be created before writing the file. Content is 96 lines — well within 200-line load limit. The sec-llm-workbench MEMORY.md (at `-Users-bruno-sec-llm-workbench/memory/MEMORY.md`) is NOT modified — it remains as-is for the meta-project.

---

## Part 4: Skills — from Agents D1, D2, D3, D4

---

### show-trace/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/show-trace/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/show-trace/SKILL.md`

**Action:**
Frontmatter diff only — body unchanged.

```
OLD frontmatter:
---
name: show-trace
description: "Mostrar ultimas entradas de logs de trazabilidad"
argument-hint: "[options]"
context: fork
---

NEW frontmatter:
---
name: show-trace
description: "Mostrar ultimas entradas de logs de trazabilidad"
argument-hint: "[options]"
user-invocable: true
context: fork
---
```

Rationale (MERGE-018): `user-invocable: true` added because this is a user-invocable skill (`/show-trace` invocation syntax). All other optional fields absent — not needed.

**Dependencies:** `.build/logs/agents/`, `.build/logs/decisions/`, `.build/logs/sessions/` — must exist in SIOPV build structure.

**Deployment notes:** Body references `.build/logs/` paths which are relative and will resolve correctly from SIOPV project root.

---

### orchestrator-protocol/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/orchestrator-protocol/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/orchestrator-protocol/SKILL.md`

**Action:**
Copy unchanged. Frontmatter already contains `user-invocable: true` (MERGE-018 satisfied). The body's `@../../workflow/07-orchestrator-invocation.md` relative path resolves correctly from `siopv/.claude/skills/orchestrator-protocol/` to `siopv/.claude/workflow/07-orchestrator-invocation.md` — same relative depth in SIOPV as in meta-project.

**Dependencies:** `/Users/bruno/siopv/.claude/workflow/07-orchestrator-invocation.md` — must exist (separate agent task).

**Deployment notes:** Prerequisite: `siopv/.claude/workflow/07-orchestrator-invocation.md` must be deployed before this skill is invoked. `user-invocable: true` already present in source — no change needed.

---

### techniques-reference/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/techniques-reference/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/techniques-reference/SKILL.md`

**Action:**
Body diff only — frontmatter unchanged.

MERGE-017 applies: `techniques.md` (C4a artifact) must be deployed before this skill is used.

The `@../../docs/techniques.md` relative path in the body resolves from `siopv/.claude/skills/techniques-reference/` to `siopv/.claude/docs/techniques.md`. This is correct IF `techniques.md` is deployed as part of C4a. No body change needed IF C4a deploys `techniques.md`.

If C4a does NOT deploy `techniques.md`:
```
OLD: @../../docs/techniques.md
NEW: [Remove @-include and add inline note:]
<!-- techniques.md not yet deployed — run C4a deployment first -->
```

MERGE-012-A note: The `/techniques-reference` skill is NOT listed in SIOPV CLAUDE.md On-Demand References table, but the skill is deployed and invocable via `/techniques-reference`. Omission from the table is intentional.

**Dependencies:** `/Users/bruno/siopv/.claude/docs/techniques.md` — must be deployed by C4a before this skill is used (MERGE-017 sequencing requirement).

**Deployment notes:** Deploy AFTER C4a has deployed `techniques.md`. Skill is invocable via `/techniques-reference` but not highlighted in CLAUDE.md table (MERGE-012-A). `user-invocable: true` is present in source frontmatter — MERGE-018 satisfied.

---

### new-project/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/new-project/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/new-project/SKILL.md`

**Action:**
Body diffs — frontmatter unchanged.

**Diff 1:**
```
OLD: Usar templates del META-PROYECTO (`$CLAUDE_PROJECT_DIR/templates/`):
NEW: Usar templates del proyecto (`$CLAUDE_PROJECT_DIR/templates/`):
```

**Diff 2:**
```
OLD: Crear `projects/[nombre].md` con path, stack, progreso.
NEW: Crear `projects/[nombre].json` con path, stack, progreso.
```
Rationale: SIOPV uses `.json` format per `projects/siopv.json`. The `.md` format in source appears to be a divergence from actual usage.

**Dependencies:** `$CLAUDE_PROJECT_DIR/templates/` — template directory in SIOPV.

**Deployment notes:** No `user-invocable` field in source frontmatter (field is absent, not false). Per MERGE-018, only add `user-invocable: true` where it has functional impact for user-invocable skills. This skill is invocable as `/new-project` — add `user-invocable: true` to frontmatter.

**Additional diff (MERGE-018):**
```
OLD frontmatter (partial):
---
name: new-project
disable-model-invocation: true
description: ...
argument-hint: ...
context: ...
allowed-tools: [...]
---

NEW frontmatter (partial — add user-invocable):
---
name: new-project
disable-model-invocation: true
description: ...
argument-hint: ...
user-invocable: true
context: ...
allowed-tools: [...]
---
```

---

### scan-vulnerabilities/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/scan-vulnerabilities/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/scan-vulnerabilities/SKILL.md`

**Action:**
Copy unchanged. Pipeline reference (`[Ingestion] → [RAG] → [Classify] → [Orchestrate]`) maps directly to SIOPV pipeline stages. No meta-project-specific paths.

**Dependencies:** None.

**Deployment notes:** No `user-invocable` field in source. Per MERGE-018, only add where functional impact applies. `/scan-vulnerabilities` is a user-invocable workflow — add `user-invocable: true` to frontmatter at copy time.

**Additional diff (MERGE-018):**
```
OLD frontmatter: [existing fields, no user-invocable]
NEW frontmatter: add `user-invocable: true`
```

---

### init-session/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file per MERGE-009-A

**Action:**
Create with the following complete content per MERGE-009-A spec:

```markdown
---
name: init-session
description: "Initialize SIOPV session: read context files and create daily checkpoint markers"
argument-hint: "[--force]"
user-invocable: true
context: fork
allowed-tools: ["Read", "Bash", "Glob"]
---

# Init Session

Read the following files to initialize SIOPV session context:

1. Read briefing: @../../briefing.md
2. Read spec findings: @../../workflow/spec-findings.md
3. Read errors log: @../../rules/errors-to-rules.md

After reading all three files, create checkpoint marker files:

```bash
TODAY=$(date +%Y-%m-%d)
mkdir -p "$CLAUDE_PROJECT_DIR/.build/checkpoints/daily"
touch "$CLAUDE_PROJECT_DIR/.build/checkpoints/daily/briefing-read-${TODAY}"
touch "$CLAUDE_PROJECT_DIR/.build/checkpoints/daily/spec-read-${TODAY}"
touch "$CLAUDE_PROJECT_DIR/.build/checkpoints/daily/errors-read-${TODAY}"
echo "Session initialized for ${TODAY}. Context files read and checkpoints created."
```

## What This Skill Does

- Reads `briefing.md` — primary SIOPV session context
- Reads `spec-findings.md` — phase specification findings
- Reads `errors-to-rules.md` — error prevention rules
- Creates daily checkpoint markers under `.build/checkpoints/daily/`
- Reports confirmation when complete

## When to Use

Run `/init-session` at the start of each SIOPV working session to ensure full context is loaded and the session is tracked.

Pass `--force` to re-initialize on the same day (overwrites existing markers).
```

**Dependencies:**
- `/Users/bruno/siopv/.claude/briefing.md` — must exist
- `/Users/bruno/siopv/.claude/workflow/spec-findings.md` — must exist
- `/Users/bruno/siopv/.claude/rules/errors-to-rules.md` — must exist
- `.build/checkpoints/daily/` directory — created by the skill itself via `mkdir -p`

**Deployment notes:** Create directory `/Users/bruno/siopv/.claude/skills/init-session/` then write `SKILL.md`. The `mkdir -p` in the skill body handles `.build/checkpoints/daily/` creation automatically. `user-invocable: true` enables `/init-session` invocation syntax (MERGE-018).

---

### run-tests/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/run-tests/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/run-tests/SKILL.md`

**Action:**
Copy unchanged. All commands use `uv run pytest` with standard paths. Coverage floor of 80% is a minimum — SIOPV's actual 83% coverage exceeds it. No meta-project references.

**Dependencies:** None.

**Deployment notes:** Per MERGE-018, `/run-tests` is user-invocable — add `user-invocable: true` to frontmatter at copy time.

**Additional diff (MERGE-018):**
```
OLD frontmatter: [existing fields, no user-invocable]
NEW frontmatter: add `user-invocable: true`
```

---

### generate-report/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/generate-report/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/generate-report/SKILL.md`

**Action:**
Copy unchanged. All body paths are relative (`.build/logs/sessions/`, `.build/logs/agents/`, `.build/logs/decisions/`, `.build/logs/reports/`). No absolute meta-project paths.

**Dependencies:** `.build/logs/` directory structure — must exist in SIOPV.

**Deployment notes:** Per MERGE-018, `/generate-report` is user-invocable — add `user-invocable: true` to frontmatter at copy time.

**Additional diff (MERGE-018):**
```
OLD frontmatter: [existing fields, no user-invocable]
NEW frontmatter: add `user-invocable: true`
```

---

### verify/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/verify/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/verify/SKILL.md`

**Action:**
Apply the following diffs. MERGE-011-A applied: remove experimental batch mode section.

**Diff 1 — Absolute path fix:**
```
OLD: python3 /Users/bruno/sec-llm-workbench/.claude/scripts/check-module-coverage.py "$TARGET"
NEW: python3 /Users/bruno/siopv/.claude/scripts/check-module-coverage.py "$TARGET"
```

**Diff 2 — MERGE-011-A: Remove entire experimental batch mode section.**
Remove lines 31–37 (the `--batch` / `⚠️ EXPERIMENTAL` section) in their entirety.

OLD (to remove):
```
[Lines 31–37: batch API experimental mode section referencing]
[.ignorar/experimental-scripts/batch-api-verification/submit-batch-verification.py]
[marked ⚠️ EXPERIMENTAL, not integrated in production workflow]
```
NEW: Delete the entire section. The referenced script does not exist in SIOPV and was meta-project specific.

**Dependencies:**
- `.claude/scripts/check-module-coverage.py` — must exist in SIOPV (SIOPV-pathed version)
- `.claude/scripts/check-reviewer-score.sh` — must exist in SIOPV

**Deployment notes:** Requires `mkdir -p /Users/bruno/siopv/.build/checkpoints/pending/` as pre-flight step (MERGE-016). The `/verify` skill writes marker files to `.build/checkpoints/pending/` — directory must exist. Script path must be correct (Diff 1 above).

---

### coding-standards-2026/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/coding-standards-2026/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/coding-standards-2026/SKILL.md`

**Action:**
Copy unchanged. Body is pure Python standards reference (type hints, Pydantic v2, httpx, structlog, pathlib, dataclasses, error handling, context managers). No meta-project paths or project-specific references.

Note: Source frontmatter already contains `user-invocable: true` — MERGE-018 satisfied without change.

**Dependencies:** None.

**Deployment notes:** `user-invocable: true` present in source — no diff needed.

---

### cve-research/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/cve-research/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/cve-research/SKILL.md`

**Action:**
Copy unchanged. Body uses external APIs (NVD, GitHub GraphQL, EPSS/FIRST, CISA KEV, Tavily OSINT) — all public external endpoints. No meta-project paths.

Note: Source frontmatter already contains `user-invocable: true` — MERGE-018 satisfied without change.

**Dependencies:** None.

**Deployment notes:** `user-invocable: true` present in source — no diff needed.

---

### langraph-patterns/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/langraph-patterns/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/langraph-patterns/SKILL.md`

**Action:**
Copy unchanged. Body is pure LangGraph 0.2+ patterns (StateGraph, nodes, parallel execution, checkpointing, human-in-the-loop, error handling, streaming, testing). No meta-project paths. The `HumanLoopState` and `classify_node` examples reference generic vulnerability domain concepts consistent with SIOPV usage.

Note: Source frontmatter already contains `user-invocable: true` — MERGE-018 satisfied without change.

**Dependencies:** None.

**Deployment notes:** `user-invocable: true` present in source — no diff needed.

---

### openfga-patterns/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/openfga-patterns/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/openfga-patterns/SKILL.md`

**Action:**
Copy unchanged. Content is tightly coupled to SIOPV domain: uses `type vulnerability`, `object_type="vulnerability"`, `object_id="CVE-2024-1234"` (placeholder), `vulnerability_report` and `audit_log` types, and references `openfga/openfga:latest` matching SIOPV's `docker-compose.yml`. Purpose-built for SIOPV.

Note: Source frontmatter has `user-invocable: false` — per MERGE-018, `user-invocable: true` is only added where it has functional impact. This skill is a reference guide, not a user-invoked workflow — leave as false.

**Dependencies:** OpenFGA service running via docker-compose (SIOPV infrastructure).

**Deployment notes:** `user-invocable: false` is intentional — no change per MERGE-018.

---

### presidio-dlp/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/presidio-dlp/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/presidio-dlp/SKILL.md`

**Action:**
Copy unchanged. Content is tightly coupled to SIOPV: uses `state: PipelineState`, `state.enrichment.nvd_description`, `state.vulnerability.cve_id`, `DLPSettings` with `env_prefix="DLP_"`, and `sanitize_vulnerability_report` — all SIOPV-specific. Purpose-built for SIOPV.

Note: Source frontmatter has `user-invocable: false` — reference guide, not a user-invoked workflow — leave as false per MERGE-018.

**Dependencies:** Presidio Analyzer and Anonymizer services.

**Deployment notes:** `user-invocable: false` is intentional — no change per MERGE-018.

---

### trivy-integration/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/trivy-integration/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/trivy-integration/SKILL.md`

**Action:**
Copy unchanged. Content directly references SIOPV domain paths: `from src.domain.entities.vulnerability import Vulnerability`, `IngestTrivyUseCase` at `src/application/use_cases/ingest_trivy.py`, SIOPV field names (`cve_id`, `cvss_v3_score`, `cvss_vector`), structlog usage pattern. Purpose-built for SIOPV.

Note: Source frontmatter has `user-invocable: false` — reference guide, not a user-invoked workflow — leave as false per MERGE-018.

**Dependencies:** Trivy binary installed; SIOPV `IngestTrivyUseCase` application layer class.

**Deployment notes:** `user-invocable: false` is intentional — no change per MERGE-018.

---

### xai-visualization/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/xai-visualization/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/xai-visualization/SKILL.md`

**Action:**
Copy unchanged. Content is tightly coupled to SIOPV Phase 3/7: `shap.TreeExplainer(model)` for XGBoost, `EvidenceTriad` with `cve_id`/`prediction`/`confidence`, `render_xai_dashboard` using Streamlit, SIOPV severity classification output. Purpose-built for SIOPV.

Note: D4 report contained a typo in the target path (`.clone/` instead of `.claude/`). Corrected here.

**Deployment prerequisite (MERGE-013):** Stage 4.2 must run `Glob path="src/siopv/adapters/ml/**"` to verify the xai-explainer model artifact path before deploying this skill. The SHAP/LIME patterns assume the XGBoost model artifact is accessible at a specific path — verify against actual deployed artifact location.

Note: Source frontmatter has `user-invocable: false` — reference guide, not a user-invoked workflow — leave as false per MERGE-018.

**Dependencies:**
- XGBoost model artifact at `src/siopv/adapters/ml/` (verify path with Glob before deployment)
- LIME and SHAP Python packages installed
- Streamlit for dashboard rendering (Phase 7)

**Deployment notes:** `user-invocable: false` is intentional — no change per MERGE-018. Run Glob verification of ML artifact path as Stage 4.2 pre-flight step (MERGE-013).

---

## Summary Table

| # | File | Target Path | Classification | MERGE Applied |
|---|------|-------------|----------------|---------------|
| 1 | verification-thresholds.md | `.claude/docs/verification-thresholds.md` | ADAPT | MERGE-010-B, MERGE-016 |
| 2 | universal-project-automation-template.md | `.claude/docs/universal-project-automation-template.md` | ADAPT | — |
| 3 | agent-reports.md | `.claude/rules/agent-reports.md` | ADAPT | — |
| 4 | placeholder-conventions.md | `.claude/rules/placeholder-conventions.md` | COPY AS-IS | — |
| 5 | tech-stack.md | `.claude/rules/tech-stack.md` | COPY AS-IS | — |
| 6 | CLAUDE.md (global) | `~/.claude/CLAUDE.md` | STAYS AT SOURCE | — |
| 7 | CLAUDE.md (SIOPV) | `.claude/CLAUDE.md` | CREATE FROM SCRATCH | MERGE-012-A |
| 8 | MEMORY.md (SIOPV) | `~/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md` | CREATE FROM SCRATCH | — |
| 9 | show-trace/SKILL.md | `.claude/skills/show-trace/SKILL.md` | ADAPT | MERGE-018 |
| 10 | orchestrator-protocol/SKILL.md | `.claude/skills/orchestrator-protocol/SKILL.md` | COPY AS-IS | MERGE-018 (already present) |
| 11 | techniques-reference/SKILL.md | `.claude/skills/techniques-reference/SKILL.md` | ADAPT | MERGE-017, MERGE-018 |
| 12 | new-project/SKILL.md | `.claude/skills/new-project/SKILL.md` | ADAPT | MERGE-018 |
| 13 | scan-vulnerabilities/SKILL.md | `.claude/skills/scan-vulnerabilities/SKILL.md` | COPY AS-IS | MERGE-018 (add at copy) |
| 14 | init-session/SKILL.md | `.claude/skills/init-session/SKILL.md` | CREATE FROM SCRATCH | MERGE-009-A, MERGE-018 |
| 15 | run-tests/SKILL.md | `.claude/skills/run-tests/SKILL.md` | COPY AS-IS | MERGE-018 (add at copy) |
| 16 | generate-report/SKILL.md | `.claude/skills/generate-report/SKILL.md` | COPY AS-IS | MERGE-018 (add at copy) |
| 17 | verify/SKILL.md | `.claude/skills/verify/SKILL.md` | ADAPT | MERGE-011-A |
| 18 | coding-standards-2026/SKILL.md | `.claude/skills/coding-standards-2026/SKILL.md` | COPY AS-IS | MERGE-018 (already present) |
| 19 | cve-research/SKILL.md | `.claude/skills/cve-research/SKILL.md` | COPY AS-IS | MERGE-018 (already present) |
| 20 | langraph-patterns/SKILL.md | `.claude/skills/langraph-patterns/SKILL.md` | COPY AS-IS | MERGE-018 (already present) |
| 21 | openfga-patterns/SKILL.md | `.claude/skills/openfga-patterns/SKILL.md` | COPY AS-IS | MERGE-018 (false — intentional) |
| 22 | presidio-dlp/SKILL.md | `.claude/skills/presidio-dlp/SKILL.md` | COPY AS-IS | MERGE-018 (false — intentional) |
| 23 | trivy-integration/SKILL.md | `.claude/skills/trivy-integration/SKILL.md` | COPY AS-IS | MERGE-018 (false — intentional) |
| 24 | xai-visualization/SKILL.md | `.claude/skills/xai-visualization/SKILL.md` | COPY AS-IS | MERGE-013, MERGE-018 (false) |

**Totals:** 24 entries | 3 CREATE FROM SCRATCH | 7 ADAPT | 13 COPY AS-IS | 1 STAYS AT SOURCE

---

## Classification Breakdown

| Classification | Count | Files |
|----------------|-------|-------|
| COPY AS-IS | 13 | scan-vulnerabilities, run-tests, generate-report, coding-standards-2026, cve-research, langraph-patterns, openfga-patterns, presidio-dlp, trivy-integration, xai-visualization, orchestrator-protocol, placeholder-conventions, tech-stack |
| ADAPT | 7 | verification-thresholds.md, universal-automation-template.md, agent-reports.md, show-trace, techniques-reference, new-project, verify |
| CREATE FROM SCRATCH | 3 | SIOPV CLAUDE.md, SIOPV MEMORY.md, init-session/SKILL.md |
| STAYS AT SOURCE | 1 | global CLAUDE.md |

---

## Deployment Sequencing (Critical Order)

1. **Pre-flight:** `mkdir -p /Users/bruno/siopv/.build/checkpoints/pending/` (MERGE-016)
2. **Pre-flight:** `mkdir -p /Users/bruno/.claude/projects/-Users-bruno-siopv/memory/` (for MEMORY.md)
3. **C4a first:** Deploy `techniques.md` before deploying techniques-reference skill (MERGE-017)
4. **Glob verification:** Run `Glob path="src/siopv/adapters/ml/**"` before deploying xai-visualization skill (MERGE-013)
5. **CLAUDE.md last:** Deploy SIOPV CLAUDE.md after all workflow files it references exist
6. **Resolve `{{ smoke_test_cve_id }}`:** Fill in actual SIOPV test CVE ID before first `/verify` run

---

## Open Items for Stage 4.2

1. `{{ smoke_test_cve_id }}` — must be filled in by Stage 4.2 deployer with the actual CVE ID used in SIOPV smoke tests.
2. `techniques.md` — must be deployed by C4a before techniques-reference skill can be used.
3. XGBoost model artifact path — must be verified with `Glob path="src/siopv/adapters/ml/**"` before deploying xai-visualization skill (MERGE-013).
4. Workflow files 01–06 in `siopv/.claude/workflow/` — must be created by a separate agent task (referenced by SIOPV CLAUDE.md @imports but not in scope of this Section C).
5. `07-orchestrator-invocation.md` — must exist in `siopv/.claude/workflow/` before orchestrator-protocol skill is invoked.
