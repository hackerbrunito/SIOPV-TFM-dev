# Stage 4.1 — Merging Proposals B (Agents B2 + C1–C4a)
> Total MERGE proposals: 6
> Files covered: B2, C1, C2, C3, C4a
> Generated by: merging-proposal-b2 (SECOND RUN — first run discarded)
> Source verification: All 5 input files read in full (B2: 448 lines, C1: 174 lines, C2: 245 lines, C3: 266 lines, C4a: 152 lines)

---

### MERGE-001: test-framework.sh — hook event binding vs standalone manual script

**Files affected:** `/Users/bruno/siopv/.claude/hooks/test-framework.sh`, `/Users/bruno/siopv/.claude/settings.json`
**Domain reports referencing this:** B2

**Context from B2 (line 93):** The script is located in `hooks/` but does NOT output JSON hook format and does NOT reference a hook event name. It outputs plain-text PASS/FAIL lines and exits 1 on failure. No hook event binding is present in the script itself. B2 explicitly flags this as UNCLEAR and defers to Stage 4.2 (B2 line 439).

**Option A:** Keep as a manually-invoked standalone validation script only.
Place it in `.claude/hooks/` as a utility script (or move to `.claude/scripts/`) with documentation that it is run manually via `bash .claude/hooks/test-framework.sh`. Do NOT register it in settings.json.
**Rationale A:** The script does not output JSON hook format — wiring it to a hook event would require substantial rewriting. Its plain-text PASS/FAIL output is useful interactively. Keeping it manual avoids unexpected blocking behavior if wired to a hook that fires at a wrong moment (e.g., TaskCompleted would fire very frequently).

**Option B:** Wire it to a hook event (e.g., `TaskCompleted` or `Stop`) as an automated framework integrity check.
Register it in settings.json as a command hook on `Stop` or `TaskCompleted`. The script would need to be modified to output JSON hook format and handle the `permissionDecision` field.
**Rationale B:** Placing a validation script in `hooks/` without binding it to any event is architecturally inconsistent — hooks should hook into events. If the intent is automated framework validation, it belongs in the hook lifecycle. Wiring to `Stop` would run it before every session exit, providing continuous integrity checking.

**Recommendation:** A (keep as manual script). Reason: script does not output JSON format and would need significant rewriting for hook compatibility; manual invocation preserves its diagnostic utility without risk of false-positive blocking.
**Human judgment required:** YES

---

### MERGE-002: UserPromptSubmit hook — keep inert vs remove for SIOPV

**Files affected:** `/Users/bruno/siopv/.claude/settings.json`
**Domain reports referencing this:** C1

**Context from C1 (lines 57, 171–172):** The `UserPromptSubmit` hook at settings.json line 240 scans `$CLAUDE_PROJECT_DIR/projects/$name.$ext` to detect project names. SIOPV is a single project and does not use a `projects/` subdirectory under `.claude/`. The hook will emit no output for SIOPV (it only echoes when a match triggers). C1 marks this UNCLEAR: "whether SIOPV adopts the `projects/` directory pattern in its `.claude/` setup; if not, this hook can be removed or kept silently."

**Option A:** Keep the hook as-is (harmless, inert).
Since SIOPV uses `projects/siopv.json` at the project root (per MEMORY.md), the hook may fire if the user types "siopv" in a prompt — it will detect the project name and inject context. No harm if it fires; no harm if it doesn't. Zero maintenance cost.
**Rationale A:** The hook is copied from the source settings.json along with everything else. If SIOPV ever gains a second project file, the hook activates automatically. Keeping it follows the COPY/ADAPT approach of Stage 4.1 — do not delete without cause.

**Option B:** Remove the UserPromptSubmit hook entirely from SIOPV's settings.json.
SIOPV is a single-project repo. The project name detection pattern (multi-project disambiguation) adds no value here. Removing it keeps settings.json lean and signals clear intent that SIOPV is single-project.
**Rationale B:** Inert hooks that serve no purpose create noise when debugging settings.json. The hook was designed for the meta-project's multi-project workflow. In SIOPV, it represents unused meta-project logic that should not be ported.

**Recommendation:** B (remove). Reason: the hook was designed for multi-project disambiguation, which is a meta-project concept; SIOPV is a single-project repo and clarity in settings.json outweighs the marginal value of carrying inert code.
**Human judgment required:** YES

---

### MERGE-003: projects/siopv.json existence — prerequisite for 01-session-start.md

**Files affected:** `/Users/bruno/siopv/.claude/workflow/01-session-start.md`, `/Users/bruno/siopv/projects/siopv.json` (creation decision)
**Domain reports referencing this:** C1, C2

**Context from C1 (lines 99–104, 173):** `01-session-start.md` is classified COPY AS-IS. However, the "Continúa con [PROYECTO]" trigger assumes `projects/siopv.json` exists to load project state. C1 flags as UNCLEAR: "whether `projects/siopv.json` exists in the SIOPV repo at the time of .claude/ porting." C2 (07-orchestrator-invocation.md, line 114) also lists `projects/siopv.json` as a cross-file dependency: "state file, must exist in siopv root." MEMORY.md records `State file: ~/siopv/projects/siopv.json` which implies it exists, but this was not verified by either truth agent.

**Option A:** Adopt 01-session-start.md as COPY AS-IS without modification.
Trust that `projects/siopv.json` already exists at `/Users/bruno/siopv/projects/siopv.json` (per MEMORY.md reference). Stage 4.2 simply copies the workflow file; no new file creation needed.
**Rationale A:** MEMORY.md explicitly lists this file under "Active Project → State file." If the meta-project has been managing SIOPV state, this file exists. The COPY AS-IS classification from C1 was made under this assumption.

**Option B:** Verify existence of `projects/siopv.json` before adopting; if absent, create it or modify 01-session-start.md.
If `projects/siopv.json` doesn't exist, the session-start "Continúa con [PROYECTO]" trigger will fail silently. Stage 4.2 should include a verification step and either (a) create a minimal `projects/siopv.json` with current state, or (b) add a note to 01-session-start.md that the multi-project logic requires this file.
**Rationale B:** Truth agents cannot access the SIOPV filesystem to verify. If the file is absent, the session-start workflow will silently degrade. Proactively verifying and creating it is safer than assuming.

**Recommendation:** B (verify then create if absent). Reason: a one-time `ls /Users/bruno/siopv/projects/siopv.json` check costs nothing; creating a minimal state file if absent is trivial and prevents silent workflow degradation.
**Human judgment required:** NO — can be resolved by Stage 4.2 implementer with a filesystem check.

---

### MERGE-004: agent-tool-schemas.md line 944 — remove meta-project reference vs replace

**Files affected:** `/Users/bruno/siopv/.claude/docs/agent-tool-schemas.md`
**Domain reports referencing this:** C4a

**Context from C4a (lines 130–131, 151):** Line 944 of the source file references `.ignorar/production-reports/phase3/2026-02-08-phase3-task34-routing-savings-validation.md` — a meta-project internal report. C4a flags as UNCLEAR: "Since SIOPV uses `.ignorar/production-reports/` too, the question is whether to remove the line or replace with a SIOPV-equivalent path. Orchestrator decision needed."

**Option A:** Remove line 944 entirely.
The referenced file is a concrete meta-project artifact (`2026-02-08-phase3-task34-routing-savings-validation.md`) that does not exist in SIOPV and has no SIOPV equivalent at the time of porting. Removing it is cleaner than inserting a fabricated replacement.
**Rationale A:** agent-tool-schemas.md is a reference document used by agents to understand tool usage patterns. A broken reference to a non-existent file could cause agents to emit incorrect tool calls or report false paths. Removing the line eliminates the risk with no functional loss — the document's structural integrity is preserved.

**Option B:** Replace line 944 with a SIOPV-equivalent placeholder path.
Replace with a generic SIOPV production report path, e.g., `.ignorar/production-reports/phase-7/EXAMPLE-phase7-agent-report.md`, to preserve the illustrative intent of the example.
**Rationale B:** The line's purpose is to demonstrate how agents should reference production report paths. A SIOPV-equivalent placeholder is more useful to agents than a missing example — it shows the correct `.ignorar/production-reports/phase-N/` pattern for SIOPV Phase 7/8 work.

**Recommendation:** B (replace with SIOPV-equivalent placeholder). Reason: the line's purpose is illustrative; a SIOPV-appropriate placeholder teaches agents the correct report path pattern without referencing a non-existent file, and costs nothing to create.
**Human judgment required:** NO — can be resolved deterministically by Stage 4.2 implementer.

---

### MERGE-005: SIOPV spec path conflict between C2 and C3

**Files affected:** `/Users/bruno/siopv/.claude/workflow/spec-findings.md`, `/Users/bruno/siopv/.claude/workflow/orchestrator-briefing.md`
**Domain reports referencing this:** C2, C3

**Conflict description:** C2 and C3 give conflicting instructions for the SIOPV spec file path.

**C2 instruction (spec-findings.md, lines 215–216):**
- OLD: `> Extracted: 2026-03-10 | Source: /Users/bruno/sec-llm-workbench/docs/SIOPV_Propuesta_Tecnica_v2.txt`
- NEW: `> Extracted: 2026-03-10 | Source: /Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt`
- C2 cross-file dependency note: "already exists" at `/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt`

**C3 instruction (orchestrator-briefing.md canonical paths, line 84):**
- NEW: `SIOPV spec → /Users/bruno/sec-llm-workbench/docs/SIOPV_Propuesta_Tecnica_v2.txt (unchanged — spec lives in meta-project)`

Both are truth agent outputs but they disagree on which path is canonical for the SIOPV session.

**Option A:** Use SIOPV path (`/Users/bruno/siopv/docs/`) in both files.
Update spec-findings.md per C2 AND update orchestrator-briefing.md to also use the siopv path (overriding C3's "unchanged" instruction).
**Rationale A:** C2 explicitly states the file "already exists" at the siopv path. If both documents are used from the SIOPV session context, they should both point to the same location. A cross-session reference to the meta-project from SIOPV's orchestrator introduces a dependency that may not be accessible when working in an isolated SIOPV session.

**Option B:** Use meta-project path (`/Users/bruno/sec-llm-workbench/docs/`) in both files.
Override C2's instruction and keep both files pointing to the meta-project location, aligning with C3's "spec lives in meta-project" rationale.
**Rationale B:** The spec file was created in the meta-project as the source of truth. Duplicating it in siopv creates two copies that can drift. A single canonical source at the meta-project is cleaner. Both sessions have filesystem access to `/Users/bruno/sec-llm-workbench/docs/`.

**Recommendation:** A (use SIOPV path in both files). Reason: C2 verified the file already exists at the siopv path; a SIOPV session should be self-contained and not depend on meta-project paths for its primary spec document. The meta-project path is a historical artifact.
**Human judgment required:** YES — depends on whether Bruno considers the spec file to have a canonical home.

---

### MERGE-006: pre-write.sh /init-session skill prerequisite — create new skill vs simplify checkpoint mechanism

**Files affected:** `/Users/bruno/siopv/.claude/hooks/pre-write.sh`, `/Users/bruno/siopv/.claude/skills/` (possible new skill)
**Domain reports referencing this:** B2

**Context from B2 (lines 84–87, 445–448):** `pre-write.sh` depends on three daily checkpoint files under `.build/checkpoints/daily/`:
- `briefing-read-${TODAY}`
- `spec-read-${TODAY}`
- `errors-read-${TODAY}`

These are created by "an `/init-session` or equivalent skill." B2 states: "Stage 4.2 must specify how this directory and its checkpoint files are created for SIOPV." There is no existing `/init-session` skill in the SIOPV `.claude/` directory — it would need to be created from scratch.

**Option A:** Create a new `/init-session` skill for SIOPV that creates the three checkpoint files.
Define a `SKILL.md` at `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md` that instructs Claude to read briefing.md, spec-findings.md, and errors-to-rules.md, then create the corresponding `.build/checkpoints/daily/` marker files.
**Rationale A:** `pre-write.sh` is the anti-autopilot enforcement mechanism — it prevents writing code before reading context documents. Without a skill to create checkpoints, the hook will block every Write/Edit with a permission prompt, making the system unusable or forcing the hook to be disabled. A proper skill preserves the safety intent.

**Option B:** Simplify pre-write.sh to remove the checkpoint dependency entirely.
Modify the SIOPV version of pre-write.sh to only inject the `additionalContext` reminder without checking checkpoint files — i.e., always emit the reminder but never block with `permissionDecision: "ask"`. This removes the need for a checkpoint creation mechanism.
**Rationale B:** The checkpoint system was designed for the meta-project's complex multi-stage workflow. For SIOPV's Phase 7/8 work, the overhead of running a skill every session start may be unnecessary. A passive reminder (always-on `additionalContext`) achieves the same anti-autopilot effect with zero maintenance overhead.

**Recommendation:** A (create /init-session skill). Reason: the blocking enforcement is the valuable part of pre-write.sh — a passive reminder can be ignored, while the checkpoint gate cannot. Creating a simple skill takes minutes and preserves the full safety intent.
**Human judgment required:** YES — depends on whether Bruno wants the full checkpoint enforcement or a lighter-weight approach.

---

## NON-CONFLICT SUMMARY

Clear-cut items from all 5 domain reports — no human decision required:

**B2 (hooks — CREATE FROM SCRATCH):**
- pre-write.sh: create at `/Users/bruno/siopv/.claude/hooks/pre-write.sh` using full script from B2 (absolute paths updated to `/Users/bruno/siopv/`)
- pre-compact.sh: create at `/Users/bruno/siopv/.claude/hooks/pre-compact.sh` using full script from B2 (paths updated)
- session-start.sh: create at `/Users/bruno/siopv/.claude/hooks/session-start.sh` using full script from B2 (paths updated)
- session-end.sh: create at `/Users/bruno/siopv/.claude/hooks/session-end.sh` using full script from B2 (paths updated)
- All 4 scripts require `/Users/bruno/siopv/.claude/workflow/briefing.md` to contain a `> Last updated:` marker line

**C1 (settings.json — ADAPT):**
- Update 3 absolute hook paths (lines 139, 189, 258) from sec-llm-workbench to siopv
- Update inline tech stack string (line 130) to include SIOPV-specific libraries
- All other sections (sandbox, permissions, statusLine, env, attribution, cleanupPeriodDays) copy unchanged
- Hook event names already camelCase — no deprecated event fixes needed

**C1 (workflow ops files — COPY AS-IS):**
- 01-session-start.md → copy unchanged (pending MERGE-003 resolution)
- 02-reflexion-loop.md → copy unchanged
- 03-human-checkpoints.md → copy unchanged
- 05-before-commit.md → copy unchanged

**C2 (workflow context files):**
- 04-agents.md → ADAPT: replace 8× `[TARGET_PROJECT]` with `/Users/bruno/siopv`; keep all other content
- 06-decisions.md → COPY AS-IS
- 07-orchestrator-invocation.md → COPY AS-IS
- briefing.md → ADAPT: 15 path/section changes detailed in C2; remove STAGE-1 through STAGE-4.2 plan; remove CRITICAL TRANSITION POINT block; update Section 7 all hook/log paths; update Section 8 reference table
- spec-findings.md → ADAPT: 1 path change (pending MERGE-005 resolution for which path)

**C3 (workflow recovery + docs batch 1):**
- orchestrator-briefing.md → ADAPT: ~12 path/section changes detailed in C3; remove meta-project stage subtitle; remove "Scope is always Phases 0–6 only" block; remove REMINDER TO HUMAN; update all canonical paths table entries
- setup-checklist.md → META-ONLY: do not include
- compaction-log.md → CREATE FROM SCRATCH: empty log file with 3-line header only (no entries)
- traceability.md → COPY AS-IS
- errors-to-rules.md → ADAPT: remove rules 4 and 14; update header project name from "sec-llm-workbench" to "siopv"; renumber remaining rules; keep all 3 "Errores Recientes" entries

**C4a (docs batch 2):**
- python-standards.md → COPY AS-IS
- techniques.md → ADAPT: remove Technique 8 "Separación META vs PROYECTO" entirely; update visual summary row to "Hexagonal Architecture"
- mcp-setup.md → COPY AS-IS
- model-selection-strategy.md → ADAPT: remove "Concrete Examples from Master Plan" section (lines 289–384); remove "Validated Savings (from Master Plan)" subsection (lines 443–453); remove Master Remediation Plan reference (line 599)
- agent-tool-schemas.md → ADAPT: update 13 example paths from sec-llm-workbench to siopv equivalents as specified in C4a lines 117–128; resolve line 944 per MERGE-004
