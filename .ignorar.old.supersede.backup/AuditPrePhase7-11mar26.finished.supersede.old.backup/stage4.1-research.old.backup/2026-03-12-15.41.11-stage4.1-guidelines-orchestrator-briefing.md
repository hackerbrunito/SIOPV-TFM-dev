# Stage 4.1 — Guidelines & Orchestrator Briefing (Research & Truth Document)
> Status: ACTIVE | Audience: Stage 4.1 orchestrator | Read completely before writing your plan.

---

## READING ORDER (mandatory before doing anything)

Read in this exact order:
1. `/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md`
2. `/Users/bruno/sec-llm-workbench/.claude/workflow/spec-findings.md`
3. `/Users/bruno/sec-llm-workbench/.claude/workflow/orchestrator-briefing.md`
4. `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage3.5/stage3.5-final-reports-summarizer-n-aggregator-for-stage4-input-brief.md`
5. `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4/2026-03-12-11.56.25-stage4-pre-task-inventory-and-classification-of-meta-project-agents-and-hooks-for-siopv-reuse.md`
6. `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/2026-03-12-stage4.1-research-pre-planning-notes-scope-decisions-and-rules.md`

Do not begin planning until all six files are read completely.

---

## ABSOLUTE RULES — NO EXCEPTIONS

1. **Model:** Every Agent() call uses `model: "sonnet"` — never Opus
2. **Paths:** Hardcoded absolute paths only — no `$HOME`, no `~`, no relative paths
3. **No directory creation:** Agents never create directories — team-lead creates all directories
4. **Output location:** All output to `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/` only
5. **`siopv/.claude/` must not exist during Stage 4.1:** Stage 4.2 creates it — never Stage 4.1
6. **Research priority:** Round 1 online research takes priority over meta-project patterns — no exception
7. **No improvisation:** Unclear instruction → SendMessage to `team-lead` and stop. Never fill gaps.
8. **Human checkpoints:** Only explicit human text is approval. Silence is NOT approval.
9. **Round sequencing:** Round 2 does NOT begin until Round 1 is complete AND human has explicitly approved
10. **Copy-only:** Meta-project files are COPIED — never moved or deleted from source
11. **Timestamp format:** `YYYY-MM-DD-HH.mm.ss` (dots between time components, not colons)
12. **Agent output:** Condensed summaries of 1,000–2,000 tokens to orchestrator — never full file dumps
13. **All communication:** Orchestrator sends all messages to `team-lead` only — no other recipient

---

## WHAT STAGE 4.1 IS

Stage 4.1 produces one deliverable: the **truth document** — a complete specification of every file that must exist in `siopv/.claude/`, informed by March 2026 online research and all prior SIOPV audit findings. Stage 4.2 implements exactly what the truth document specifies.

**Core purpose:** Take every file in the meta-project's `.claude/` directory. Research current Claude Code best practices (March 2026) for each file type. Compare what we have against what the research finds. Produce the truth document specifying — file by file — whether to COPY AS-IS, ADAPT, or CREATE FROM SCRATCH, with full justification.

**Content adaptation applies to every file without exception** — agents, hooks, skills, settings.json, workflow, rules, docs, memory, CLAUDE.md. No file is assumed correct until verified against March 2026 best practices. Report both backwards-compatible improvements AND breaking changes explicitly.

## WHAT STAGE 4.1 IS NOT

- NOT implementation — that is Stage 4.2
- NOT writing to `siopv/.claude/` — document output only
- NOT planning Stage 4.2 execution
- NOT deleting anything from the meta-project
- NOT self-approving — human reviews before Stage 4.2 is authorized

---

## ROUND 1 — Online Research

**Purpose:** Scan current Claude Code best practices as of March 2026. Produce research reports that tell Round 2 agents HOW to write every file correctly.

**Agent config:** model: sonnet | tools: WebSearch, WebFetch, Context7 | 2–4 parallel researchers | each writes its own output file (no conflicts)

**Mandatory research topics — ALL required:**

| # | Topic | Priority |
|---|-------|----------|
| 1 | Skills 2.0 — skill file format | CRITICAL — research first |
| 2 | Agent definition file format | HIGH |
| 3 | Hook script formats + all valid event types | HIGH |
| 4 | `settings.json` structure | HIGH |
| 5 | `CLAUDE.md` patterns (global vs. project-level, `@file` syntax) | MEDIUM |
| 6 | Memory file patterns (`.claude/memory/`, MEMORY.md index format) | MEDIUM |
| 7 | Workflow file patterns (`.claude/workflow/` conventions) | MEDIUM |

**Skills 2.0 is CRITICAL.** If its research is inconclusive, Round 1 is not done. Do not proceed to Round 2.

**Each research report must contain per topic:** verified format/schema, minimal working example, what changed from previous format (if known), what to avoid, source URL, confidence level (HIGH/MEDIUM/LOW).

**Web content safeguard:** After each web fetch, write key findings to disk immediately. Never accumulate more than 3 unprocessed fetches. Summarize before fetching more.

**Context7 coverage gaps:** LIME, Jira v3, Redis asyncio have no Context7 coverage — use WebSearch/WebFetch with official docs directly.

**Output:** `YYYY-MM-DD-HH.mm.ss-stage4.1-researcher-{slug}.md` → `stage4.1-research/`

**Round 1 done when:** All researchers confirm completion + Skills 2.0 at HIGH or MEDIUM confidence + orchestrator sends summary to team-lead + human explicitly approves.

---

## ROUND 2 — Truth Document Synthesis

**Purpose:** Merge Round 1 research + existing meta-project files + catalog into the truth document.

**Agent config:** model: sonnet | 4 mandatory domains (minimum) | split further if any agent exceeds 50–60% context

**Mandatory domain split:**
- **Agent A:** All agent definitions (16 SIOPV-applicable + 4 adaptable — split into sub-agents as needed by 5-file limit)
- **Agent B:** All 9 hooks (4 adaptable → path surgery; 5 META-ONLY → create from scratch for SIOPV)
- **Agent C:** settings.json + all workflow files + all docs + all rules (split as needed)
- **Agent D:** All skill files (Skills 2.0 format) + CLAUDE.md + memory files

Sub-agent splitting is required when context demands it. Alternative domain structure requires human approval before execution.

**Inputs for every Round 2 agent:**
1. Round 1 research report(s) — primary authority on format and patterns
2. Catalog file (tells WHICH files to copy/adapt/discard)
3. Stage 3.5 brief
4. Actual meta-project source files (read for COPY/ADAPT classified files)
5. `briefing.md` for SIOPV known issues

**Per file, each domain agent must:** read current file → read Round 1 findings → compare → classify (COPY AS-IS / ADAPT / CREATE FROM SCRATCH) → specify exact diff for ADAPT/CREATE → flag backwards-incompatible changes explicitly.

**Agent A — adaptable agents adaptation spec:**
- `xai-explainer`: update scope/paths to SIOPV XGBoost classifier (`src/siopv/adapters/ml/`), SIOPV feature schema, Phase 3 context
- `researcher-1`: scope → integration + contract testing for SIOPV async stack (LangGraph, httpx, Presidio, OpenFGA)
- `researcher-2`: scope → OTel + LangSmith + structlog for SIOPV pipeline (FastAPI, httpx, SQLAlchemy)
- `researcher-3`: scope → CI/CD + Docker Compose for SIOPV stack (OpenFGA, Keycloak, Postgres, ChromaDB)

Adaptation = update the agent definition file only (frontmatter, system prompt, scope). Not Python code.

**3 META-ONLY agents to DISCARD** (do not include in siopv/.claude/agents/): `final-report-agent`, `report-summarizer`, `vulnerability-researcher`.

**Aggregation — two-step (mandatory):**

**Step 1 — Merging Proposal Agent** (after all domain agents complete): reads all domain reports + Round 1 research → identifies every decision point with two or more approaches → produces structured proposals report (Decision ID: MERGE-NNN, files affected, Option A + rationale, Option B + rationale, recommendation, human-judgement flag) → saves to `stage4.1-research/` → orchestrator sends path to team-lead → human reviews every proposal → provides decisions → only then Step 2 runs.

**Step 2 — Final Aggregator Agent** (after human decisions received): reads all domain reports + approved merge proposals → assembles single final truth document implementing exactly the approved decisions → saves as `YYYY-MM-DD-HH.mm.ss-stage4.1-truth-document-final.md`.

**Output:** `YYYY-MM-DD-HH.mm.ss-stage4.1-truth-agent-{slug}.md` (domain reports) | `...-merging-proposals.md` | `...-truth-document-final.md`

---

## TRUTH DOCUMENT — REQUIRED CONTENT

The truth document must be complete enough that Stage 4.2 agents implement everything without inference or guesswork.

**For every file in `siopv/.claude/`, specify:**
1. Exact absolute path
2. Complete file skeleton (all sections, in order, with headings)
3. Contents of each section (what text/schema/rules go in it)
4. Cross-file dependencies (hook filenames in settings.json must match hooks/ filenames exactly)
5. Action classification: `COPY AS-IS` / `ADAPT` (with explicit diff) / `CREATE FROM SCRATCH` (with full content)
6. Obsolete content to NOT carry over

**Files to cover:** every file in agents/ (full list in catalog), hooks/, workflow/ (6 files), docs/, rules/, memory/MEMORY.md, skills/ (all in Skills 2.0 format), settings.json, CLAUDE.md.

**Skills 2.0:** State verified format, provide skeleton per skill file, mark old format obsolete with explicit diff showing old → new for at least one example.

**Hooks:** Every hook needs old path refs → new SIOPV path refs + ADAPT vs CREATE FROM SCRATCH classification. The 5 META-ONLY hooks (pre-compact, session-start, session-end, pre-write, test-framework) must be created from scratch — they reference `sec-llm-workbench/` paths and cannot carry over.

---

## HUMAN CHECKPOINTS

3 checkpoints. Silence is NOT approval. Only explicit human text is approval.

**CP1 — After Round 1:** Orchestrator sends summary (topics, findings, confidence levels, gaps) → stops → team-lead relays to human → human approves Round 2 or requests more research → team-lead relays go-ahead.

**CP2 — After Merge Proposals:** Orchestrator sends proposals report path + decision count → stops → team-lead relays → human reviews every proposal, provides decisions per MERGE-NNN → team-lead relays decisions → final aggregator runs.

**CP3 — After Final Truth Document:** Orchestrator sends truth document path + summary → stops → team-lead relays → human reviews in full → explicitly authorizes Stage 4.2 or requests corrections.

---

## FIRST TASK

1. Read the six files in READING ORDER above
2. Produce execution plan: how many Round 1 researchers + topics per researcher, how many Round 2 domain agents + split rationale (enforcing 5-file limit and 50–60% context safety per agent), aggregation structure, all file names + save paths
3. Send plan to `team-lead` via SendMessage
4. Stop and wait — do not proceed until explicit human approval is relayed

---

## AGENT FAILURE PROTOCOL

Agent does not confirm completion → orchestrator sends SendMessage to team-lead immediately (which agent, what task, last known state) → orchestrator stops → human decides.

**Only autonomous exception:** Round 1 research agent completes with LOW confidence on a topic → orchestrator may spawn a targeted follow-up researcher for that topic only. All other failures escalate to human.

---

## INCREMENTAL WRITING

Every agent writes findings to its output file as it works — not only at the end. Partial findings must be on disk at all times.

---

## FILE NAMING

`YYYY-MM-DD-HH.mm.ss-stage4.1-{role}-{slug}.md` | dots between time components | all files to `stage4.1-research/`

Final truth document: `YYYY-MM-DD-HH.mm.ss-stage4.1-truth-document-final.md`

---

## CANONICAL PATHS

| Item | Path |
|------|------|
| Stage 4.1 output | `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/` |
| Briefing | `/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md` |
| Spec findings | `/Users/bruno/sec-llm-workbench/.claude/workflow/spec-findings.md` |
| Orchestrator-briefing | `/Users/bruno/sec-llm-workbench/.claude/workflow/orchestrator-briefing.md` |
| Stage 3.5 brief | `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage3.5/stage3.5-final-reports-summarizer-n-aggregator-for-stage4-input-brief.md` |
| Catalog | `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4/2026-03-12-11.56.25-stage4-pre-task-inventory-and-classification-of-meta-project-agents-and-hooks-for-siopv-reuse.md` |
| Pre-planning notes | `/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/2026-03-12-stage4.1-research-pre-planning-notes-scope-decisions-and-rules.md` |
| Meta-project agents | `/Users/bruno/sec-llm-workbench/.claude/agents/` |
| Meta-project hooks | `/Users/bruno/sec-llm-workbench/.claude/hooks/` |
| Meta-project workflow | `/Users/bruno/sec-llm-workbench/.claude/workflow/` |
| Meta-project docs | `/Users/bruno/sec-llm-workbench/.claude/docs/` |
| Meta-project rules | `/Users/bruno/sec-llm-workbench/.claude/rules/` |
| SIOPV future `.claude/` | `/Users/bruno/siopv/.claude/` (does NOT exist yet — Stage 4.2 creates it) |

---

## CONTEXT SAFETY

Hard limit: every agent completes its task within 50–60% of its 200k context window. Orchestrator sizes tasks before spawning — split before spawning, never after. Maximum per agent: 5 files read, 300 lines of output.

---

**STRICTLY FOLLOW ALL THESE INSTRUCTIONS. Do not improvise, do not create, do not suggest, do not infer, and do not make any deductions from anything in the Rules, Tasks, or Research found. Your task is to orchestrate the teams in Stage 4.1. Stage 4.1 is for research only.**
