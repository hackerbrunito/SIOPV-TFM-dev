# Stage 4.1 — Merging Proposals C (Agents C4b + D1–D4)
> Total MERGE proposals: 6
> Files covered: C4b, D1, D2, D3, D4
> Produced by: merging-proposal-c2 (second run)
> Timestamp: 2026-03-12-22.18.22

---

### MERGE-001: Smoke-test CVE ID in verification-thresholds.md

**Files affected:** `/Users/bruno/siopv/.claude/docs/verification-thresholds.md` (line 201)
**Domain reports referencing this:** C4b

**Context:** `verification-thresholds.md` line 201 (smoke-test-runner pipeline runs check) currently uses `CVE-2024-1234` — a synthetic placeholder from the meta-project. C4b explicitly flags this as UNCLEAR: "SIOPV team must specify" the actual CVE ID used for smoke testing.

**Option A:** Replace with a specific, known SIOPV test CVE ID (e.g., a CVE already present in SIOPV's test fixtures or integration test data).
**Rationale A:** Most accurate — the smoke test verifies real pipeline behavior. A real CVE ID used in existing SIOPV tests makes the check meaningful and reproducible.

**Option B:** Replace with a parameterized placeholder `{{ smoke_test_cve_id }}` (Cookiecutter convention per `placeholder-conventions.md`), leaving resolution to the implementer.
**Rationale B:** Keeps the file deployable without requiring an upfront decision. The implementer running Stage 4.2 can substitute the value based on available test data.

**Recommendation:** B — parameterize. The smoke-test CVE ID is an implementation detail, not an architectural decision. Using `{{ smoke_test_cve_id }}` signals clearly that substitution is required, which is safer than leaving `CVE-2024-1234` (which could silently pass on wrong data) or blocking Stage 4.2 on a human decision.
**Human judgment required:** YES — a human must confirm whether Option A (specific CVE) or Option B (parameterize) is preferred. If A, the human must also specify which CVE ID.

---

### MERGE-002: `.build/` Directory Existence in SIOPV

**Files affected:** `/Users/bruno/siopv/.claude/docs/verification-thresholds.md` (line 374); `/Users/bruno/siopv/.claude/skills/verify/SKILL.md` (body section)
**Domain reports referencing this:** C4b

**Context:** `verification-thresholds.md` line 374 references `.build/checkpoints/pending/` as the directory cleaned by `/verify`. The `verify/SKILL.md` also uses `.build/` paths. C4b UNCLEAR note: "source files do not confirm this directory exists in SIOPV."

SIOPV's MEMORY.md (session context) shows `.build/current-session-id` in the meta-project git status (modified), but this is a meta-project file, not SIOPV. The SIOPV project structure has not been explicitly confirmed to contain `.build/`.

**Option A:** Assume `.build/` exists in SIOPV (it mirrors meta-project conventions that SIOPV inherits) and copy references unchanged. No directory creation action in Stage 4.2.
**Rationale A:** SIOPV was built as part of the meta-project framework and has followed the same conventions throughout. `init-session/SKILL.md` (D2 COPY AS-IS) also references `$CLAUDE_PROJECT_DIR/.build/checkpoints/daily/` — if this skill is used unchanged, `.build/` must already exist or be expected to exist.

**Option B:** Add an explicit Stage 4.2 pre-flight step: verify `.build/checkpoints/pending/` exists in SIOPV; create it if absent. Update verification-thresholds.md with a note confirming this requirement.
**Rationale B:** Defensive — avoids silent failure if `.build/` doesn't exist. The pre-commit hook references this path; missing directory = silent hook failures.

**Recommendation:** B — add explicit creation as a Stage 4.2 pre-flight step. The risk of `.build/` not existing in SIOPV is low, but the consequence (broken pre-commit hooks) is HIGH. Creating an already-existing directory is a no-op (`mkdir -p`); creating a missing one prevents failures.
**Human judgment required:** NO — a human can decide, but Option B is safe regardless of current state.

---

### MERGE-003: verify/SKILL.md Experimental Batch Mode Section

**Files affected:** `/Users/bruno/siopv/.claude/skills/verify/SKILL.md` (lines 31–37 of source)
**Domain reports referencing this:** D3

**Context:** `verify/SKILL.md` body contains a batch mode section (lines 31–37 in source) marked `⚠️ EXPERIMENTAL` referencing `.ignorar/experimental-scripts/batch-api-verification/submit-batch-verification.py`. D3 explicitly flags this as UNCLEAR requiring human decision: "If SIOPV does not have this script, the batch mode section will fail if invoked."

The script lives in the meta-project's `.ignorar/` directory, which is excluded from git and not part of SIOPV. SIOPV's `.ignorar/` contains only its own production reports.

**Option A:** Remove the entire experimental batch mode section from the SIOPV copy of verify/SKILL.md.
**Rationale A:** The referenced script does not exist in SIOPV. Keeping a broken `--batch` flag in a production skill is misleading and could cause confusing failures. The standard (non-batch) verification workflow is all SIOPV needs for Phases 7–8.

**Option B:** Keep the experimental batch mode section as-is (copy unchanged alongside the required line 576 path fix).
**Rationale B:** The section is marked EXPERIMENTAL and describes an optional feature. Removing it loses potentially useful documentation. The batch script could be created in SIOPV's `.ignorar/` in the future.

**Recommendation:** A — remove the experimental section. SIOPV is in active development (Phases 7–8 imminent). A non-functional `--batch` flag in a critical `/verify` skill is a maintenance liability. The standard workflow is sufficient. If batch mode is needed later, it can be re-added.
**Human judgment required:** YES — removing documented functionality, even experimental, warrants explicit human approval.

---

### MERGE-004: techniques-reference @-include — Cross-Agent C4a Dependency

**Files affected:** `/Users/bruno/siopv/.claude/skills/techniques-reference/SKILL.md` (body line referencing `@../../docs/techniques.md`)
**Domain reports referencing this:** D2

**Context:** `techniques-reference/SKILL.md` body contains `@../../docs/techniques.md`. This resolves correctly to `siopv/.claude/docs/techniques.md` — but only if that file exists in SIOPV. D2 flags this as UNCLEAR: "depends on whether `/Users/bruno/siopv/.claude/docs/techniques.md` will be created as part of Stage 4.1/4.2."

The Stage 4.1 briefing (known unclear items, item 3) states: "C4a says ADAPT for techniques.md, meaning yes — resolves this." Agent C4a (Docs Batch 1–2) is a sibling agent in Stage 4.1 that analyzes techniques.md and classifies it ADAPT for SIOPV. This means the file WILL be created in SIOPV's `.claude/docs/` as part of Stage 4.2 deployment.

**Option A:** Proceed with COPY AS-IS for techniques-reference/SKILL.md (the `@../../docs/techniques.md` path resolves correctly after C4a's techniques.md is deployed).
**Rationale A:** C4a's classification ADAPT for techniques.md implies it will exist at the correct path. The relative path is correct. Deployment order guarantee: C4a artifacts must be deployed before D2 artifacts — a sequencing note, not a blocker.

**Option B:** Replace `@../../docs/techniques.md` with inline content or a note: `<!-- TODO: ensure /Users/bruno/siopv/.claude/docs/techniques.md is created before activating this skill -->`.
**Rationale B:** Defensive against deployment ordering failures. If techniques.md is deployed after this skill is first used, the skill silently loads nothing.

**Recommendation:** A — trust C4a's confirmed ADAPT classification. Add a Stage 4.2 deployment note in the master checklist that techniques.md must be deployed before techniques-reference skill is used. No change to the SKILL.md file itself.
**Human judgment required:** NO — C4a's classification resolves this. Deployment sequencing is a Stage 4.2 concern.

---

### MERGE-005: `/techniques-reference` Skill Inclusion in SIOPV CLAUDE.md

**Files affected:** `/Users/bruno/siopv/.claude/CLAUDE.md` (On-Demand References table)
**Domain reports referencing this:** D1 (CLAUDE.md skeleton), D2 (techniques-reference ADAPT classification)

**Context:** A genuine cross-agent conflict:
- **D1** explicitly marks `/techniques-reference` as **OBSOLETE** in SIOPV CLAUDE.md with note "meta-project only." D1's CLAUDE.md skeleton does NOT include it in the On-Demand References table.
- **D2** classifies techniques-reference/SKILL.md as **ADAPT** (copy to SIOPV with body path update) — meaning the skill WILL exist in SIOPV's `.claude/skills/techniques-reference/`.

If the skill exists in SIOPV but CLAUDE.md doesn't reference it, it's still invocable via `/techniques-reference` (skills are auto-discovered), but it won't appear in the documented reference table. The question is whether the techniques catalog (13 Vibe Coding 2026 techniques) is conceptually relevant to SIOPV or is a meta-project-only tool.

**Option A:** Exclude `/techniques-reference` from SIOPV CLAUDE.md (follow D1's recommendation). The skill exists and is invocable but isn't highlighted in the reference table.
**Rationale A:** The techniques catalog documents meta-project development patterns (Vibe Coding 2026). These are process patterns for the meta-project's own agents, not SIOPV domain logic. Cluttering SIOPV CLAUDE.md with meta-framework references reduces focus on SIOPV-specific context.

**Option B:** Include `/techniques-reference` in SIOPV CLAUDE.md's On-Demand References table, since the skill will physically exist in the project.
**Rationale B:** The 13 Vibe Coding techniques are applicable to any project built with this framework. Having the skill present but undocumented in CLAUDE.md creates discoverability inconsistency — if it's worth copying, it's worth referencing.

**Recommendation:** A — follow D1's recommendation. The techniques catalog is meta-framework documentation, not SIOPV domain documentation. SIOPV CLAUDE.md should stay focused on SIOPV context. The skill can still be invoked by someone who knows it exists.
**Human judgment required:** YES — D1 explicitly marks it obsolete, D2 explicitly copies it. This is a genuine conflict about what "SIOPV-relevant" means for framework patterns.

---

### MERGE-006: Skills 2.0 Missing Optional Fields — Required vs Optional Designation

**Files affected:** All ADAPT skills with missing schema fields (show-trace, techniques-reference, new-project, verify, generate-report, init-session, run-tests, coding-standards-2026, cve-research, langraph-patterns)
**Domain reports referencing this:** D2 (UNCLEAR), D3 (observed, not assessed), D4 (resolved: only name+description required)

**Context:** A cross-agent disagreement in confidence:
- **D2**: UNCLEAR — "Which of the 11 schema fields are required vs optional — cannot be determined from source files alone." D2 notes all 5 analyzed skills are missing 5–6 fields from the 11-field schema.
- **D3**: Notes the gap but states "schema does not designate which fields are mandatory vs optional" — treats it as observed, not a problem.
- **D4**: Resolves it definitively: "only name and description appear required by the schema. The absence of optional fields does not constitute an error, only an incomplete schema."

The Round 1 findings (inline context) confirm: 11-field schema exists with fields name, description, argument-hint, disable-model-invocation, user-invocable, allowed-tools, model, context, agent, hooks, version. Only `name` and `description` are definitively required per D4.

**Option A:** Accept D4's resolution — only `name` and `description` are required. Copy/adapt skills without adding missing optional fields. No retrofitting needed.
**Rationale A:** D4 has the most explicit and confident finding on this question. D2's uncertainty was due to not having the authoritative schema file; D4 analyzed the same schema and concluded definitively. Adding 8 optional fields to 10+ skills purely for completeness adds noise without value.

**Option B:** Add a minimal recommended set of optional fields to all skills during ADAPT/copy: `user-invocable` (true/false), `context` (fork), and `version` (1.0). Avoids future confusion about invocability.
**Rationale B:** `user-invocable` in particular has functional significance — it controls whether the `/skill-name` syntax works. Several skills (init-session, run-tests, generate-report, new-project, scan-vulnerabilities) may be user-invocable but are missing this field. D1 already recommends adding `user-invocable: true` to show-trace as a required change.

**Recommendation:** B for `user-invocable` only — it has functional impact and each agent already analyzed whether their skills are user-invocable. All other optional fields (model, hooks, agent, version, context) can remain absent. This is a hybrid: accept D4's conclusion that most fields are optional, but add `user-invocable` selectively where functional impact is clear.
**Human judgment required:** NO — but the Stage 4.2 implementer should audit each skill's `user-invocable` value before deployment.

---

## NON-CONFLICT SUMMARY

Clear-cut items — one line each, informational only:

- **C4b / agent-reports.md:** ADAPT — update timestamp format from `HHmmss` to `HH.mm.ss` (universal template wins), update `phase-{N}` → `{stage-name}` in directory paths and prompt templates, update wave timing header.
- **C4b / universal-automation-template.md:** ADAPT — replace `{project-name}` → `siopv`, `.ignore/` → `.ignorar/` throughout, update `$HOME` in shutdown command to `/Users/bruno/siopv/`.
- **C4b / verification-thresholds.md:** ADAPT — update import check to `import siopv` (line 200), update date to 2026-03-12.
- **C4b / placeholder-conventions.md:** COPY AS-IS — no project-specific references.
- **C4b / tech-stack.md:** COPY AS-IS — SIOPV stack confirmed matching.
- **D1 / Global CLAUDE.md:** STAYS AT SOURCE — no action, applies globally.
- **D1 / SIOPV CLAUDE.md:** CREATE FROM SCRATCH at `siopv/.claude/CLAUDE.md` using D1's skeleton (note: fix duplicate rule #5 numbering in D1 skeleton — `EXCEPTION: LIME/Jira/Redis` should be renumbered to avoid duplicate).
- **D1 / SIOPV MEMORY.md:** CREATE FROM SCRATCH at `~/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md` using D1's skeleton (200-line limit respected).
- **D1 / show-trace/SKILL.md:** ADAPT — add `user-invocable: true` to frontmatter; body unchanged.
- **D1 / orchestrator-protocol/SKILL.md:** COPY AS-IS — relative path resolves correctly; prerequisite: `siopv/.claude/workflow/07-orchestrator-invocation.md` must exist.
- **D2 / techniques-reference/SKILL.md:** ADAPT — body path `@../../docs/techniques.md` valid after C4a deploys `techniques.md`; no frontmatter changes needed (see MERGE-004).
- **D2 / new-project/SKILL.md:** ADAPT — "META-PROYECTO" → "proyecto" (line 32), `projects/[nombre].md` → `projects/[nombre].json` (line 51).
- **D2 / scan-vulnerabilities/SKILL.md:** COPY AS-IS.
- **D2 / init-session/SKILL.md:** COPY AS-IS.
- **D2 / run-tests/SKILL.md:** COPY AS-IS.
- **D3 / generate-report/SKILL.md:** COPY AS-IS.
- **D3 / verify/SKILL.md:** ADAPT — one path fix: `/Users/bruno/sec-llm-workbench/` → `/Users/bruno/siopv/` (line 576). Batch mode section per MERGE-003.
- **D3 / coding-standards-2026/SKILL.md:** COPY AS-IS.
- **D3 / cve-research/SKILL.md:** COPY AS-IS.
- **D3 / langraph-patterns/SKILL.md:** COPY AS-IS.
- **D4 / openfga-patterns/SKILL.md:** COPY AS-IS — tightly coupled to SIOPV domain.
- **D4 / presidio-dlp/SKILL.md:** COPY AS-IS — tightly coupled to SIOPV domain.
- **D4 / trivy-integration/SKILL.md:** COPY AS-IS — tightly coupled to SIOPV domain.
- **D4 / xai-visualization/SKILL.md:** COPY AS-IS — fix typo in deployment step: target path `.clone/skills/` → `.claude/skills/`.
