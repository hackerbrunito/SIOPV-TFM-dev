# Stage 4.1 Truth Document — Agent C1: settings.json + Workflow Ops Files
> Status: COMPLETE
> Files: settings.json, 01-session-start, 02-reflexion-loop, 03-human-checkpoints, 05-before-commit

---

## Summary

| File | Classification | Hardcoded Paths | Deprecated Events |
|------|---------------|-----------------|-------------------|
| settings.json | ADAPT | 3 absolute paths | 0 (all camelCase) |
| 01-session-start.md | COPY AS-IS | 0 | N/A |
| 02-reflexion-loop.md | COPY AS-IS | 0 | N/A |
| 03-human-checkpoints.md | COPY AS-IS | 0 | N/A |
| 05-before-commit.md | COPY AS-IS | 0 | N/A |

---

### settings.json
**Source:** `/Users/bruno/sec-llm-workbench/.claude/settings.json`
**Target:** `/Users/bruno/siopv/.claude/settings.json`
**Classification:** ADAPT

**Action:**
Three absolute hardcoded paths must change. All other hooks use `"$CLAUDE_PROJECT_DIR"` (dynamic, resolves automatically) — no change needed for those.

Exact diffs (line numbers from source):

1. Line 139 — SessionStart / matcher: "compact" hook:
   - OLD: `"command": "/Users/bruno/sec-llm-workbench/.claude/hooks/session-start.sh"`
   - NEW: `"command": "/Users/bruno/siopv/.claude/hooks/session-start.sh"`

2. Line 189 — PreCompact hook:
   - OLD: `"command": "/Users/bruno/sec-llm-workbench/.claude/hooks/pre-compact.sh"`
   - NEW: `"command": "/Users/bruno/siopv/.claude/hooks/pre-compact.sh"`

3. Line 258 — SessionEnd hook:
   - OLD: `"command": "/Users/bruno/sec-llm-workbench/.claude/hooks/session-end.sh"`
   - NEW: `"command": "/Users/bruno/siopv/.claude/hooks/session-end.sh"`

**Additional ADAPT: inline tech stack string (line 130):**
The post-compact re-injection hook has a hardcoded tech stack string:
- OLD: `echo \"Tech Stack: Python 3.11+, uv, Pydantic v2, httpx, structlog, pathlib\"`
- NEW: `echo \"Tech Stack: Python 3.11+, uv, Pydantic v2, httpx, structlog, LangGraph, FastAPI, Presidio, OpenFGA, ChromaDB, XGBoost\"`

**Hooks using `$CLAUDE_PROJECT_DIR` (no change needed — dynamic):**
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/session-start.sh` (line 105, SessionStart)
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-write.sh` (line 150, PreToolUse Write|Edit)
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-git-commit.sh` (line 159, PreToolUse Bash)
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/post-code.sh` (line 170, PostToolUse Write|Edit)

**Deprecated event names found:** NONE. All events use camelCase:
`SessionStart`, `PreToolUse`, `PostToolUse`, `PreCompact`, `Stop`, `PostToolUseFailure`,
`SubagentStart`, `SubagentStop`, `UserPromptSubmit`, `SessionEnd`, `Notification`, `PermissionRequest`

**Obsolete content:**
- The `UserPromptSubmit` hook (line 240) detects project names by looking up `$CLAUDE_PROJECT_DIR/projects/$name.$ext`. SIOPV is a single project and does not use a `projects/` directory. This hook will emit no output for SIOPV (it only echoes when a match triggers) — it is harmless but serves no purpose. UNCLEAR: whether SIOPV adopts the `projects/` directory pattern in its `.claude/` setup; if not, this hook can be removed or kept silently.

**Cross-file dependencies:**
- References hooks: `session-start.sh`, `pre-write.sh`, `pre-git-commit.sh`, `post-code.sh`, `pre-compact.sh`, `session-end.sh` — all must exist in `/Users/bruno/siopv/.claude/hooks/`
- References `.build/current-phase`, `.build/checkpoints/pending/`, `.build/checkpoints/daily/`, `.build/logs/agents/`, `.build/logs/sessions/`, `.build/current-session-id` — all relative to project root; these directories must exist in `/Users/bruno/siopv/`
- References `.mcp.json` and `.env` (with `CONTEXT7_API_KEY`) — must exist in `/Users/bruno/siopv/`

**Note:** `sandbox`, `permissions`, `statusLine`, `env`, `attribution`, `cleanupPeriodDays` sections contain no meta-project-specific values and copy unchanged.

**SIOPV hook registration spec — hooks that must be registered in siopv/.claude/settings.json:**

| Event | Matcher | Script Path | Type |
|-------|---------|-------------|------|
| SessionStart | (none) | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/session-start.sh` | command |
| SessionStart | (none) | inline: daily checkpoint reminder | command |
| SessionStart | (none) | inline: MCP health check | command |
| SessionStart | compact | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/session-start.sh` → ADAPT to `/Users/bruno/siopv/.claude/hooks/session-start.sh` | command |
| SessionStart | compact | inline: post-compact re-injection (ADAPT tech stack string) | command |
| PreToolUse | Write\|Edit | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-write.sh` | command |
| PreToolUse | Bash | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-git-commit.sh` | command |
| PostToolUse | Write\|Edit | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/post-code.sh` (timeout: 60) | command |
| PreCompact | (none) | inline: context preservation echo | command |
| PreCompact | (none) | `/Users/bruno/siopv/.claude/hooks/pre-compact.sh` | command |
| Stop | (none) | inline: pending verification warning | command |
| PostToolUseFailure | Write\|Edit | inline: JSONL error logging | command |
| SubagentStart | (none) | inline: JSONL event logging | command |
| SubagentStop | (none) | inline: JSONL event logging | command |
| UserPromptSubmit | (none) | inline: project name detection (UNCLEAR for SIOPV — see Obsolete content) | command |
| SessionEnd | (none) | inline: JSONL session end + tmp cleanup | command |
| SessionEnd | (none) | `/Users/bruno/siopv/.claude/hooks/session-end.sh` | command |
| Notification | (none) | inline: JSONL notification logging | command |
| PermissionRequest | (none) | inline: JSONL permission logging | command |

---

### 01-session-start.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/workflow/01-session-start.md`
**Target:** `/Users/bruno/siopv/.claude/workflow/01-session-start.md`
**Classification:** COPY AS-IS

**Action:** Copy source to target unchanged.

**Obsolete content:** The "Crea nuevo proyecto [NOMBRE]" trigger and multi-project listing logic are meta-project features not applicable to SIOPV (single project). However, they reference only generic placeholders (`[NOMBRE]`, `projects/*.json`) and will not trigger unless the user explicitly types those phrases. No content actively breaks SIOPV.

**Cross-file dependencies:**
- References `projects/[proyecto].json` (generic pattern)
- References `/new-project` skill (meta-project skill, not present in SIOPV unless ported)
- `pyproject.toml` reference in NECESITA_SETUP state

**Note:** File has `<!-- version: 2026-02 -->` and `<!-- COMPACT-SAFE: ... -->` comment on line 4. These are workflow infrastructure markers — carry over unchanged.

---

### 02-reflexion-loop.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/workflow/02-reflexion-loop.md`
**Target:** `/Users/bruno/siopv/.claude/workflow/02-reflexion-loop.md`
**Classification:** COPY AS-IS

**Action:** Copy source to target unchanged.

**Obsolete content:** None. All paths are relative (`.build/`, `.ignorar/`, `.claude/`). All agent names are generic (code-implementer, best-practices-enforcer, etc.). All tool references (Context7, `/verify`) are project-agnostic.

**Cross-file dependencies:**
- References `.claude/scripts/orchestrate-parallel-verification.py` — must exist in siopv
- References `.claude/docs/verification-thresholds.md` — must exist in siopv
- References `.claude/docs/model-selection-strategy.md` — must exist in siopv
- References `.build/checkpoints/pending/`, `.build/logs/agents/` — directories must exist
- References `.ignorar/production-reports/` — directory must exist
- References `/verify` skill (`.claude/skills/verify.md` or equivalent)

**Note:** File specifies Wave 1 (~7 min) + Wave 2 (~5 min) = ~12 min total verification. Orchestration script path: `.claude/scripts/orchestrate-parallel-verification.py`.

---

### 03-human-checkpoints.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/workflow/03-human-checkpoints.md`
**Target:** `/Users/bruno/siopv/.claude/workflow/03-human-checkpoints.md`
**Classification:** COPY AS-IS

**Action:** Copy source to target unchanged.

**Obsolete content:** None. All content is generic checkpoint policy (when to pause vs. auto-continue). No project-specific references, no hardcoded paths.

**Cross-file dependencies:** None (pure policy document).

**Note:** File has `<!-- COMPACT-SAFE: PAUSE only for: phase transitions, destructive actions, post-verification synthesis. CONTINUE for: agent delegation, Context7 queries, file reads, report generation. -->` — this compact-safe annotation is the canonical summary for post-compact context.

---

### 05-before-commit.md
**Source:** `/Users/bruno/sec-llm-workbench/.claude/workflow/05-before-commit.md`
**Target:** `/Users/bruno/siopv/.claude/workflow/05-before-commit.md`
**Classification:** COPY AS-IS

**Action:** Copy source to target unchanged.

**Obsolete content:** None. All references are relative or generic:
- `/verify` skill
- `ruff format + ruff check`, `mypy src`, `pytest` — standard Python toolchain
- `.build/checkpoints/pending/`
- `.claude/docs/verification-thresholds.md`
- `pre-git-commit.sh` hook

**Cross-file dependencies:**
- References `.claude/docs/verification-thresholds.md` — must exist in siopv
- References `/verify` skill
- References `pre-git-commit.sh` hook at `$CLAUDE_PROJECT_DIR/.claude/hooks/pre-git-commit.sh`

**Note:** File specifies 3-threshold PASS rules: best-practices-enforcer (0 violations), security-auditor (0 CRITICAL/HIGH), hallucination-detector (0 hallucinations). Full thresholds in `verification-thresholds.md` (referenced on-demand).

---

## UNCLEAR Items

1. **UserPromptSubmit hook scope for SIOPV:** The hook scans for project names in `$CLAUDE_PROJECT_DIR/projects/`. SIOPV is a single project without a `projects/` subdirectory. Whether to keep (harmless but inert) or remove this hook is not determinable from the source files alone — depends on SIOPV's session workflow design.

2. **`projects/[proyecto].json` pattern in 01-session-start.md:** The session-start workflow assumes a `projects/` directory with JSON state files. SIOPV may use `projects/siopv.json` (referenced in MEMORY.md at `~/siopv/projects/siopv.json`). If that file exists, the session-start triggers will function correctly. UNCLEAR: whether `projects/siopv.json` exists in the SIOPV repo at the time of .claude/ porting.
