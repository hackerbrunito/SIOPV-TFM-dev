# Stage 4.1 Truth Document — Agent A1: Agent Definitions Batch 1
> Status: COMPLETE
> Files: async-safety-auditor, circular-import-detector, code-implementer, config-validator, dependency-scanner

---

## Preamble

All 5 source files read. No `color` or `capabilities` fields found in any file.
Official required fields verified: `name` and `description` present in all 5 files.
All ADAPT — every file contains meta-project-specific content in the body.

---

### async-safety-auditor.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/async-safety-auditor.md
**Target:** /Users/bruno/siopv/.claude/agents/async-safety-auditor.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all within official 13-field schema.
`memory: project` — valid per schema. No prohibited fields (no color, no capabilities).
No missing required fields.

**Action:**
Replace "Project Context (CRITICAL)" section (lines 12–24 in source) with SIOPV-specific context:

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "asyncio.run" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands **must use `cd` with the target path**:
  ```bash
  TARGET="/Users/bruno/siopv"
  cd "$TARGET" && grep -rn "asyncio.run" src/
  ```
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Additionally, update all inline bash blocks that reference `$(cat .build/active-project)` or `sec-llm-workbench/.ignorar/`:
- Lines 42–43: Replace `TARGET="${TARGET/#\~/$HOME}"` preamble; set `TARGET="/Users/bruno/siopv"` directly.
- Lines 48–49: Same pattern replacement.
- Lines 63–69: Same pattern replacement.
- Lines 86–91: Same pattern replacement.
- Lines 100–103: Same pattern replacement.
- Line 138: `sec-llm-workbench/.ignorar/production-reports/` → `.ignorar/production-reports/` (relative to SIOPV root)

**Obsolete content:** All references to `sec-llm-workbench/`, `.build/active-project`, "meta-project" framing.

**Cross-file dependencies:** None.

---

### circular-import-detector.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/circular-import-detector.md
**Target:** /Users/bruno/siopv/.claude/agents/circular-import-detector.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all within official 13-field schema.
No missing required fields. No prohibited fields.

**Action:**
Replace "Project Context (CRITICAL)" section (lines 12–17 in source):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project.
- Target project path is provided in your invocation prompt or read from `.build/active-project`.
- Expand `~` to `$HOME` before use.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project).
```

NEW:
```
## Project Context (CRITICAL)

- You are working on the **SIOPV project** at `/Users/bruno/siopv/`.
- Target project path: `/Users/bruno/siopv/`
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`).
```

Replace Action step 1 (line 28):
OLD: `1. Read `.build/active-project` to get TARGET. Expand `~` to `$HOME`.`
NEW: `1. Set TARGET="/Users/bruno/siopv"`

Update inline bash in step 3 (lines 36–37):
OLD: `cd "$TARGET" && python3 - <<'PYEOF'` (where TARGET was read from `.build/active-project`)
NEW: `TARGET="/Users/bruno/siopv" && cd "$TARGET" && python3 - <<'PYEOF'`

Update report path in step 6 (line 121):
OLD: `.ignorar/production-reports/circular-import-detector/phase-{N}/{TIMESTAMP}-...`
NEW: (path remains same relative structure — no change needed; already relative)
Note: The path is already expressed as relative `.ignorar/...` — no absolute path change required in the report naming line itself. Only the Project Context preamble needs updating to set root correctly.

**Obsolete content:** References to `sec-llm-workbench/`, `.build/active-project`, "meta-project" framing.

**Cross-file dependencies:** None.

---

### code-implementer.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/code-implementer.md
**Target:** /Users/bruno/siopv/.claude/agents/code-implementer.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present: name, description, tools, model, memory, permissionMode, skills — all within official 13-field schema.
`skills: [coding-standards-2026]` — valid per schema.
`permissionMode: acceptEdits` — valid value per permissionMode field.
No missing required fields. No prohibited fields.

**Action:**

Replace "Project Context (CRITICAL)" section (lines 12–29 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Write, Edit, Glob, Grep) must target the **target project**, not the meta-project
- All git operations **must use `git -C`** with the expanded target path — never rely on the current working directory:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  git -C "$TARGET" add <file>
  git -C "$TARGET" commit -m "..."
  git -C "$TARGET" status
  ```
- All `uv run` commands (ruff, mypy, pytest) must run from the **target project directory** using `cd "$TARGET" && uv run ...`
- Never commit target project code to `sec-llm-workbench/`
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project) — this is the only thing that stays in the meta-project
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Write, Edit, Glob, Grep) must target `/Users/bruno/siopv/`
- All git operations **must use `git -C`** with the target path:
  ```bash
  TARGET="/Users/bruno/siopv"
  git -C "$TARGET" add <file>
  git -C "$TARGET" commit -m "..."
  git -C "$TARGET" status
  ```
- All `uv run` commands (ruff, mypy, pytest) must run from the target directory: `cd "$TARGET" && uv run ...`
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Update "Before Writing Code" step 2 (line 49):
OLD: `2. **Read `.claude/docs/python-standards.md`** → Document standards applied`
NEW: `2. **Read `.claude/docs/python-standards.md`** (in SIOPV project root) → Document standards applied`
Note: UNCLEAR — it is not confirmed whether `/Users/bruno/siopv/.claude/docs/python-standards.md` exists. Stage 4.2 must verify before deploying.

Update "Before Writing Code" step 3 (line 50):
OLD: `3. **Read `.claude/rules/tech-stack.md`** → Document rules applied`
NEW: `3. **Read `.claude/rules/tech-stack.md`** (in SIOPV project root) → Document rules applied`
Note: UNCLEAR — it is not confirmed whether `/Users/bruno/siopv/.claude/rules/tech-stack.md` exists. Stage 4.2 must verify before deploying.

Update Execution Checklist step 10 (line 434):
OLD: `10. ☐ Save report to `.ignorar/production-reports/code-implementer/phase-{N}/{NNN}-{slug}.md``
NEW: `10. ☐ Save report to `.ignorar/production-reports/code-implementer/phase-{N}/{TIMESTAMP}-phase-{N}-code-implementer-{slug}.md``
Reason: `{NNN}` sequential format contradicts timestamp-based naming used everywhere else in this file and in `.claude/rules/agent-reports.md`.

**Obsolete content:** All references to `sec-llm-workbench/`, `.build/active-project`, "meta-project" framing. The "Never commit target project code to `sec-llm-workbench/`" warning is obsolete in SIOPV context.

**Cross-file dependencies:**
- `.claude/docs/python-standards.md` (referenced in body, must exist in SIOPV)
- `.claude/rules/tech-stack.md` (referenced in body, must exist in SIOPV)

---

### config-validator.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/config-validator.md
**Target:** /Users/bruno/siopv/.claude/agents/config-validator.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present: name, description, tools, model, memory, permissionMode, disallowedTools — all within official 13-field schema.
No missing required fields. No prohibited fields.

**Action:**

Replace "Project Context (CRITICAL)" section (lines 12–24 in source):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "settings\." src/ --include="*.py"
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All commands **must use `cd` with the target path**:
  ```bash
  TARGET="/Users/bruno/siopv"
  cd "$TARGET" && grep -rn "settings\." src/ --include="*.py"
  ```
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Update Action step 1 bash block (lines 42–48):
OLD:
```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "ERROR: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
```
NEW:
```bash
TARGET="/Users/bruno/siopv"
if [ ! -d "$TARGET" ]; then
    echo "ERROR: SIOPV directory not found at $TARGET"
    exit 1
fi
```

**Obsolete content:** All references to `sec-llm-workbench/`, `.build/active-project`, "meta-project" framing.

**Cross-file dependencies:** None.

---

### dependency-scanner.md
**Source:** /Users/bruno/sec-llm-workbench/.claude/agents/dependency-scanner.md
**Target:** /Users/bruno/siopv/.claude/agents/dependency-scanner.md
**Classification:** ADAPT

**Frontmatter check:**
Fields present: name, description, tools, model, memory, permissionMode — all within official 13-field schema.
No `disallowedTools` field — absent, not required.
No missing required fields. No prohibited fields.

**Action:**

Replace "Project Context (CRITICAL)" section (lines 11–17 in source):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project itself.
- Target project path is provided in your invocation prompt. If not provided, read `.build/active-project`.
- Expand `~` to `$HOME` in all paths before use.
- All `uv run` and `cd` commands must use the fully expanded target project path.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project directory).
```

NEW:
```
## Project Context (CRITICAL)

- You are working on the **SIOPV project** at `/Users/bruno/siopv/`.
- Target project path: `/Users/bruno/siopv/`
- All `uv run` and `cd` commands must use `/Users/bruno/siopv/` as the working directory.
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`).
```

Update Action step 1 (line 29):
OLD: `1. Read `.build/active-project` to get TARGET. Run: `TARGET=$(cat .build/active-project); TARGET="${TARGET/#\~/$HOME}"``
NEW: `1. Set `TARGET="/Users/bruno/siopv"``

Update Action step 7 — `.build/current-phase` reference (line 57):
OLD: `where {N} = content of `.build/current-phase`, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
NEW: `where {N} = phase number provided in your invocation prompt, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
Reason: `.build/current-phase` is a meta-project mechanism; SIOPV does not use it.

**Obsolete content:** All references to `sec-llm-workbench/`, `.build/active-project`, `.build/current-phase`, "meta-project" framing.

**Cross-file dependencies:** None. (`.build/current-phase` is removed as obsolete.)

---

## Summary

| Agent | Classification | Frontmatter Issues | Key Body Changes |
|-------|---------------|-------------------|-----------------|
| async-safety-auditor | ADAPT | None | Project Context + bash blocks |
| circular-import-detector | ADAPT | None | Project Context + ACTION step 1 |
| code-implementer | ADAPT | None | Project Context + step 10 {NNN}→timestamp fix |
| config-validator | ADAPT | None | Project Context + step 1 bash block |
| dependency-scanner | ADAPT | None | Project Context + step 1 + step 7 `.build/current-phase` |

**All 5: ADAPT. 0 COPY AS-IS.**

## UNCLEAR Items

1. `code-implementer`: Whether `/Users/bruno/siopv/.claude/docs/python-standards.md` exists — Stage 4.2 must verify.
2. `code-implementer`: Whether `/Users/bruno/siopv/.claude/rules/tech-stack.md` exists — Stage 4.2 must verify.
