# SIOPV Stage 4.1 — Final Truth Document
> Stage: 4.1 — Claude Code Configuration Research & Truth Document
> Status: COMPLETE
> Total files documented: 74 (24 Section A + 26 Section B + 24 Section C)
> Generated: 2026-03-12
> Feeds: Stage 4.2 (implementation)

---

## How to Use This Document

This is the authoritative input for Stage 4.2. For each file listed in the Master Table of Contents, Stage 4.2 must execute the specified Action exactly as written. Classifications mean: **COPY AS-IS** = copy source file unchanged to target path; **ADAPT** = apply the explicit diff or changes listed under the file's Action section; **CREATE FROM SCRATCH** = create a new file at the target path using the skeleton/content provided; **DISCARD** = do not include in `siopv/.claude/`; **STAYS AT SOURCE** = file already exists at global location and requires no action. The Deployment Sequence section defines the order in which files must be deployed to satisfy cross-file dependencies.

---

## Master Table of Contents

### Section A — Agent Definitions + Adaptable Hooks

1. `/Users/bruno/siopv/.claude/agents/async-safety-auditor.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/async-safety-auditor.md`)
2. `/Users/bruno/siopv/.claude/agents/circular-import-detector.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/circular-import-detector.md`)
3. `/Users/bruno/siopv/.claude/agents/code-implementer.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/code-implementer.md`)
4. `/Users/bruno/siopv/.claude/agents/config-validator.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/config-validator.md`)
5. `/Users/bruno/siopv/.claude/agents/dependency-scanner.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/dependency-scanner.md`)
6. `/Users/bruno/siopv/.claude/agents/hallucination-detector.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/hallucination-detector.md`)
7. `/Users/bruno/siopv/.claude/agents/import-resolver.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/import-resolver.md`)
8. `/Users/bruno/siopv/.claude/agents/integration-tracer.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/integration-tracer.md`)
9. `/Users/bruno/siopv/.claude/agents/regression-guard.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/regression-guard.md`)
10. `/Users/bruno/siopv/.claude/agents/semantic-correctness-auditor.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/semantic-correctness-auditor.md`)
11. `/Users/bruno/siopv/.claude/agents/smoke-test-runner.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/smoke-test-runner.md`)
12. `/Users/bruno/siopv/.claude/agents/static-checks-agent.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/static-checks-agent.md`)
13. `/Users/bruno/siopv/.claude/agents/test-generator.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/test-generator.md`)
14. `/Users/bruno/siopv/.claude/agents/best-practices-enforcer.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/best-practices-enforcer.md`)
15. `/Users/bruno/siopv/.claude/agents/security-auditor.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/security-auditor.md`)
16. `/Users/bruno/siopv/.claude/agents/code-reviewer.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/code-reviewer.md`)
17. `/Users/bruno/siopv/.claude/agents/xai-explainer.md` — ADAPT / DEPLOYMENT BLOCKED (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/xai-explainer.md`)
18. `/Users/bruno/siopv/.claude/agents/researcher-1.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-1.md`)
19. `/Users/bruno/siopv/.claude/agents/researcher-2.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-2.md`)
20. `/Users/bruno/siopv/.claude/agents/researcher-3.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-3.md`)
21. `/Users/bruno/siopv/.claude/hooks/pre-commit.sh` — ADAPT / manual utility only (Source: `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-commit.sh`)
22. `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh` — ADAPT / manual utility only (Source: `/Users/bruno/sec-llm-workbench/.claude/hooks/verify-best-practices.sh`)
23. `/Users/bruno/siopv/.claude/hooks/post-code.sh` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/hooks/post-code.sh`)
24. `/Users/bruno/siopv/.claude/hooks/pre-git-commit.sh` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-git-commit.sh`)

### Section B — Meta Hooks + Settings.json + Workflow + Docs (C4a)

25. `/Users/bruno/siopv/.claude/hooks/pre-write.sh` — CREATE FROM SCRATCH
26. `/Users/bruno/siopv/.claude/hooks/test-framework.sh` — CREATE FROM SCRATCH (manual, MERGE-006-A)
27. `/Users/bruno/siopv/.claude/hooks/pre-compact.sh` — CREATE FROM SCRATCH
28. `/Users/bruno/siopv/.claude/hooks/session-start.sh` — CREATE FROM SCRATCH
29. `/Users/bruno/siopv/.claude/hooks/session-end.sh` — CREATE FROM SCRATCH
30. `/Users/bruno/siopv/.claude/settings.json` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/settings.json`)
31. `/Users/bruno/siopv/.claude/workflow/01-session-start.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/01-session-start.md`)
32. `/Users/bruno/siopv/.claude/workflow/02-reflexion-loop.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/02-reflexion-loop.md`)
33. `/Users/bruno/siopv/.claude/workflow/03-human-checkpoints.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/03-human-checkpoints.md`)
34. `/Users/bruno/siopv/.claude/workflow/05-before-commit.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/05-before-commit.md`)
35. `/Users/bruno/siopv/.claude/workflow/04-agents.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/04-agents.md`)
36. `/Users/bruno/siopv/.claude/workflow/06-decisions.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/06-decisions.md`)
37. `/Users/bruno/siopv/.claude/workflow/07-orchestrator-invocation.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/07-orchestrator-invocation.md`)
38. `/Users/bruno/siopv/.claude/workflow/briefing.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md`)
39. `/Users/bruno/siopv/.claude/workflow/spec-findings.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/spec-findings.md`)
40. `/Users/bruno/siopv/.claude/workflow/orchestrator-briefing.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/workflow/orchestrator-briefing.md`)
41. `/Users/bruno/sec-llm-workbench/.claude/workflow/setup-checklist.md` — DISCARD (META-ONLY)
42. `/Users/bruno/siopv/.claude/workflow/compaction-log.md` — CREATE FROM SCRATCH
43. `/Users/bruno/siopv/.claude/docs/traceability.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/traceability.md`)
44. `/Users/bruno/siopv/.claude/docs/errors-to-rules.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/errors-to-rules.md`)
45. `/Users/bruno/siopv/.claude/docs/python-standards.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/python-standards.md`)
46. `/Users/bruno/siopv/.claude/docs/techniques.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/techniques.md`)
47. `/Users/bruno/siopv/.claude/docs/mcp-setup.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/mcp-setup.md`)
48. `/Users/bruno/siopv/.claude/docs/model-selection-strategy.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/model-selection-strategy.md`)
49. `/Users/bruno/siopv/.claude/docs/agent-tool-schemas.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/agent-tool-schemas.md`)
50. `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md` — CREATE FROM SCRATCH (MERGE-009-A)

### Section C — Docs/Rules + CLAUDE.md + Memory + Skills

51. `/Users/bruno/siopv/.claude/docs/verification-thresholds.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/verification-thresholds.md`)
52. `/Users/bruno/siopv/.claude/docs/universal-project-automation-template.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/docs/2026-03-12-11.27.33-universal-project-automation-template-teamcreate-orchestrator-agents-rounds-checkpoints-shutdown-file-updates.md`)
53. `/Users/bruno/siopv/.claude/rules/agent-reports.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/rules/agent-reports.md`)
54. `/Users/bruno/siopv/.claude/rules/placeholder-conventions.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/rules/placeholder-conventions.md`)
55. `/Users/bruno/siopv/.claude/rules/tech-stack.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/rules/tech-stack.md`)
56. `/Users/bruno/.claude/CLAUDE.md` — STAYS AT SOURCE (global config, no action)
57. `/Users/bruno/siopv/CLAUDE.md` — CREATE FROM SCRATCH
58. `/Users/bruno/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md` — CREATE FROM SCRATCH
59. `/Users/bruno/siopv/.claude/skills/show-trace/SKILL.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/show-trace/SKILL.md`)
60. `/Users/bruno/siopv/.claude/skills/orchestrator-protocol/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/orchestrator-protocol/SKILL.md`)
61. `/Users/bruno/siopv/.claude/skills/techniques-reference/SKILL.md` — COPY AS-IS + deployment note (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/techniques-reference/SKILL.md`)
62. `/Users/bruno/siopv/.claude/skills/new-project/SKILL.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/new-project/SKILL.md`)
63. `/Users/bruno/siopv/.claude/skills/scan-vulnerabilities/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/scan-vulnerabilities/SKILL.md`)
64. `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md` — CREATE FROM SCRATCH (MERGE-009-A) [also documented in Section B entry 50]
65. `/Users/bruno/siopv/.claude/skills/run-tests/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/run-tests/SKILL.md`)
66. `/Users/bruno/siopv/.claude/skills/generate-report/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/generate-report/SKILL.md`)
67. `/Users/bruno/siopv/.claude/skills/verify/SKILL.md` — ADAPT (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/verify/SKILL.md`)
68. `/Users/bruno/siopv/.claude/skills/coding-standards-2026/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/coding-standards-2026/SKILL.md`)
69. `/Users/bruno/siopv/.claude/skills/cve-research/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/cve-research/SKILL.md`)
70. `/Users/bruno/siopv/.claude/skills/langraph-patterns/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/langraph-patterns/SKILL.md`)
71. `/Users/bruno/siopv/.claude/skills/openfga-patterns/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/openfga-patterns/SKILL.md`)
72. `/Users/bruno/siopv/.claude/skills/presidio-dlp/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/presidio-dlp/SKILL.md`)
73. `/Users/bruno/siopv/.claude/skills/trivy-integration/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/trivy-integration/SKILL.md`)
74. `/Users/bruno/siopv/.claude/skills/xai-visualization/SKILL.md` — COPY AS-IS (Source: `/Users/bruno/sec-llm-workbench/.claude/skills/xai-visualization/SKILL.md`)

**Total: 74 entries** (24 Section A + 26 Section B + 24 Section C)
**Note:** Entry 50 (init-session/SKILL.md in Section B/B7) and entry 64 (init-session/SKILL.md in Section C/C3) document the same physical file — Section B holds the authoritative content; Section C provides the deployment notes from aggregator-c's perspective.

---

## Section A: Agent Definitions + Adaptable Hooks

> Source file: `2026-03-12-22.35.10-stage4.1-truth-doc-section-a.md`
> Produced by: final-aggregator-a
> Total entries: 24 (20 agents + 4 hooks)

# Stage 4.1 Truth Document — Section A: Agent Definitions + Adaptable Hooks
> Files covered: A1 (5 agents), A2 (5 agents), A3 (5 agents), A4 (5 agents), B1 (4 hooks)
> MERGE decisions applied: MERGE-001-A, MERGE-002-A, MERGE-003-B, MERGE-004-B, MERGE-005-B, MERGE-006-A
> Status: COMPLETE
> Total entries: 24 (20 agents + 4 hooks)
> Produced by: final-aggregator-a
> Timestamp: 2026-03-12-223510

---

## MERGE Decisions Applied (reference)

| ID | Decision | Summary |
|----|----------|---------|
| MERGE-001-A | Keep `memory` values as-is | `memory: project` stays in smoke-test-runner, test-generator, xai-explainer. `memory: true` stays in best-practices-enforcer, security-auditor, code-reviewer. No standardization. |
| MERGE-002-A | Keep `code-implementer` doc references | `.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md` remain in code-implementer body. Note "(create in Stage 4.2 if not present)" appended. |
| MERGE-003-B | Graceful-skip for `check-reviewer-score.sh` | Do not create a stub. The `if [ -f "$SCORE_CHECK_SCRIPT" ]` guard in pre-git-commit.sh handles absence cleanly. |
| MERGE-004-B | pre-commit.sh and verify-best-practices.sh = manual utilities only | Do NOT register in settings.json as lifecycle hooks. Document in SIOPV CLAUDE.md under "Manual Utilities". |
| MERGE-005-B | Block xai-explainer deployment | Hold until Stage 4.2 verifies `class_names` against `src/siopv/adapters/ml/` with a Glob read. |
| MERGE-006-A | test-framework.sh = manual standalone script | Place in `.claude/hooks/` as utility. NOT wired to any hook event. Note: test-framework.sh is not covered by the 5 domain reports in this section — it is out of scope here. MERGE-006-A is noted for cross-reference only. |

---

## Section A1: Agent Definitions — Batch 1

---

### async-safety-auditor.md
**Target path:** `/Users/bruno/siopv/.claude/agents/async-safety-auditor.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/async-safety-auditor.md`

**Action:**
Replace the "Project Context (CRITICAL)" section (body lines 12–24) with SIOPV-specific context:

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "asyncio.run" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands **must use `cd` with the target path**:
  ```bash
  TARGET="/Users/bruno/siopv"
  cd "$TARGET" && grep -rn "asyncio.run" src/
  ```
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Additionally, update all inline bash blocks that reference `$(cat .build/active-project)` or `sec-llm-workbench/.ignorar/`:
- Replace every `TARGET=$(cat .build/active-project)` / `TARGET="${TARGET/#\~/$HOME}"` preamble with `TARGET="/Users/bruno/siopv"` (applies to lines ~42–43, ~48–49, ~63–69, ~86–91, ~100–103).
- Replace `sec-llm-workbench/.ignorar/production-reports/` with `.ignorar/production-reports/` (line ~138).

**Dependencies:** None.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### circular-import-detector.md
**Target path:** `/Users/bruno/siopv/.claude/agents/circular-import-detector.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/circular-import-detector.md`

**Action:**
Replace "Project Context (CRITICAL)" section (body lines 12–17):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project.
- Target project path is provided in your invocation prompt or read from `.build/active-project`.
- Expand `~` to `$HOME` before use.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project).
```

NEW:
```
## Project Context (CRITICAL)

- You are working on the **SIOPV project** at `/Users/bruno/siopv/`.
- Target project path: `/Users/bruno/siopv/`
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`).
```

Replace Action step 1 (line ~28):
- OLD: `1. Read `.build/active-project` to get TARGET. Expand `~` to `$HOME`.`
- NEW: `1. Set TARGET="/Users/bruno/siopv"`

Update inline bash in step 3 (lines ~36–37):
- OLD: `cd "$TARGET" && python3 - <<'PYEOF'` (where TARGET was read from `.build/active-project`)
- NEW: `TARGET="/Users/bruno/siopv" && cd "$TARGET" && python3 - <<'PYEOF'`

Report path (step 6): already expressed as relative `.ignorar/...` — no absolute change required.

**Dependencies:** None.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### code-implementer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/code-implementer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/code-implementer.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 12–29):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Write, Edit, Glob, Grep) must target the **target project**, not the meta-project
- All git operations **must use `git -C`** with the expanded target path — never rely on the current working directory:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  git -C "$TARGET" add <file>
  git -C "$TARGET" commit -m "..."
  git -C "$TARGET" status
  ```
- All `uv run` commands (ruff, mypy, pytest) must run from the **target project directory** using `cd "$TARGET" && uv run ...`
- Never commit target project code to `sec-llm-workbench/`
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project) — this is the only thing that stays in the meta-project
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Write, Edit, Glob, Grep) must target `/Users/bruno/siopv/`
- All git operations **must use `git -C`** with the target path:
  ```bash
  TARGET="/Users/bruno/siopv"
  git -C "$TARGET" add <file>
  git -C "$TARGET" commit -m "..."
  git -C "$TARGET" status
  ```
- All `uv run` commands (ruff, mypy, pytest) must run from the target directory: `cd "$TARGET" && uv run ...`
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Change 2 — "Before Writing Code" step 2 (line ~49) — MERGE-002-A applied:
- OLD: `2. **Read `.claude/docs/python-standards.md`** → Document standards applied`
- NEW: `2. **Read `.claude/docs/python-standards.md`** (in SIOPV project root, create in Stage 4.2 if not present) → Document standards applied`

Change 3 — "Before Writing Code" step 3 (line ~50) — MERGE-002-A applied:
- OLD: `3. **Read `.claude/rules/tech-stack.md`** → Document rules applied`
- NEW: `3. **Read `.claude/rules/tech-stack.md`** (in SIOPV project root, create in Stage 4.2 if not present) → Document rules applied`

Change 4 — Execution Checklist step 10 (line ~434):
- OLD: `10. ☐ Save report to `.ignorar/production-reports/code-implementer/phase-{N}/{NNN}-{slug}.md``
- NEW: `10. ☐ Save report to `.ignorar/production-reports/code-implementer/phase-{N}/{TIMESTAMP}-phase-{N}-code-implementer-{slug}.md``
- Reason: `{NNN}` sequential format contradicts timestamp-based naming used by `.claude/rules/agent-reports.md`.

**Dependencies:**
- `.claude/docs/python-standards.md` (referenced in body — create in Stage 4.2 if not present)
- `.claude/rules/tech-stack.md` (referenced in body — create in Stage 4.2 if not present)

**Deployment notes:** May be deployed before python-standards.md and tech-stack.md exist (MERGE-002-A); agent body notes the create-if-absent caveat. Verify file existence at Stage 4.2.

---

### config-validator.md
**Target path:** `/Users/bruno/siopv/.claude/agents/config-validator.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/config-validator.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 12–24):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "settings\." src/ --include="*.py"
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** at `/Users/bruno/siopv/`.

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All commands **must use `cd` with the target path**:
  ```bash
  TARGET="/Users/bruno/siopv"
  cd "$TARGET" && grep -rn "settings\." src/ --include="*.py"
  ```
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`)
```

Change 2 — Replace Action step 1 bash block (body lines ~42–48):

OLD:
```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "ERROR: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
```

NEW:
```bash
TARGET="/Users/bruno/siopv"
if [ ! -d "$TARGET" ]; then
    echo "ERROR: SIOPV directory not found at $TARGET"
    exit 1
fi
```

**Dependencies:** None.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### dependency-scanner.md
**Target path:** `/Users/bruno/siopv/.claude/agents/dependency-scanner.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/dependency-scanner.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 11–17):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project itself.
- Target project path is provided in your invocation prompt. If not provided, read `.build/active-project`.
- Expand `~` to `$HOME` in all paths before use.
- All `uv run` and `cd` commands must use the fully expanded target project path.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project directory).
```

NEW:
```
## Project Context (CRITICAL)

- You are working on the **SIOPV project** at `/Users/bruno/siopv/`.
- Target project path: `/Users/bruno/siopv/`
- All `uv run` and `cd` commands must use `/Users/bruno/siopv/` as the working directory.
- Reports go to `.ignorar/production-reports/` (relative to `/Users/bruno/siopv/`).
```

Change 2 — Replace Action step 1 (line ~29):
- OLD: `1. Read `.build/active-project` to get TARGET. Run: `TARGET=$(cat .build/active-project); TARGET="${TARGET/#\~/$HOME}"``
- NEW: `1. Set `TARGET="/Users/bruno/siopv"``

Change 3 — Replace step 7 `.build/current-phase` reference (line ~57):
- OLD: `where {N} = content of `.build/current-phase`, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
- NEW: `where {N} = phase number provided in your invocation prompt, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
- Reason: `.build/current-phase` is a meta-project mechanism; not used in SIOPV.

**Dependencies:** None. (`.build/current-phase` removed as obsolete.)
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

## Section A2: Agent Definitions — Batch 2

---

### hallucination-detector.md
**Target path:** `/Users/bruno/siopv/.claude/agents/hallucination-detector.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/hallucination-detector.md`

**Action:**
Replace the entire "Project Context (CRITICAL)" block (body lines 12–24):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run mypy src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && uv run mypy src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

Note: HTML comment `<!-- version: 2026-02 -->` (before the `---` opener in source) is outside frontmatter — carry as-is. All other body content (Role Definition, Verification Process, Tool Invocation, Role Reinforcement, Actions, Report Persistence, Report Format) is generic and applicable to SIOPV unchanged.

**Dependencies:** References `mcp__context7__resolve-library-id` and `mcp__context7__query-docs` (MCP tools, not file references). None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### import-resolver.md
**Target path:** `/Users/bruno/siopv/.claude/agents/import-resolver.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/import-resolver.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" block (body lines 12–19):

OLD:
```
## Project Context (CRITICAL)

- You are invoked from the meta-project (sec-llm-workbench/). You are NOT working on the meta-project.
- Target project path is provided in your invocation prompt or read from `.build/active-project`.
- Expand `~` to `$HOME` before use.
- All `uv run` and `cd` commands must use the fully expanded target project path.
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project directory).
```

NEW:
```
## Project Context (CRITICAL)

- You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).
- Target project path is `/Users/bruno/siopv`. Use `TARGET=/Users/bruno/siopv` in all commands.
- All `uv run` and `cd` commands must use `/Users/bruno/siopv` as the project path.
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Replace Step 1 of Actions (body lines 35–38):

OLD:
```
1. Read `.build/active-project` to get TARGET. Run:
   ```bash
   TARGET=$(cat .build/active-project); TARGET="${TARGET/#\~/$HOME}"
   ```
```

NEW:
```
1. Set TARGET to the SIOPV project path:
   ```bash
   TARGET=/Users/bruno/siopv
   ```
```

Change 3 — Replace report path reference in Step 9 (body line ~158):
- OLD: `where {N} = content of `.build/current-phase`, {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``
- NEW: `where {N} = current SIOPV phase number (provide at invocation time), {TIMESTAMP} = `date +%Y-%m-%d-%H%M%S``

Note: HTML comment `<!-- version: 2026-03 -->` (before `---` opener) is outside frontmatter — carry as-is.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### integration-tracer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/integration-tracer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/integration-tracer.md`

**Action:**
Replace the entire "Project Context (CRITICAL)" block (body lines 12–24):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "add_node" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && grep -rn "add_node" src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

Note: HTML comment `<!-- version: 2026-03 -->` (before `---` opener) is outside frontmatter — carry as-is. All other body content is generic and applicable to SIOPV unchanged.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### regression-guard.md
**Target path:** `/Users/bruno/siopv/.claude/agents/regression-guard.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/regression-guard.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" block (body lines 12–23):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) and git commands must target the **target project directory**.
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run pytest ...
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project), NOT the target project.
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) and git commands must target `/Users/bruno/siopv/`.
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && uv run pytest ...
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Replace Step 1 "Read active project path" bash block (body lines ~40–47):

OLD:
```
### 1. Read active project path

```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "ERROR: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```
```

NEW:
```
### 1. Set project path

```bash
TARGET=/Users/bruno/siopv
if [ ! -d "$TARGET" ]; then
    echo "ERROR: SIOPV project directory not found: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```
```

Note: `permissionMode: default` (differs from other agents using `plan`) — carry as-is per source. HTML comment `<!-- version: 2026-03 -->` outside frontmatter — carry as-is.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### semantic-correctness-auditor.md
**Target path:** `/Users/bruno/siopv/.claude/agents/semantic-correctness-auditor.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/semantic-correctness-auditor.md`

**Action:**
Replace the "Project Context (CRITICAL)" block (body lines 12–24):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && grep -rn "field_validator" src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working directly on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv` (use `TARGET=/Users/bruno/siopv`)
- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/`
- All `uv run` commands must use `cd` with the SIOPV path:
  ```bash
  TARGET=/Users/bruno/siopv
  cd "$TARGET" && grep -rn "field_validator" src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

Note: HTML comment `<!-- version: 2026-02 -->` outside frontmatter — carry as-is. All other body content is generic and applicable to SIOPV unchanged.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

## Section A3: Agent Definitions — Batch 3

---

### smoke-test-runner.md
**Target path:** `/Users/bruno/siopv/.claude/agents/smoke-test-runner.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/smoke-test-runner.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 11–23):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`). You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt. If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**.
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run python -c "import siopv"
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project).
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run python -c "import siopv"
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Step 1 "Resolve Target Project" bash block:

OLD:
```bash
TARGET=$(cat .build/active-project 2>/dev/null || echo "")
TARGET="${TARGET/#\~/$HOME}"
if [ -z "$TARGET" ] || [ ! -d "$TARGET" ]; then
    echo "FAIL: .build/active-project is missing or invalid: '$TARGET'"
    exit 1
fi
echo "Target project: $TARGET"
```

NEW:
```bash
TARGET="/Users/bruno/siopv"
echo "Target project: $TARGET"
```

Change 3 — Step 9 "Save Report" bash block:

OLD:
```bash
PHASE=$(cat .build/current-phase 2>/dev/null || echo "0")
TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
REPORT_DIR=".ignorar/production-reports/smoke-test-runner/phase-${PHASE}"
mkdir -p "$REPORT_DIR"
REPORT_PATH="${REPORT_DIR}/${TIMESTAMP}-phase-${PHASE}-smoke-test-runner-smoke-test.md"
```

NEW:
```bash
TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
REPORT_DIR="/Users/bruno/siopv/.ignorar/production-reports/smoke-test-runner"
mkdir -p "$REPORT_DIR"
REPORT_PATH="${REPORT_DIR}/${TIMESTAMP}-smoke-test-runner-smoke-test.md"
```

Change 4 — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/smoke-test-runner/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/smoke-test-runner/`

Note: `memory: project` — kept as-is per MERGE-001-A. Line 5 comment `# Este hook se ejecuta en el PROYECTO DESTINO, no en el META-PROYECTO` can be removed (non-functional annotation).

**Dependencies:** None.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### static-checks-agent.md
**Target path:** `/Users/bruno/siopv/.claude/agents/static-checks-agent.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/static-checks-agent.md`

**Action:**
Copy source to target unchanged. Body uses `{PROJECT_PATH}`, `{REPORT_PATH}`, `{FIXED_TIMESTAMP}` as invocation-time placeholders — no hardcoded meta-project paths. No `sec-llm-workbench` references in body. No `color` or `capabilities` fields present.

**Dependencies:** None.
**Deployment notes:** No edits required. Copy operation only.

---

### test-generator.md
**Target path:** `/Users/bruno/siopv/.claude/agents/test-generator.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/test-generator.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 11–26):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Write, Glob, Grep) must target the **target project**, not the meta-project
- All git operations (`git add`, `git commit`, `git status`, `git diff`) must run from the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run pytest tests/ --cov=src
  ```
- Never commit target project code to `sec-llm-workbench/`
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project) — this is the only thing that stays in the meta-project
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Write, Glob, Grep) target the SIOPV project directory.
- All git operations must run from `/Users/bruno/siopv/`.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run pytest tests/ --cov=src
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/test-generator/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/test-generator/`

Note: `memory: project` — kept as-is per MERGE-001-A.

**Dependencies:** None.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### best-practices-enforcer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/best-practices-enforcer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/best-practices-enforcer.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 12–26):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run ruff check src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run ruff check src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Remove Tool Invocation section reference to meta-project doc (body line ~172):
- OLD: `**Reference:** For complete tool schemas, parameter definitions, and agent-specific examples, read `.claude/docs/agent-tool-schemas.md` on demand`
- NEW: (Remove this line entirely — `.claude/docs/agent-tool-schemas.md` does not exist in SIOPV)

Change 3 — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/best-practices-enforcer/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/best-practices-enforcer/`

Note: `memory: true` — kept as-is per MERGE-001-A. `skills: [coding-standards-2026]` — valid schema field; skill resolution is runtime concern, carry forward.

**Dependencies:**
- `.claude/docs/agent-tool-schemas.md` — meta-project-only doc, reference removed (Change 2).

**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### security-auditor.md
**Target path:** `/Users/bruno/siopv/.claude/agents/security-auditor.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/security-auditor.md`

**Action:**

Change 1 — Replace "Project Context (CRITICAL)" section (body lines 11–25):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run bandit -r src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are working on the **SIOPV project** (`/Users/bruno/siopv/`).

- **Target project path:** `/Users/bruno/siopv/`
- All file operations (Read, Glob, Grep) target the SIOPV project directory.
- All `uv run` commands must `cd` to absolute path:
  ```bash
  cd /Users/bruno/siopv && uv run bandit -r src/
  ```
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`.
```

Change 2 — Self-consistency voting experimental script path (body, `sys.path.insert` line):
- OLD: `sys.path.insert(0, '.ignorar/experimental-scripts/self-consistency-voting')`
- NEW: `sys.path.insert(0, '/Users/bruno/siopv/.ignorar/experimental-scripts/self-consistency-voting')`
- Note: Script does not currently exist in SIOPV. Section is labeled EXPERIMENTAL. Carry the section forward with updated path; script must be separately copied from meta-project to SIOPV if used.

Change 3 — Report Persistence section directory path:
- OLD: `.ignorar/production-reports/security-auditor/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/security-auditor/`

Note: `memory: true` — kept as-is per MERGE-001-A.

**Dependencies:**
- Experimental script at `.ignorar/experimental-scripts/self-consistency-voting/self-consistency-vote.py` — outside `.claude/` scope, requires manual copy to SIOPV if used.

**Deployment notes:** No prerequisites for core functionality. Experimental script copy is optional/deferred.

---

## Section A4: Agent Definitions — Adaptable Agents (code-reviewer + researcher-1/2/3 + xai-explainer)

---

### code-reviewer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/code-reviewer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/code-reviewer.md`

**Action:**

Change 1 — Strip pre-frontmatter comment:
- Pre-frontmatter line 1: `<!-- version: 2026-02 -->` — obsolete HTML comment before `---` opener. Do NOT carry to SIOPV version.

Change 2 — Replace "Project Context (CRITICAL)" section (body lines 12–24):

OLD:
```
## Project Context (CRITICAL)

You are being invoked from the **meta-project** (`sec-llm-workbench/`), which is the orchestrator. You are NOT working on the meta-project itself.

- **Target project path** will be provided in your invocation prompt (e.g. `<path/to/project>`). If not provided, read `.build/active-project` to discover it.
- All file operations (Read, Glob, Grep) must target the **target project directory**
- All `uv run` commands **must use `cd` with the expanded target path**:
  ```bash
  TARGET=$(cat .build/active-project)
  TARGET="${TARGET/#\~/$HOME}"   # expand ~ to absolute path
  cd "$TARGET" && uv run radon cc src/
  ```
- Reports go to `sec-llm-workbench/.ignorar/production-reports/` (meta-project)
```

NEW:
```
## Project Context (CRITICAL)

You are being invoked for the **SIOPV project** (`/Users/bruno/siopv/`).

- All file operations (Read, Glob, Grep) must target `/Users/bruno/siopv/src/siopv/`
- All `uv run` commands must use `cd /Users/bruno/siopv && uv run radon cc src/`
- Reports go to `/Users/bruno/siopv/.ignorar/production-reports/`
```

Change 3 — Report Persistence directory:
- OLD: `.ignorar/production-reports/code-reviewer/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/code-reviewer/phase-{N}/`

Note: `memory: true` — kept as-is per MERGE-001-A.

**Dependencies:** References `uv run radon cc` — assumes uv toolchain present in SIOPV. Not a file dependency.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### xai-explainer.md
**Target path:** `/Users/bruno/siopv/.claude/agents/xai-explainer.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/xai-explainer.md`

**⚠ MERGE-005-B — DEPLOYMENT BLOCKED:**
Do not deploy until Stage 4.2 verifies `class_names` (currently `["LOW", "MEDIUM", "HIGH", "CRITICAL"]`) against `src/siopv/adapters/ml/` with a Glob read to confirm actual risk-level labels used by the XGBoost classifier. Also verify actual model artifact path inside `src/siopv/adapters/ml/`.

**Action:**

Change 1 — Frontmatter description:
- OLD: `description: Invoke to generate ML model explanations using SHAP (global) and LIME (local) for audit and transparency requirements`
- NEW: `description: Invoke to generate ML model explanations using SHAP (global) and LIME (local) for SIOPV XGBoost classifier (src/siopv/adapters/ml/) audit and transparency — Phase 3`

Change 2 — System prompt — model path:
- OLD: `model_path = Path("models/xgboost_classifier.json")`
- NEW: `model_path = Path("src/siopv/adapters/ml/models/xgboost_classifier.json")`
- ⚠ Path to be verified by Stage 4.2 Glob read before deployment.

Change 3 — System prompt — error message:
- OLD: `raise ModelNotFoundError("Train model first")`
- NEW: `raise ModelNotFoundError("Train SIOPV XGBoost classifier first (Phase 3)")`

Change 4 — Report directory:
- OLD: `.ignorar/production-reports/xai-explainer/phase-{N}/`
- NEW: `/Users/bruno/siopv/.ignorar/production-reports/xai-explainer/phase-{N}/`

Change 5 — Naming convention (sequential → timestamp):
- OLD: `{NNN}-phase-{N}-xai-explainer-{descriptive-slug}.md`
- NEW: `{TIMESTAMP}-phase-{N}-xai-explainer-{descriptive-slug}.md`

Replace "How to Determine Next Number" section:

OLD:
```
### How to Determine Next Number
1. List files in `.ignorar/production-reports/xai-explainer/phase-{N}/`
2. Find the highest existing number
3. Increment by 1 (or start at 001 if empty)
```

NEW:
```
### How to Determine Timestamp
Use: `TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)`
```

Change 6 — Naming convention examples:
- OLD: `001-phase-3-xai-explainer-shap-global-analysis.md` / `002-phase-3-xai-explainer-lime-cve-2024-1234.md`
- NEW: `2026-03-12-181925-phase-3-xai-explainer-shap-global-analysis.md` / `2026-03-12-182030-phase-3-xai-explainer-lime-cve-2024-1234.md`

Note: `memory: project` — kept as-is per MERGE-001-A.

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** BLOCKED by MERGE-005-B. Stage 4.2 must run a Glob read against `src/siopv/adapters/ml/` before deployment to confirm: (1) model artifact path, (2) `class_names` match actual risk-level labels.

---

### researcher-1.md
**Target path:** `/Users/bruno/siopv/.claude/agents/researcher-1.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-1.md`

**Action:**

Change 1 — Frontmatter description:
- OLD: `description: "Deep online researcher for testing frameworks, integration testing, and contract testing patterns for async Python projects"`
- NEW: `description: "Deep online researcher for integration testing and contract testing patterns for SIOPV async stack (LangGraph, httpx, Presidio, OpenFGA)"`

Change 2 — System prompt — coordinator reference:
- OLD: `When done, send a message to "claude-main" confirming completion.`
- NEW: `When done, send a message to "orchestrator" confirming completion.`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### researcher-2.md
**Target path:** `/Users/bruno/siopv/.claude/agents/researcher-2.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-2.md`

**Action:**

Change 1 — Frontmatter description:
- OLD: `description: "Deep online researcher for observability, tracing, OpenTelemetry, and runtime monitoring patterns for async Python projects"`
- NEW: `description: "Deep online researcher for OTel, LangSmith, and structlog patterns for SIOPV pipeline (FastAPI, httpx, SQLAlchemy)"`

Change 2 — System prompt — coordinator reference:
- OLD: `When done, send a message to "claude-main" confirming completion.`
- NEW: `When done, send a message to "orchestrator" confirming completion.`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

### researcher-3.md
**Target path:** `/Users/bruno/siopv/.claude/agents/researcher-3.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/agents/researcher-3.md`

**Action:**

Change 1 — Frontmatter description:
- OLD: `description: "Deep online researcher for CI/CD pipelines, smoke testing, chaos engineering, and Docker Compose test environments"`
- NEW: `description: "Deep online researcher for CI/CD pipelines and Docker Compose for SIOPV stack (OpenFGA, Keycloak, Postgres, ChromaDB)"`

Change 2 — System prompt — coordinator reference:
- OLD: `When done, send a message to "claude-main" confirming completion.`
- NEW: `When done, send a message to "orchestrator" confirming completion.`

**Dependencies:** None to other `.claude/` files.
**Deployment notes:** No prerequisites. Deploy in Stage 4.2 implementation pass.

---

## Section B1: Adaptable Hooks

---

### pre-commit.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-commit.sh`

**Event type:** UNCLEAR — script reads no stdin JSON; takes positional argument (`$1`). Not a standard Claude Code lifecycle hook command. Confirmed non-blocking (no JSON output, no `hookSpecificOutput`).

**⚠ MERGE-004-B applied:** Register as manual-invocation utility ONLY. Do NOT register in `/Users/bruno/siopv/.claude/settings.json` as a lifecycle hook. Document in SIOPV CLAUDE.md under "Manual Utilities".

**Action:**
No literal path surgery required. No hardcoded `/Users/bruno/sec-llm-workbench/` found. Script uses `PROJECT_DIR="${1:-.}"` positional argument.

Single optional change:
- Line 5: comment `# Este hook se ejecuta en el PROYECTO DESTINO, no en el META-PROYECTO` — non-functional meta-project annotation. Remove or replace with: `# Manual utility: run as .claude/hooks/pre-commit.sh /Users/bruno/siopv`

**Deprecation check:** No deprecated `decision` field. Script produces no JSON output.

**Dependencies:** Requires `uv`, `ruff`, `mypy`, `pytest` installed in SIOPV environment.
**Deployment notes:** Place in `/Users/bruno/siopv/.claude/hooks/pre-commit.sh`. DO NOT register in settings.json. Document invocation in SIOPV CLAUDE.md.

---

### verify-best-practices.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/verify-best-practices.sh`

**Event type:** UNCLEAR — script reads no stdin JSON; takes positional argument (`$1`). Not a standard Claude Code lifecycle hook command. Non-blocking (no JSON output).

**⚠ MERGE-004-B applied:** Register as manual-invocation utility ONLY. Do NOT register in `/Users/bruno/siopv/.claude/settings.json` as a lifecycle hook. Document in SIOPV CLAUDE.md under "Manual Utilities".

**Action:**
No literal path surgery required. No hardcoded `/Users/bruno/sec-llm-workbench/` found. Script uses `PROJECT_DIR="${1:-.}"` and `grep -rn ... src/` (relative to project dir after `cd`).

No obsolete content identified. All checks (type hints, Pydantic v2, requests/httpx, os.path, print statements) are applicable to SIOPV's Python codebase.

**Deprecation check:** No deprecated `decision` field. Script produces no JSON output.

**Dependencies:** Requires `grep` (standard shell utility).
**Deployment notes:** Place in `/Users/bruno/siopv/.claude/hooks/verify-best-practices.sh`. DO NOT register in settings.json. Document invocation in SIOPV CLAUDE.md.

---

### post-code.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/post-code.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/post-code.sh`

**Event type:** PostToolUse — reads stdin JSON, extracts `.tool_input.file_path` and `.tool_name`. Fires after Write or Edit tool calls succeed. Non-blocking.

**Action:**
One critical logical block must be removed. No literal path surgery required elsewhere.

Remove lines 54–58 (META_PROJECT_DIR exclusion block) entirely:

OLD (lines 54–58):
```bash
# No procesar archivos del META-PROYECTO
META_PROJECT_DIR="${CLAUDE_PROJECT_DIR:-}"
if [ -n "$META_PROJECT_DIR" ] && [[ "$FILE_PATH" == "$META_PROJECT_DIR"* ]]; then
    exit 0
fi
```

NEW: (delete entirely — 5 lines removed, no replacement)

**Reason:** In SIOPV, `CLAUDE_PROJECT_DIR` will be `/Users/bruno/siopv/`. All SIOPV Python files start with that prefix. This block would exit 0 early for ALL SIOPV files, skipping ruff formatting and verification marker creation entirely — the inverse of intended behavior.

All other lines use `${CLAUDE_PROJECT_DIR}` or `$PROJECT_DIR` env vars and require no changes.

**Deprecation check:** No deprecated `decision` field. Script writes to log file only, exits with `exit 0`.

**Dependencies:**
- `jq` binary (stdin JSON parsing)
- `uuidgen` (with fallback to `date +%s`)
- `uv` binary (ruff format/check)
- Writes to: `${CLAUDE_PROJECT_DIR}/.build/logs/decisions/`
- Writes to: `$PROJECT_DIR/.build/checkpoints/pending/` (verification markers consumed by pre-git-commit.sh)
- Reads: `$PROJECT_DIR/pyproject.toml` (project root detection)

**Deployment notes:** Must be registered in `/Users/bruno/siopv/.claude/settings.json` under PostToolUse event. Part of verification chain with pre-git-commit.sh — deploy both together or neither.

---

### pre-git-commit.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/pre-git-commit.sh`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/hooks/pre-git-commit.sh`

**Event type:** PreToolUse — reads stdin JSON, extracts `.tool_input.command`; fires before Bash tool calls. Intercepts `git commit` commands. **Blocking** — outputs `hookSpecificOutput.permissionDecision: "deny"` to block commits; `"allow"` to permit.

**Action:**
One meta-project indirection block must be replaced. No literal path surgery required elsewhere.

Replace lines 51–58 (active-project indirection block):

OLD (lines 51–58):
```bash
# Check the active project's pending dir (post-code.sh writes markers there)
ACTIVE_PROJECT=$(cat "${CLAUDE_PROJECT_DIR:-.}/.build/active-project" 2>/dev/null || echo "")
ACTIVE_PROJECT="${ACTIVE_PROJECT/#\~/$HOME}"  # expand ~ to absolute path
if [ -n "$ACTIVE_PROJECT" ] && [ -d "$ACTIVE_PROJECT" ]; then
    VERIFICATION_DIR="$ACTIVE_PROJECT/.build/checkpoints/pending"
else
    VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
fi
```

NEW (collapse to single line):
```bash
VERIFICATION_DIR="${CLAUDE_PROJECT_DIR:-.}/.build/checkpoints/pending"
```

**Reason:** The indirection reads `.build/active-project` to find which child project is active (meta-project pattern). In SIOPV, `CLAUDE_PROJECT_DIR` is `/Users/bruno/siopv/` directly — no active-project file, no indirection needed. The `else` branch already produces the correct result.

Line 104 (check-reviewer-score.sh reference):
```bash
SCORE_CHECK_SCRIPT="${CLAUDE_PROJECT_DIR:-.}/.claude/scripts/check-reviewer-score.sh"
```
No change required. Uses `${CLAUDE_PROJECT_DIR}` env var. **MERGE-003-B applied:** The `if [ -f "$SCORE_CHECK_SCRIPT" ]` guard on line 106 handles absence gracefully. Do not create a stub.

All other lines use `${CLAUDE_PROJECT_DIR}` env var — no literal path changes needed.

**Deprecation check:** Already uses `hookSpecificOutput.permissionDecision` throughout (lines ~41–47, ~63–71, ~91–99, ~115–123, ~140–147). Fully compliant with current non-deprecated standard.

**Dependencies:**
- `jq` binary (stdin JSON parsing)
- `uuidgen` (with fallback)
- `find` (pending marker count)
- Reads: `$VERIFICATION_DIR/` (pending markers written by post-code.sh)
- Writes to: `${CLAUDE_PROJECT_DIR}/.build/logs/decisions/`
- Reads (optional, graceful skip): `${CLAUDE_PROJECT_DIR}/.claude/scripts/check-reviewer-score.sh`

**Deployment notes:** Must be registered in `/Users/bruno/siopv/.claude/settings.json` under PreToolUse event. Part of verification chain with post-code.sh — deploy both together or neither. Chain after adaptation: post-code.sh writes markers to `/Users/bruno/siopv/.build/checkpoints/pending/` → pre-git-commit.sh reads from same path via `CLAUDE_PROJECT_DIR`. Chain is self-consistent.

---

## Classification Summary

| File | Domain Report | Classification | Changes Required |
|------|---------------|---------------|-----------------|
| async-safety-auditor.md | A1 | ADAPT | Project Context + bash blocks |
| circular-import-detector.md | A1 | ADAPT | Project Context + step 1 |
| code-implementer.md | A1 | ADAPT | Project Context + step 10 timing fix + MERGE-002-A doc refs |
| config-validator.md | A1 | ADAPT | Project Context + step 1 bash block |
| dependency-scanner.md | A1 | ADAPT | Project Context + step 1 + step 7 `.build/current-phase` |
| hallucination-detector.md | A2 | ADAPT | Project Context |
| import-resolver.md | A2 | ADAPT | Project Context + step 1 + step 9 phase ref |
| integration-tracer.md | A2 | ADAPT | Project Context |
| regression-guard.md | A2 | ADAPT | Project Context + step 1 bash block |
| semantic-correctness-auditor.md | A2 | ADAPT | Project Context |
| smoke-test-runner.md | A3 | ADAPT | Project Context + step 1 + step 9 bash blocks + report path |
| static-checks-agent.md | A3 | COPY AS-IS | None |
| test-generator.md | A3 | ADAPT | Project Context + report path |
| best-practices-enforcer.md | A3 | ADAPT | Project Context + remove agent-tool-schemas.md ref + report path |
| security-auditor.md | A3 | ADAPT | Project Context + experimental script path + report path |
| code-reviewer.md | A4 | ADAPT | Strip `<!-- version -->` comment + Project Context + report path |
| xai-explainer.md | A4 | ADAPT (BLOCKED) | 6 changes + MERGE-005-B deployment block |
| researcher-1.md | A4 | ADAPT | description + coordinator name |
| researcher-2.md | A4 | ADAPT | description + coordinator name |
| researcher-3.md | A4 | ADAPT | description + coordinator name |
| pre-commit.sh | B1 | ADAPT (manual only) | Remove meta-project comment; MERGE-004-B |
| verify-best-practices.sh | B1 | ADAPT (manual only) | No changes; MERGE-004-B registration restriction |
| post-code.sh | B1 | ADAPT | Remove 5-line META_PROJECT_DIR exclusion block |
| pre-git-commit.sh | B1 | ADAPT | Collapse 8-line active-project indirection to 1 line; MERGE-003-B |

**Totals:** 1 COPY AS-IS, 22 ADAPT (1 deployment-blocked), 1 ADAPT manual-only (no code changes), 1 ADAPT manual-only (comment-only change) = **24 total entries**

---

## Open Items for Stage 4.2

1. **xai-explainer — BLOCKED:** Verify `class_names` against `src/siopv/adapters/ml/` with Glob read before deploying (MERGE-005-B).
2. **code-implementer — UNCLEAR:** Verify whether `/Users/bruno/siopv/.claude/docs/python-standards.md` exists. Create if absent (MERGE-002-A).
3. **code-implementer — UNCLEAR:** Verify whether `/Users/bruno/siopv/.claude/rules/tech-stack.md` exists. Create if absent (MERGE-002-A).
4. **pre-commit.sh / verify-best-practices.sh:** Document invocation syntax in SIOPV CLAUDE.md under "Manual Utilities" section (MERGE-004-B).
5. **post-code.sh + pre-git-commit.sh:** Register in `/Users/bruno/siopv/.claude/settings.json` (PostToolUse and PreToolUse respectively). Verify `.build/checkpoints/pending/` and `.build/logs/decisions/` directories exist or are created on first run.

---

## Section B: Meta Hooks + Settings.json + Workflow + Docs (C4a)

> Source file: `2026-03-12-22.35.31-stage4.1-truth-doc-section-b.md`
> Files covered: B2 (5 meta hooks), C1 (settings.json + 4 workflow), C2 (4 workflow + briefing), C3 (workflow recovery + docs), C4a (docs batch 2)
> Total entries: 26

# Stage 4.1 Truth Document — Section B: Meta Hooks + Settings + Workflow + Docs (C4a)
> Files covered: B2 (5 meta hooks), C1 (settings.json + 4 workflow), C2 (4 workflow + briefing), C3 (workflow recovery + docs), C4a (docs batch 2)
> MERGE decisions applied: MERGE-006-A, MERGE-007-B, MERGE-008-A, MERGE-009-A
> Status: COMPLETE

---

## Section B1: Meta Hooks (from B2)

---

### pre-write.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/pre-write.sh`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (meta-project version had hardcoded sec-llm-workbench paths)

**Action:**
Full script content for SIOPV (with SIOPV-specific paths and checkpoint names):

```bash
#!/usr/bin/env bash
# Pre-write hook: Anti-autopilot checkpoint reminder
# Event: PreToolUse (Write, Edit)
# Output format: hookSpecificOutput with permissionDecision
set -euo pipefail

TODAY=$(date +%Y%m%d)
SIOPV_ROOT="/Users/bruno/siopv"
MISSING_DOCS=()

if [[ ! -f "${SIOPV_ROOT}/.build/checkpoints/daily/briefing-read-${TODAY}" ]]; then
    MISSING_DOCS+=("SIOPV briefing.md")
fi

if [[ ! -f "${SIOPV_ROOT}/.build/checkpoints/daily/spec-read-${TODAY}" ]]; then
    MISSING_DOCS+=("SIOPV project specification")
fi

if [[ ! -f "${SIOPV_ROOT}/.build/checkpoints/daily/errors-read-${TODAY}" ]]; then
    MISSING_DOCS+=("errors-to-rules.md")
fi

if [[ ${#MISSING_DOCS[@]} -gt 0 ]]; then
    DOCS_LIST=$(printf '%s' "${MISSING_DOCS[0]}"; for doc in "${MISSING_DOCS[@]:1}"; do printf ', %s' "$doc"; done)

    cat <<EOF
{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "ask",
    "permissionDecisionReason": "Faltan checkpoints de lectura de documentacion SIOPV.",
    "additionalContext": "RECORDATORIO: Antes de Write/Edit, verifica que hayas consultado: ${DOCS_LIST}. Esto previene errores de piloto automatico."
  }
}
EOF
    exit 0
fi

cat <<EOF
{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "allow"
  }
}
EOF
exit 0
```

**Dependencies:**
- `/Users/bruno/siopv/.build/checkpoints/daily/briefing-read-${TODAY}` — created by `/init-session` skill
- `/Users/bruno/siopv/.build/checkpoints/daily/spec-read-${TODAY}` — created by `/init-session` skill
- `/Users/bruno/siopv/.build/checkpoints/daily/errors-read-${TODAY}` — created by `/init-session` skill
- `.build/checkpoints/daily/` directory must exist in SIOPV project root

**Deployment notes:**
- This hook is blocking (PreToolUse with `permissionDecision: "ask"`) — it will prompt the user until checkpoint files exist for the current day.
- The `/init-session` skill (MERGE-009-A) must be deployed first. Without it, this hook will block every Write/Edit on first use of the day.
- Must be registered in settings.json under `PreToolUse` event, matcher `Write|Edit`.
- The `SIOPV_ROOT` is hardcoded to `/Users/bruno/siopv` — not environment-variable-based.

---

### test-framework.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/test-framework.sh`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (meta-project version validated meta-project agent names, workflow files, and structure)

**Action:**
MERGE-006-A applied: this script is a **manual standalone validation utility**, NOT wired to any hook event. Run manually as `bash .claude/hooks/test-framework.sh`.

Full script content for SIOPV (validates SIOPV .claude/ configuration integrity):

```bash
#!/usr/bin/env bash
# Framework self-testing script — SIOPV
# Validates SIOPV .claude/ configuration integrity
# Run manually: bash .claude/hooks/test-framework.sh
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
CLAUDE_DIR="$PROJECT_ROOT/.claude"

PASS=0
FAIL=0

echo "=== SIOPV Framework Validation ==="

pass() {
  echo "[PASS] $1"
  PASS=$((PASS + 1))
}

fail() {
  echo "[FAIL] $1"
  FAIL=$((FAIL + 1))
}

# ============================================================
# 1. settings.json structure
# ============================================================

SETTINGS="$CLAUDE_DIR/settings.json"

if [ ! -f "$SETTINGS" ]; then
  fail "settings.json: file not found"
else
  if jq -e '.permissions.deny | type == "array"' "$SETTINGS" > /dev/null 2>&1; then
    pass "settings.json: permissions.deny exists (array)"
  else
    fail "settings.json: missing or invalid permissions.deny"
  fi

  if jq -e '.permissions.ask | type == "array"' "$SETTINGS" > /dev/null 2>&1; then
    pass "settings.json: permissions.ask exists (array)"
  else
    fail "settings.json: missing or invalid permissions.ask"
  fi

  if jq -e '.permissions.allow | type == "array"' "$SETTINGS" > /dev/null 2>&1; then
    pass "settings.json: permissions.allow exists (array)"
  else
    fail "settings.json: missing or invalid permissions.allow"
  fi
fi

# ============================================================
# 2. Agent YAML frontmatter validation
# ============================================================

AGENTS_DIR="$CLAUDE_DIR/agents"
VALID_MODELS="sonnet haiku opus"
REQUIRED_AGENT_FIELDS="name description tools model"

for agent_file in "$AGENTS_DIR"/*.md; do
  [ -f "$agent_file" ] || continue
  basename_file=$(basename "$agent_file")
  frontmatter=$(sed -n '/^---$/,/^---$/p' "$agent_file" | sed '1d;$d')

  if [ -z "$frontmatter" ]; then
    fail "agent $basename_file: no YAML frontmatter found"
    continue
  fi

  for field in $REQUIRED_AGENT_FIELDS; do
    value=$(echo "$frontmatter" | grep "^${field}:" | head -1 | sed "s/^${field}:[[:space:]]*//" | sed 's/^"//' | sed 's/"$//')
    if [ -z "$value" ]; then
      fail "agent $basename_file: missing '$field' field"
    else
      case "$field" in
        model)
          if echo "$VALID_MODELS" | grep -qw "$value"; then
            pass "agent $basename_file: model '$value' is valid"
          else
            fail "agent $basename_file: model '$value' is not one of: $VALID_MODELS"
          fi
          ;;
        *)
          pass "agent $basename_file: has '$field' field"
          ;;
      esac
    fi
  done
done

# ============================================================
# 3. Workflow files validation
# ============================================================

WORKFLOW_DIR="$CLAUDE_DIR/workflow"
WORKFLOW_FILES=(
  "briefing.md"
  "compaction-log.md"
)

for wf in "${WORKFLOW_FILES[@]}"; do
  wf_path="$WORKFLOW_DIR/$wf"
  if [ ! -f "$wf_path" ]; then
    fail "workflow $wf: file not found"
  else
    pass "workflow $wf: exists"
  fi
done

# ============================================================
# Results
# ============================================================

echo ""
echo "=== Results: $PASS passed, $FAIL failed ==="

if [ "$FAIL" -gt 0 ]; then
  exit 1
fi

exit 0
```

**Dependencies:**
- `/Users/bruno/siopv/.claude/settings.json` (validates its structure)
- `/Users/bruno/siopv/.claude/agents/*.md` (validates agent frontmatter)
- `/Users/bruno/siopv/.claude/workflow/briefing.md`
- `/Users/bruno/siopv/.claude/workflow/compaction-log.md`

**Deployment notes:**
- MERGE-006-A: NOT registered in settings.json under any hook event. Manual invocation only: `bash /Users/bruno/siopv/.claude/hooks/test-framework.sh`.
- Requires `jq` to be available in PATH.
- Run after initial `.claude/` setup is complete and after any structural changes to agents/ or workflow/.
- Exit code 1 on any failure, exit code 0 on all pass.

---

### pre-compact.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/pre-compact.sh`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (meta-project version hardcoded sec-llm-workbench paths)

**Action:**
Full script content for SIOPV (updates briefing.md timestamp and appends to compaction-log.md on PreCompact events):

```bash
#!/usr/bin/env bash
# pre-compact.sh — Runs before every Claude Code compaction event (SIOPV)
#
# Purpose:
#   1. Update the "Last updated" timestamp in briefing.md
#   2. Append a one-line entry to compaction-log.md
#
# Safety: Never blocks compaction — always exits 0.

trap 'exit 0' ERR

BRIEFING="/Users/bruno/siopv/.claude/workflow/briefing.md"
COMPACT_LOG="/Users/bruno/siopv/.claude/workflow/compaction-log.md"
TIMESTAMP="$(date -u +%Y-%m-%dT%H:%M:%SZ)"

if [[ ! -f "$BRIEFING" ]]; then
  echo "[pre-compact WARNING] briefing.md not found at $BRIEFING — skipping update" >&2
  exit 0
fi

if grep -q "^> Last updated:" "$BRIEFING"; then
  perl -i -pe "s|^> Last updated:.*|> Last updated: ${TIMESTAMP}|" "$BRIEFING"
else
  echo "[pre-compact WARNING] Could not find '> Last updated:' marker in briefing.md" >&2
fi

if [[ ! -f "$COMPACT_LOG" ]]; then
  cat > "$COMPACT_LOG" <<'EOF'
# Compaction Log

Entries added automatically by pre-compact.sh before each compaction event.

EOF
fi

echo "- ${TIMESTAMP} — PreCompact fired (manual/auto)" >> "$COMPACT_LOG"

exit 0
```

**Dependencies:**
- `/Users/bruno/siopv/.claude/workflow/briefing.md` — must contain `> Last updated:` marker line
- `/Users/bruno/siopv/.claude/workflow/compaction-log.md` — created on first run if absent

**Deployment notes:**
- Register in settings.json under `PreCompact` event (no matcher needed).
- Non-blocking — always exits 0 via `trap 'exit 0' ERR`.
- briefing.md MUST contain a line matching `^> Last updated:` or the timestamp update will be skipped with a warning.
- Uses `perl -i -pe` for macOS/Linux portable in-place edit.

---

### session-start.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/session-start.sh`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (meta-project version hardcoded sec-llm-workbench paths)

**Action:**
Full script content for SIOPV (prints briefing.md to stdout and last 5 compaction-log entries at session start):

```bash
#!/usr/bin/env bash
# session-start.sh — Runs on every Claude Code session start (SIOPV)
# Includes post-compaction resume sessions.
#
# Purpose:
#   1. Print the full contents of briefing.md to stdout so Claude reads it
#   2. Print the last 5 lines of compaction-log.md so Claude knows when the last compaction was
#
# Safety: Never blocks session start — always exits 0.

trap 'exit 0' ERR

BRIEFING="/Users/bruno/siopv/.claude/workflow/briefing.md"
COMPACT_LOG="/Users/bruno/siopv/.claude/workflow/compaction-log.md"

if [[ -f "$BRIEFING" ]]; then
  echo "========================================================"
  echo "  SIOPV SESSION START — Loading master briefing"
  echo "========================================================"
  cat "$BRIEFING"
  echo ""
else
  echo "[session-start WARNING] briefing.md not found at $BRIEFING" >&2
fi

if [[ -f "$COMPACT_LOG" ]]; then
  echo "========================================================"
  echo "  COMPACTION LOG — Last 5 entries"
  echo "========================================================"
  tail -n 5 "$COMPACT_LOG"
  echo ""
fi

exit 0
```

**Dependencies:**
- `/Users/bruno/siopv/.claude/workflow/briefing.md` — primary context injection, must exist for session continuity
- `/Users/bruno/siopv/.claude/workflow/compaction-log.md` — optional; silently skipped if absent

**Deployment notes:**
- Register in settings.json under `SessionStart` event (no matcher needed).
- Also register under `SessionStart` with matcher `"compact"` for post-compaction resume sessions (see settings.json entry).
- Non-blocking — always exits 0 via `trap 'exit 0' ERR`.
- briefing.md must exist before this hook fires or the session will receive no context injection.

---

### session-end.sh
**Target path:** `/Users/bruno/siopv/.claude/hooks/session-end.sh`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (meta-project version hardcoded sec-llm-workbench paths)

**Action:**
Full script content for SIOPV (updates briefing.md timestamp and appends to compaction-log.md on SessionEnd events):

```bash
#!/usr/bin/env bash
# session-end.sh — Runs on every Claude Code session end (SIOPV)
# Includes /exit sessions.
#
# Purpose:
#   1. Update the "Last updated" timestamp in briefing.md
#   2. Append a one-line entry to compaction-log.md
#
# Safety: Never blocks session end — always exits 0.

trap 'exit 0' ERR

BRIEFING="/Users/bruno/siopv/.claude/workflow/briefing.md"
COMPACT_LOG="/Users/bruno/siopv/.claude/workflow/compaction-log.md"
TIMESTAMP="$(date -u +%Y-%m-%dT%H:%M:%SZ)"

if [[ ! -f "$BRIEFING" ]]; then
  echo "[session-end WARNING] briefing.md not found at $BRIEFING — skipping update" >&2
  exit 0
fi

if grep -q "^> Last updated:" "$BRIEFING"; then
  perl -i -pe "s|^> Last updated:.*|> Last updated: ${TIMESTAMP}|" "$BRIEFING"
else
  echo "[session-end WARNING] Could not find '> Last updated:' marker in briefing.md" >&2
fi

if [[ ! -f "$COMPACT_LOG" ]]; then
  cat > "$COMPACT_LOG" <<'EOF'
# Compaction Log

Entries added automatically by pre-compact.sh and session-end.sh.

EOF
fi

echo "- ${TIMESTAMP} — SessionEnd fired (normal exit)" >> "$COMPACT_LOG"

exit 0
```

**Dependencies:**
- `/Users/bruno/siopv/.claude/workflow/briefing.md` — must contain `> Last updated:` marker line
- `/Users/bruno/siopv/.claude/workflow/compaction-log.md` — created on first run if absent

**Deployment notes:**
- Register in settings.json under `SessionEnd` event (no matcher needed).
- Non-blocking — always exits 0 via `trap 'exit 0' ERR`.
- briefing.md MUST contain a line matching `^> Last updated:` or the timestamp update will be skipped with a warning.
- Uses `perl -i -pe` for macOS/Linux portable in-place edit.
- Same timestamp-update pattern as pre-compact.sh; both scripts update briefing.md independently.

---

## Section B2: settings.json (from C1)

---

### settings.json
**Target path:** `/Users/bruno/siopv/.claude/settings.json`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/settings.json`

**Action:**
Copy source and apply the following changes:

**Path changes (3 absolute paths):**

1. SessionStart / matcher "compact" hook:
   - OLD: `"command": "/Users/bruno/sec-llm-workbench/.claude/hooks/session-start.sh"`
   - NEW: `"command": "/Users/bruno/siopv/.claude/hooks/session-start.sh"`

2. PreCompact hook:
   - OLD: `"command": "/Users/bruno/sec-llm-workbench/.claude/hooks/pre-compact.sh"`
   - NEW: `"command": "/Users/bruno/siopv/.claude/hooks/pre-compact.sh"`

3. SessionEnd hook:
   - OLD: `"command": "/Users/bruno/sec-llm-workbench/.claude/hooks/session-end.sh"`
   - NEW: `"command": "/Users/bruno/siopv/.claude/hooks/session-end.sh"`

**Tech stack string (post-compact re-injection hook):**
- OLD: `echo \"Tech Stack: Python 3.11+, uv, Pydantic v2, httpx, structlog, pathlib\"`
- NEW: `echo \"Tech Stack: Python 3.11+, uv, Pydantic v2, httpx, structlog, LangGraph, FastAPI, Presidio, OpenFGA, ChromaDB, XGBoost\"`

**MERGE-007-B: Remove UserPromptSubmit hook entirely.**
The `UserPromptSubmit` hook (detects project names by scanning `$CLAUDE_PROJECT_DIR/projects/$name.$ext`) is a multi-project disambiguation hook that serves no purpose in SIOPV (single-project repo without a `projects/` directory). Remove the entire `UserPromptSubmit` hook block from settings.json.

**Hooks using `$CLAUDE_PROJECT_DIR` (no change needed — dynamic path):**
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/session-start.sh` (SessionStart)
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-write.sh` (PreToolUse Write|Edit)
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-git-commit.sh` (PreToolUse Bash)
- `"$CLAUDE_PROJECT_DIR"/.claude/hooks/post-code.sh` (PostToolUse Write|Edit)

**Sections unchanged:** `sandbox`, `permissions`, `statusLine`, `env`, `attribution`, `cleanupPeriodDays`.

**Final hook registration table for SIOPV settings.json:**

| Event | Matcher | Handler | Type |
|-------|---------|---------|------|
| SessionStart | (none) | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/session-start.sh` | command |
| SessionStart | (none) | inline: daily checkpoint reminder | command |
| SessionStart | (none) | inline: MCP health check | command |
| SessionStart | compact | `/Users/bruno/siopv/.claude/hooks/session-start.sh` | command |
| SessionStart | compact | inline: post-compact re-injection (SIOPV tech stack) | command |
| PreToolUse | Write\|Edit | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-write.sh` | command |
| PreToolUse | Bash | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/pre-git-commit.sh` | command |
| PostToolUse | Write\|Edit | `"$CLAUDE_PROJECT_DIR"/.claude/hooks/post-code.sh` (timeout: 60) | command |
| PreCompact | (none) | inline: context preservation echo | command |
| PreCompact | (none) | `/Users/bruno/siopv/.claude/hooks/pre-compact.sh` | command |
| Stop | (none) | inline: pending verification warning | command |
| PostToolUseFailure | Write\|Edit | inline: JSONL error logging | command |
| SubagentStart | (none) | inline: JSONL event logging | command |
| SubagentStop | (none) | inline: JSONL event logging | command |
| SessionEnd | (none) | inline: JSONL session end + tmp cleanup | command |
| SessionEnd | (none) | `/Users/bruno/siopv/.claude/hooks/session-end.sh` | command |
| Notification | (none) | inline: JSONL notification logging | command |
| PermissionRequest | (none) | inline: JSONL permission logging | command |

**Dependencies:**
- All hooks in `/Users/bruno/siopv/.claude/hooks/` must exist before settings.json is activated
- `.build/current-phase`, `.build/checkpoints/pending/`, `.build/checkpoints/daily/`, `.build/logs/agents/`, `.build/logs/sessions/`, `.build/current-session-id` directories/files must exist in `/Users/bruno/siopv/`
- `.mcp.json` and `.env` (with `CONTEXT7_API_KEY`) must exist in `/Users/bruno/siopv/`

**Deployment notes:**
- All event names use camelCase (validated — no deprecated colon-separated format present).
- UserPromptSubmit hook removed per MERGE-007-B.
- Verify with `jq . /Users/bruno/siopv/.claude/settings.json` after write.
- Deploy after all hook scripts are written to `/Users/bruno/siopv/.claude/hooks/`.

---

## Section B3: Workflow Ops Files (from C1)

---

### 01-session-start.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/01-session-start.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/01-session-start.md`

**Action:** Copy unchanged.

**Dependencies:**
- References `projects/[proyecto].json` (generic pattern — SIOPV uses `projects/siopv.json` at project root)
- References `/new-project` skill (meta-project skill; inert in SIOPV unless explicitly triggered)
- `pyproject.toml` reference for NECESITA_SETUP state

**Deployment notes:**
- The "Crea nuevo proyecto" and multi-project listing triggers are meta-project features but are inert in SIOPV unless user explicitly types those phrases.
- `projects/siopv.json` must exist at `/Users/bruno/siopv/projects/siopv.json` for session-start detection to function correctly.
- File contains `<!-- version: 2026-02 -->` and `<!-- COMPACT-SAFE: ... -->` markers — carry over unchanged.

---

### 02-reflexion-loop.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/02-reflexion-loop.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/02-reflexion-loop.md`

**Action:** Copy unchanged.

**Dependencies:**
- `.claude/scripts/orchestrate-parallel-verification.py` — must exist in siopv
- `.claude/docs/verification-thresholds.md` — must exist in siopv
- `.claude/docs/model-selection-strategy.md` — must exist in siopv
- `.build/checkpoints/pending/`, `.build/logs/agents/` — directories must exist
- `.ignorar/production-reports/` — directory must exist
- `/verify` skill (`.claude/skills/verify.md` or equivalent)

**Deployment notes:**
- All directory references are relative — no absolute paths requiring substitution.
- Wave 1 (~7 min) + Wave 2 (~5 min) = ~12 min total verification cycle defined here.

---

### 03-human-checkpoints.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/03-human-checkpoints.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/03-human-checkpoints.md`

**Action:** Copy unchanged.

**Dependencies:** None (pure policy document, no file references).

**Deployment notes:**
- Contains COMPACT-SAFE annotation with canonical checkpoint policy summary — carry over unchanged.
- No meta-project-specific content; applies generically to SIOPV sessions.

---

### 05-before-commit.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/05-before-commit.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/05-before-commit.md`

**Action:** Copy unchanged.

**Dependencies:**
- `.claude/docs/verification-thresholds.md` — must exist in siopv
- `/verify` skill
- `pre-git-commit.sh` hook at `$CLAUDE_PROJECT_DIR/.claude/hooks/pre-git-commit.sh`

**Deployment notes:**
- All references are relative or generic (ruff, mypy, pytest, `.build/checkpoints/pending/`).
- Three PASS thresholds defined: best-practices-enforcer (0 violations), security-auditor (0 CRITICAL/HIGH), hallucination-detector (0 hallucinations).

---

## Section B4: Workflow Context Files (from C2)

---

### 04-agents.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/04-agents.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/04-agents.md`

**Action:**
Single required change — replace all `[TARGET_PROJECT]` placeholder occurrences with `/Users/bruno/siopv`.

Affected example prompts (one replacement per agent):
- async-safety-auditor example prompt
- semantic-correctness-auditor example prompt
- smoke-test-runner example prompt
- config-validator example prompt
- regression-guard example prompt
- dependency-scanner example prompt
- circular-import-detector example prompt
- import-resolver example prompt

All other content (agent definitions, wave structure, 14 verification agents, hybrid model strategy, report conventions) carries over unchanged. All other paths in the file already reference SIOPV paths (`projects/siopv.json`, `~/siopv/src/siopv/`) — no additional substitution needed.

**Obsolete content (keep but label):**
- The `⚠️ EXPERIMENTAL` hybrid-verification script block (references `.ignorar/experimental-scripts/`) — the directory does not exist in SIOPV. Keep as reference only; do not execute.
- Phase 4 references in Hybrid Model Strategy section describe meta-project research phases — label as historical context, no deletion required.

**Dependencies:**
- `.claude/docs/python-standards.md` — must exist in siopv/.claude/docs/
- `.claude/rules/tech-stack.md` — must exist in siopv/.claude/rules/
- `.claude/docs/verification-thresholds.md`
- `.claude/docs/model-selection-strategy.md`
- `.claude/workflow/02-reflexion-loop.md`
- `.build/active-project` — must exist in siopv/.build/ containing the SIOPV project path

**Deployment notes:**
- The 8 `[TARGET_PROJECT]` replacements are the only required changes — all are in example invocation blocks.
- `.build/active-project` must contain `/Users/bruno/siopv` for Wave 3 agents (smoke-test-runner, config-validator, dependency-scanner, circular-import-detector, regression-guard) to function correctly.

---

### 06-decisions.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/06-decisions.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/06-decisions.md`

**Action:** Copy unchanged.

**Dependencies:**
- `.claude/docs/python-standards.md`
- `.claude/rules/tech-stack.md`
- `.claude/docs/model-selection-strategy.md`

**Deployment notes:**
- All auto-decision rules (dependency management, type hints, Pydantic v2, httpx, structlog, pathlib, model routing) are fully generic and apply to SIOPV.

---

### 07-orchestrator-invocation.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/07-orchestrator-invocation.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/07-orchestrator-invocation.md`

**Action:** Copy unchanged.

**Dependencies:**
- `.claude/workflow/02-reflexion-loop.md`
- `projects/siopv.json`
- `CONTEXT7_API_KEY` env var (referenced in MCP troubleshooting table)

**Deployment notes:**
- All code examples already reference SIOPV paths (`~/siopv/src/siopv/`, `projects/siopv.json`, Phase 5 OpenFGA) — no substitutions needed.

---

### briefing.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/briefing.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md`

**Action:**
Copy source and apply the following explicit changes. Each OLD → NEW pair is an exact in-place replacement.

**Section 2 — Infrastructure Status block:**
- OLD: `"**Claude Code config:** Layer 2 preparation — **STAGE-1 ✅ Complete | STAGE-2 ✅ Complete | STAGE-3 ✅ Complete | STAGE-3.5 ✅ Complete | STAGE-4.1 PENDING | STAGE-4.2 PENDING**"`
- NEW: `"**Claude Code config:** \`siopv/.claude/\` — configured by STAGE-4.2 ✅ Complete"`

- OLD: `"**State file:** \`/Users/bruno/sec-llm-workbench/projects/siopv.json\`"`
- NEW: `"**State file:** \`/Users/bruno/siopv/projects/siopv.json\`"`

**Section 3 — Full Plan block:**
- OLD: Entire sentence "All stages run from `/Users/bruno/sec-llm-workbench/` until the TRANSITION POINT."
- NEW: `"All work runs from \`/Users/bruno/siopv/\`."`

- OLD: STAGE-1, STAGE-2, STAGE-3, STAGE-3.5, STAGE-4.1, STAGE-4.2 subsections (entire audit stage plan block)
- NEW: Replace with:
  ```
  ### Pre-Work Complete
  STAGE-1 through STAGE-4.2 completed in meta-project (`/Users/bruno/sec-llm-workbench/`). Audit reports at `/Users/bruno/siopv/AuditPrePhase7-11mar26/`.
  ```

- OLD: Entire `⚠️ CRITICAL TRANSITION POINT` block (including "DO NOT run..." warning)
- NEW: Remove entirely. (Transition has already occurred; this is the siopv session.)

- OLD: `"### REMEDIATION-HARDENING (runs from \`/Users/bruno/siopv/\`)"`
- NEW: `"### REMEDIATION-HARDENING"`

- OLD: `"### Phase 7: Human-in-the-Loop — Streamlit (runs from \`/Users/bruno/siopv/\`)"`
- NEW: `"### Phase 7: Human-in-the-Loop — Streamlit"`

- OLD: `"### Phase 8: Output — Jira + PDF (runs from \`/Users/bruno/siopv/\`)"`
- NEW: `"### Phase 8: Output — Jira + PDF"`

**Section 7 — Compaction Protocol block:**
- OLD: `"**How it works:**\n- \`PreCompact\` hook (\`/Users/bruno/sec-llm-workbench/.claude/hooks/pre-compact.sh\`) updates..."`
- NEW: `"**How it works:**\n- \`PreCompact\` hook (\`/Users/bruno/siopv/.claude/hooks/pre-compact.sh\`) updates..."`

- OLD: `"- \`SessionEnd\` hook (\`/Users/bruno/sec-llm-workbench/.claude/hooks/session-end.sh\`) updates..."`
- NEW: `"- \`SessionEnd\` hook (\`/Users/bruno/siopv/.claude/hooks/session-end.sh\`) updates..."`

- OLD: `"- \`SessionStart\` hook (\`/Users/bruno/sec-llm-workbench/.claude/hooks/session-start.sh\`) prints..."`
- NEW: `"- \`SessionStart\` hook (\`/Users/bruno/siopv/.claude/hooks/session-start.sh\`) prints..."`

- OLD: `"Check \`/Users/bruno/sec-llm-workbench/.claude/workflow/compaction-log.md\` for when the last compaction occurred"`
- NEW: `"Check \`/Users/bruno/siopv/.claude/workflow/compaction-log.md\` for when the last compaction occurred"`

- OLD: `"The PreCompact and SessionStart hooks are registered in \`/Users/bruno/sec-llm-workbench/.claude/settings.json\`..."`
- NEW: `"The PreCompact and SessionStart hooks are registered in \`/Users/bruno/siopv/.claude/settings.json\`..."`

- OLD: `"Hook scripts location: \`/Users/bruno/sec-llm-workbench/.claude/hooks/\`"`
- NEW: `"Hook scripts location: \`/Users/bruno/siopv/.claude/hooks/\`"`

- OLD: `"Briefing file location: \`/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md\`"`
- NEW: `"Briefing file location: \`/Users/bruno/siopv/.claude/workflow/briefing.md\`"`

- OLD: `"Compaction log location: \`/Users/bruno/sec-llm-workbench/.claude/workflow/compaction-log.md\`"`
- NEW: `"Compaction log location: \`/Users/bruno/siopv/.claude/workflow/compaction-log.md\`"`

**Section 8 — Reference Files table:**
- OLD: `"| **Spec findings (Phase 7/8 detail, thesis gates, KPIs)** | \`/Users/bruno/sec-llm-workbench/.claude/workflow/spec-findings.md\` |"`
- NEW: `"| **Spec findings (Phase 7/8 detail, thesis gates, KPIs)** | \`/Users/bruno/siopv/.claude/workflow/spec-findings.md\` |"`

- OLD: Row `"| Full audit plan & extracted conversation | \`/Users/bruno/sec-llm-workbench/Claude-JSONL-conversaition-full-extraction/siopv-compaction-proof-master-briefing-phases0-8.md\` |"`
- NEW: Replace with: `"| Stage 4.1 truth documents | \`/Users/bruno/siopv/AuditPrePhase7-11mar26/stage4.1-research/\` |"`

**Total changes: 15 in-place replacements / removals.**

**Dependencies:**
- `/Users/bruno/siopv/.claude/hooks/pre-compact.sh`
- `/Users/bruno/siopv/.claude/hooks/session-end.sh`
- `/Users/bruno/siopv/.claude/hooks/session-start.sh`
- `/Users/bruno/siopv/.claude/workflow/compaction-log.md`
- `/Users/bruno/siopv/.claude/settings.json`
- `/Users/bruno/siopv/.claude/workflow/spec-findings.md`
- `/Users/bruno/siopv/projects/siopv.json`

**Deployment notes:**
- briefing.md MUST contain `> Last updated:` marker line for pre-compact.sh and session-end.sh to update the timestamp.
- This file is injected into context at every session start by session-start.sh — keep it concise (target < 200 lines after edits).
- SIOPV audit-stage descriptions (STAGE-1 through STAGE-4.2 plan) must be removed to prevent confusion in SIOPV sessions.

---

### spec-findings.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/spec-findings.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/spec-findings.md`

**Action:**
Copy source and apply one path change (MERGE-008-A):

- OLD: `"> Extracted: 2026-03-10 | Source: \`/Users/bruno/sec-llm-workbench/docs/SIOPV_Propuesta_Tecnica_v2.txt\`"`
- NEW: `"> Extracted: 2026-03-10 | Source: \`/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt\`"`

All other content carries over unchanged — the entire file is SIOPV-specific (Phase 7/8 requirements, Tríada de Evidencia, Dashboard polling via SQLite, Timeout Escalation Cascade, Jira ticket schema, PDF requirements, LangSmith audit trail, external integration details, architectural constraints, thesis KPIs).

**Dependencies:**
- `/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt` — source spec file (already exists at SIOPV path per MERGE-008-A)

**Deployment notes:**
- MERGE-008-A: Use `/Users/bruno/siopv/docs/` path (not meta-project path).
- One-line change in frontmatter only.

---

## Section B5: Workflow Recovery + Docs Batch 1 (from C3)

---

### orchestrator-briefing.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/orchestrator-briefing.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/orchestrator-briefing.md`

**Action:**
Copy source and apply the following changes. MERGE-008-A applied for SIOPV spec path.

**Title (line 2):**
- OLD: `# Orchestrator Briefing — SIOPV Meta-Project`
- NEW: `# Orchestrator Briefing — SIOPV Project`

**Subtitle (line 3):**
- OLD: `# Stages 1, 2, 3, 3.5, 4.1, 4.2 + Remediation-Hardening`
- NEW: Remove this line entirely.

**READING ORDER (lines 12–14):**
- OLD: `1. \`/Users/bruno/sec-llm-workbench/.claude/workflow/briefing.md\` — project context`
- NEW: `1. \`/Users/bruno/siopv/.claude/workflow/briefing.md\` — project context`

- OLD: `2. \`/Users/bruno/sec-llm-workbench/.claude/workflow/spec-findings.md\` — spec detail`
- NEW: `2. \`/Users/bruno/siopv/.claude/workflow/spec-findings.md\` — spec detail`

- OLD: `3. \`/Users/bruno/sec-llm-workbench/.claude/workflow/orchestrator-briefing.md\` — this file, your behavioral rules`
- NEW: `3. \`/Users/bruno/siopv/.claude/workflow/orchestrator-briefing.md\` — this file, your behavioral rules`

**Scope rule block (lines ~40–41):**
- OLD: `### Scope is always Phases 0–6 only` (entire scope block including expected-missing explanation for Phase 7/8 dependencies)
- NEW: Remove entire section. (Audit-context rule; scope in SIOPV is defined per-task in the orchestrator's assigned briefing.)

**Report path (line ~117):**
- OLD: `All reports saved to: \`/Users/bruno/siopv/AuditPrePhase7-11mar26/stage{N}/\``
- NEW: `All reports saved to: \`/Users/bruno/siopv/.ignorar/production-reports/{agent}/phase-{N}/\``

**Round aggregator report path (lines ~125–126):**
- OLD: `saved to: \`/Users/bruno/siopv/AuditPrePhase7-11mar26/stage{N}/{TIMESTAMP}-stage{N}-round{R}-aggregated.md\``
- NEW: `saved to: \`/Users/bruno/siopv/.ignorar/production-reports/aggregators/{TIMESTAMP}-phase{N}-round{R}-aggregated.md\``

**Final aggregator report path (lines ~135–136):**
- OLD: `saved to: \`/Users/bruno/siopv/AuditPrePhase7-11mar26/stage{N}/{TIMESTAMP}-stage{N}-final-report.md\``
- NEW: `saved to: \`/Users/bruno/siopv/.ignorar/production-reports/aggregators/{TIMESTAMP}-phase{N}-final-report.md\``

**CANONICAL PATHS TABLE (lines ~159–171) — replace entire table:**

| Path name | Value |
|-----------|-------|
| Briefing | `/Users/bruno/siopv/.claude/workflow/briefing.md` |
| Spec findings | `/Users/bruno/siopv/.claude/workflow/spec-findings.md` |
| Orchestrator briefing | `/Users/bruno/siopv/.claude/workflow/orchestrator-briefing.md` |
| SIOPV spec | `/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt` (MERGE-008-A) |
| SIOPV source code | `/Users/bruno/siopv/src/` |
| Production reports | `/Users/bruno/siopv/.ignorar/production-reports/` |
| Compaction log | `/Users/bruno/siopv/.claude/workflow/compaction-log.md` |
| Hook: pre-compact | `/Users/bruno/siopv/.claude/hooks/pre-compact.sh` |
| Hook: session-start | `/Users/bruno/siopv/.claude/hooks/session-start.sh` |

Remove rows for: setup-checklist.md (META-ONLY), meta-project settings.json.

**REMINDER TO HUMAN section (lines ~176–177):**
- OLD: Entire "REMINDER TO HUMAN" section (one-time setup instruction for meta-project migration)
- NEW: Remove entirely.

**Dependencies:**
- `briefing.md` (reading order, canonical paths)
- `spec-findings.md` (reading order)
- `compaction-log.md` (canonical paths)
- `pre-compact.sh`, `session-start.sh` (canonical paths)

**Deployment notes:**
- MERGE-008-A applied: SIOPV spec path uses `/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt`.
- All behavioral rules (TeamCreate, SendMessage, checkpoints, context safety, agent size rule, round-based execution) carry over unchanged.

---

### setup-checklist.md
**Target path:** N/A
**Classification:** DISCARD
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/workflow/setup-checklist.md`

**Action:** Do not include. This file tracked the one-time migration of SIOPV's `.claude/` to the meta-project. All 11 checklist items are completed `[x]`. The process is done and irreversible. No applicable recovery steps remain for SIOPV.

**Dependencies:** None.

**Deployment notes:** N/A.

---

### compaction-log.md
**Target path:** `/Users/bruno/siopv/.claude/workflow/compaction-log.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (source file contains meta-project session events only)

**Action:**
Create as an empty log file with this exact content — no entries, no dates:

```
# Compaction Log

Entries added automatically by pre-compact.sh before each compaction event.
```

The hook scripts (pre-compact.sh, session-end.sh, session-start.sh) will write new entries automatically once configured to `/Users/bruno/siopv/.claude/workflow/compaction-log.md`.

**Dependencies:**
- `pre-compact.sh` (appends entries via `COMPACT_LOG` variable)
- `session-end.sh` (appends entries via `COMPACT_LOG` variable)
- `session-start.sh` (reads this file via `COMPACT_LOG` variable)
- `orchestrator-briefing.md` (canonical paths table references this file)

**Deployment notes:**
- Start with zero entries — do not copy meta-project timestamps.
- The file header line format must match exactly what pre-compact.sh creates on first run (both use same header).

---

### traceability.md
**Target path:** `/Users/bruno/siopv/.claude/docs/traceability.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/traceability.md`

**Action:** Copy unchanged.

**Dependencies:**
- `session-start.sh`, `post-code.sh`, `pre-git-commit.sh` (hooks that write logs — referenced by documentation)
- CLAUDE.md must register `/show-trace` and `/generate-report` as skills in SIOPV's CLAUDE.md

**Deployment notes:**
- All directory references are relative (`.build/logs/agents/`, `.build/logs/sessions/`).
- `project_dir` field in session summaries is populated dynamically at runtime.
- No meta-project absolute paths.

---

### errors-to-rules.md
**Target path:** `/Users/bruno/siopv/.claude/docs/errors-to-rules.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/errors-to-rules.md`

**Action:**
Copy source and apply the following changes:

**Header line:**
- OLD: `Registro condensado de errores y reglas **de este proyecto** (sec-llm-workbench).`
- NEW: `Registro condensado de errores y reglas **de este proyecto** (siopv).`

**Rule count line:**
- OLD: `## Reglas Activas (18 errores → 15 reglas unicas)`
- NEW: `## Reglas Activas (16 errores → 13 reglas unicas)`

**Rule table — remove two rows:**
- Remove: `| 4 | META-PROYECTO separado del PROYECTO. Nunca .claude/ ni memory-bank/ en proyectos destino | 2026-01-21 |`
- Remove: `| 14 | Al hablar de configuracion, SIEMPRE aclarar scope: ~/.claude/ vs proyecto/.claude/ vs META/.claude/ | 2026-01-26 |`
- Renumber remaining rules: old 5→4, old 6→5, ..., old 13→12, old 15→13, old 16→14, old 17→15, old 18→16.

**Rules to KEEP (16 total after removal and renumbering):**
1. SIEMPRE uv (nunca pip/venv)
2. Agentes EJECUTAN, no solo documentan
3. No pedir confirmacion en tareas estandar
4. (was 5) Verificar manualmente README.md, CLAUDE.md, .env.example post-audit
5. (was 6) SIEMPRE consultar Context7 antes de escribir config o codigo
6. (was 7) Ejecutar agentes primero cuando algo falla
7. (was 8) Flujo COMPLETO del orchestrator antes de CADA commit
8. (was 9) TODO modulo Python con logica necesita tests
9. (was 10) Documentar errores INMEDIATAMENTE
10. (was 11) NUNCA afirmar "X recomienda Y" sin cita exacta
11. (was 12) Para afirmar "mas popular": necesitas 20+ fuentes
12. (was 13) Verificar filesystem antes de afirmar sobre archivos
13. (was 15) Cuando falles en seguir docs existentes: admitirlo
14. (was 16) Agentes DEBEN guardar sus reportes en `.ignorar/production-reports/{agent}/phase-{N}/`
15. (was 17) NUNCA decidir PASS/FAIL arbitrariamente
16. (was 18) Antes de afirmar "entiendo el workflow", VERIFICAR

**Sections unchanged:**
- "Errores Recientes" section (3 entries from 2026-02-04) — keep all
- "Patron Raiz: PILOTO AUTOMATICO" section — keep as-is
- "Patrones a Evitar" section — keep as-is

**Dependencies:**
- CLAUDE.md must reference this file with `@.claude/docs/errors-to-rules.md` for it to be auto-loaded.

**Deployment notes:**
- Rule 4 (META-PROYECTO separation) removed — not applicable in single-project context.
- Rule 14 (three-way scope distinction) removed — no META layer exists in SIOPV.
- "Errores Recientes" entries (agent reporting failures, PASS/FAIL arbitrariness) are applicable to SIOPV — retain all 3.

---

## Section B6: Docs Batch 2 (from C4a)

---

### python-standards.md
**Target path:** `/Users/bruno/siopv/.claude/docs/python-standards.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/python-standards.md`

**Action:** Copy unchanged.

**Dependencies:**
- Referenced by `techniques.md` line 124 (same relative path — no change needed).

**Deployment notes:**
- All content (type hints, Pydantic v2, httpx, structlog, pathlib, error handling, async patterns, testing) is generic Python 2026 standards applicable to SIOPV's Python 3.11+ stack.
- Code snippet imports are illustrative only — not actual project paths.

---

### techniques.md
**Target path:** `/Users/bruno/siopv/.claude/docs/techniques.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/techniques.md`

**Action:**
Copy source and apply two changes:

1. Remove entire Technique 8 block:
   - OLD (lines 88–98):
     ```
     ## 8. Separación META vs PROYECTO

     **Reemplaza:** Configuración dentro del proyecto

     META-PROYECTO (framework)     →    PROYECTO (código limpio)
     <meta-proyecto>/              →    ~/<proyecto>/

     El proyecto generado es exportable sin el META-PROYECTO.
     ```
   - NEW: Remove entirely.

2. Update visual summary row:
   - OLD: `│  Estructura:          META separado de PROYECTO              │`
   - NEW: `│  Estructura:          Hexagonal Architecture                 │`

All other techniques (1–7, 9–13) carry over unchanged.

**Dependencies:**
- References `python-standards.md` (same relative path — no change needed).

**Deployment notes:**
- Technique 8 "Separación META vs PROYECTO" is the meta-framework's own structural concept — not applicable to SIOPV itself.
- After removing Technique 8, renumber subsequent techniques if numbered sequentially.

---

### mcp-setup.md
**Target path:** `/Users/bruno/siopv/.claude/docs/mcp-setup.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/mcp-setup.md`

**Action:** Copy unchanged.

**Dependencies:** None.

**Deployment notes:**
- Covers generic Context7 MCP setup (API key, `.mcp.json` structure, troubleshooting, agent usage).
- `.mcp.json` and `.mcp.json.example` convention applies to SIOPV project root.

---

### model-selection-strategy.md
**Target path:** `/Users/bruno/siopv/.claude/docs/model-selection-strategy.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/model-selection-strategy.md`

**Action:**
Copy source and apply the following removals:

1. Remove entire section `## Concrete Examples from Master Plan` (lines 289–384):
   All 12 examples reference meta-project internal tasks (audit remediation plan items). Not applicable to SIOPV.

2. Remove subsection `### Validated Savings (from Master Plan)` including the phase table:
   ```
   - Phase 1: $0.41 total (83% Haiku usage)
   - Phase 2: $2.16 total (80% Sonnet usage)
   - Phase 3: $4.68 total (17% Opus for parallel execution)
   - Phase 4: $2.96 total (0% Opus, avoided where possible)
   - Phase 5: $2.63 total (0% Opus for validation work)

   **Total for 32 tasks:** $12.84 (vs. ~$50 if all Opus)
   **Actual savings:** 74% reduction
   ```
   References meta-project's own 32-task improvement plan.

3. Remove line: `- **Master Remediation Plan:** \`.ignorar/2026-02-08-claude-config-sota-audit-and-remediation-plan--delete-after-successful-implementation/team4-master-plan/MASTER-REMEDIATION-PLAN.md\` (Lines 759-811)`
   References meta-project internal remediation artifact.

All other content (model specs, decision tree Sections A–F, cost comparison table, override guidelines, cost monitoring, monitoring & adjustment) carries over unchanged.

**Dependencies:**
- References `agent-tool-schemas.md`, `workflow/04-agents.md`, `workflow/02-reflexion-loop.md`, `CLAUDE.local.md` — all same relative paths in SIOPV.

**Deployment notes:**
- "Concrete Examples from Master Plan" and "Validated Savings" are the only meta-project-specific sections.
- Decision tree and model routing rules are fully applicable to SIOPV orchestration.

---

### agent-tool-schemas.md
**Target path:** `/Users/bruno/siopv/.claude/docs/agent-tool-schemas.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/agent-tool-schemas.md`

**Action:**
Copy source and apply the following path substitutions in JSON example blocks only:

| Line | OLD | NEW |
|------|-----|-----|
| 135 | `"file_path": "/Users/bruno/sec-llm-workbench/src/api/client.py"` | `"file_path": "/Users/bruno/siopv/src/siopv/adapters/http/client.py"` |
| 145 | `"file_path": "/Users/bruno/sec-llm-workbench/src/models/user.py"` | `"file_path": "/Users/bruno/siopv/src/siopv/domain/entities/vulnerability.py"` |
| 157 | `"file_path": "/Users/bruno/sec-llm-workbench/pyproject.toml"` | `"file_path": "/Users/bruno/siopv/pyproject.toml"` |
| 224 | `"path": "/Users/bruno/sec-llm-workbench"` | `"path": "/Users/bruno/siopv"` |
| 292 | `"path": "/Users/bruno/sec-llm-workbench/src"` | `"path": "/Users/bruno/siopv/src"` |
| 313 | `"path": "/Users/bruno/sec-llm-workbench/src"` | `"path": "/Users/bruno/siopv/src"` |
| 612 | `"file": "/Users/bruno/sec-llm-workbench/src/models/user.py"` | `"file": "/Users/bruno/siopv/src/siopv/domain/entities/vulnerability.py"` |
| 625 | `"file": "/Users/bruno/sec-llm-workbench/src/api/client.py"` | `"file": "/Users/bruno/siopv/src/siopv/adapters/http/client.py"` |
| 653 | `"file": "/Users/bruno/sec-llm-workbench/src/config/credentials.py"` | `"file": "/Users/bruno/siopv/src/siopv/infrastructure/config/settings.py"` |
| 699 | `"file_path": "src/models/user.py"` | `"file_path": "src/siopv/domain/entities/vulnerability.py"` |
| 840 | `"file_path": "src/handlers/complex_handler.py"` | `"file_path": "src/siopv/application/orchestration/graph.py"` |
| 879 | `"file_path": "src/validators/input_validator.py"` | `"file_path": "src/siopv/domain/validators/input_validator.py"` |

**Line 944 — UNCLEAR item resolved:** Remove the line referencing `.ignorar/production-reports/phase3/2026-02-08-phase3-task34-routing-savings-validation.md`. This is a meta-project internal report path that has no counterpart in SIOPV. Since the path is illustrative only and the file does not exist in SIOPV, remove it rather than substituting a non-existent SIOPV path.

Line 56 (`"command": "uv run ruff check src/ --config pyproject.toml"`) — keep unchanged (generic command).

All other content (tool schemas, agent-specific tool usage, token impact analysis, Context7 fallback, health check, observability) carries over unchanged.

**Dependencies:**
- Assumes Context7 MCP setup from `mcp-setup.md`
- References `workflow/04-agents.md`
- References `.claude/skills/verify/SKILL.md` (must also be ported — out of scope for this section)

**Deployment notes:**
- 12 path substitutions + 1 line removal = 13 total changes.
- All changes are in JSON example blocks only — purely illustrative, but should reflect SIOPV structure for agent accuracy.

---

## Section B7: New File (MERGE-009-A)

---

### init-session/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file

**Action:**
MERGE-009-A: Create the `/init-session` skill for SIOPV. This skill instructs Claude to read briefing.md, spec-findings.md, and errors-to-rules.md, then create the checkpoint marker files under `.build/checkpoints/daily/`.

Full skill content:

```markdown
# /init-session — SIOPV Session Initialization

## Purpose

Loads SIOPV context at the start of each work session and creates daily checkpoint markers
so the pre-write.sh hook does not block Write/Edit operations for the rest of the day.

## Steps

When invoked, execute the following in order:

### 1. Read context documents

Read these three files in full:

1. `/Users/bruno/siopv/.claude/workflow/briefing.md` — master project briefing
2. `/Users/bruno/siopv/.claude/workflow/spec-findings.md` — Phase 7/8 spec detail, thesis gates, KPIs
3. `/Users/bruno/siopv/.claude/docs/errors-to-rules.md` — project-specific error rules

### 2. Create daily checkpoint markers

Run the following shell commands to create checkpoint marker files for today's date:

```bash
TODAY=$(date +%Y%m%d)
CHECKPOINT_DIR="/Users/bruno/siopv/.build/checkpoints/daily"
mkdir -p "$CHECKPOINT_DIR"
touch "${CHECKPOINT_DIR}/briefing-read-${TODAY}"
touch "${CHECKPOINT_DIR}/spec-read-${TODAY}"
touch "${CHECKPOINT_DIR}/errors-read-${TODAY}"
echo "Checkpoints created for ${TODAY}: briefing-read, spec-read, errors-read"
```

### 3. Confirm

After creating checkpoints, confirm to the user:
- Which files were read
- That checkpoint markers were created
- Current date
- Brief summary of where the project stands (from briefing.md)

## Notes

- This skill must be run once per calendar day before doing any Write/Edit operations.
- The pre-write.sh hook checks for these checkpoint files and will prompt the user if any are missing.
- Checkpoint files are date-stamped — they expire at midnight and must be recreated each day.
- The `.build/checkpoints/daily/` directory is gitignored.
```

**Dependencies:**
- `/Users/bruno/siopv/.claude/workflow/briefing.md`
- `/Users/bruno/siopv/.claude/workflow/spec-findings.md`
- `/Users/bruno/siopv/.claude/docs/errors-to-rules.md`
- `/Users/bruno/siopv/.build/checkpoints/daily/` directory (created by the skill itself)
- `.claude/hooks/pre-write.sh` (uses these checkpoint files to gate Write/Edit)

**Deployment notes:**
- This skill must be deployed BEFORE or TOGETHER WITH pre-write.sh — if pre-write.sh is active without this skill, every Write/Edit will be blocked until the user finds another way to create the checkpoints.
- Register in SIOPV's CLAUDE.md with an @import or skills reference so Claude knows it exists.
- The `skills/init-session/` directory must be created (Claude Code auto-discovers skills in `.claude/skills/`).

---

## Complete File Count Summary

| # | File | Classification | Section |
|---|------|---------------|---------|
| 1 | hooks/pre-write.sh | CREATE FROM SCRATCH | B1 |
| 2 | hooks/test-framework.sh | CREATE FROM SCRATCH (manual, MERGE-006-A) | B1 |
| 3 | hooks/pre-compact.sh | CREATE FROM SCRATCH | B1 |
| 4 | hooks/session-start.sh | CREATE FROM SCRATCH | B1 |
| 5 | hooks/session-end.sh | CREATE FROM SCRATCH | B1 |
| 6 | settings.json | ADAPT (MERGE-007-B: UserPromptSubmit removed) | B2 |
| 7 | workflow/01-session-start.md | COPY AS-IS | B3 |
| 8 | workflow/02-reflexion-loop.md | COPY AS-IS | B3 |
| 9 | workflow/03-human-checkpoints.md | COPY AS-IS | B3 |
| 10 | workflow/05-before-commit.md | COPY AS-IS | B3 |
| 11 | workflow/04-agents.md | ADAPT | B4 |
| 12 | workflow/06-decisions.md | COPY AS-IS | B4 |
| 13 | workflow/07-orchestrator-invocation.md | COPY AS-IS | B4 |
| 14 | workflow/briefing.md | ADAPT (15 changes) | B4 |
| 15 | workflow/spec-findings.md | ADAPT (MERGE-008-A) | B4 |
| 16 | workflow/orchestrator-briefing.md | ADAPT (MERGE-008-A) | B5 |
| 17 | workflow/setup-checklist.md | DISCARD (META-ONLY) | B5 |
| 18 | workflow/compaction-log.md | CREATE FROM SCRATCH | B5 |
| 19 | docs/traceability.md | COPY AS-IS | B5 |
| 20 | docs/errors-to-rules.md | ADAPT (remove rules 4+14) | B5 |
| 21 | docs/python-standards.md | COPY AS-IS | B6 |
| 22 | docs/techniques.md | ADAPT | B6 |
| 23 | docs/mcp-setup.md | COPY AS-IS | B6 |
| 24 | docs/model-selection-strategy.md | ADAPT | B6 |
| 25 | docs/agent-tool-schemas.md | ADAPT | B6 |
| 26 | skills/init-session/SKILL.md | CREATE FROM SCRATCH (MERGE-009-A) | B7 |

**Total: 26 file entries**

By classification:
- COPY AS-IS: 9
- ADAPT: 10
- CREATE FROM SCRATCH: 6
- DISCARD: 1

---

## Section C: Docs/Rules + CLAUDE.md + Memory + Skills

> Source file: `2026-03-12-22.36.22-stage4.1-truth-doc-section-c.md`
> Files covered: C4b (docs/rules), D1 (CLAUDE.md + MEMORY.md + 2 skills), D2–D4 (skills)
> Total entries: 24

# Stage 4.1 Truth Document — Section C: Docs/Rules + CLAUDE.md + Memory + Skills
> Files covered: C4b (docs/rules), D1 (CLAUDE.md + MEMORY.md + 2 skills), D2–D4 (skills)
> MERGE decisions applied: MERGE-009-A, MERGE-010-B, MERGE-011-A, MERGE-012-A, MERGE-013/016/017/018 (auto)
> Status: COMPLETE
> Total file entries: 24
> Compiled by: final-aggregator-c
> Timestamp: 2026-03-12-22.36.22

---

## Section C1 — Docs and Rules Files (from C4b)

---

### verification-thresholds.md
**Target path:** `/Users/bruno/siopv/.claude/docs/verification-thresholds.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/verification-thresholds.md`

**Action:**
Apply these line-level changes (all other lines copy unchanged):

```
CHANGE 1 — import reference (pipeline import check):
OLD: Pipeline imports cleanly ('uv run python -c "import [project]"')
NEW: Pipeline imports cleanly ('uv run python -c "import siopv"')

CHANGE 2 — smoke test CVE ID (MERGE-010-B):
OLD: Pipeline runs start-to-end with CVE-2024-1234 without unhandled exception
NEW: Pipeline runs start-to-end with {{ smoke_test_cve_id }} without unhandled exception
(Note: {{ smoke_test_cve_id }} is a Cookiecutter-style placeholder — Stage 4.2 must substitute with SIOPV's actual smoke-test CVE ID before using.)

CHANGE 3 — last verified date:
OLD: Last verified: 2026-02-08
NEW: Last verified: 2026-03-12
```

No other path or content changes needed — relative paths are already correct for SIOPV; `.ignorar/` convention matches SIOPV; `.build/checkpoints/pending/` referenced in the file also applies to SIOPV.

**Dependencies:**
- `.claude/workflow/05-before-commit.md`
- `.claude/workflow/04-agents.md`
- `.claude/hooks/pre-git-commit.sh`
- `.claude/scripts/check-module-coverage.py`
- `.claude/scripts/check-reviewer-score.sh`

**Deployment notes:**
- MERGE-016 (auto): Before using this file in SIOPV, run: `mkdir -p /Users/bruno/siopv/.build/checkpoints/pending/` as a Stage 4.2 pre-flight step. The file references this directory and the pre-commit hook writes to it — the directory must exist before first commit.
- The `{{ smoke_test_cve_id }}` placeholder must be replaced with SIOPV's actual test CVE ID. If SIOPV has no designated smoke-test CVE ID yet, this decision should be made before Phase 7 testing begins.
- UNCLEAR resolved by MERGE-010-B: CVE-2024-1234 → `{{ smoke_test_cve_id }}` (not a real CVE, placeholder).

---

### universal-project-automation-template.md
**Target path:** `/Users/bruno/siopv/.claude/docs/universal-project-automation-template.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/docs/2026-03-12-11.27.33-universal-project-automation-template-teamcreate-orchestrator-agents-rounds-checkpoints-shutdown-file-updates.md`

**Action:**
Drop the timestamp prefix from the filename (use fixed name `universal-project-automation-template.md`). Then apply these replacements:

```
CHANGE 1 — ALL occurrences of {project-name}:
OLD: {project-name}
NEW: siopv

CHANGE 2 — directory name conflict throughout file:
OLD: .ignore/   (as the local scratch directory name)
NEW: .ignorar/  (SIOPV uses .ignorar/ per agent-reports.md and MEMORY.md)
Affects: directory structure section (line 43), directory rules (lines 47–48),
         Point 19 backup section, Point 26 .gitignore entries.

CHANGE 3 — Point 26 .gitignore entry:
OLD: # Local scratch and experiments — never commit
     .ignore/
NEW: # Local scratch and experiments — never commit
     .ignorar/

CHANGE 4 — Point 10 (shutdown cleanup command, absolute path):
OLD: rm -rf $HOME/.claude/teams/{team-name} $HOME/.claude/tasks/{team-name}
NEW: rm -rf /Users/bruno/siopv/.claude/teams/{team-name} /Users/bruno/siopv/.claude/tasks/{team-name}

CHANGE 5 — version header:
OLD: Version: 1.0 / Created: 2026-03-12-11.27.33
NEW: Version: 1.0-siopv / Created: 2026-03-12
```

Keep as-is: `{stage-name}`, `{username}`, `{TIMESTAMP}` — these are runtime placeholders.
The "HOW TO USE THIS TEMPLATE" onboarding section may be removed or retained (not harmful).

**Dependencies:**
- `.claude/workflow/briefing.md`
- `.claude/workflow/spec-findings.md`
- `.claude/workflow/orchestrator-briefing.md`
- `.claude/workflow/compaction-log.md`
- `.claude/rules/errors-to-rules.md`
- `.claude/memory/` (referenced generically)

**Deployment notes:**
All referenced workflow/rules files must exist in SIOPV before this template is used operationally.

---

### agent-reports.md
**Target path:** `/Users/bruno/siopv/.claude/rules/agent-reports.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/rules/agent-reports.md`

**Auto-loaded:** Yes (rules/ — automatically active in Claude Code context)

**Action:**
Apply these line-level changes:

```
CHANGE 1 — report path prompt template (line 5):
OLD: "Save your report to '.ignorar/production-reports/{agent-name}/phase-{N}/{TIMESTAMP}-phase-{N}-{agent-name}-{slug}.md'"
NEW: "Save your report to '.ignorar/production-reports/{agent-name}/{stage-name}/{TIMESTAMP}-{stage-name}-{agent-name}-{slug}.md'"

CHANGE 2 — directory pattern (line 17):
OLD: Directory: '.ignorar/production-reports/{agent}/phase-{N}/'
NEW: Directory: '.ignorar/production-reports/{agent}/{stage-name}/'

CHANGE 3 — TIMESTAMP format (line 13):
OLD: **TIMESTAMP format:** 'YYYY-MM-DD-HHmmss' (24-hour format)
NEW: **TIMESTAMP format:** 'YYYY-MM-DD-HH.mm.ss' (24-hour format, dots between time components)

CHANGE 4 — bash timestamp command (line 23):
OLD: TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
NEW: TIMESTAMP=$(date +%Y-%m-%d-%H.%M.%S)

CHANGE 5 — Agent Wave Timing section header (line 27):
OLD: ## Agent Wave Timing (Phase 1 - Parallel Execution)
NEW: ## Agent Wave Timing (Stage-based Parallel Execution)
```

Reason for timestamp format change: `universal-automation-template.md` Point 21 mandates `YYYY-MM-DD-HH.mm.ss` with dots. This file uses `HHmmss` without dots — a cross-file conflict resolved here in favor of the more recent standard (2026-03-12).

**Dependencies:** `.claude/workflow/` files referenced generically — exist in SIOPV.

**Deployment notes:** This file is auto-loaded by Claude Code (rules/ dir). Changes take effect immediately on next session start. The wave agent list (best-practices-enforcer, security-auditor, hallucination-detector, code-reviewer, test-generator) and timing (~12 min parallel) remain valid for SIOPV.

---

### placeholder-conventions.md
**Target path:** `/Users/bruno/siopv/.claude/rules/placeholder-conventions.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/rules/placeholder-conventions.md`

**Action:** Copy unchanged. Content is fully generic (placeholder syntax conventions: `{{ }}`, `< >`, `[ ]`, `${ }`). No project-specific paths or meta-project references. The "Usage in Workflows" section references `.claude/workflow/` generically — correct for SIOPV.

**Dependencies:** `.claude/workflow/` (generic reference)
**Deployment notes:** Auto-loaded (rules/ dir). No changes needed.

---

### tech-stack.md
**Target path:** `/Users/bruno/siopv/.claude/rules/tech-stack.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/rules/tech-stack.md`

**Action:** Copy unchanged. The tech stack (Python 3.11+, uv, Pydantic v2, httpx async, structlog, pathlib) exactly matches SIOPV's architecture per MEMORY.md. The `paths: ["**/*.py", "pyproject.toml"]` frontmatter scope is correct for SIOPV. The `/verify` command and Context7 MCP instructions are universally applicable.

**Dependencies:**
- `.claude/docs/mcp-setup.md` — referenced in "Before Write/Edit" section. This file is handled by agent C4a. C4a's `mcp-setup.md` must be deployed to SIOPV before this rules file is active, otherwise the cross-reference is a dead link.

**Deployment notes:** Auto-loaded (rules/ dir). Deploy after C4a's `mcp-setup.md`.

---

## Section C2 — CLAUDE.md + MEMORY.md (from D1)

---

### Global CLAUDE.md
**Target path:** N/A — stays at source location
**Classification:** STAYS AT SOURCE
**Source path:** `/Users/bruno/.claude/CLAUDE.md`

**Action:** No action. This is the global Claude Code config. It applies automatically to ALL projects including SIOPV. The TeamCreate default, orchestrator spawn protocol, error prevention (@errors-to-rules.md), and language preferences apply without any SIOPV-side configuration.

**Dependencies:** `~/.claude/rules/errors-to-rules.md` (auto-imported via @)
**Deployment notes:** SIOPV CLAUDE.md must NOT repeat or override any global CLAUDE.md content.

---

### CLAUDE.md (SIOPV project)
**Target path:** `/Users/bruno/siopv/CLAUDE.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (sec-llm-workbench CLAUDE.md is meta-project specific and must NOT be copied)

**Action:**
Create the following file at `/Users/bruno/siopv/CLAUDE.md`:

```markdown
# SIOPV — Security Intelligence & Orchestration Platform for Vulnerabilities

## Project Identity
- Root: /Users/bruno/siopv/
- Tech stack: Python 3.11+, LangGraph, FastAPI, httpx, Presidio, OpenFGA, ChromaDB, XGBoost
- Architecture: Hexagonal (ports + adapters), async-first, DI via lru_cache factories

## CRITICAL RULES

**YOU MUST** at session start:
1. Read @.claude/workflow/01-session-start.md
2. Read @.claude/briefing.md (SIOPV briefing — primary context source)

**YOU MUST** when writing code:
3. Query Context7 MCP before using external libraries
4. Follow `.claude/workflow/02-reflexion-loop.md` (read on demand)
5. EXCEPTION: LIME, Jira v3, Redis have NO Context7 coverage — use official docs directly

**YOU MUST** before commit:
6. Execute `/verify` (runs 5 verification agents)
7. Follow @.claude/workflow/05-before-commit.md

**IMPORTANT** — Human checkpoints:
8. See @.claude/workflow/03-human-checkpoints.md

## References (read on demand, NOT auto-loaded)

| Topic | File |
|-------|------|
| Reflexion Loop | `.claude/workflow/02-reflexion-loop.md` |
| Agents | `.claude/workflow/04-agents.md` |
| Auto decisions | `.claude/workflow/06-decisions.md` |

## On-Demand References (loaded via skills, NOT at startup)

| Topic | Skill / File |
|-------|-------------|
| Orchestrator invocation | `/orchestrator-protocol` |
| Python standards | `/coding-standards-2026` |
| Agent tool schemas | Read `.claude/docs/agent-tool-schemas.md` |
| Model selection | Read `.claude/docs/model-selection-strategy.md` |
| Verification thresholds | Read `.claude/docs/verification-thresholds.md` |

## Compact Instructions

When compacting context, preserve:
- Current phase number and name
- Active task description and progress
- Pending verification file list
- Error patterns from errors-to-rules.md
- Key architectural decisions made in this session
- STAGE audit status (which STAGEs are complete)
```

**MERGE-012-A applied:** `/techniques-reference` is intentionally excluded from the On-Demand References table above. The skill will exist and is invocable, but is not highlighted in the reference table.

**Dependencies:**
- `@.claude/briefing.md` — must exist in SIOPV
- `@.claude/workflow/05-before-commit.md` — must exist in SIOPV
- `@.claude/workflow/03-human-checkpoints.md` — must exist in SIOPV
- `@.claude/workflow/01-session-start.md` — must exist in SIOPV

**Deployment notes:**
- Do NOT commit CLAUDE.md to git (see global errors-to-rules.md: "NEVER commit .claude/ or CLAUDE.md to git"). Add to `.gitignore` before first commit.
- Create this file only after the workflow files (01-session-start.md, 03-human-checkpoints.md, 05-before-commit.md) exist in SIOPV, since they are @-imported.
- The 5-hop @import recursion limit applies; ensure no circular imports through briefing.md.

---

### MEMORY.md (SIOPV project memory)
**Target path:** `/Users/bruno/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A — new file (sec-llm-workbench memory MEMORY.md is a different project's memory)

**Action:**
Create the directory `/Users/bruno/.claude/projects/-Users-bruno-siopv/memory/` if it does not exist, then create MEMORY.md with the following content:

```markdown
# SIOPV Project Memory

## Active Project
- **Name:** SIOPV
- **Path:** `/Users/bruno/siopv/`
- **Current Phase:** 7 (Human-in-the-Loop — Streamlit) PENDING
- **Phase 8:** Output (Jira + PDF) PENDING
- **Briefing:** `/Users/bruno/siopv/.claude/briefing.md`

## Phase Status
| Phase | Name | Status |
|-------|------|--------|
| 0 | Setup | ✅ Completed |
| 1 | Ingesta y Preprocesamiento | ✅ Completed |
| 2 | Enriquecimiento (CRAG / RAG) | ✅ Completed |
| 3 | Clasificación ML (XGBoost) | ✅ Completed |
| 4 | Orquestación (LangGraph) | ✅ Completed |
| 5 | Autorización (OpenFGA) | ✅ Completed |
| 6 | Privacidad (DLP / Presidio) | ✅ Completed |
| 7 | Human-in-the-Loop (Streamlit) | ⏳ PENDING |
| 8 | Output (Jira + PDF) | ⏳ PENDING |

## Metrics (as of 2026-03-05)
- Tests: 1,404 passed, 12 skipped
- Coverage: 83% overall
- mypy: 0 errors / ruff: 0 errors

## Key Architecture
- Graph: START → authorize → ingest → dlp → enrich → classify → [escalate] → END
- State: TypedDict (not Pydantic) — by design
- DI: lru_cache factory functions in infrastructure/di/
- Checkpointing: SQLite (siopv_checkpoints.db)
- Docker: docker-compose.yml runs OpenFGA + Keycloak + Postgres

## Key File Paths
| Component | Path |
|-----------|------|
| Graph | src/siopv/application/orchestration/graph.py |
| State | src/siopv/application/orchestration/state.py |
| CLI | src/siopv/interfaces/cli/main.py |
| Settings | src/siopv/infrastructure/config/settings.py |
| DI container | src/siopv/infrastructure/di/__init__.py |
| DLP DI | src/siopv/infrastructure/di/dlp.py |
| Constants | src/siopv/domain/constants.py |
| Logging | src/siopv/infrastructure/logging/setup.py |

## Open Issues (Priority Order — from Pre-Phase-7 Audit)
| # | Sev | Location | Issue |
|---|-----|----------|-------|
| 1 | CRITICAL | application/use_cases/ingest_trivy.py:17 | Imports TrivyParser from adapters |
| 2 | CRITICAL | application/use_cases/classify_risk.py:18 | Imports FeatureEngineer from adapters |
| 3 | HIGH | interfaces/cli/main.py | All 8 adapter ports = None, DI never wired |
| 4 | MEDIUM | adapters/dlp/dual_layer_adapter.py | No explicit DLPPort inheritance |
| 5 | MEDIUM | infrastructure/di/authorization.py | 3 uncached OpenFGAAdapter instances |
| 6 | MEDIUM | application/orchestration/nodes/ingest_node.py | Directly instantiates use case |
| 7 | LOW | application/orchestration/edges.py:calculate_batch_discrepancies() | Domain logic in edge routing |

## Pre-Phase-7 Audit Reports
- Reports root: /Users/bruno/siopv/AuditPrePhase7-11mar26/
- Stage 4.1 input brief: stage3.5/stage3.5-final-reports-summarizer-n-aggregator-for-stage4-input-brief.md

## Memory Convention
Memory files use frontmatter: name, description, type (user/feedback/project/reference).
This is a user-defined convention, not an official Claude Code standard.
Index: this file (MEMORY.md) — first 200 lines loaded per session.
```

**Notes:**
- The 200-line hard limit is critical: this skeleton is ~65 lines — stays well within the limit.
- The 4-type frontmatter schema (user/feedback/project/reference) is a USER-DEFINED CONVENTION per D1 findings — document it in the memory convention note as shown.
- Content from sec-llm-workbench MEMORY.md intentionally excluded: meta-project Stage status tables, detailed Stage 1/2/3/3.5 findings bodies (too long), hook/agent classification catalogs (meta-project only).

**Dependencies:** `/Users/bruno/siopv/.claude/briefing.md` — must exist when MEMORY.md is initialized.
**Deployment notes:** Create parent directory with `mkdir -p /Users/bruno/.claude/projects/-Users-bruno-siopv/memory/` before writing this file.

---

## Section C3 — Skills (from D1, D2, D3, D4)

---

### show-trace/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/show-trace/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/show-trace/SKILL.md`

**Action:**
Frontmatter change only (body unchanged):

```
OLD frontmatter:
---
name: show-trace
description: "Mostrar ultimas entradas de logs de trazabilidad"
argument-hint: "[options]"
context: fork
---

NEW frontmatter (MERGE-018 applied — user-invocable: true added):
---
name: show-trace
description: "Mostrar ultimas entradas de logs de trazabilidad"
argument-hint: "[options]"
user-invocable: true
context: fork
---
```

Rationale: `user-invocable: true` is required for `/show-trace` invocation syntax to work. Other missing schema fields (allowed-tools, model, agent, hooks, version) are optional — omit per MERGE-018 instructions (only add where functional impact).

**Dependencies:** `.build/logs/agents/`, `.build/logs/decisions/`, `.build/logs/sessions/` — these directories must exist in SIOPV.
**Deployment notes:** Log directories must be created before first use.

---

### orchestrator-protocol/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/orchestrator-protocol/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/orchestrator-protocol/SKILL.md`

**Action:** Copy unchanged. The `user-invocable: true` field is already present and correct. The `@../../workflow/07-orchestrator-invocation.md` body reference resolves identically from `.claude/skills/orchestrator-protocol/SKILL.md` in SIOPV — same relative path, same directory depth.

**Dependencies:** `/Users/bruno/siopv/.claude/workflow/07-orchestrator-invocation.md` — must be created before this skill is invoked.
**Deployment notes:** Prerequisite: deploy `07-orchestrator-invocation.md` first (separate agent task).

---

### techniques-reference/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/techniques-reference/SKILL.md`
**Classification:** ADAPT (MERGE-017: copy as-is, with deployment sequencing note)
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/techniques-reference/SKILL.md`

**Action:**
Copy as-is per MERGE-017. The `@../../docs/techniques.md` body reference resolves identically from `.claude/skills/techniques-reference/SKILL.md` in SIOPV — same relative path, resolves to `/Users/bruno/siopv/.claude/docs/techniques.md`.

Frontmatter (`user-invocable` is present in source — keep whatever value it has).

**Dependencies:** `/Users/bruno/siopv/.claude/docs/techniques.md` — MUST be deployed before this skill is used.
**Deployment notes:**
- MERGE-017 (auto): `techniques.md` (C4a coverage) must be deployed before this skill is invoked. If `techniques.md` does not exist in `siopv/.claude/docs/`, the @-include will silently fail or error.
- MERGE-012-A (auto): Although this skill is deployed, it is intentionally excluded from SIOPV CLAUDE.md's On-Demand References table. Users may still invoke it via `/techniques-reference` — it's available but not prominently referenced.

---

### new-project/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/new-project/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/new-project/SKILL.md`

**Action:**
Two body changes only (frontmatter unchanged):

```
CHANGE 1 — line 32:
OLD: Usar templates del META-PROYECTO (`$CLAUDE_PROJECT_DIR/templates/`):
NEW: Usar templates del proyecto (`$CLAUDE_PROJECT_DIR/templates/`):

CHANGE 2 — line 51:
OLD: Crear `projects/[nombre].md` con path, stack, progreso.
NEW: Crear `projects/[nombre].json` con path, stack, progreso.
(Note: SIOPV uses .json format per projects/siopv.json — .md is a divergence from actual convention)
```

**Dependencies:** `$CLAUDE_PROJECT_DIR/templates/` directory — must exist in SIOPV for this skill's template-creation steps to work.
**Deployment notes:** The `projects/` directory exists in SIOPV. The `.json` format aligns with `projects/siopv.json` already present.

---

### scan-vulnerabilities/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/scan-vulnerabilities/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/scan-vulnerabilities/SKILL.md`

**Action:** Copy entire skill directory unchanged. Pipeline reference (Ingestion → RAG → Classify → Orchestrate) maps directly to SIOPV's pipeline stages. No meta-project-specific paths or project names.

**Dependencies:** None beyond SIOPV pipeline being operational.
**Deployment notes:** None.

---

### init-session/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md`
**Classification:** CREATE FROM SCRATCH
**Source path:** N/A (MERGE-009-A overrides D2's COPY AS-IS recommendation — create with SIOPV-specific content)

**Action:**
Create the following file at `/Users/bruno/siopv/.claude/skills/init-session/SKILL.md`:

```markdown
---
name: init-session
description: "Initialize SIOPV session — read briefing, spec-findings, and errors-to-rules, then create daily checkpoint markers"
user-invocable: true
context: fork
allowed-tools: ["Read", "Bash", "Glob"]
---

# Init Session

## Purpose

Initialize a SIOPV working session by loading required context documents and creating checkpoint markers so subsequent tools can verify the session has been initialized.

## Step 1 — Read Required Documents

Read these files in order:

1. Read `$CLAUDE_PROJECT_DIR/.claude/briefing.md`
   — Primary SIOPV context: current phase, pending tasks, architectural decisions
2. Read `$CLAUDE_PROJECT_DIR/.claude/workflow/spec-findings.md`
   — Pre-Phase-7 audit findings: open issues, STAGE statuses, remediation priorities
3. Read `~/.claude/rules/errors-to-rules.md`
   — Error prevention: past mistakes to avoid repeating

## Step 2 — Create Daily Checkpoint Markers

```bash
TODAY=$(date +%Y-%m-%d)
CHECKPOINT_DIR="$CLAUDE_PROJECT_DIR/.build/checkpoints/daily"
mkdir -p "$CHECKPOINT_DIR"
touch "$CHECKPOINT_DIR/briefing-read-${TODAY}"
touch "$CHECKPOINT_DIR/spec-read-${TODAY}"
touch "$CHECKPOINT_DIR/errors-read-${TODAY}"
echo "Session initialized: ${TODAY}"
echo "Markers created in: ${CHECKPOINT_DIR}"
```

## Step 3 — Report

After reading, summarize:
- Current phase and next pending task (from briefing.md)
- Any open CRITICAL/HIGH issues (from spec-findings.md)
- Any relevant recent errors to avoid (from errors-to-rules.md)
```

**Notes on CREATE FROM SCRATCH decision (MERGE-009-A):**
- The existing source skill (sec-llm-workbench) reads workflow/*.md files and projects/*.json. SIOPV's session context comes from `briefing.md` + `spec-findings.md` instead.
- MERGE-018 applied: `user-invocable: true` added (functional: user invokes via `/init-session`).

**Dependencies:**
- `/Users/bruno/siopv/.claude/briefing.md`
- `/Users/bruno/siopv/.claude/workflow/spec-findings.md`
- `~/.claude/rules/errors-to-rules.md`
- `/Users/bruno/siopv/.build/checkpoints/daily/` (created by the skill itself)

**Deployment notes:** `briefing.md` and `spec-findings.md` must exist before this skill is invoked. The skill creates the checkpoints/daily directory automatically via `mkdir -p`.

---

### run-tests/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/run-tests/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/run-tests/SKILL.md`

**Action:** Copy entire skill directory unchanged. All commands use `uv run pytest` with standard paths (`tests/unit`, `tests/integration`, `tests/`) and `--cov=src` — applicable to SIOPV's test structure. `uv run bandit -r src/ -f json` applies. Coverage minimum `80%` is a floor; SIOPV's 83% actual coverage already exceeds it.

**Dependencies:** SIOPV's test directories (`tests/unit`, `tests/integration`) and `uv` environment.
**Deployment notes:** None.

---

### generate-report/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/generate-report/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/generate-report/SKILL.md`

**Action:** Copy entire skill directory unchanged. All body paths are relative (`.build/logs/sessions/`, `.build/logs/agents/`, `.build/logs/decisions/`, `.build/logs/reports/`). No absolute meta-project paths.

**Dependencies:** `.build/logs/` directory tree must exist in SIOPV.
**Deployment notes:** None.

---

### verify/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/verify/SKILL.md`
**Classification:** ADAPT
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/verify/SKILL.md`

**Action:**
Two changes:

```
CHANGE 1 — absolute path (line 576):
OLD: python3 /Users/bruno/sec-llm-workbench/.claude/scripts/check-module-coverage.py "$TARGET"
NEW: python3 /Users/bruno/siopv/.claude/scripts/check-module-coverage.py "$TARGET"

CHANGE 2 — remove experimental batch mode section (MERGE-011-A):
Remove the entire section that covers the --batch optional flag and references:
  .ignorar/experimental-scripts/batch-api-verification/submit-batch-verification.py
This section is marked ⚠️ EXPERIMENTAL / "not integrated in production workflow" in the source.
The script does not exist in SIOPV's .ignorar/. Removing it prevents invocation errors.
```

MERGE-011-A applied: Experimental batch section removed entirely.

**Dependencies:**
- `/Users/bruno/siopv/.claude/scripts/check-module-coverage.py`
- `/Users/bruno/siopv/.claude/scripts/check-reviewer-score.sh`

**Deployment notes:** The check-module-coverage.py and check-reviewer-score.sh scripts must be deployed to SIOPV before `/verify` is used. These are covered by other section agents (C4a/C4b scope).

---

### coding-standards-2026/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/coding-standards-2026/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/coding-standards-2026/SKILL.md`

**Action:** Copy entire skill directory unchanged. Body is pure Python standards reference (type hints, Pydantic v2, httpx, structlog, pathlib, dataclasses, error handling, context managers). No meta-project paths. The `user-invocable` field is already present in frontmatter — keep as-is per MERGE-018 (field already present, no change needed).

**Dependencies:** None.
**Deployment notes:** None.

---

### cve-research/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/cve-research/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/cve-research/SKILL.md`

**Action:** Copy entire skill directory unchanged. Body uses external APIs (NVD, GitHub GraphQL, EPSS/FIRST, CISA KEV, Tavily OSINT) — all public external endpoints. No meta-project paths. The `user-invocable` field is already present in frontmatter — keep as-is.

**Dependencies:** External API access (NVD, GitHub, EPSS).
**Deployment notes:** None.

---

### langraph-patterns/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/langraph-patterns/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/langraph-patterns/SKILL.md`

**Action:** Copy entire skill directory unchanged. Body is pure LangGraph 0.2+ patterns (StateGraph, nodes, parallel execution, checkpointing, human-in-the-loop). No meta-project paths. The `HumanLoopState` and `classify_node` examples reference generic vulnerability domain concepts consistent with SIOPV. The `user-invocable` field is already present — keep as-is.

**Dependencies:** None.
**Deployment notes:** None.

---

### openfga-patterns/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/openfga-patterns/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/openfga-patterns/SKILL.md`

**Action:** Copy entire skill directory unchanged. Content is tightly coupled to SIOPV domain: `type vulnerability`, `object_type="vulnerability"`, `CVE-2024-1234` example, `vulnerability_report`/`audit_log` types. Docker Compose uses `openfga/openfga:latest` and port `8080` — matches SIOPV's docker-compose.yml. `user-invocable: false` (source value) — keep per MERGE-018 (no functional reason to expose as user-invocable command).

**Dependencies:** OpenFGA running (Docker Compose).
**Deployment notes:** None.

---

### presidio-dlp/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/presidio-dlp/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/presidio-dlp/SKILL.md`

**Action:** Copy entire skill directory unchanged. Content is SIOPV-purpose-built: uses `PipelineState`, `state.enrichment.nvd_description`, `state.vulnerability.cve_id`, `DLPSettings` with `env_prefix="DLP_"`, settings path `src/infrastructure/config/settings.py` — all exact SIOPV paths and types. `user-invocable: false` — keep per MERGE-018.

**Dependencies:** Presidio libraries installed in SIOPV environment.
**Deployment notes:** None.

---

### trivy-integration/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/trivy-integration/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/trivy-integration/SKILL.md`

**Action:** Copy entire skill directory unchanged. Content is SIOPV-purpose-built: imports `from src.domain.entities.vulnerability import Vulnerability`, uses `IngestTrivyUseCase`, SIOPV field names (`cve_id`, `cvss_v3_score`, `cvss_vector`), severity ordering (CRITICAL/HIGH/MEDIUM/LOW/UNKNOWN), structlog convention. Note: STAGE-2 finding #1 flagged `ingest_trivy.py:17` for importing `TrivyParser` from adapters — this skill documents the use case but does not replicate the violation. `user-invocable: false` — keep per MERGE-018.

**Dependencies:** Trivy installed; SIOPV domain layer (`src/domain/entities/vulnerability.py`) present.
**Deployment notes:** MERGE-013 (auto): Stage 4.2 must run `Glob pattern="src/siopv/adapters/ml/**"` to verify xai-explainer model artifact path before deployment. This is a deployment prerequisite for the full ML pipeline, including XAI features that may be invoked via trivy-related workflows.

---

### xai-visualization/SKILL.md
**Target path:** `/Users/bruno/siopv/.claude/skills/xai-visualization/SKILL.md`
**Classification:** COPY AS-IS
**Source path:** `/Users/bruno/sec-llm-workbench/.claude/skills/xai-visualization/SKILL.md`

**Action:** Copy entire skill directory unchanged. Content is SIOPV-purpose-built: `shap.TreeExplainer` for XGBoost (Phase 3), `EvidenceTriad` with `cve_id`/`prediction`/`confidence`, prediction labels `["LOW", "MEDIUM", "HIGH", "CRITICAL"]`, `render_xai_dashboard` with Streamlit (Phase 7), LIME + SHAP combination matching STAGE-3 xai-explainer pattern. `user-invocable: false` — keep per MERGE-018.

**NOTE from D4 agent:** Target path in D4 report contained a typo (`.clone/` instead of `.claude/`). Corrected above.

**Dependencies:**
- XGBoost model artifact at `src/siopv/adapters/ml/**` — verify with Glob before deployment (MERGE-013).
- SHAP and LIME libraries installed in SIOPV environment.
- Streamlit (Phase 7) deployed.

**Deployment notes:**
- MERGE-013 (auto): Before deploying, run `Glob pattern="src/siopv/adapters/ml/**"` to verify the XGBoost model artifact path exists. If the artifact is missing, SHAP explanations will fail at runtime.
- Deploy after Phase 3 (XGBoost) and Phase 7 (Streamlit) are confirmed operational.

---

## Appendix: Full File Count and Classification Summary

| # | File | Type | Classification | MERGE Applied |
|---|------|------|---------------|---------------|
| 1 | verification-thresholds.md | docs | ADAPT | MERGE-010-B, MERGE-016 note |
| 2 | universal-project-automation-template.md | docs | ADAPT | — |
| 3 | agent-reports.md | rules | ADAPT | — |
| 4 | placeholder-conventions.md | rules | COPY AS-IS | — |
| 5 | tech-stack.md | rules | COPY AS-IS | — |
| 6 | Global CLAUDE.md | global | STAYS AT SOURCE | — |
| 7 | CLAUDE.md (SIOPV project) | project | CREATE FROM SCRATCH | MERGE-012-A |
| 8 | MEMORY.md (SIOPV project memory) | memory | CREATE FROM SCRATCH | — |
| 9 | show-trace/SKILL.md | skill | ADAPT | MERGE-018 |
| 10 | orchestrator-protocol/SKILL.md | skill | COPY AS-IS | (already user-invocable: true) |
| 11 | techniques-reference/SKILL.md | skill | COPY AS-IS + note | MERGE-017, MERGE-012-A |
| 12 | new-project/SKILL.md | skill | ADAPT | — |
| 13 | scan-vulnerabilities/SKILL.md | skill | COPY AS-IS | — |
| 14 | init-session/SKILL.md | skill | CREATE FROM SCRATCH | MERGE-009-A, MERGE-018 |
| 15 | run-tests/SKILL.md | skill | COPY AS-IS | — |
| 16 | generate-report/SKILL.md | skill | COPY AS-IS | — |
| 17 | verify/SKILL.md | skill | ADAPT | MERGE-011-A |
| 18 | coding-standards-2026/SKILL.md | skill | COPY AS-IS | MERGE-018 (already present) |
| 19 | cve-research/SKILL.md | skill | COPY AS-IS | MERGE-018 (already present) |
| 20 | langraph-patterns/SKILL.md | skill | COPY AS-IS | MERGE-018 (already present) |
| 21 | openfga-patterns/SKILL.md | skill | COPY AS-IS | MERGE-018 (keep false) |
| 22 | presidio-dlp/SKILL.md | skill | COPY AS-IS | MERGE-018 (keep false) |
| 23 | trivy-integration/SKILL.md | skill | COPY AS-IS | MERGE-018 (keep false) |
| 24 | xai-visualization/SKILL.md | skill | COPY AS-IS | MERGE-013 deployment note |

**Totals by classification:**
- STAYS AT SOURCE: 1
- CREATE FROM SCRATCH: 3 (CLAUDE.md, MEMORY.md, init-session/SKILL.md)
- ADAPT: 6 (verification-thresholds.md, universal-automation-template.md, agent-reports.md, show-trace/SKILL.md, new-project/SKILL.md, verify/SKILL.md)
- COPY AS-IS: 14

**MERGE decisions applied:** MERGE-009-A, MERGE-010-B, MERGE-011-A, MERGE-012-A, MERGE-013, MERGE-016, MERGE-017, MERGE-018

---

## Appendix: Unresolved Items (for human decision)

| # | Item | Location | Decision Needed |
|---|------|----------|-----------------|
| 1 | `{{ smoke_test_cve_id }}` placeholder | verification-thresholds.md | SIOPV team must designate the actual CVE ID used for smoke testing before Phase 7 |
| 2 | `.build/checkpoints/pending/` directory | verification-thresholds.md + pre-commit hook | MERGE-016 adds mkdir pre-flight; confirm this directory pattern aligns with SIOPV's existing `.build/` structure |
| 3 | `techniques.md` content | techniques-reference skill dependency | C4a agent must deploy `techniques.md` to `siopv/.claude/docs/` before techniques-reference skill is usable |
| 4 | `07-orchestrator-invocation.md` | orchestrator-protocol skill dependency | Must be created in `siopv/.claude/workflow/` before orchestrator-protocol skill is invocable |
| 5 | xai-explainer model artifact | xai-visualization deployment | Stage 4.2 pre-flight: `Glob pattern="src/siopv/adapters/ml/**"` to verify path before deployment |

---

## Deployment Sequence for Stage 4.2

Derived from cross-file dependencies found across Sections A, B, and C.

**Step 1 — Infrastructure setup (directories, `.build/` structure)**

Create all required directories before writing any files:
```bash
mkdir -p /Users/bruno/siopv/.claude/agents
mkdir -p /Users/bruno/siopv/.claude/hooks
mkdir -p /Users/bruno/siopv/.claude/workflow
mkdir -p /Users/bruno/siopv/.claude/docs
mkdir -p /Users/bruno/siopv/.claude/rules
mkdir -p /Users/bruno/siopv/.claude/skills/init-session
mkdir -p /Users/bruno/siopv/.claude/skills/show-trace
mkdir -p /Users/bruno/siopv/.claude/skills/orchestrator-protocol
mkdir -p /Users/bruno/siopv/.claude/skills/techniques-reference
mkdir -p /Users/bruno/siopv/.claude/skills/new-project
mkdir -p /Users/bruno/siopv/.claude/skills/scan-vulnerabilities
mkdir -p /Users/bruno/siopv/.claude/skills/run-tests
mkdir -p /Users/bruno/siopv/.claude/skills/generate-report
mkdir -p /Users/bruno/siopv/.claude/skills/verify
mkdir -p /Users/bruno/siopv/.claude/skills/coding-standards-2026
mkdir -p /Users/bruno/siopv/.claude/skills/cve-research
mkdir -p /Users/bruno/siopv/.claude/skills/langraph-patterns
mkdir -p /Users/bruno/siopv/.claude/skills/openfga-patterns
mkdir -p /Users/bruno/siopv/.claude/skills/presidio-dlp
mkdir -p /Users/bruno/siopv/.claude/skills/trivy-integration
mkdir -p /Users/bruno/siopv/.claude/skills/xai-visualization
mkdir -p /Users/bruno/siopv/.build/checkpoints/daily
mkdir -p /Users/bruno/siopv/.build/checkpoints/pending
mkdir -p /Users/bruno/siopv/.build/logs/agents
mkdir -p /Users/bruno/siopv/.build/logs/sessions
mkdir -p /Users/bruno/siopv/.build/logs/decisions
mkdir -p /Users/bruno/siopv/.build/logs/reports
mkdir -p /Users/bruno/siopv/.ignorar/production-reports
mkdir -p /Users/bruno/.claude/projects/-Users-bruno-siopv/memory
echo "/Users/bruno/siopv" > /Users/bruno/siopv/.build/active-project
```

**Step 2 — Rules files** (auto-loaded by Claude Code; no other files depend on them at write time)

1. `rules/placeholder-conventions.md` — COPY AS-IS (entry 54)
2. `rules/agent-reports.md` — ADAPT (entry 53; change timestamp format and stage-based paths)
3. `rules/tech-stack.md` — COPY AS-IS (entry 55; depends on `docs/mcp-setup.md` for cross-reference — deploy `mcp-setup.md` in Step 3)

**Step 3 — Docs batch 1** (referenced by rules and CLAUDE.md)

1. `docs/mcp-setup.md` — COPY AS-IS (entry 47; required by `rules/tech-stack.md`)
2. `docs/python-standards.md` — COPY AS-IS (entry 45; required by `code-implementer.md` and `04-agents.md`)
3. `docs/traceability.md` — COPY AS-IS (entry 43)
4. `docs/techniques.md` — ADAPT (entry 46; required by `techniques-reference` skill)
5. `docs/errors-to-rules.md` — ADAPT (entry 44; required by `init-session` skill and CLAUDE.md)
6. `docs/model-selection-strategy.md` — ADAPT (entry 48)
7. `docs/agent-tool-schemas.md` — ADAPT (entry 49)
8. `docs/verification-thresholds.md` — ADAPT (entry 51; required by `02-reflexion-loop.md` and `05-before-commit.md`)
9. `docs/universal-project-automation-template.md` — ADAPT (entry 52)

**Step 4 — Workflow files** (referenced by CLAUDE.md @imports; must exist before CLAUDE.md is written)

1. `workflow/01-session-start.md` — COPY AS-IS (entry 31)
2. `workflow/02-reflexion-loop.md` — COPY AS-IS (entry 32)
3. `workflow/03-human-checkpoints.md` — COPY AS-IS (entry 33)
4. `workflow/04-agents.md` — ADAPT (entry 35; replace `[TARGET_PROJECT]` × 8)
5. `workflow/05-before-commit.md` — COPY AS-IS (entry 34)
6. `workflow/06-decisions.md` — COPY AS-IS (entry 36)
7. `workflow/07-orchestrator-invocation.md` — COPY AS-IS (entry 37)
8. `workflow/spec-findings.md` — ADAPT (entry 39; MERGE-008-A: one path change)
9. `workflow/orchestrator-briefing.md` — ADAPT (entry 40)
10. `workflow/compaction-log.md` — CREATE FROM SCRATCH (entry 42; empty log)
11. `workflow/briefing.md` — ADAPT (entry 38; 15 replacements; deploy after hooks in Step 5 so paths are accurate)

**Step 5 — Hook scripts** (must exist before settings.json is written)

1. `hooks/pre-compact.sh` — CREATE FROM SCRATCH (entry 27; depends on `workflow/compaction-log.md` existing)
2. `hooks/session-start.sh` — CREATE FROM SCRATCH (entry 28)
3. `hooks/session-end.sh` — CREATE FROM SCRATCH (entry 29)
4. `hooks/post-code.sh` — ADAPT (entry 23; remove 5-line META_PROJECT_DIR block)
5. `hooks/pre-git-commit.sh` — ADAPT (entry 24; collapse active-project indirection)
6. `hooks/pre-write.sh` — CREATE FROM SCRATCH (entry 25)
7. `hooks/pre-commit.sh` — ADAPT / manual only (entry 21; MERGE-004-B: NOT in settings.json)
8. `hooks/verify-best-practices.sh` — ADAPT / manual only (entry 22; MERGE-004-B: NOT in settings.json)
9. `hooks/test-framework.sh` — CREATE FROM SCRATCH / manual only (entry 26; MERGE-006-A: NOT in settings.json)

**Step 6 — Settings.json** (wires hooks to events; must be written after all hook scripts exist)

1. `settings.json` — ADAPT (entry 30; 3 absolute path changes + MERGE-007-B: remove UserPromptSubmit hook + update tech stack string)
2. Verify after write: `jq . /Users/bruno/siopv/.claude/settings.json`

**Step 7 — Agent definitions** (depend on docs/ files already deployed)

Deploy all 20 agent files (entries 1–20):
- Deploy in parallel: entries 1–16, 18–20 (no cross-dependencies)
- HOLD: entry 17 (xai-explainer) — BLOCKED by MERGE-005-B until model artifact path verified

**Step 8 — Skills** (depend on workflow/ and docs/ files already deployed)

1. `skills/init-session/SKILL.md` — CREATE FROM SCRATCH (entries 50/64; deploy BEFORE or TOGETHER WITH pre-write.sh activation)
2. `skills/verify/SKILL.md` — ADAPT (entry 67)
3. `skills/show-trace/SKILL.md` — ADAPT (entry 59; add `user-invocable: true`)
4. `skills/new-project/SKILL.md` — ADAPT (entry 62)
5. `skills/techniques-reference/SKILL.md` — COPY AS-IS (entry 61; requires `docs/techniques.md` — already deployed in Step 3)
6. `skills/orchestrator-protocol/SKILL.md` — COPY AS-IS (entry 60; requires `workflow/07-orchestrator-invocation.md` — already deployed in Step 4)
7. All remaining COPY AS-IS skills: entries 63, 65, 66, 68, 69, 70, 71, 72, 73, 74

**Step 9 — CLAUDE.md** (references rules + workflow via @imports; write after all @-imported files exist)

1. `CLAUDE.md` (SIOPV project) — CREATE FROM SCRATCH (entry 57; at `/Users/bruno/siopv/CLAUDE.md`)
2. IMPORTANT: Add `CLAUDE.md` to `/Users/bruno/siopv/.gitignore` BEFORE the first commit

**Step 10 — Memory** (standalone; no other files depend on it)

1. `MEMORY.md` — CREATE FROM SCRATCH (entry 58; at `/Users/bruno/.claude/projects/-Users-bruno-siopv/memory/MEMORY.md`)

**Step 11 — Verification** (run after all files deployed)

1. Run `bash /Users/bruno/siopv/.claude/hooks/test-framework.sh` to validate settings.json structure and agent frontmatter
2. Run `/verify` from `/Users/bruno/siopv/` to confirm verification agent chain wiring
3. Verify xai-explainer prerequisite: `ls /Users/bruno/siopv/src/siopv/adapters/ml/`
4. If verification passes, deploy xai-explainer.md (entry 17) to `.claude/agents/`

---

## Open Items for Stage 4.2

Consolidated from all UNCLEAR/PREREQUISITE/VERIFY notes across Sections A, B, and C.

1. [PREREQUISITE] **xai-explainer deployment BLOCKED (MERGE-005-B)** — Run `Glob pattern="src/siopv/adapters/ml/**"` to verify XGBoost model artifact path and confirm `class_names` match actual risk-level labels before deploying `xai-explainer.md`. Source: Section A.

2. [VERIFY] **python-standards.md existence** — Verify whether `/Users/bruno/siopv/.claude/docs/python-standards.md` already exists in SIOPV. Create if absent (MERGE-002-A). Referenced by `code-implementer.md` agent. Source: Section A.

3. [VERIFY] **tech-stack.md existence** — Verify whether `/Users/bruno/siopv/.claude/rules/tech-stack.md` already exists in SIOPV. Create if absent (MERGE-002-A). Referenced by `code-implementer.md` agent. Source: Section A.

4. [PREREQUISITE] **Manual utility documentation** — Add `pre-commit.sh` and `verify-best-practices.sh` invocation syntax to SIOPV CLAUDE.md under a "Manual Utilities" section (MERGE-004-B). Source: Section A.

5. [PREREQUISITE] **post-code.sh + pre-git-commit.sh registration** — Register post-code.sh under PostToolUse and pre-git-commit.sh under PreToolUse in `settings.json`. Verify `.build/checkpoints/pending/` and `.build/logs/decisions/` directories exist before first use. Source: Section A.

6. [PREREQUISITE] **init-session skill deployment order** — Deploy `skills/init-session/SKILL.md` BEFORE activating `hooks/pre-write.sh`. Without the skill, every Write/Edit will be blocked by the hook until checkpoint markers are manually created. Source: Section B.

7. [VERIFY] **settings.json UserPromptSubmit removal (MERGE-007-B)** — Confirm the UserPromptSubmit hook block is entirely absent from the adapted settings.json. Source: Section B.

8. [PREREQUISITE] **briefing.md `> Last updated:` marker** — The adapted `workflow/briefing.md` must contain a line matching `^> Last updated:` before pre-compact.sh and session-end.sh are activated. Source: Section B.

9. [VERIFY] **`.build/active-project` file** — Create `/Users/bruno/siopv/.build/active-project` containing `/Users/bruno/siopv`. Required by `workflow/04-agents.md` for Wave 3 agent invocations. Source: Section B.

10. [VERIFY] **MCP setup** — `.mcp.json` and `.env` (with `CONTEXT7_API_KEY`) must exist in `/Users/bruno/siopv/` before settings.json MCP health check hook fires. Source: Section B.

11. [PREREQUISITE] **`{{ smoke_test_cve_id }}` placeholder in verification-thresholds.md** — SIOPV team must designate the actual CVE ID used for smoke testing and replace the placeholder before Phase 7 testing begins (MERGE-010-B). Source: Section C.

12. [VERIFY] **`.build/checkpoints/pending/` directory** — Create as Stage 4.2 pre-flight step; referenced by `verification-thresholds.md` and `pre-git-commit.sh` (MERGE-016). Source: Section C.

13. [PREREQUISITE] **`techniques.md` before `techniques-reference` skill** — `docs/techniques.md` must be deployed before the `techniques-reference` skill is usable. If absent, the @-include will silently fail (MERGE-017). Source: Section C.

14. [PREREQUISITE] **`07-orchestrator-invocation.md` before `orchestrator-protocol` skill** — Must exist in `siopv/.claude/workflow/` before the `orchestrator-protocol` skill is invocable. Source: Section C.

15. [VERIFY] **xai-visualization model artifact path** — Before first use of `skills/xai-visualization/SKILL.md`, run `Glob pattern="src/siopv/adapters/ml/**"` to verify XGBoost model artifact path exists (MERGE-013). Source: Section C.

16. [VERIFY] **MEMORY.md parent directory** — Create `mkdir -p /Users/bruno/.claude/projects/-Users-bruno-siopv/memory/` before writing MEMORY.md. Source: Section C.

17. [PREREQUISITE] **briefing.md depends on `spec-findings.md`** — `workflow/spec-findings.md` must be deployed before `workflow/briefing.md` is considered fully functional; briefing.md Section 8 references spec-findings.md. Source: Section B.

18. [VERIFY] **experimental self-consistency-voting script** — `security-auditor.md` references `.ignorar/experimental-scripts/self-consistency-voting/self-consistency-vote.py`. This script does not currently exist in SIOPV. Copy manually from meta-project to SIOPV only if the experimental section will be used; otherwise the reference is a labeled dead link. Source: Section A.

**Total Open Items: 18**

---

## MERGE Decisions Applied

All 18 decisions (MERGE-001 through MERGE-018) applied across Sections A, B, and C:

| ID | Decision | Applied In |
|----|---------|------------|
| MERGE-001-A | Keep `memory` values as-is — `memory: project` in smoke-test-runner/test-generator/xai-explainer; `memory: true` in best-practices-enforcer/security-auditor/code-reviewer. No standardization. | Section A (agents) |
| MERGE-002-A | Keep `code-implementer` doc references — `.claude/docs/python-standards.md` and `.claude/rules/tech-stack.md` remain in body with "(create in Stage 4.2 if not present)" note. | Section A (code-implementer) |
| MERGE-003-B | Graceful-skip for `check-reviewer-score.sh` — do not create stub; `if [ -f "$SCORE_CHECK_SCRIPT" ]` guard in pre-git-commit.sh handles absence cleanly. | Section A (pre-git-commit.sh) |
| MERGE-004-B | `pre-commit.sh` and `verify-best-practices.sh` = manual utilities only. NOT registered in settings.json. Document in SIOPV CLAUDE.md under "Manual Utilities". | Section A (hooks B1) |
| MERGE-005-B | Block xai-explainer deployment — hold until Stage 4.2 verifies `class_names` against `src/siopv/adapters/ml/` with Glob read. | Section A (xai-explainer.md) |
| MERGE-006-A | `test-framework.sh` = manual standalone script. Place in `.claude/hooks/` as utility. NOT wired to any hook event. | Sections A (B1 hooks) and B (B1 meta hooks) |
| MERGE-007-B | Remove UserPromptSubmit hook from settings.json — multi-project disambiguation hook, no purpose in single-project SIOPV. | Section B (settings.json) |
| MERGE-008-A | SIOPV spec path uses `/Users/bruno/siopv/docs/SIOPV_Propuesta_Tecnica_v2.txt`. Applied to spec-findings.md, orchestrator-briefing.md, canonical paths table. | Section B (spec-findings.md, orchestrator-briefing.md) |
| MERGE-009-A | Create `/init-session` skill from scratch for SIOPV — source skill reads meta-project workflow files; SIOPV session context comes from `briefing.md` + `spec-findings.md`. | Sections B (B7) and C (C3) |
| MERGE-010-B | CVE-2024-1234 → `{{ smoke_test_cve_id }}` placeholder in verification-thresholds.md. Not a real CVE; must be substituted with actual SIOPV smoke-test CVE ID. | Section C (verification-thresholds.md) |
| MERGE-011-A | Remove experimental batch mode section from `verify/SKILL.md` — references non-existent `.ignorar/experimental-scripts/batch-api-verification/` script in SIOPV. | Section C (verify/SKILL.md) |
| MERGE-012-A | Exclude `/techniques-reference` from SIOPV CLAUDE.md On-Demand References table. Skill is deployed and invocable but not prominently referenced. | Section C (CLAUDE.md) |
| MERGE-013 | Pre-flight Glob check `src/siopv/adapters/ml/**` before deploying `xai-explainer.md` and `xai-visualization/SKILL.md` to verify XGBoost model artifact path. | Sections A (xai-explainer) and C (xai-visualization) |
| MERGE-014 | Auto-resolved — no explicit action item recorded in any section. | N/A |
| MERGE-015 | Auto-resolved — no explicit action item recorded in any section. | N/A |
| MERGE-016 | Pre-flight `mkdir -p .build/checkpoints/pending/` as Stage 4.2 pre-flight step. Referenced by verification-thresholds.md and pre-git-commit.sh. | Section C (verification-thresholds.md deployment notes) |
| MERGE-017 | `techniques-reference/SKILL.md` copy as-is with deployment sequencing note — `techniques.md` must be deployed to `siopv/.claude/docs/` before skill is invoked. | Section C (techniques-reference/SKILL.md) |
| MERGE-018 | Add `user-invocable: true` only where functionally needed — `show-trace/SKILL.md` and `init-session/SKILL.md` require it; other skills keep existing values or omit if not present. | Sections B (init-session), C (show-trace; orchestrator-protocol/coding-standards-2026/cve-research/langraph-patterns already had it; openfga-patterns/presidio-dlp/trivy-integration keep false) |

