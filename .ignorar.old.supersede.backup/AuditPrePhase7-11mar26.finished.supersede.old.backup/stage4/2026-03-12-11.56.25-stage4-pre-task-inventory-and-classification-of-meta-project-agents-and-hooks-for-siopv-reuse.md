# STAGE 4 — Pre-Task Inventory: Meta-Project Agents and Hooks Classification for SIOPV Reuse

**Generated:** 2026-03-12-11.56.25
**Source:** `/Users/bruno/sec-llm-workbench/.claude/agents/` and `.claude/hooks/`
**Purpose:** Classify each meta-project agent and hook as META-ONLY, SIOPV-APPLICABLE, or ADAPTABLE before Stage 4 remediation work begins.

---

## SECTION 1 — AGENTS (23 files)

---

### `async-safety-auditor.md` — SIOPV-APPLICABLE

- **Purpose:** Audits async/sync boundary violations — `asyncio.run()` in async contexts (LangGraph, FastAPI, Streamlit), missing `await` on coroutines, sync blocking in `async def`, event loop nesting.
- **Applicability to SIOPV:** Directly addresses SIOPV known issue #7 (`asyncio.run()` in sync nodes) and is critically relevant to Phase 7 (Streamlit handlers and LangGraph nodes both run inside event loops where `asyncio.run()` causes `RuntimeError: This event loop is already running`).
- **Relevant SIOPV issues/phases:** Issue #7 (asyncio.run() in sync nodes), Phase 7 (Streamlit HITL dashboard, LangGraph interrupt/resume)

---

### `circular-import-detector.md` — SIOPV-APPLICABLE

- **Purpose:** Detects Python circular imports using AST-based import graph analysis; circular imports cause `ImportError` at runtime and pass all static linters undetected.
- **Applicability to SIOPV:** Applicable to any phase of SIOPV; especially relevant after Phase 7/8 add new modules (Streamlit UI, Jira adapter, fpdf2 adapter) that import from existing infrastructure — new circular paths are possible with each phase added.
- **Relevant SIOPV issues/phases:** Phase 7, Phase 8 (new adapters may introduce cycles)

---

### `code-implementer.md` — SIOPV-APPLICABLE

- **Purpose:** Implements production code following hexagonal architecture, Python 2026 standards, and Context7 library verification; generates detailed implementation reports.
- **Applicability to SIOPV:** The primary builder agent for all SIOPV remediation-hardening tasks (STAGE-2 hexagonal violations, STAGE-3 priorities P1-P8) and for Phase 7/8 feature implementation.
- **Relevant SIOPV issues/phases:** Issues #1–#9 (remediation), Phase 7 (Streamlit HITL), Phase 8 (Jira + PDF)

---

### `config-validator.md` — SIOPV-APPLICABLE

- **Purpose:** Validates that all required environment variables are documented in `.env.example` and that `docker-compose.yml` service names/ports match what the code expects.
- **Applicability to SIOPV:** SIOPV STAGE-1 found `.env.example` missing entirely (TOP MISSING #3) and uses `docker-compose.yml` for OpenFGA + Keycloak + Postgres; this agent directly addresses that gap and will be needed before Phase 7/8 deployment.
- **Relevant SIOPV issues/phases:** TOP MISSING #3 (.env.example), Phase 7 (OpenFGA service config), Phase 8 (deployment readiness)

---

### `dependency-scanner.md` — SIOPV-APPLICABLE

- **Purpose:** Scans all project dependencies in `pyproject.toml` for known CVEs using `uv pip audit`, reporting CRITICAL and HIGH severity vulnerabilities.
- **Applicability to SIOPV:** Applicable to any Python project including SIOPV; especially important before Phase 7/8 add new dependencies (streamlit, jira client, fpdf2, httpx).
- **Relevant SIOPV issues/phases:** Phase 7, Phase 8 (new dependencies added), general security hygiene

---

### `final-report-agent.md` — META-ONLY

- **Purpose:** Reads 5 compact agent summaries and the static checks report from the `/verify` pipeline, applies thresholds, and sends a compact final verdict as a message to the operator.
- **Applicability to SIOPV:** This is the terminal node of the meta-project's `/verify` orchestration workflow; it is part of the meta-project's internal verification pipeline, not a standalone agent usable for SIOPV code auditing.
- **Relevant SIOPV issues/phases:** none

---

### `hallucination-detector.md` — SIOPV-APPLICABLE

- **Purpose:** Verifies code syntax against official documentation using Context7 MCP, detecting hallucinated APIs, invalid parameters, and deprecated patterns in external library usage.
- **Applicability to SIOPV:** Essential before Phase 7/8 implementation — STAGE-3 verified critical library facts (Streamlit `@st.fragment`, Jira ADF format, fpdf2 `fname` required, LangGraph `interrupt()` checkpointer requirement) that are easily hallucinated; this agent guards against those exact errors.
- **Relevant SIOPV issues/phases:** Issue #6 (hardcoded Haiku model IDs — may be wrong API syntax), Phase 7 (Streamlit, LangGraph, OpenFGA), Phase 8 (Jira API v3 ADF, fpdf2)

---

### `import-resolver.md` — SIOPV-APPLICABLE

- **Purpose:** Detects unresolvable imports in the target project — imports that will cause `ImportError` or `ModuleNotFoundError` at runtime — using AST-based analysis with `uv run python3`.
- **Applicability to SIOPV:** Applicable after each implementation wave; especially relevant for Phase 7/8 where new adapter modules (Streamlit, Jira, fpdf2) are added alongside existing hexagonal structure — import mismatches are a common post-implementation failure mode.
- **Relevant SIOPV issues/phases:** Phase 7, Phase 8 (new module imports)

---

### `integration-tracer.md` — SIOPV-APPLICABLE

- **Purpose:** Traces execution paths from all entry points (CLI, graph nodes, API) through full call chains, detecting hollow endpoints, parameter dropping, dead exports, and broken call chains.
- **Applicability to SIOPV:** Directly addresses SIOPV known issues #1 (CLI hollow) and #2 (SanitizeVulnerabilityUseCase dead code/orphaned); also validates that Phase 7 HITL interrupt/resume call chain connects correctly from graph → Streamlit callback → resume logic.
- **Relevant SIOPV issues/phases:** Issue #1 (CLI hollow), Issue #2 (SanitizeVulnerabilityUseCase orphaned), Issue #4 (run_pipeline drops enrichment clients), Phase 7 (interrupt/resume chain)

---

### `regression-guard.md` — SIOPV-APPLICABLE

- **Purpose:** Detects cross-phase regressions by finding recently changed files, building a reverse dependency map, running pytest on affected modules only, and flagging previously-passing tests that now fail.
- **Applicability to SIOPV:** Critical for SIOPV's multi-phase structure — remediation of issues in Phases 0–6 (STAGE-2/3 findings) risks breaking the 1,404 passing tests; this agent runs targeted regression checks without running the full 83%-coverage suite every time.
- **Relevant SIOPV issues/phases:** All remediation issues #1–#9, Phase 7/8 (changes to graph.py affect Phases 1–6)

---

### `report-summarizer.md` — META-ONLY

- **Purpose:** Reads one full agent verification report and produces a compact 20-30 line summary file; used by the `/verify` batched workflow after all batches for an agent type complete.
- **Applicability to SIOPV:** Internal to the meta-project's `/verify` orchestration pipeline; does not operate on SIOPV code directly and has no standalone applicability to SIOPV audit tasks.
- **Relevant SIOPV issues/phases:** none

---

### `semantic-correctness-auditor.md` — SIOPV-APPLICABLE

- **Purpose:** Detects syntactically valid but semantically wrong code — no-op Pydantic validators, hollow functions (docstring but empty body), swallowed exceptions, wrong fallback returns in except blocks.
- **Applicability to SIOPV:** Directly targets Issue #3 (LLM confidence heuristic — likely a no-op or wrong fallback pattern) and Issue #8 (no tests for Phase 2 adapters implies hollow or incorrect adapter methods); also validates Phase 7 Streamlit callbacks are not stub implementations.
- **Relevant SIOPV issues/phases:** Issue #2 (SanitizeVulnerabilityUseCase — possibly hollow), Issue #3 (LLM confidence heuristic — possibly no-op), Phase 7 (Streamlit handler bodies)

---

### `smoke-test-runner.md` — SIOPV-APPLICABLE

- **Purpose:** Executes the SIOPV end-to-end pipeline with a synthetic CVE input (`CVE-2024-1234`) and verifies it runs from START to END without crashing, with output containing required fields.
- **Applicability to SIOPV:** Directly designed for SIOPV (hardcodes `CVE-2024-1234` as synthetic input, checks for `classification`, `severity`, `cve_id` fields); serves as the runtime validation gate after every remediation or phase implementation.
- **Relevant SIOPV issues/phases:** Issue #1 (CLI hollow — smoke test would catch this), Issue #7 (asyncio.run() causes runtime crash — smoke test catches it), Phase 7 (pipeline must still run after HITL nodes added)

---

### `static-checks-agent.md` — SIOPV-APPLICABLE

- **Purpose:** Runs `ruff format`, `ruff check`, `mypy`, and `pytest` in the active project and saves the full stdout/stderr output to a report file.
- **Applicability to SIOPV:** Directly applicable — SIOPV already uses ruff, mypy (0 errors), and pytest (1,404 tests, 83% coverage); this agent provides automated execution of all four checks as part of the verification pipeline for Phase 7/8 additions.
- **Relevant SIOPV issues/phases:** Issue #9 (structlog deprecation — mypy would catch it), Phase 7/8 (verify new code passes all static checks)

---

### `test-generator.md` — SIOPV-APPLICABLE

- **Purpose:** Generates unit tests automatically for code without coverage, targeting minimum 80% line coverage with success, edge case, and error path scenarios.
- **Applicability to SIOPV:** Directly addresses Issue #8 (no tests for Phase 2 adapters) and will be required for all new Phase 7/8 code (Streamlit UI tests, Jira adapter tests, fpdf2 tests); SIOPV is already at 83% but Phase 7/8 additions may drop coverage below 80%.
- **Relevant SIOPV issues/phases:** Issue #8 (no tests for Phase 2 adapters), Phase 7, Phase 8 (new adapters need test coverage)

---

### `vulnerability-researcher.md` — META-ONLY

- **Purpose:** Performs deep CVE research using OSINT sources (NVD API, GitHub Security Advisories, EPSS, Exploit-DB) and compiles a structured research report per CVE.
- **Applicability to SIOPV:** SIOPV's domain is analyzing CVEs (it is the consumer of CVE data), not researching them; this agent is for the meta-project framework's own security research workflow, not for SIOPV application code quality or Phase 7/8 implementation.
- **Relevant SIOPV issues/phases:** none

---

### `xai-explainer.md` — ADAPTABLE

- **Purpose:** Generates ML model explanations using SHAP (global feature importance) and LIME (local per-prediction) for audit and transparency requirements.
- **Applicability to SIOPV:** SIOPV's Phase 3 uses XGBoost classification — this agent is directly relevant to SIOPV's audit/transparency requirements for the classifier. However, it needs adaptation: (1) the model path must be updated to `~/siopv/models/xgboost_classifier.json`, (2) class names must be updated to match SIOPV severity levels, (3) feature names must come from SIOPV's `FeatureEngineer` output. The agent is SIOPV-specific in intent but requires path/config adaptation.
- **Relevant SIOPV issues/phases:** Phase 3 (XGBoost classifier transparency), Phase 7 (Streamlit HITL dashboard could display LIME/SHAP explanations per CVE)

---

### `best-practices-enforcer.md` — SIOPV-APPLICABLE

- **Purpose:** Verifies Python 2026 best practices violations (type hints, Pydantic v2, httpx, structlog, pathlib) by scanning code and reporting deviations from modern standards.
- **Applicability to SIOPV:** Applicable to any Python project including SIOPV; directly catches Issue #9 (structlog deprecation patterns), Issue #6 (hardcoded model strings instead of settings fields), and will validate all Phase 7/8 new code follows 2026 standards.
- **Relevant SIOPV issues/phases:** Issue #6 (hardcoded Haiku model IDs — `requests` style patterns), Issue #9 (structlog deprecation), Phase 7/8 (new code must follow 2026 standards)

---

### `security-auditor.md` — SIOPV-APPLICABLE

- **Purpose:** Audits code for OWASP Top 10 vulnerabilities — SQL/command injection, hardcoded secrets, path traversal, insecure deserialization, LLM prompt injection, sensitive data exposure.
- **Applicability to SIOPV:** Applicable to any Python project; particularly relevant to SIOPV given it processes CVE data through an LLM pipeline (prompt injection risk) and exposes a Streamlit UI in Phase 7 (input sanitization required), plus hardcoded Haiku model IDs (Issue #6) are a secret-management concern.
- **Relevant SIOPV issues/phases:** Issue #6 (hardcoded model IDs as secrets), Phase 7 (Streamlit UI input → LLM prompt injection surface), Phase 8 (Jira API credentials)

---

### `code-reviewer.md` — SIOPV-APPLICABLE

- **Purpose:** Performs automatic code review focused on quality, maintainability, cyclomatic complexity, naming consistency, documentation, DRY violations, and code smells.
- **Applicability to SIOPV:** Applicable to any Python project; useful for Phase 7/8 new code quality gate and for reviewing remediation changes to existing Phase 0–6 code (STAGE-2 violations involve complex DI and orchestration files).
- **Relevant SIOPV issues/phases:** STAGE-2 violations (complex files in di/, orchestration/), Phase 7/8 (new code quality gate)

---

### `researcher-1.md` — ADAPTABLE

- **Purpose:** General-purpose deep online researcher using WebSearch and WebFetch; originally configured for testing frameworks, integration testing, and contract testing patterns for async Python.
- **Applicability to SIOPV:** The agent shell is fully reusable for SIOPV research tasks, but its description topic (testing frameworks/contract testing) is partially relevant — Phase 7 needs async testing patterns (pytest-asyncio, Streamlit testing). Needs adaptation: update description to match the specific SIOPV research topic when invoked (e.g., Streamlit HITL patterns, LangGraph interrupt/resume, OpenFGA Python SDK).
- **Relevant SIOPV issues/phases:** Phase 7 (Streamlit testing, LangGraph interrupt patterns), Phase 8 (Jira API v3 ADF format research)

---

### `researcher-2.md` — ADAPTABLE

- **Purpose:** General-purpose deep online researcher using WebSearch and WebFetch; originally configured for observability, tracing, OpenTelemetry, and runtime monitoring patterns for async Python.
- **Applicability to SIOPV:** The agent shell is reusable; its topic (observability/OTel) is relevant to SIOPV's Phase 7 since STAGE-3 verified that `HTTPXClientInstrumentor` does NOT cover module-level `httpx.get()` calls — but for SIOPV Phase 7/8 it would be redirected to research Streamlit caching, fpdf2 patterns, or OpenFGA authorization flows. Adapt description per invocation.
- **Relevant SIOPV issues/phases:** Phase 7 (OpenFGA UI auth, async bridge patterns), Phase 8 (fpdf2 advanced layout)

---

### `researcher-3.md` — ADAPTABLE

- **Purpose:** General-purpose deep online researcher using WebSearch and WebFetch; originally configured for CI/CD pipelines, smoke testing, chaos engineering, and Docker Compose test environments.
- **Applicability to SIOPV:** The agent shell is reusable; its topic (CI/CD, Docker Compose testing) partially overlaps with SIOPV's missing Dockerfile (TOP MISSING #1) and no detect-secrets hook (TOP MISSING #2). Can be redirected to research Streamlit component testing, Jira ADF schema, or SIOPV-specific topics per invocation.
- **Relevant SIOPV issues/phases:** TOP MISSING #1 (Dockerfile), TOP MISSING #2 (detect-secrets), Phase 7/8 deployment research

---

## SECTION 2 — HOOKS (9 files)

---

### `pre-commit.sh` — ADAPTABLE

- **Purpose:** Runs in the target project before each commit: verifies uv availability, runs `ruff format`, `ruff check --fix`, `mypy`, and `pytest tests/unit` with soft failures (warnings, not exits) for mypy and tests.
- **Applicability to SIOPV:** SIOPV already uses uv, ruff, mypy, and pytest — this hook is directly applicable. However it needs adaptation: (1) SIOPV's test directory is `tests/` not `tests/unit`, (2) mypy currently runs with 0 errors and should fail hard, not emit warnings, (3) the hook must be registered in SIOPV's own `.claude/` or as a git pre-commit hook. The soft-failure design (warnings only for mypy/pytest) is weaker than SIOPV's current zero-error mypy standard.
- **Relevant SIOPV issues/phases:** Phase 7/8 (commit gating), Issue #9 (structlog mypy would catch deprecation)

---

### `pre-write.sh` — META-ONLY

- **Purpose:** Middleware hook that checks for daily session checkpoint markers (orchestrator-read, spec-read, errors-read) and injects a reminder via `additionalContext` if checkpoints are missing; does not block writes.
- **Applicability to SIOPV:** This hook enforces the meta-project's own daily session initialization discipline using `.build/checkpoints/daily/` markers specific to `sec-llm-workbench`; it is not applicable to SIOPV's codebase and would fire false warnings in SIOPV's project directory.
- **Relevant SIOPV issues/phases:** none

---

### `verify-best-practices.sh` — ADAPTABLE

- **Purpose:** Shell script that greps SIOPV's `src/` for Python 2026 violations: legacy type hints (`List`, `Dict`, `Optional`), Pydantic v1 patterns (`class Config:`, `@validator`), sync `requests` import, `os.path` usage, and `print()` statements.
- **Applicability to SIOPV:** Directly applicable to SIOPV — the grep patterns target `src/` which matches SIOPV's layout. Needs minimal adaptation: (1) the script should be copied to SIOPV's `.claude/hooks/` or run from the SIOPV project root, (2) the `print()` check excludes `cli.py` which aligns with SIOPV's CLI boundary. Ready to use with path adjustment only.
- **Relevant SIOPV issues/phases:** Issue #6 (hardcoded strings — `requests` pattern), Issue #9 (structlog — print() check), Phase 7/8 (validate new code before commit)

---

### `test-framework.sh` — META-ONLY

- **Purpose:** Framework self-testing script that validates the meta-project's own Claude Code configuration integrity: `settings.json` structure, agent YAML frontmatter (required fields, valid model names, `memory: project`), skill YAML frontmatter, workflow file existence and version headers, and cross-references between agent files and `04-agents.md`.
- **Applicability to SIOPV:** This script tests the meta-project's own agent/settings/workflow structure integrity; it is not applicable to SIOPV's application code and would produce meaningless results when run against SIOPV (SIOPV has no `.claude/agents/` or workflow files of its own).
- **Relevant SIOPV issues/phases:** none

---

### `post-code.sh` — ADAPTABLE

- **Purpose:** PostToolUse hook that fires after every Write/Edit: logs the decision to a JSONL audit trail, runs `ruff format` + `ruff check --fix` on the edited Python file, and creates a pending-verification marker in `.build/checkpoints/pending/` to block commits until `/verify` is run.
- **Applicability to SIOPV:** The auto-format behavior (ruff on every save) is directly applicable to SIOPV development; the pending-marker system is also applicable if SIOPV uses the meta-project's commit-gating workflow. Needs adaptation: (1) the meta-project directory exclusion guard (`$META_PROJECT_DIR`) must be updated to exclude SIOPV's path, (2) the pending marker directory should resolve to SIOPV's `.build/` — the current logic already handles this via `pyproject.toml` discovery. Mostly plug-and-play.
- **Relevant SIOPV issues/phases:** Phase 7/8 (auto-format new Streamlit/Jira/fpdf2 code on save), general development hygiene

---

### `pre-git-commit.sh` — ADAPTABLE

- **Purpose:** PreToolUse hook that intercepts `git commit` commands, checks for pending verification markers in the active project's `.build/checkpoints/pending/`, blocks the commit if any Python files are unverified, and optionally checks the code-reviewer score threshold (>= 9.0/10).
- **Applicability to SIOPV:** The commit-gating pattern is directly applicable to SIOPV — it reads `.build/active-project` to find the active project path, which already points to SIOPV. Needs adaptation: (1) the `check-reviewer-score.sh` script referenced does not currently exist in SIOPV's `.claude/scripts/` and the score-check block would silently skip (graceful degradation), (2) the hook is already registered in the meta-project's `settings.json` and would fire for SIOPV commits if `.build/active-project` points to SIOPV. Functional as-is for commit blocking; score gate needs the script added.
- **Relevant SIOPV issues/phases:** Phase 7/8 (prevent unverified code from being committed), general workflow gating

---

### `pre-compact.sh` — META-ONLY

- **Purpose:** Runs before every Claude Code compaction event: updates the `Last updated:` timestamp in the meta-project's `briefing.md` and appends a one-line entry to `compaction-log.md`.
- **Applicability to SIOPV:** This hook maintains the meta-project's own session continuity files (`briefing.md`, `compaction-log.md`) at hardcoded absolute paths `/Users/bruno/sec-llm-workbench/.claude/workflow/`; it is entirely meta-project-specific and has no applicability to SIOPV.
- **Relevant SIOPV issues/phases:** none

---

### `session-start.sh` — META-ONLY

- **Purpose:** Runs on every Claude Code session start (including post-compaction resume): prints the full contents of the meta-project's `briefing.md` and the last 5 lines of `compaction-log.md` to stdout so Claude reads them as session context.
- **Applicability to SIOPV:** This hook loads the meta-project's orchestration briefing at session start — it is the meta-project's session continuity mechanism. SIOPV does not have its own briefing.md. The pattern is reusable conceptually (SIOPV could have its own briefing file loaded at session start), but the current implementation is hardcoded to meta-project paths and is not applicable to SIOPV.
- **Relevant SIOPV issues/phases:** none

---

### `session-end.sh` — META-ONLY

- **Purpose:** Runs on every Claude Code session end: updates the `Last updated:` timestamp in `briefing.md` and appends a `SessionEnd` entry to `compaction-log.md`, both at hardcoded meta-project paths.
- **Applicability to SIOPV:** Symmetric to `session-start.sh` and `pre-compact.sh` — maintains the meta-project's own continuity log. Hardcoded to meta-project paths. Not applicable to SIOPV.
- **Relevant SIOPV issues/phases:** none

---

## SUMMARY TABLES

### Agents by Classification

| Classification | Count | Agent Files |
|----------------|-------|-------------|
| SIOPV-APPLICABLE | 16 | async-safety-auditor, circular-import-detector, code-implementer, config-validator, dependency-scanner, hallucination-detector, import-resolver, integration-tracer, regression-guard, semantic-correctness-auditor, smoke-test-runner, static-checks-agent, test-generator, best-practices-enforcer, security-auditor, code-reviewer |
| ADAPTABLE | 4 | xai-explainer, researcher-1, researcher-2, researcher-3 |
| META-ONLY | 3 | final-report-agent, report-summarizer, vulnerability-researcher |

### Hooks by Classification

| Classification | Count | Hook Files |
|----------------|-------|------------|
| ADAPTABLE | 4 | pre-commit.sh, verify-best-practices.sh, post-code.sh, pre-git-commit.sh |
| META-ONLY | 5 | pre-write.sh, test-framework.sh, pre-compact.sh, session-start.sh, session-end.sh |
| SIOPV-APPLICABLE | 0 | (hooks require path/config adaptation before use in SIOPV) |

### Priority for SIOPV Stage 4 Use

| Priority | Agent/Hook | Why |
|----------|------------|-----|
| P1 | `code-implementer` | Primary builder for all remediation and Phase 7/8 |
| P1 | `async-safety-auditor` | Issue #7 + Phase 7 LangGraph/Streamlit event loop |
| P1 | `integration-tracer` | Issue #1 (CLI hollow) + Issue #4 (parameter dropping) |
| P1 | `hallucination-detector` | Phase 7/8 library API verification (Streamlit, Jira ADF, fpdf2) |
| P2 | `semantic-correctness-auditor` | Issue #2 + Issue #3 (no-op validators, hollow functions) |
| P2 | `regression-guard` | Prevent remediation from breaking 1,404 passing tests |
| P2 | `smoke-test-runner` | Runtime gate after every implementation wave |
| P2 | `test-generator` | Issue #8 (Phase 2 adapter coverage gaps) |
| P3 | `static-checks-agent` | Continuous ruff/mypy/pytest across all phases |
| P3 | `best-practices-enforcer` | Issue #6, Issue #9, Phase 7/8 standards |
| P3 | `security-auditor` | Phase 7 LLM prompt injection, Phase 8 Jira credentials |
| P3 | `circular-import-detector` | Phase 7/8 new module import safety |
| P3 | `import-resolver` | Phase 7/8 new adapter import validation |
| P3 | `dependency-scanner` | Phase 7/8 new dependency CVE check |
| P4 | `config-validator` | Missing .env.example (TOP MISSING #3) |
| P4 | `xai-explainer` | Phase 3 transparency + Phase 7 dashboard explanations |
| P4 | `code-reviewer` | Phase 7/8 new code quality gate |
| P4 | `researcher-1/2/3` | Online research before Phase 7/8 implementation |
| P4 | `verify-best-practices.sh` | Quick shell-level scan (subset of best-practices-enforcer) |
| P4 | `post-code.sh` | Auto-format on save during Phase 7/8 development |
| P4 | `pre-git-commit.sh` | Commit gate for Phase 7/8 work |

---

*End of Stage 4 Pre-Task Inventory*
