# Fix Report: Hex #6 — ingest_node.py DI

**Timestamp:** 2026-03-15  
**File:** `src/siopv/application/orchestration/nodes/ingest_node.py`  
**Issue:** Direct instantiation of `IngestTrivyReportUseCase` inside `ingest_node()` and `ingest_node_from_dict()` — no DI.

## Changes Made

Both `ingest_node()` and `ingest_node_from_dict()` now accept an optional `use_case: IngestTrivyReportUseCase | None = None` parameter.

- If `use_case` is provided (DI path), it is used directly.
- If `use_case` is `None` (default/fallback), a local import of `TrivyParser` creates a default instance.

This resolves the violation while preserving backward compatibility with the graph wiring.

## Verification Results

| Check | Result |
|-------|--------|
| `ruff format` | ✅ 1 file left unchanged |
| `ruff check` | ✅ All checks passed |
| `mypy` | ✅ Success: no issues found in 1 source file |
| `pytest tests/unit/application/orchestration/` | ✅ 111 passed in 1.32s |

## Status: COMPLETE ✅
