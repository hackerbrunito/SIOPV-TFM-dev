# Fix Report: CLI DI Wiring (Hex #3 + Known #1)

**Date:** 2026-03-15
**File:** `src/siopv/interfaces/cli/main.py`
**Status:** COMPLETE

## Changes Made

In `process_report()` command (lines 72-97):

1. Added lazy imports for `get_authorization_port` and `get_dual_layer_dlp_port` from `siopv.infrastructure.di`
2. Obtain `settings` via `get_settings()` (already imported at module level)
3. Create `authorization_port` and `dlp_port` from DI factory functions
4. Pass both ports to `run_pipeline()` call
5. Added `# TODO(phase-7)` comment for enrichment clients and classifier (not yet wired in DI)

## NOT Modified (as instructed)

- `main()` callback — unchanged
- `dashboard()` command — unchanged
- `train_model()` command — unchanged
- `version()` command — unchanged

## Verification Results

| Check | Result |
|-------|--------|
| `ruff format` | 1 file left unchanged |
| `ruff check` | All checks passed |
| `mypy` | Success: no issues found in 1 source file |
| Import test | CLI imports OK |
| Unit tests | No tests exist for `interfaces/` (expected) |

## Impact

- **Phase 5 (Authorization):** Now wired — `authorization_port` passed to pipeline
- **Phase 6 (DLP):** Now wired — `dlp_port` passed to pipeline
- **Phase 2 (Enrichment):** Still None — requires Phase 7 DI work
- **Phase 3 (Classifier):** Still None — requires Phase 7 DI work
