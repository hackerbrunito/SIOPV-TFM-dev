# Fix Report: Hex #1 — IngestTrivyReportUseCase adapter import

**Date:** 2026-03-15T14:00:00Z
**Worker:** remediation-worker (hex-1-ingest)
**Violation:** Stage 2 Hex #1 CRITICAL

---

## Problem

`application/use_cases/ingest_trivy.py:17` imported `TrivyParser` directly from
`siopv.adapters.external_apis.trivy_parser` at module level — a hexagonal architecture
violation (use cases must depend on ports, not concrete adapters).

---

## Changes Made

### 1. Created `src/siopv/application/ports/parsing.py`
New `@runtime_checkable` Protocol `TrivyParserPort` with:
- `parse_file(path: Path) -> list[VulnerabilityRecord]`
- `parse_dict(data: dict[str, object]) -> list[VulnerabilityRecord]`
- Properties: `parsed_count: int`, `skipped_count: int`

### 2. Updated `src/siopv/application/ports/__init__.py`
Added import and export of `TrivyParserPort`.

### 3. Modified `src/siopv/application/use_cases/ingest_trivy.py`
- Removed module-level `from siopv.adapters.external_apis.trivy_parser import TrivyParser`
- `IngestTrivyReportUseCase.__init__` now accepts `parser: TrivyParserPort` parameter
- `ingest_trivy_report()` convenience function accepts optional `parser` param;
  defaults to `TrivyParser()` via a local import (backwards-compat, isolated to function body)

### 4. Updated `src/siopv/application/orchestration/nodes/ingest_node.py`
Both `ingest_node()` and `ingest_node_from_dict()` now use local import of `TrivyParser`
to construct `IngestTrivyReportUseCase(parser=TrivyParser())`.

### 5. Updated `tests/unit/application/test_ingest_trivy.py`
Updated `use_case` fixture to pass `parser=TrivyParser()` to the constructor.

---

## Verification Results

| Check | Result |
|-------|--------|
| `grep "^from siopv.adapters" ingest_trivy.py` | 0 matches ✅ |
| `ruff format` on changed files | Clean ✅ |
| `ruff check` on changed files | All checks passed ✅ |
| `mypy` on changed files | Success: no issues ✅ |
| `pytest tests/unit/application/test_ingest_trivy.py` | 11 passed ✅ |
| `pytest tests/unit/application/` | 219 passed, 6 pre-existing failures (classify_risk + edges — unrelated) ✅ |

---

## Pre-existing failures (not caused by this fix)

- `test_classify_risk.py` (3 tests) — Hex #2 violation, handled by another worker
- `test_edges.py` (3 tests) — Hex #7 violation, handled by another worker
