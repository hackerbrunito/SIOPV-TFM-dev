# Remediation Report: Hex #7 — Domain Logic in edges.py

**Date:** 2026-03-15T22:09:00Z
**Violation:** #7 — `application/orchestration/edges.py` contains domain logic (`calculate_discrepancy`, `calculate_batch_discrepancies`)
**Status:** FIXED

## Changes Made

### 1. Created `src/siopv/domain/services/discrepancy.py`
- Moved `calculate_discrepancy()` and `calculate_batch_discrepancies()` functions
- Used deferred imports via `_import_state_types()` helper to break circular import chain:
  `domain.services.discrepancy → application.orchestration.state → application.__init__ → orchestration.__init__ → edges → domain.services.discrepancy`
- `TYPE_CHECKING` guard for type annotations, runtime import via module-level accessor
- Single `noqa: PLC0415` on the deferred import (ruff rule for non-top-level imports)

### 2. Updated `src/siopv/application/orchestration/edges.py`
- Removed inline function definitions (lines 82-200)
- Added import from new domain service module
- Kept both functions in `__all__` for backward compatibility (re-exports)

### 3. No changes needed to `src/siopv/domain/services/__init__.py`
- Directory already existed with `__init__.py`

## Verification Results

| Check | Result |
|-------|--------|
| `ruff format` | 0 changes needed |
| `ruff check` | All checks passed |
| `mypy` | Success: 0 errors in 2 files |
| `pytest test_edges.py test_state.py` | 32 passed |
| Import from `edges` (re-export) | OK |
| Import from `domain.services.discrepancy` (direct) | OK |
| Coverage `edges.py` | 100% |
| Coverage `discrepancy.py` | 100% |

## Pre-existing Failures (NOT introduced by this change)
- `test_ingest_node.py::TestIngestNode::test_ingest_node_success` — fails due to hex violation #6 (`ingest_node.py` directly instantiates `IngestTrivyReportUseCase()` without DI/parser argument)

## Files Modified
- `src/siopv/domain/services/discrepancy.py` (NEW)
- `src/siopv/application/orchestration/edges.py` (MODIFIED)
